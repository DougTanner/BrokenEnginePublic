# Shader Review — Overview

Generated by one Opus `/glsl-review` subagent per shader file across all 60 first-party shaders under `Engine/Data/Shaders/`.

## Per-subsystem plans

All per-subsystem plan files below were executed-and-deleted or never authored — this list is the historical organization of the review, not a live index. Outcomes are tracked in `Documents/Plans/Order.md` `## Debt Score (Graphics — Shaders)`.

- `01_Model.md` — Model (6 files)
- `02_Water.md` — Water (2 files)
- `03_Terrain.md` — Terrain (6 files)
- `04_Particles.md` — Particles (9 files)
- `05_Quads.md` — Quads (4 files)
- `06_Lighting.md` — Lighting (8 files)
- `07_Objects.md` — HexShield / Objects (3 files)
- `08_Smoke.md` — Smoke (4 files)
- `09_Wind.md` — Wind (4 files)
- `10_Shadow.md` — Shadow (5 files)
- `12_Misc.md` — Log, Clear, DebugTexture (3 files)

## Summary counts

| Status | Files |
|---|---|
| PASS (no findings) | 21 |
| PASS with minor notes | 13 |
| NEEDS FIXES | 26 |

## Highest-priority cross-cutting fixes

1. **Billboard singularity on top-down camera** — `cross(viewDir, worldUp)` degenerates when the camera looks straight down (the stated RTS use case). Affected: `SquareParticlesRender.vert`, `LongParticlesRender.vert`. Fix: derive right axis from the view matrix, or epsilon-fallback to `vec3(1,0,0)`.
2. **Unguarded `normalize()` on zero-length vectors** — widespread NaN source. Affected: `Model.frag` (tangent Gram-Schmidt), `Water.frag` (sampled-normal sum), `HexShieldLighting.frag` (xy at pole), `Shadow.comp`, `LongParticlesRender.vert`, plus several others. Canonical fix: `v / max(length(v), 1e-6)`.
3. **Unguarded `pow(base, non-integer)` with possibly-negative base** — UB / NaN on most drivers. Affected: `HexShield.frag:55`, `HexShield.vert:75`, `LightingSpread.frag`, `LightCombine.comp`, `DebugTexture.frag`, `WindSpreadCommon.h:70` (shared helper). Fix: `pow(max(base, 0.0), exp)`.
4. **Compute-shader implicit-LOD `texture()`** — undefined without quad derivatives. Affected: `Shadow.comp`, `ObjectShadowsBlurH.comp`, `WindSpreadOne.comp`, `WindSpreadTwo.comp` (via shared helper). Fix: replace with `textureLod(sampler, uv, 0.0)` throughout compute-stage sampling (no mip chain is in use anyway).
5. **Missing `writeonly` on output `image2D`** — lost driver optimization. Affected: every `*Blur*.comp` (Lighting, Shadow, ObjectShadows) and `Shadow.comp`. One-line qualifier per binding.
6. **`ParticlesUpdate.comp` non-atomic RMW on allocation bitmap** — 32 neighboring particles share a word; concurrent deallocations race. Fix: `atomicAnd(puiAllocated[i/32], ~(1u << (i%32)))`.
7. ~~**`Log.vert` hard bug — `gl_Position.w = 0.0`** produces NaN clip coords; vertex is culled/undefined. Fix: use `1.0f`.~~ **LANDED** — `Log.vert` now writes `w = 1.0f`.
8. **`ParticlesSpawn.comp` dual-language type mismatch** — header declares `uint16_t pbAllocated[]`, shader writes `uint8_t`. Latent scalar-layout bug (path currently dead behind `ENABLE_32_BIT_BOOL`).
9. **Shadow blur workgroup = 512** exceeds the 32-256 range; review against `ObjectShadowsBlur` siblings which use 64.
10. **Descriptor-set discipline audit** — several compute shaders omit `set = N` and default to set 0, which per `Shaders/CLAUDE.md` is reserved for global. Most smoke/wind/blur compute shaders share this deviation. Decide subsystem-wide: either add explicit `set = 1` or document the convention.

## Recurring project conventions worth codifying

- Shared helpers in `ShaderFunctions.h` (`ReadLighting`, `SmokeShadow`, `LightingDepositEdgeFade`) are under-used — several frags reinvent them.
- The `gl_Position = vec4(0,0,-100,0)` kill pattern with `w=0` appears in multiple files as a "cull vertex" trick — this relies on undefined perspective divide behavior. Prefer `vec4(2,2,2,1)` (guaranteed outside clip) or emit a degenerate triangle deliberately.
- The `int(flatFloatVarying + 0.4f)` rounding bias in `AreaLight.frag` / `PointLight.frag` is likely wrong — should be `+ 0.5f` for round-to-nearest. Pass integer varyings as `flat int`.

## Files reviewed

See per-subsystem plans for the full list.
