# Plan Execution Order

All refactor and bugfix plans — debt reduction that doesn't add a new engine capability — sorted by score (lowest = highest priority).

Score = Effort − Impact + Risks (lower = higher priority)

`Tier` column is an informal size/risk descriptor (**Quick Win** / **Small** / **Medium** / **Large** / **Architectural**); it does not affect score or ordering.

Counterpart: `Documents/Features/Order.md` holds brand-new additions. See `Documents/CLAUDE.md` for the distinction. Scores are not comparable across the two files — they were originally derived from a single sort and then split.

## Debt Score (DataPacker, this run): MODERATE

Distribution: 6 Medium + 1 Architectural. No critical bugs, but `ExportScene.cpp` is approaching the `/reduce-file` threshold and a public header pulls tinygltf into unrelated TUs.

## Debt Score (Network, this run): MODERATE

Distribution: 7 Quick Win + 6 Medium + 1 Architectural across 14 plans. Network subtree is structurally sound — the main concentrations are (1) engine → game surgery in `ServerReceive.cpp` save/load/timespeed handlers and the `*SessionBase.cpp` CoordFrames mutation, (2) one replay-determinism bug (fleet RNG re-seeded from wall clock on load), and (3) mechanical duplication across ~20 `Send*` methods, ~14 connected-gate sites, and ~12 handshake-gate sites. `ServerFleetManager.cpp` (808 lines) and `ReconcileReplay.cpp` (668 lines) are genuine `/reduce-file` candidates.

## Debt Score (Graphics — Shaders, this run): MODERATE

60 first-party GLSL shaders reviewed via one `/glsl-review` Opus subagent per file. Original distribution: 9 Quick Win + 3 Medium across 12 subsystem plans. **Six performance-neutral bugfix plans landed this session** via parallel Opus subagents: `03_Terrain`, `05_Quads`, `07_Objects`, `04_Particles` fully executed and deleted; `09_Wind` and `12_Misc` partially executed (descriptor-set audit and `nonuniformEXT` audit deferred — both require C++ coordination). `08_Smoke` landed in a follow-up session (full execution, deleted). The particles sweep landed cross-cutting fixes folded in: 5 `writeonly` Shadow image2D sites, 2 `writeonly` Lighting blur sites, 4 `writeonly` Smoke/Wind spread sites, `LightCombine.comp` Uchimura `pow` base clamps, and the redundant outer `normalize` drop in `Model.frag`. No per-subsystem plan files remain on disk (each was executed-and-deleted or never authored); the residual Water defensive polish and the deferred descriptor-set / `nonuniformEXT` audits have no live plans — re-author if wanted. **`06_Lighting` landed this session**: its correctness/consistency items were either already mitigated by existing slider clamps or executed (ring-count slider min raised to 2 to guard the `LightingSpread` `fNorm` divide; `scalar` qualifier added to `lightOccupancyBuffer` in `AreaLight`/`PointLight`); the deferred performance items were split into the new `06_LightingPerf` plan, which **landed in a follow-up session** (incremental rotation recurrence for the `LightingSpread` direction-loop `cos`/`sin`, hue-preserving Reinhard dedup, `LightCombine` Uchimura skip at `fHuePreserve>=0.999`, blur reciprocal hoist, `AreaLight`/`PointLight` early-alpha discard; plus folded-in Pattern-3 pow-skip guards in `TerrainElevation.frag`, `Water.frag`, and `ShaderFunctions.h`). No `inverse()` driver-hang violations. Remaining recurring hazards: (1) unguarded `normalize()` on possibly-zero vectors (Model, Water — Particles now guarded), (2) `pow(neg, non-integer)` → NaN in `LightingSpread.frag` (LightCombine clamped), (3) implicit-LOD `texture()` in compute (Shadow, ObjectShadowsBlur). Standalone hard bugs: `Terrain.frag:76` (landed), `Log.vert:38` (landed), `ParticlesUpdate.comp` non-atomic RMW race (landed). Overview at `Graphics/ShaderReview/00_Overview.md`; all per-subsystem plan files have been executed-and-deleted or were never authored.

## Debt Score (Common, this run): MODERATE

29 files reviewed (one Opus `/external-deep-analysis` subagent per `.h`/`.cpp` pair, then a second Opus validation pass against source before authoring). 28 plans authored — 26 per-file + 2 cross-cutting themes (`Determinism_IndeterminateBytes`, `ThreadSafety_SharedStaticBuffers`); 3 files produced no plan (`Common.h` works-as-designed, `ScopedLambda` all-YAGNI, `Serialization` all error-handling/theme-deferred). Distribution: 7 Quick Win + 18 Small + 3 Medium; no Architectural. The foundation layer is structurally sound, but validation kept genuine correctness bugs worth fixing despite their low effort: a `Log` buffer-overrun, UB in `TextureFormat` / `WindowsUtils` (`FILETIME` cast) / `MathUtils` (`RoundUp`), an exception-unsafe `Dispatch` join (use-after-scope), and several determinism/desync sources (`Crc` `char` sign-extension + padding-byte hashing, `ConvexHull` transcendentals on the CRC path, indeterminate `.pack` bytes). Two themes coordinate the systemic issues. Validation dropped the majority of report findings as cosmetic, speculative-API, or defensive-validation the project policy forbids; the original `ErrorUtils` report was a hallucination and was re-authored from source.

## Debt Score (Repo-wide CLAUDE.md refresh, this run): LOW

70 CLAUDE.md docs refreshed by 140 Fable agents (summarize directory code, then update doc); 46 raw conflicts deduped into the refresh report's open-items sections. 16 follow-up plans authored from those sections: 7 correctness/cleanup bugs (fleet-sync overread, blaster degenerate march, unsubscribe handshake gate, relink-load ordering, stale `kTransferPlayer` assert, headerless-raw format claims, dead Missiles SOA field), 4 grouped cleanup sweeps (dead-code/unused-includes, stale code comments, vcxproj/build-config, stale doc claims), and 5 doc-rule **decision** plans (aggregation/scope-rule qualifiers, DataPacker LOG float-spec exception, Collections straight-line-steering rule, parent-rule framing reconciliation, plan-doc governance). The codebase is structurally sound — the only genuine correctness bug with runtime reach is the unvalidated `ParseFleetSync` network read; the blaster march and Missiles dead field are latent/visual. Most findings are documentation drift (the refresh's purpose). The five decision plans are tagged "Decision plan (present options)" and resolve via `/external-grill-plan` before any edit. No determinism/CRC regressions surfaced; the one CRC-touching plan (Missiles field removal) is a clean `kiVersion`-bumped layout change.

**Second-pass routing (this run): 7 more plans authored** from a code-side / cross-doc audit that surfaced findings out of scope of the doc edits, plus 5 appends to the existing sweep plans. New plans: 4 code/investigation (`Graphics/PipelineGraphicsGlobalSet0Indexing` — Set-0 framebuffer-index discrepancy; `Network/StaticDataEpochCheckOnSubscribingSlot` — epoch check on epochless `kSubscribing` slot; `Ui/ChineseFontPushMissingScreens` — missing CJK font push on Pause/Sound; `Network/BroadcastTickAllocationGuardScope` — blanket suppression nullifies the documented armed tracker), 1 shader investigation (`Graphics/ParticlesSpawnStructCopyGlslangBug` — whole-struct SSBO copy vs glslang trailing-field bug), 2 doc-reconciliation (`Graphics/NvidiaPipelineHangAttributionReconcile` — mat4-array vs `inverse()` hang attribution; `Common/DocCanonicalHomeReconciliation` — 6 cross-doc canonical-home/pick-one-wording decisions). Appends: `StaleCodeCommentsSweep` (+4: Smoke Pass-Two coords, Wind "Manhattan-2", RawInput legacy-mouse, Model ACES "Stephen Hill" → Narkowicz), `DeadCodeAndUnusedIncludesSweep` (+2: dead `FrameBase` fwd-decl, unused `FrameInput::operator==`), `StaleDocClaimsSweep` (item 1 R16/R32 marked RESOLVED-during-audit; item 11 FrameUpdatePipeline expanded with the missing Server-loop quickload/replay/resends/autosave steps). One audit finding was **not filed** — a `Model.frag` IBL `fPbrSun^3` comment flagged as "cubic comment vs quadratic math" turned out to be a correct, internally-consistent comment describing an *avoided* cubic (the code is linear in `fPbrSun`); no defect. The `cmft.cpp` missing-`<Filter>` finding was already item 4 of `VcxprojAndBuildConfigReconciliation` (not re-filed), and the IslandChainPlacement.h log-spiral comment was already item 5 of `StaleCodeCommentsSweep`. The EWNS deposit-rule framing conflict stays in `Graphics/ParentRuleFramingReconciliation`. No new CRC/determinism exposure (the two Network plans are client-receive / server-debug-tracking only; the Graphics Set-0 plan is a possible GPU-hazard fix, not determinism).

## Debt Score (Audio, this run): LOW

`/external-deep-analysis` on `Engine/Source/Audio` (10 files, ~1835 LOC, non-recursive). Distribution: 3 Quick Win + 4 Small — no Medium/Large/Architectural. The area is structurally healthy: all documented CLAUDE.md invariants verified against source (frame-phase asserts, replay guards, streaming Wait/Wake alternation, atomic slot machine, deferred device-reset clears), allocation discipline fully compliant, no layer violations, and no viable ThirdParty library replacements (the only candidate — extending DirectXTK Audio coverage — is blocked by `SoundEffect`'s buffer-ownership API vs FileManager `LazyChunk`). Findings are mechanical: include hygiene, two misplaced helpers (`DestroyXAudio2SourceVoice` semantic cycle + dtor-order brittleness), one verified presentation-only replay-invariant deviation (RNG advance before the `kRecalculated` guard), an avoidable recursive mutex, a ~330-line lifecycle function with O(n²) bookkeeping, an audible new-voice start pop, and a constructor that skips its own documented device fallback.

## Debt Score (Engine/Source/Debug, this run): LOW

`/external-deep-analysis` on `Engine/Source/Debug` (2 files: `EnumToString.h` + `CLAUDE.md`, non-recursive). One deep, healthy module: thread-safe immutable maps, client-only gating verified at both gates (Engine.h `BT_CLIENT` span + client-only vcxproj), no determinism/CRC exposure, allocation-discipline compliant, no layer violations, zero dead maps. Single Small plan — replace the hand tables with the SDK-shipped generated `vk_enum_string_helper.h` — subsumes every line-level finding as well (the missing-enumerator gaps and `Convert`'s five duplicated `if constexpr` dispatch blocks), so the refactor-clean phase produced no separate plans.

## Debt Score (Engine/Source/File, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/File` (3 files, ~1496 LOC, non-recursive). Distribution: 2 Quick Win + 5 Small + 1 Architectural decision. The directory is deliberate, documented performance architecture (unbuffered sector-aligned I/O, pool-layout-by-iteration-order, atomic chunk state machine) with full ExternalHeaders/allocation-discipline compliance and zero viable ThirdParty replacements (PhysFS/cereal/bsdiff all rejected for fit — the `.pack` system and DifferenceStream are engine-specific by design). Debt concentrates in one cross-layer leak (FileManager parsing GLTF scene layout into the Graphics `gAnimationDataMap`), latent boot/teardown-window thread-lifecycle holes (server fast-exit dtor can join a not-yet-joinable thread), interface leaks (public zero-user members, const-laundering, and the four-file chunk state machine — filed as a decision plan), and a hand-duplicated versioned-header convention. No critical bugs; no determinism/CRC exposure in any finding.

## Debt Score (Frame, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/Frame` (22 files, ~5,330 LOC, non-recursive — `Collections/` excluded). 14 plans authored (8 Architecture + 6 Refactor); distribution: 8 Quick Win + 5 Small + 1 Medium, no Large/Architectural. The core is structurally strong — determinism discipline verified exemplary (decorrelated seeded RNG streams, `DeterministicSinCos` coverage, index-tie-broken A* heap, commutative-max elevation blending, MXCSR asserts), thread model sound (thread_local everything, per-coord workers), layer integrity clean, and no viable ThirdParty replacements (Recast/Detour evaluated in depth and rejected on determinism/architecture fit; Clipper2 already absorbed the hard geometry). What pushes past LOW: three genuine latent-correctness items — a release-silent `sLayerBaseOffsets` array overrun on the collision layer-overflow path, the runtime-tunable `gBaseHeight` Ui `Wrapper` read by deterministic nav/spawn code, and XOR CRC combining that cancels even-multiplicity desyncs — plus the known oversized-file pair already queued as splits. All 14 plans passed a Fable verification pass against source (2 rewritten for incomplete reader list / wrong invariant claim; 0 deleted).

## Debt Score (Engine/Source/Graphics — top-level, this run): LOW

`/external-deep-analysis` on `Engine/Source/Graphics` (14 files, ~2,091 LOC, non-recursive — `Debug/`/`Managers/`/`Objects/`/`Render/` subtrees excluded). 8 plans authored (6 Architecture + 2 Refactor); distribution: 3 Quick Win + 5 Small, no Medium/Large/Architectural. The directory is structurally healthy: zero std-lib include violations, full aggregation-header compliance, determinism clean (no RNG, no sim writes from the render path), thread/frame-phase contracts verified against `Graphics/CLAUDE.md`, and zero viable ThirdParty replacements (the only library-shaped cluster, `AnimationData`, was rejected — ozz-animation would cost a pack-format/DataPacker rewrite plus an allocation-discipline conflict to delete ~400 working glTF-spec lines; stb/volk/VMA/DirectXMath already sit at the right seams). The standout finding is one latent deadlock: `SaveScreenshot` waits a just-reset per-framebuffer fence that only the subsequent UI submit signals — masked today by `kbScreenshots=false`. Everything else is mechanical: include/fwd-decl drift, a game-typed by-value member on `Graphics` that only `GameBase` actually uses (held up by PCH include ordering), comment-synchronized cross-module constants, a dead `Execute(bool)` parameter whose false-path would free a pending command buffer, the ~173-line `Refresh()` poll ladder, and four copy-pasted corner unprojects. All 8 plans passed a Fable verification pass against source (3 rewritten — two EncapsulationSeams items and one InVisibleArea item dropped, one constant-pairing claim corrected; 0 deleted).

## Debt Score (Engine/Source/Input, this run): LOW

`/external-deep-analysis` on `Engine/Source/Input` (2 files, 326 LOC, non-recursive; the requested `Engine/Source/Frame/Input` does not exist — `Engine/Source/Input` is the engine's only input directory). Distribution: 3 Quick Win + 2 Small (one a keep-in-house decision plan) — no Medium/Large/Architectural. The module is genuinely deep and healthy: layering clean (the one engine→game edge is the sanctioned `game::gpGame->ShouldTrapCursor()` virtual), client/server guard placement exactly the documented narrowest-practical scope, determinism firewall intact (nothing in the snapshot feeds `FrameInput`; the scroll accumulator and resolution-normalized mouse are handled game-side), all access main-thread (the two-phase scratch→snapshot design is frame-coherence, not concurrency), and zero unused includes / full ExternalHeaders.h compliance. Findings are mechanical: an over-wide public surface + dual-sourced HWND, a missed trigger/dpad clear on gamepad disconnect, a swallowed `WM_INPUT` `DefWindowProc` pass, a two-call raw-input fetch, and a bool-array→`Flags` pattern alignment. Two findings deduped into existing sweeps (mouse `RIDEV_INPUTSINK` → `Engine/DeadCodeAndUnusedIncludesSweep`; the wrong cpp:42 comment → `Common/StaleCodeCommentsSweep`). All five plans passed a Fable verification pass against source (2 citation fixes; 0 deleted). A second verification in the companion Input+Memory run consolidated parallel-authored duplicates: `GamepadDisconnectClear` → `Architecture_GamepadDisconnectSnapshot.md` (zeroing the consumer-less `f2Triggers` replaced by deleting the dead field) and `InputInterfaceTightening` → `Architecture_InterfaceNarrowingAndDocs.md` (absorbed unchanged).

## Debt Score (Engine/Source/Memory, this run): LOW

Companion to the Input sweep above (one `/external-deep-analysis` run on `Engine/Source/Input` + `Engine/Source/Memory`; Memory = 2 files, ~111 LOC, non-recursive). One Quick Win plan, plus two appends to existing sweeps: `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 (the `CollectionMemory.h:3` stale-include channel that accidentally distributes `MemoryManager.h` to every TU, paired with `ProfileManagerBase.cpp`'s missing direct include) and `Engine/VcxprojAndBuildConfigReconciliation.md` item 6 (`RawInputManager.h` server `ClInclude`). The module is an exemplary deep module: a 3-line header exposing exactly the two cross-TU symbols with real consumers (`Main.cpp`, `ProfileManagerBase.cpp`) while 16 linker-injected operator replacements + the arena/SIGABRT/stats machinery stay invisible; allocation-tracking gates verified (counter `kbProfiling`-gated, tripwire always-on, deletes never tracked, suppression thread-local); no determinism/CRC exposure anywhere. The one genuine code finding is the benign-by-sequencing `sbTrackingReady` data race (formal UB, free fix). Zero viable ThirdParty replacements — mimalloc's stock `mimalloc-new-delete.h` has no `TrackAllocation` hook point and partial use breaks ODR / the upstream-pristine rule; the in-house forwarders are correctly understood as a tracked fork of it (maintenance note filed). Both CLAUDE.md docs verified claim-by-claim (one misleading participation example fixed via the plan). All citations re-verified by a Fable verification pass; 0 items dropped.

## Debt Score (Engine/Source/Ui, this run): LOW

`/external-deep-analysis` on `Engine/Source/Ui` (30 files, ~1,832 LOC, non-recursive — `Ui/Screens/` excluded; the requested `Engine/Source/Frame/Ui` does not exist — `Engine/Source/Ui` is the engine's only Ui directory). Distribution: 6 Quick Win + 1 Small — no Medium/Large/Architectural. The directory is a near-perfect leaf: zero direct std-lib/third-party includes (full ExternalHeaders.h compliance), zero include cycles, zero dependencies on the rest of `Engine/Source`, no hot-path allocation (all wrapper globals are static-init; `CurveData` capacity is bounded by ctor reserve + 16-point cap), and determinism clean except the already-plan-covered `gBaseHeight` seam (deduped into the pre-existing `Frame/Architecture_BaseHeightWrapperDeterminism.md` — not re-filed). One viable ThirdParty replacement: rebuild `CurveWidget` on already-vendored ImPlot. Findings are mechanical: an unused include silently dragging `CurveData.h` into the game PCH, a zero-caller change-detection sub-interface on `CurveData`, a stale water-normal "must match" comment whose constant has no consumers, undocumented threading / float-storage / `Toggle()` contracts on `Wrapper` (verification surfaced a real exactness example: `VK_PRESENT_MODE_FIFO_LATEST_READY_KHR` = 1000361000 exceeds the 2^24 float ceiling and corrupts to 1000361024 in `gPresentMode`'s allowed set — latent, never UI-exposed), and a `// DT: TEMP` tunable shipping end to end. All seven plans passed a Fable verification pass against source (4 rewritten — added ctor-init deletion lines, corrected a false "max 255" exactness claim, fixed two engine-vs-game TweaksScreen paths, concretized the shader-consumer scope; 0 deleted).

## Debt Score (Engine/Source/Profile, this run): LOW

`/external-deep-analysis` on `Engine/Source/Profile` (3 files, ~1,212 LOC, non-recursive; the requested `Engine/Source/Frame/Profile` does not exist — `Engine/Source/Profile` is the engine's only profile directory). 6 plans authored (5 Architecture + 1 Refactor); distribution: 4 Quick Win + 2 Small, no Medium/Large/Architectural. The subsystem is structurally healthy: deep recording interface (two-call CPU scope hiding per-thread maps, cross-thread pairing, allocation diffing, smoothing), determinism clean (steady_clock only, no sim writes, no RNG), ExternalHeaders/allocation discipline fully compliant, and zero viable ThirdParty replacements (Tracy/microprofile/Optick/Remotery all rejected — every external profiler is viewer-oriented and architecturally violates the in-engine overlay + headless GDI server + per-scope allocation attribution + `kbProfiling` compile-out requirements; Palanteer license-rejected, AGPL viewer). The standout finding is one real display-correctness bug: `SmoothCpuTimers` double-latches every `bSmoothNow` timer except hard-coded index 0, halving the displayed client network timers and the server's headline frame-time row. Everything else is mechanical: guard-scope misalignment (whole-body `BT_CLIENT` on `UpdateProfileText`, ungated GPU-timer state, mis-gated mimalloc fields), a comment-only engine↔game enum contract, include drift (`MemoryManager.h` in the wrong sibling TU, unused `Game.h`), three stale CLAUDE.md claims, and in-function items (two-bool `CpuStop`, ~158-line `FormatGpuScreen`). All 6 plans passed a Fable verification pass against source (2 rewritten — one boot-crashing ASSERT item dropped, one count corrected; 0 deleted).

## Debt Score (Engine/Source/Network — engine transport, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/Network` (recursive; 23 files, ~3,965 LOC — engine transport only; the earlier "Network" debt score above covered the game-layer sweep). 12 plans authored (8 Architecture + 4 Refactor); distribution: 3 Quick Win + 8 Small + 1 Large. The wire path is determinism-safe (raw-memcpy scalars, byte-symmetric layouts verified for all message types, version-gated handshake), strictly main-thread with zero locks, and already outsources transport/compression to ENet/LZ4 at the right seams (ThirdParty replacement review: clean null result). What pushes past LOW: two genuine latent protocol bugs — the paused-server subscribe/resync queue drop that wedges clients in `kWaitingFullState` (no timeout exists), and the `0xFF` reject sentinel misread as a range check that permanently leaks server slots — plus the systemic hand-mirrored wire-format duplication (lockstep by convention only; one Large plan). All 12 plans passed a Fable verification pass against source (6 corrected in place — message-type count, purge-predicate split, handler name, StatusChange POD claim, call-rate precision, dead-param caller chain; 0 deleted).

## Debt Score (Engine/Source/Graphics/Debug, this run): LOW

`/external-deep-analysis` on `Engine/Source/Graphics/Debug` (2 files, ~190 LOC, non-recursive; the requested `Engine/Source/Frame/Graphics/Debug` does not exist — `Engine/Source/Graphics/Debug` is the only graphics-debug directory in the engine). Distribution: 2 Quick Win — no Small/Medium/Large/Architectural. The module is an exemplary deep module: seven static calls hiding lazy 128K-slot staging, CRC-keyed dynamic-buffer resize, update-after-bind descriptor refresh, and record-once-compatible indirect-count management in 165 lines. Everything verified clean: sanctioned `kbDebugRender` layering (game-PCH constexpr, same pattern as `kbProfiling`), whole-file `BT_CLIENT` wrap + client-only vcxproj membership, full main-thread call-site census (no `Dispatch`-region submissions), zero determinism/CRC exposure (the only sim-path `kbDebugRender` code writes client-only CRC-excluded SOA fields), shader/CPU layout shared via the dual-language `ShaderLayoutsBase.h` header with the set-1/binding-2 contract verified end-to-end, allocation discipline compliant, teardown/recreate ownership safe. Zero viable ThirdParty replacements (debug-draw / im3d / Jolt DebugRenderer / ImDrawList all rejected — per-frame CPU draw-list models fight the record-once-CB + instanced-indirect architecture; net LOC negative). Findings: dead public API (`Box`/`Sphere` — zero callers, full Debug-config GPU plumbing behind them) and one systemic hazard that verification proved unreachable (the `RenderFrameMain` empty-active-set early return skipping all indirect zero-writes — `ComputeActiveSet`/reset/boot all force a non-empty set; downgraded to invariant documentation). The three doc nits from this run ride `Common/StaleDocClaimsSweep.md` items 15-17. Both plans passed the Fable verification pass against source (1 rewritten, 0 deleted).

## Plans

| # | Plan | Tier | Effort | Impact | Risks | Score | Notes |
|---|------|--------|--------|--------|-------|-------|-------|
| 1 | [Frame/Refactor_CollisionOverflowGuards.md](Frame/Refactor_CollisionOverflowGuards.md) | Quick Win | 1 | 3 | 1 | -1 | Five overflow-growth resizes missing the documented `ScopedSuppressAllocationTracking` + `// Heap:` (`Collision.cpp:51/114/125/238`, `AreaDamage.cpp:26`) re-trip the tracker mid-recovery, plus a real latent overrun: `AddLayer` overflow grows `sLayers` past `kiCollisionLayerPreallocate` (16) while `sLayerBaseOffsets` stays a fixed C array (write `:353`, reads `:301/:339/:564/:569`) — `kbDebugBreak=false` in Profile/Release makes it silent memory corruption. Add suppressions; make layer overflow fatal or bound the loop (grill). Overflow-path-only, no CRC/determinism exposure |
| 2 | [Profile/Architecture_SmoothNowDoubleLatch.md](Profile/Architecture_SmoothNowDoubleLatch.md) | Quick Win | 1 | 3 | 1 | -1 | `SmoothCpuTimers` re-latches every `bSmoothNow` timer's just-zeroed accumulator (exclusion hard-coded to index 0, `ProfileManagerBase.cpp:375`), so the smoothing ring alternates V,0 → displayed values ~halved for `kCpuTimerNetworkSend`/`PollReconcile` (client overlay) and the server GDI display's headline `kCpuTimerFrameUpdate` row (the `<50us` cull can hide them entirely). Replace the magic index with a per-timer `bSmoothAtStop` flag set in `CpuStop`. Profiling display only — no CRC/determinism exposure |
| 3 | [Network/ParseFleetSyncWireCountValidation.md](Network/ParseFleetSyncWireCountValidation.md) | Small | 2 | 3 | 1 | 0 | `game::ParseFleetSync` (`PlayerEvents.cpp:64-98`) reads wire-supplied fleet/member counts and `resize`s/reads with **no size validation** — a truncated/malformed `kServerFleetSync` overreads `rPayload` (heap overread, UB) and a garbage count drives an unbounded `resize`. Violates the root trust-boundary directive; the `Network/CLAUDE.md` hub already flags the asymmetry vs the size-checked `ParsePlayerEvents` sibling. Bound every read against payload end (prefer `NetworkCursor` bounded reads) and reject malformed packets. Client-side selection state — no CRC/determinism exposure. One grill decision: malformed-packet failure policy |
| 4 | [Network/NetworkSerializationStaleStaticAssert.md](Network/NetworkSerializationStaleStaticAssert.md) | Quick Win | 1 | 1 | 0 | 0 | `CompressStatusChangeBatch` (`NetworkSerialization.cpp:354-356`) comment + `static_assert` claim `kTransferPlayer` serializes to **113** bytes; the actual `SerializeTransferPlayer` writer (`:55-75`) totals **116** (the tally omits `uint16 uiPlayerFlags` + a trailing `uint8`). No overflow (`kiMaxBytesPerItem=120 ≥ 116`), but the guard number is stale. Correct the comment + assert to 116; optionally link the guard to the writer (sibling of the `DataFile.h` layout-lock asserts). No wire/`kiVersion` change |
| 5 | [Common/StaleCodeCommentsSweep.md](Common/StaleCodeCommentsSweep.md) | Quick Win | 1 | 1 | 0 | 0 | Comment-only sweep of 9 stale **source-code** comments: DataPacker RDO "current production knobs" (real = lookback 1024 / lambda 4.0 / uber 4) + `ExportIsland.cpp:202` "uses all" threads (= `HardwareCoreCount()-2`); `MainUniforms.cpp:176-177`/`PipelineManager.cpp:374` "terrain no longer uses indirect draws" (it does, `CommandBufferRecordMain.cpp:257-264`); `FileManager` "pre-faulted" (= `MEM_COMMIT` only); `IslandChainPlacement.h` log-spiral (= contact-growth); Smoke "Manhattan-2" (= 5×5 box); `WaterSkyboxOne.frag` drifted line cites; `Players.cpp` "(RGBA)" (= ABGR); `SoundWrappers.cpp` "volumes are 0" (mostly non-zero). Zero behavior |
| 6 | [Common/StaleDocClaimsSweep.md](Common/StaleDocClaimsSweep.md) | Quick Win | 1 | 1 | 0 | 0 | Doc-only sweep of CLAUDE.md/architecture/plan-doc claims stale against code (outside the refresh agents' edit scope): Graphics elevation-RTT format (R16 not R32), BufferManager occupancy buffer (write-only single index), PipelineManager descriptor-verify location (Global only), CommandBufferManager re-record framing, ParticleManager wind claim, Profile timer-by-name, Network dangling pointers, DataPacker↔ExportJobs circular delegation; plus `FrameUpdatePipeline.md` 64→32 Hz (via `/update-architecture-diagrams`), ShaderReview Overview fixed-item reconcile, Order.md LANDED-entry confirm, `ocean-fragment-shader-overview.md` per-user path. Root-CLAUDE.md item tracked-not-edited here; +items 15-17 (Graphics/Debug run): the Graphics "(BT_DEBUG only)" Debug bullet → kbDebugRender-gated, the Debug staging resize-not-reserve wording, the flush-position wording |
| 7 | [DataPacker/DataPackerLogFloatSpecException.md](DataPacker/DataPackerLogFloatSpecException.md) | Quick Win | 1 | 1 | 0 | 0 | **Decision plan (present options).** Root CLAUDE.md bans float format specs in `LOG(...)` (heap-alloc → trips the tracker), but DataPacker `Main.cpp` RDO-sweep LOGs use `{:4.2f}` — and DataPacker has no main-loop allocation tracker (suppression inert in tools builds). Scope the ban to allocation-tracked/engine builds (recommended) vs convert the LOGs vs leave-as-is. Doc-wording resolution; no code change expected. Root-CLAUDE.md edit lands under the normal gated process |
| 8 | [Frame/NavigationStraightLineSteeringRule.md](Frame/NavigationStraightLineSteeringRule.md) | Quick Win | 1 | 1 | 0 | 0 | **Decision plan (present options).** Collections hub "must use `NavQueryDirection`, never straight-line steering" is contradicted twice: `PlayersNavigation.cpp` (`:282-289`/`:338-345`/`:396-405`) falls back to straight-line normalize when nav returns near-zero (intentional failure fallback), and `SpaceshipsNavigation.cpp` never calls `NavQueryDirection` (reactive direct steering by design). Scope the rule to nav-destination entities + sanctioned fallback (recommended, doc-only) vs soften vs change the code (de-recommended — alters deterministic sim). Doc resolution; code option is a separate sim-behavior plan |
| 9 | [Graphics/ParentRuleFramingReconciliation.md](Graphics/ParentRuleFramingReconciliation.md) | Quick Win | 1 | 1 | 0 | 0 | **Decision plan (present options).** Three parent-rule framings in tension with leaf realities: deposit-shader EWNS convention vs `HexShieldLighting.frag`'s center-normal/zero-fallback (narrow to light-source deposit shaders); engine layer-violation wording vs `ImGuiManager` owning `game::*Screen` members (align with the by-design `game::gp*`-read position); "Managers: singletons via `gp*`" vs `MemoryManager` (free functions) / `DebugDraw` (static); plus a scope ruling on whether a broader Graphics-managers file-docs consistency pass is wanted. Doc-wording only; root/`Engine/Source` edits gated |
| 10 | [Graphics/NvidiaPipelineHangAttributionReconcile.md](Graphics/NvidiaPipelineHangAttributionReconcile.md) | Quick Win | 1 | 1 | 0 | 0 | Reconcile the conflicting NVIDIA pipeline-creation-hang attribution: `Model/ModelCommon.h:46` blames "large mat4 arrays", `Shaders/CLAUDE.md` "Known Issues" blames `inverse()`, and `Model/CLAUDE.md` conflates both under one `inverse()`/pipeline-hang name. Investigate whether these are two distinct triggers (document both distinctly) or one mis-attribution; make the three sites consistent. Doc-only (one source-comment line); mitigations (compact joints, CPU-precomputed inverses, no large `mat4[]`) all stay |
| 11 | [Audio/Architecture_ThreadContractDocs.md](Audio/Architecture_ThreadContractDocs.md) | Quick Win | 1 | 1 | 0 | 0 | Comment/doc-only: document the main-loop temporal-exclusion contract on `StaticVoices`' lock-free listener/fade fields (main-thread `UpdateListenerPosition` writes vs worker-thread `PlayOneShot3d` reads — race-free only via the ClientUpdate→Render→audio sequencing in `Main.cpp`) and `GetStreamCount`'s deliberate lockless read; fix two `Audio/CLAUDE.md` nuances ("mark-only passes" vs the invalidation `Stop` shortcut; `Clear`-under-mutex vs `Update`'s deferred drain). Land last in a co-scheduled Audio session |
| 12 | [Debug/Architecture_LibraryReplacement.md](Debug/Architecture_LibraryReplacement.md) | Small | 2 | 3 | 1 | 0 | Replace the five hand-maintained `engine::EnumToString` VkEnum→string maps (`EnumToString.h`, ~400 lines, ~293 entries, five heap-allocating static-init maps) with the SDK-shipped generated `vulkan/vk_enum_string_helper.h` (Apache-2.0, Vulkan-Utility-Libraries — already on the `$(VK_SDK_PATH)` include path; Volk/Vma SDK-header precedent). ~18 Graphics call sites simplify (the `Convert` workbuffer/RAII plumbing drops); permanently kills the table-lags-SDK class — the VkResult map is missing driver-plausible `VK_ERROR_UNKNOWN` (+3 more), the colorspace map `VK_COLOR_SPACE_DISPLAY_NATIVE_AMD`, each a `DEBUG_BREAK` inside the logger on a legitimate driver value. Accepted change: unknown values render "Unhandled VkX" instead of tripping the SDK-lag tripwire. Client-only logging, no CRC/determinism exposure. One grill decision: ThirdParty library approval (policy-required) vs fallback hand-table refresh |
| 13 | [File/Refactor_ClientGuardAndWideApi.md](File/Refactor_ClientGuardAndWideApi.md) | Quick Win | 1 | 2 | 1 | 0 | Two FileManager mechanical items: remove the redundant nested `#if defined(BT_CLIENT)` at `FileManager.cpp:370-374` (already inside the line-320 client block; first body line mis-indented) and switch the ctor's ANSI `GetTempPath`/`GetModuleFileName` (char buffer → `-A` variants) to the `W` forms so non-ACP-representable user/install paths stop mangling `mTempDirectory`/`mDataDirectory`. Startup-only, no CRC/determinism exposure. Nested-guard item evaporates if `AnimationDataLoadPlacement` lands first — check before executing |
| 14 | [Frame/Refactor_DiagnosticAndDeadCodeRemoval.md](Frame/Refactor_DiagnosticAndDeadCodeRemoval.md) | Quick Win | 1 | 2 | 1 | 0 | Four verified zero-dependency deletions: self-marked `kTemp` fill diagnostic logging per-cell at kInfo in all configs (`IslandChainPlacement.cpp:397-414`), O(vertices) bounds scan feeding a compiled-out kVerbose LOG every cell build (`NavBuild.cpp:603-626`), dead `TimeStep::GetAverageDelta` (zero callers; returns a never-written value in 2 of 3 configs), dead `Alignments::RemoveAlignment` (zero callers). NavBuild item co-schedules with `NavBuildSplit` |
| 15 | [Frame/Architecture_IncludeHygiene.md](Frame/Architecture_IncludeHygiene.md) | Quick Win | 1 | 2 | 1 | 0 | Remove unused includes (`IslandTerrain.cpp` PipelineManager.h + Data.h; `TimeStep.cpp` Game.h→game Frame.h), 3 redundant explicit `Pch.h`, dead `game::Frame` fwd-decl; add the missing direct includes headers currently get via load-bearing PCH order (`Collision.h`→`Alignments.h` — the one edge making `Engine.h` non-self-contained; `GridCoord.h` into 4 headers; game `Frame.h` into `Collision.cpp`/`FrameBase.cpp`). Includes-only, compile-checked |
| 16 | [Frame/Architecture_LogDifferencesServerCollections.md](Frame/Architecture_LogDifferencesServerCollections.md) | Quick Win | 1 | 2 | 1 | 0 | Engine `LogDifferences` hardcodes `explosions`/`pushers` (`FrameBase.cpp:29-30/100-101`) while `Crcs()` folds `ServerCollections()` — a third shared collection would join the CRC but silently vanish from desync diagnosis; fold the same tuple (the `Frame/CLAUDE.md` contract already claims it does). Diagnostic-path only, no CRC change |
| 17 | [Frame/Architecture_CollisionHeaderSurface.md](Frame/Architecture_CollisionHeaderSurface.md) | Quick Win | 1 | 2 | 1 | 0 | Move `PendingCollisionResult`/`ZoneRange`/`ZonePair`/`LayerPairZones` (`Collision.h:68-112`; repo-wide grep: referenced only in Collision.h/.cpp) into the .cpp with forward decls (incomplete-`T` `std::vector` member decl verified legal), adding the missing member initializers while moving — ~45 lines of internals off a deep module's header |
| 18 | [Frame/Architecture_ContractDocGaps.md](Frame/Architecture_ContractDocGaps.md) | Quick Win | 1 | 1 | 0 | 0 | Doc/comment-only: record the intentional shader/CPU elevation divergences (undersea pow curve, LINEAR-vs-nearest sampling), the `FrameInterpolate::Update` dual-dt contract where collection authors look, and the Write/Read `BT_CLIENT` stream asymmetry (server-only save/load convention guard) |
| 19 | [Graphics/Architecture_InvariantHardening.md](Graphics/Architecture_InvariantHardening.md) | Quick Win | 1 | 1 | 0 | 0 | Two convention-held Graphics invariants get in-place guards: a comment at `gAnimationDataMap` (`AnimationData.h:41`) warning that the sim-phase animation clock it feeds is deliberately CRC-excluded (promoting it would couple determinism to client-only pack data), and a thread-contract `ASSERT` in `OneShotCommandBuffer::Execute` (shared one-shot pool/fence, "single-threaded" per `DeviceManager.cpp:217`) — must encode **non-concurrency**, not boot-thread identity (the dormant screenshot path constructs one on the `mSubmitMain` worker). Comment + debug-assert only |
| 20 | [Input/Architecture_InterfaceNarrowingAndDocs.md](Input/Architecture_InterfaceNarrowingAndDocs.md) | Quick Win | 1 | 2 | 1 | 0 | Narrow `RawInputManager`'s public surface to its real seam (grep-verified): zero-external-reader `mpbKeyboardKeysDown`/`mMouse` → private; single-source `mHwnd` (assign in `UpdateFocus`, drop the external `Main.cpp:155` write — focus-before-first-read proven via the `sbHasFocus` chain) then private; delete the vestigial `struct RawInput;` fwd-decl (`Game.h:19`); document `RIDEV_NOLEGACY` ImGui-keyboard starvation + the load-bearing manager-before-CreateWindow `Mouse`-singleton order in `Input/CLAUDE.md`. Client-only, visibility/doc only. Consolidates the parallel-authored `InputInterfaceTightening` (deleted at verification) |
| 21 | [Input/Refactor_RawInputHandling.md](Input/Refactor_RawInputHandling.md) | Quick Win | 1 | 2 | 1 | 0 | Pass `WM_INPUT` to `DefWindowProc` per the Win32 contract (`Main.cpp:577` `return 0;` → `break;` — fall-through to the trailing `DefWindowProc` at `:611` verified) and collapse `HandleRawInput`'s size-query + workbuffer + second-fetch (`RawInputManager.cpp:217-240`) into one `GetRawInputData` into a stack `RAWINPUT` (only fixed-size mouse/keyboard usages registered, no RAWHID; check the `(UINT)-1` error return). Client-only display-rate path |
| 22 | [Input/Architecture_GamepadDisconnectSnapshot.md](Input/Architecture_GamepadDisconnectSnapshot.md) | Quick Win | 1 | 2 | 1 | 0 | Gamepad disconnect branch (`RawInputManager.cpp:192-213`) clears thumbsticks+buttons but not `f2Dpad` — stale dpad keeps feeding ImGui menu nav (game `Input.cpp:99-102`, sole consumer) after disconnect while a menu is up; zero it in the clear block. Deletes the write-only `RawInput::f2Triggers` (zero consumers repo-wide) instead of zeroing it; update `Input/CLAUDE.md:21`. Display-rate only, no CRC/determinism exposure. Supersedes `GamepadDisconnectClear` (deleted at verification) |
| 23 | [Graphics/Architecture_DebugRenderDeadPrimitives.md](Graphics/Architecture_DebugRenderDeadPrimitives.md) | Quick Win | 1 | 2 | 1 | 0 | `DebugRender::Box`/`Sphere` are dead public API — zero callers repo-wide, full Debug-config GPU plumbing behind them (two never-drawn pipelines + unit meshes + per-frame recorded indirect draws and zero-writes). Remove (recommended; remove-vs-keep pre-staged for grill per the never-remove-features gate): declarations/definitions/`sTypes` rows, `kPipelineDebugBox`/`kPipelineDebugSphere` enum values + `CreateDebugRenderPipelines` rows, `mDebugBox`/`mDebugSphereVertexBuffer` meshes, the two `CommandBufferRecordMain.cpp` records, two doc mentions. Enum-shift verified serialization-free; client/Debug-config-only, no CRC/determinism exposure
| 24 | [Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md](Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md) | Quick Win | 1 | 1 | 0 | 0 | `RenderFrameMain` early-returns on `rActiveCoords.empty()` (`MainUniforms.cpp:163-166`), skipping every indirect-count zero-write while the record-once Main CB still submits — ghost-draw hazard, but verification traced `Game::ComputeActiveSet`/`Reset`/boot-prerender and proved the empty state unreachable on the client. Comment-only: document the never-empty invariant + ghost-draw consequence at the early return; defensive flush verified viable and pre-staged as the grill alternative. No CRC/determinism exposure
| 25 | [Memory/Architecture_AllocationTrackingFlagAndDocs.md](Memory/Architecture_AllocationTrackingFlagAndDocs.md) | Quick Win | 1 | 2 | 1 | 0 | Make `sbTrackingReady` (`MemoryManager.cpp:10`) `std::atomic<bool>` relaxed — formal data race: main thread writes (`Main.cpp:256/:317`) while live tracked threads (Multithreading workers, TextureUpload/FileManager load threads, DxDiag async) read per-allocation (`:19`); benign-by-sequencing today, fix mirrors `giAllocationsThisFrame`'s relaxed discipline. Plus `<new>`/`<cstdlib>` into `ExternalHeaders.h` (the allocator TU's std vocabulary rides MSVC-internal transitive includes), the `Memory/CLAUDE.md` DxDiag-`std::async`-participation example fix, and a `mimalloc-new-delete.h` fork-maintenance note (stock header has no `TrackAllocation` hook). Diagnostics-only, no CRC/determinism exposure |
| 26 | [Ui/Architecture_IncludeHygiene.md](Ui/Architecture_IncludeHygiene.md) | Quick Win | 1 | 2 | 1 | 0 | Remove the unused `CurveData.h` include from `WrapperBase.h:4` (no symbol used) — it silently drags the curve editor into the game PCH via `Pch.h:94-95` → game wrapper headers, defeating the documented wrapper recompile-isolation for that header; comment the load-bearing `Pch.h:92-97` include order (`ShaderLayouts.h` before `Engine.h` — the repo's sole include site for `shaders::` constants); anchor `<cmath>`/`<cstdio>` in `ExternalHeaders.h`. Includes/comments only, compile-checked |
| 27 | [Ui/Architecture_CurveDataDeadChangeDetection.md](Ui/Architecture_CurveDataDeadChangeDetection.md) | Quick Win | 1 | 2 | 1 | 0 | Delete `CurveData`'s zero-caller change-detection sub-interface — `Changed()` (`CurveData.h:40-48`), `ComputeHash()` (`:201-209`), `mPreviousSize`/`mfPreviousHash` (`:214-215`) plus their ctor inits (`:27-28`); the sole consumer `LightingUniforms.cpp:40-48` re-bakes every frame without polling, and the float-accumulator hash is weak anyway. Drop the stale `Ui/CLAUDE.md` contract sentence. Client-only header, dead code, compile-checked |
| 28 | [Ui/Architecture_WrapperContractDocs.md](Ui/Architecture_WrapperContractDocs.md) | Quick Win | 1 | 1 | 0 | 0 | Comment-only: document `Wrapper`'s unstated single-writer/sequenced-reader thread contract (writes in ImGui Prepare; wrappers read inside `Dispatch()` regions must have no runtime writers), the float-storage 2^24 exactness ceiling (real latent example: `VK_PRESENT_MODE_FIFO_LATEST_READY_KHR` = 1000361000 corrupts to 1000361024 in `gPresentMode`'s allowed set — never UI-exposed today), and `Toggle()`'s bool-only constraint (writes `mfCurrent` raw, bypassing snap/clamp/allowed-set). Land last of any co-scheduled Wrapper session |
| 29 | [Ui/Refactor_StyleMechanics.md](Ui/Refactor_StyleMechanics.md) | Quick Win | 1 | 1 | 0 | 0 | Mechanical sweep: drop five no-op `std::move` on `Wrapper` ctor prvalue temporaries (`WaterWrappersBase.cpp:70/88`, `GraphicsSettingsWrappersBase.cpp:7/9/15`), normalize the two MSVC-only `31i64`/`255i64` literals to the in-file `int64_t {N}` form, delete the dead `iCount` + `std::ignore` pair (`CurveData.h:151/171`), delete three stale `// DT: TEMP` markers on shipping water wrappers (`:116-118`), add two `Ui/CLAUDE.md` sentences (scratch Test sliders, combine-curve A/B scaffolding). Zero behavior; one grill question (document vs delete the Test sliders) |
| 30 | [Profile/Architecture_InvariantHardening.md](Profile/Architecture_InvariantHardening.md) | Quick Win | 1 | 2 | 1 | 0 | Engine-side `static_assert`s pinning the engine↔game enum contract the engine compiles against by name (`game::kCpuTimerFrameUpdate == kEngineCpuTimerCount` + counter sibling — today comment-only; an omitted game-enum initializer silently misroutes every game index into the engine arrays); `Create()` manager-ordering precondition comment + `ASSERT(gpSwapchainManager)`; `FormatCpuTimersText` takes `mCpuTimerMutex` via a new `LockCpuTimers()` accessor (formal data race vs submit-worker writes; sibling `LogTimers` already locks). Cross-thread-stop ASSERT dropped at verification (first-frame stop-before-start is load-bearing) |
| 31 | [Profile/Architecture_IncludeHygiene.md](Profile/Architecture_IncludeHygiene.md) | Quick Win | 1 | 2 | 1 | 0 | Move `Memory/MemoryManager.h` to the sibling that uses it (`ProfileManagerBase.cpp` reads `giAllocationsThisFrame` only via a fragile game-PCH chain through `CollectionMemory.h`; `ProfileScreens.cpp` includes it unused); replace unused `Game.h` with explicit `Graphics/Camera.h` (`gpCamera` is the only game symbol); delete dead `DeviceManager` fwd-decl; move the three .cpp-local screen-formatter decls into the header's `BT_CLIENT` span (signature drift currently link-time-only); guard comment on the header's zero-include `Engine.h`-position-#1 requirement; fix Profile CLAUDE.md:37. Satisfies the include-first prerequisite of `Engine/DeadCodeAndUnusedIncludesSweep` item 12 |
| 32 | [Profile/Architecture_DocAccuracy.md](Profile/Architecture_DocAccuracy.md) | Quick Win | 1 | 1 | 0 | 0 | Doc/comment-only: correct three Profile CLAUDE.md claims ("all entry points wrapped in `kbProfiling`" — cadence/formatters/accessors deliberately aren't; suppression "never pollutes its own counts" — the counter increments before the suppression check, only the `DEBUG_BREAK` tripwire is silenced; per-timer allocations are process-wide during the scope's window, not scope-local) + an intended-granularity comment on `mCpuTimerMutex` (coarse phase scopes, not per-entity). Lands last in the Profile session |
| 33 | [Network/Architecture_IncludeHygiene.md](Network/Architecture_IncludeHygiene.md) | Quick Win | 1 | 2 | 1 | 0 | Remove 3 unused includes (`NetworkCursor.h` ×2, `Game.h`); add `ServerSessionBase.cpp`'s missing self-include (sole deviation in the area); fwd-declare `game::StatusChange` in `ServerTypes.h` (rides a sibling header's decl via Engine.h ordering); fix `ServerSessionBase.h`'s inline-ctor complete-type fragility; move `Game.h` inside both build guards; add `GridCoord.h` to the 5 network headers riding game-PCH ordering (mirrors Frame hygiene). MemoryManager.h/player_t/aiResendTicks stay with the Engine sweep. Includes-only, compile-checked |
| 34 | [Network/Architecture_SpecDocReconciliation.md](Network/Architecture_SpecDocReconciliation.md) | Quick Win | 1 | 1 | 0 | 0 | Doc-only: `Network.md` resend cap is per-slot-per-call (×16 the spec's "per call") and its ACK spec omits the trailing 8-byte timestamp echo driving pipeline RTT; "channel 1 reserved" is false in `NetworkManager.h` + hub CLAUDE.md (the ACK stream rides it); narrow the hub's "full protocol flow" coverage claim (spec has no handshake/subscription/discovery); add the verified main-thread-only invariant line and the receive-vs-apply slot-activation contract to the CLAUDE.mds |
| 35 | [Network/Refactor_QuickWinMechanics.md](Network/Refactor_QuickWinMechanics.md) | Quick Win | 1 | 2 | 1 | 0 | Six mechanical fixes: bound the exactly-full-today `activeCoords[16]` stack array (engine supports 64); bounded-copy the non-null-terminated `string_view` address before `enet_address_set_host`; chrono-type `kiDiscoveryScanMs` (style rule 12); drop the dead `iTick` param chain (`SendNewSubscriptionFullStates` → game `SubscriptionUpdates`); hoist the slot-index check above the full LZ4 decompress in `ServerCoordFullState`; add the first `IsMainThread()` affinity assert at the `Poll` entries (helper exists, uncalled) |
| 36 | [Common/ThreadSafety_SharedStaticBuffers.md](Common/ThreadSafety_SharedStaticBuffers.md) | Small | 2 | 3 | 2 | 1 | **Theme.** Make the racy shared `static` char buffers per-thread across 3 sites: `Log`'s `spcLogBuffer` fallback (`Log.h:82`, `static thread_local`), `LogStackWalker::Emit`'s LOG (`StackWalker.h:97`, fed by `FilteredStackWalker::OnCallstackEntry` — fixed for free by the Log change; fault path stays alloc-free), and `WindowsUtils::HresultToString`/`LastErrorString` (`WindowsUtils.cpp:8/15` — return an owned `std::string`; tools/startup callers, no alloc-tracking concern) |
| 37 | [DataPacker/ExportTextureHeaderlessRawFormatClaims.md](DataPacker/ExportTextureHeaderlessRawFormatClaims.md) | Small | 2 | 2 | 1 | 1 | `ExportTexture::Handles` (`ExportTexture.cpp:28`) claims `.R8_UNORM`/`.R8G8B8A8_UNORM`/`.R16G16_UNORM`, but the raw-route gate `bRawTexture` (`:85`) omits them → they fall to the image-decode path that can't read headerless raw files (dormant support or latent trap). Plus `.R32_SFLOAT` is in `bRawTexture`/format-select but not in `Handles`. Reconcile the three sites to one consistent set: wire into the raw route (if used) or drop from `Handles` (if dead). Offline DataPacker only. One grill decision (resolved by an asset-tree search) |
| 38 | [Engine/DeadCodeAndUnusedIncludesSweep.md](Engine/DeadCodeAndUnusedIncludesSweep.md) | Small | 2 | 2 | 1 | 1 | Grouped dead-code/unused-include removal (each grep-verified, independent): ~12 Engine TUs with stale `Memory/MemoryManager.h` includes (post `AllocationTracking.h` move); unreferenced `SharedMembers` on `SpawnPlayerData`/`UpdatePlayerData`/`UpdateFleetData`; `game Input::GetGamepadMode()` (no callers); engine `Client.h` unused `game::player_t` alias; `ServerSend.cpp` `aiResendTicks`; `HexShield.vert:65` dead local; `QuadsAxisAlignedVisibleArea.vert` unconsumed location-6 varying (DataPacker recompile); `RawInputManager` mouse `RIDEV_INPUTSINK` (**behavior-verify**, defer if unsure); stale `Collection.h:18` `struct FrameBase;` forward decl (defined nowhere); unused `FrameInput::operator==` (`Input.h:60`, no callers — diff-skip compares `Crc()`). Any CRC/serialized Explosions field (`pfLightPercents`/`pfSmokePercents`) routes OUT to a member-removal plan, not this sweep. +Item 12 (Input/Memory run): the `CollectionMemory.h:3` stale `MemoryManager.h` include — the accidental every-TU distribution channel — paired with `ProfileManagerBase.cpp` gaining the direct include it currently rides that chain for (include first, then drop) |
| 39 | [Common/DocCanonicalHomeReconciliation.md](Common/DocCanonicalHomeReconciliation.md) | Small | 2 | 1 | 0 | 1 | **Decision plan (present options).** Six cross-doc canonical-home / pick-one-wording decisions from the CLAUDE.md audit: DataPacker bake/split sentinel-contract home (overlaps `StaleDocClaimsSweep` item 9's loop-break); `Shaders/CLAUDE.md` claims Water documents skip-normalize call sites it doesn't; "single `ThirdParty.lib`" duplicated in two ThirdParty docs; "Common.h aggregation" duplicated in `Common/CLAUDE.md` + root; `Water.frag:157-159` in-shader camera-height lerp vs Render's "lerp CPU-side / upload one float" rule (exception-vs-refactor — only one with a code branch); SmokeTrails vs WindTrails conflicting `uiFrameId` rationale. Five doc-only; root edit only for the Common.h item (tracked-not-edited). Resolve in `/external-grill-plan` |
| 40 | [Common/Flags.md](Common/Flags.md) | Small | 2 | 3 | 2 | 1 | `Flags<ENUM>::Toggle` (`Flags.h:84-107`) `DEBUG_BREAK`s + silently no-ops on a partially-set multi-bit mask (single-bit callers safe today, reachable footgun) — replace the 3-way branch with a plain XOR. The two `operator&` overloads return different kinds (`Flags & ENUM`→`bool`, `Flags & Flags`→`underlying_t`); rename the `Flags & Flags` intersection to a named `Mask()` accessor (option A) so the same `&` stops meaning two things. Only raw-int consumers `PushersUpdate.cpp:146,152`; serialized `meFlags` layout untouched (CRC/replay safe) |
| 41 | [Common/LogTypes.md](Common/LogTypes.md) | Small | 2 | 2 | 1 | 1 | Derive `kiLogCategoryCount` from a `kCount` enum sentinel + `static_assert` the downstream table lengths (`kpcLogCategoryNames`, `keLogLevels`, `gLogRingBuffers`) so the hand-synchronized category lists can't silently drift into an out-of-bounds index. The intentional global-namespace enumerator aliases (the unqualified-`kError` LOG design) and the `<cstdint>` include were dropped |
| 42 | [Graphics/LightingDepositEdgeFadeVisibleWindow.md](Graphics/LightingDepositEdgeFadeVisibleWindow.md) | Quick Win | 1 | 1 | 1 | 1 | After `LightingDepositSpreadFullPort` 3x-bumped the deposit texture, `LightingDepositEdgeFade` (`ShaderFunctions.h:257`) fades within 5% of the now-off-screen deposit-texture edge instead of the screen edge, so on-screen lights no longer soften near the frame (consumers: `PointLight`/`AreaLight`/`HexShieldLighting` frags). Decide intent: confirm texture-boundary-only (document + close) or reframe the fade to the visible window via `WorldToVisibleArea(world, f4VisibleArea)`. Shader-only, client/graphics-only |
| 43 | [Engine/WrapperGetIndexFailLoud.md](Engine/WrapperGetIndexFailLoud.md) | Quick Win | 1 | 2 | 2 | 1 | `Wrapper::GetIndex()` (`WrapperBase.h:185`) `DEBUG_BREAK`s then `return 0;` on a value absent from `mAllowed` → release-silent index-0 snap on the discrete-enum settings `Set()` path (`:154`). Audit reachability first (stale/older-version persisted values via `ClientSettings`); if bug-only, swap to `ASSERT(false)` (keep the now-unreachable trailing return, `Assert` isn't `[[noreturn]]`); if legitimately reachable, clamp/snap at the Set/load boundary instead of throwing. Sibling of the fail-soft-`default:` hardening; `EnumToString.h`'s `UNKNOWN_VK_ENUM` fallback explicitly excluded (working-as-intended diagnostic) |
| 44 | [Ui/MenuUtilsAppendUtf8DanglingPointer.md](Ui/MenuUtilsAppendUtf8DanglingPointer.md) | Small | 2 | 2 | 1 | 1 | `game::AppendUtf8` (`MenuUtils.cpp:6-37`) opens its own workbuffer frame, encodes UTF-8, then returns `View().data()` after the local arena pops at function return → dangling use-after-pop pointer that works today only because all ~12 client-only menu call sites (`PauseMenuScreen`/`MainMenuScreen`/`SoundMenuScreen`) consume it inline. Return an RAII handle (game-side wrapper holding the arena by value, `operator const char*`) so the frame stays open through the caller's full-expression, preserving the per-call string isolation the `PauseMenuScreen` width loop needs; fix the misleading `MenuUtils.h:27` lifetime comment. Latent correctness, mechanical + compile-checked, client-only |
| 45 | [Ui/ChineseFontPushMissingScreens.md](Ui/ChineseFontPushMissingScreens.md) | Small | 2 | 2 | 1 | 1 | Only `MainMenuScreen.cpp` pushes `gpImGuiManager->mpChineseFont`; `PauseMenuScreen.cpp` (5 `TranslatedString` buttons + the `CalcTextSize` width loop) and `SoundMenuScreen.cpp` (`kStringDefaults`) render translated strings under the EFIGS-only default font (the two fonts are separate atlases, no glyph merge — `ImGuiManager.cpp:64-74`), so a Chinese locale shows missing/box glyphs there (and mis-measured button widths). Add the gated push/pop mirroring `MainMenuScreen` (or narrow the `Screens/CLAUDE.md` pattern if intentionally exempt); audit the other localized screens for the same gap. Client-only, visual |
| 46 | [Graphics/ParticlesSpawnStructCopyGlslangBug.md](Graphics/ParticlesSpawnStructCopyGlslangBug.md) | Quick Win | 1 | 1 | 1 | 1 | `ParticlesSpawn.comp:88` does a whole-struct SSBO→SSBO copy (`particles.pParticles[i] = spawn.pParticles[...]`) from a scalar-block-layout SSBO — the same pattern that hits glslang's trailing-field-drop bug documented at `Terrain.vert:70-72`. `Particles/CLAUDE.md` claims it is safe because it is buffer-to-buffer "not into a local". Investigate whether the bug affects SSBO→SSBO stores (SPIR-V inspection or documented-symptom reasoning + the at-risk trailing field): if safe, tighten the comment + close; if latent, split into per-field stores. Shader-only; particles are non-deterministic VFX (no CRC/replay exposure). DataPacker recompile + visual smoke-test if fixed |
| 47 | [Network/BroadcastTickAllocationGuardScope.md](Network/BroadcastTickAllocationGuardScope.md) | Quick Win | 1 | 1 | 1 | 1 | `ServerSession::BroadcastTick` (`ServerSession.cpp:65-78`) opens a **function-scope** `ScopedSuppressAllocationTracking` that stays in scope across its call to `BroadcastStatusChanges` — which the Server `CLAUDE.md` invariant documents as deliberately **unguarded** "so the tracker stays armed". Since suppression is a thread-local counter (`AllocationTracking.h`), the caller's blanket guard nullifies the documented armed intent. Narrow the caller's guard to its real allocating callees (leaving `BroadcastStatusChanges` outside, recommended) vs fix the doc to admit the caller suppresses. Server-only, debug-tracking only, no CRC/wire exposure. One grill decision |
| 48 | [Common/AggregationAndScopeRuleQualifiers.md](Common/AggregationAndScopeRuleQualifiers.md) | Small | 2 | 1 | 0 | 1 | **Decision plan (present options).** Four broadly-stated parent/root rules vs sanctioned leaf exceptions, each resolved by an "unless noted at leaf" qualifier: ExternalHeaders.h "all external" (narrow to engine-consumed; DataPacker locals exempt); BT_CLIENT "narrowest scope" vs whole-file vcxproj+`Engine.h` gating; Engine.h "single aggregation" vs 3 deliberate exceptions (game `Input.h`, `NetworkCursor.h`, Ui wrappers); engine→game compile-time-constant read (`keNetworkSimulation`). Doc-wording only; root/`Common`/`Engine/Source` edits gated |
| 49 | [Engine/SingletonPublishGlobalGuardSweep.md](Engine/SingletonPublishGlobalGuardSweep.md) | Small | 2 | 2 | 1 | 1 | Extend the just-landed `ThreadLocal` ctor `ASSERT(gpThreadLocal == nullptr)` one-instance-ownership guard to the other 28 publish-global singletons (`gpFoo = this` ctor / `gpFoo = nullptr` dtor): add a ctor entry `ASSERT(gpFoo == nullptr)` (fail loud on double-construct) + wrap the dtor null in `if (gpFoo == this)` (order-independent teardown, mirrors `Multithreading.cpp:29`). Spans Common (`DiagnosticLog` array-slot variant), Engine network/input/audio/frame/file/graphics managers, game-layer `Camera`/`Game`/`ProfileManager`/`*Session`, DataPacker `FileManager`. Debug-`ASSERT`-only, no CRC/network/determinism exposure, compile + silent-on-boot checked |
| 50 | [Graphics/TextureCacheValidatePayloadSize.md](Graphics/TextureCacheValidatePayloadSize.md) | Small | 2 | 2 | 1 | 1 | Sibling of this session's `TextureCache` header-guard landing: validate the cached-texture **payload** in `TryLoadCachedTexture` (`TextureCache.cpp:232-239`). `header.iDataSize` is never checked against the size `Texture::UploadImageData` computes from the validated dims/format (`Texture.cpp:248-256`), so a header-valid file with too-small `iDataSize` overreads `data` in the upload `memcpy` (UB) and a negative `iDataSize` blows up the `std::vector` ctor; the payload `read()` at `:233` is also unchecked (truncation → zeroed texels). Extract the per-mip `SizeInBytes` loop into a shared helper, reject on `iDataSize != computed` before allocating + on `!fileStream` after the read; fold in the mislabeled "sourceCrc mismatch" `kWarning` at `:227`. File read = trust boundary (policy-compliant). Client-only, startup/pipeline-recreate, no CRC/determinism exposure |
| 51 | [DataPacker/ExportJobCleanPathCachedChunkReadCheck.md](DataPacker/ExportJobCleanPathCachedChunkReadCheck.md) | Quick Win | 1 | 1 | 1 | 1 | Sibling of this session's `CheckDirty` sentinel-guard landing: the clean-path cached-chunk payload `read()` in `ExportJob::RunExport` (`ExportJob.cpp:145`) has no stream-state check before the bytes are baked into the `.pack` — a short/failed read (TOCTOU only: a chunk file truncated/deleted between `CheckDirty` and this read) silently emits resize-zeroed chunk bytes. Add `ASSERT(fileStream)` after the read (recommended; matches the file's fail-loud posture) or fall back to the dirty `Export()` path — one grill decision. Offline DataPacker only, no runtime/CRC/determinism exposure. Quick Win |
| 52 | [Common/DeterminismHandlerMutexReentry.md](Common/DeterminismHandlerMutexReentry.md) | Quick Win | 1 | 1 | 1 | 1 | `sMutex` sibling of the just-landed `gDbgHelpMutex` recursive/`try_to_lock` decision: the SEH-translator/invalid-parameter/vectored-exception handlers (`Determinism.cpp:24/46/80`) all `try_to_lock` the file-static `std::mutex sMutex` (`:6`), which is formal UB on owned re-entry (a nested fault inside a handler holding `sMutex`). MSVC-benign today (SRWLOCK `try_lock` returns false on an owned lock); fix is `std::recursive_mutex` (mirror `gDbgHelpMutex`) or documented accept — one grill decision. Fault-path-only, no determinism/CRC/version exposure |
| 53 | [Common/DeterministicPowSimPaths.md](Common/DeterministicPowSimPaths.md) | Small | 2 | 4 | 3 | 1 | Follow-up to the landed `DeterministicSinCos` sweep: strip the two remaining libm `pow` sites on the shared-CRC sim path (both builds). `PushersInterpolate::ApplyPush` (`PushersUpdate.cpp:173`) `XMVectorPow` → exact repeated-multiply — the exponent is provably integer (`pfPowers` only ever `kfSpaceshipPusherPower=3.0f` / `kfPlayerPusherPower=2.0f`, no slider), result flows `ApplyClampedPush`→CRC'd velocity. `SpawnDeathExplosions` (`PlayersCombat.cpp:476`) `std::pow(base, kfDeathRadialPower=0.3f)` (CRC'd spawn `vecPosition`) → restructure the easing curve (`sqrt`/poly, an artistic-feel change — grill) or a general Padé `common::DeterministicPow` exp2/log2 helper in `MathUtils.h` (mirror `DeterministicSinCos`/`ExponentialDecay`). `XMVectorPow` routes to libm `powf` per component (SDK `.inl` verified) — not a self-contained polynomial. No serialized-layout/`kiVersion` bump; locally-recorded replays reset (same accepted class as sincos) |
| 54 | [Audio/Architecture_IncludeHygiene.md](Audio/Architecture_IncludeHygiene.md) | Quick Win | 1 | 1 | 1 | 1 | Remove the unused `Game.h` include from `AudioManager.cpp`; add the missing direct includes the Audio files currently ride `Engine.h`/PCH ordering for (`StaticVoice.h` in `StaticVoices.h` for the `std::vector<StaticVoice>` member, `StreamingVoice.h` in `StreamingVoices.cpp`, `File/FileManager.h` in both voice .cpps, `Sounds.h` for `sound_t` in `StaticVoice.h`). `MemoryManager.h` removals stay with `Engine/DeadCodeAndUnusedIncludesSweep.md` |
| 55 | [Audio/Architecture_AudioHelperPlacement.md](Audio/Architecture_AudioHelperPlacement.md) | Small | 2 | 2 | 1 | 1 | Relocate `DestroyXAudio2SourceVoice` (inline in top-level `AudioManager.h:67-85`, reads `gpAudioManager`) + `VolumeToPower` (`StaticVoice.h`) into a new `AudioUtility.h`, re-signatured to take `AudioEngine*` — breaks the AudioManager→voices→AudioManager semantic cycle and the dtor-order brittleness (`gpAudioManager` nulled before members destruct); `StreamingVoice` gains an engine pointer. New client-only header → `Engine.h` + vcxproj wiring |
| 56 | [Audio/Architecture_OneShotPathCleanup.md](Audio/Architecture_OneShotPathCleanup.md) | Small | 2 | 2 | 1 | 1 | `PlayOneShot3d` advances the presentation RNG and locks **before** the inner `kRecalculated` replay guard — contradicts the documented "replay never mutates audio state"; hoist the guards. Replace the recursive one-shot mutex with `std::mutex` + private `PlayOneShotLocked` (dedupes the duplicated pitch-randomization block); public `PlayOneShot` returns `void` (no caller captures the raw `IXAudio2SourceVoice*`, which dangles on device reset). Grill note: surface the randomized pitch to `Apply3dVolume` |
| 57 | [Audio/Refactor_VoiceStartClickDiscipline.md](Audio/Refactor_VoiceStartClickDiscipline.md) | Quick Win | 1 | 2 | 2 | 1 | New persistent voices start at unattenuated 2D volume (`StaticVoice` ctor `SetVolume`+`Start`) while the reactivation path carefully does `SetVolume(0)` before `Start` — audible pop window until the same-frame `Apply3dVolume`; zero the ctor volume and apply the 3D mix at the creation site. Also `LoadXAudio2SourceVoice`'s `bool bOneShot, bool b3d` pair → `common::Flags<LoadVoiceFlags>` |
| 58 | [Audio/Refactor_AudioDeviceFallback.md](Audio/Refactor_AudioDeviceFallback.md) | Small | 2 | 2 | 1 | 1 | `AudioManager` ctor early-returns (`GetDefaultAudioEndpoint` failure — no log at all; null `GetId`/`EnumAudioEndpoints`) skip the documented fall-back-to-first-active-device block → silent no-audio on machines with active devices; restructure to best-effort default-id lookup + always-enumerate + fallback. Promote failure logs `kDebug`→`kWarning` (`kAudio`'s compile-time threshold compiles `kDebug` out). OS-API trust boundary; startup-only |
| 59 | [File/Architecture_IncludeHygiene.md](File/Architecture_IncludeHygiene.md) | Quick Win | 1 | 1 | 1 | 1 | Remove the unused `Data/Data.h` include (`FileManager.cpp:7` — every `data::` symbol arrives via `DataTypes.h` from `FileManager.h:3`); add the direct includes the File sources ride `Engine.h` ordering for: `DifferenceStream.h` → `File/FileManager.h` (`FileFlags_t`/`gpFileManager`, retires the load-bearing `Engine.h:11` ordering comment) and `FileManager.cpp` → `TextureUploadManager.h`/`AnimationData.h` under `BT_CLIENT`. Include-only, compile-checked; the `AnimationData.h` item evaporates if `AnimationDataLoadPlacement` lands first |
| 60 | [File/Architecture_FileManagerEncapsulation.md](File/Architecture_FileManagerEncapsulation.md) | Small | 2 | 2 | 1 | 1 | Make the zero-external-user public members `mpChunkLocations`/`mLoadingFuture` private (`FileManager.h:142-143`, grep-verified repo-wide) and fix `GetEagerChunkMap() const` laundering its future-drain mutation through `gpFileManager->` (`FileManager.cpp:385-395`) — non-const method (all call sites already use the non-const global) or `mutable` future. Compile-checked visibility/const-correctness only; co-schedule with `LoadThreadLifecycleSafety` (same function) |
| 61 | [File/Refactor_FunctionDecomposition.md](File/Refactor_FunctionDecomposition.md) | Small | 2 | 2 | 1 | 1 | Mechanical extraction of the three oversized/deep-nested File functions: `LoadPackFiles` (~177 lines → named stages, preserving the pool-walk iteration-order invariant from `File/CLAUDE.md`), `LoadChunk`'s 4-deep sector-aligned copy loop → helper, and the ~107-line `DifferenceStreamReader` ctor → per-file-section helpers (checksum stage stays clear-and-continue). Behavior, lock scopes, and state transitions unchanged. Sequence after `AnimationDataLoadPlacement` (shared `LoadPackFiles` lines) |
| 62 | [Frame/Architecture_BaseHeightWrapperDeterminism.md](Frame/Architecture_BaseHeightWrapperDeterminism.md) | Small | 2 | 3 | 2 | 1 | `gBaseHeight` is a runtime-tunable Ui `Wrapper` (`WrapperBase.h:214`) read by the deterministic sim — engine NavQuery waypoint Z/thresholds plus ~8 game sim sites incl. spawn positions; binding it to a slider would desync every nav/spawn decision with no parity check. Convert to `constexpr float kfBaseHeight = 6.0f` (bit-identical — no CRC/replay impact) across the complete ~25-site grep-derived reader list. Grill: constant's home |
| 63 | [Frame/Architecture_DeterminismGuards.md](Frame/Architecture_DeterminismGuards.md) | Small | 2 | 2 | 1 | 1 | Turn two comment-only determinism invariants into debug asserts: at-most-two entries per cell-edge key in `ChainEdgesIntoPolygons`' unordered_multimap (guards future marching-squares edits from bucket-order-dependent NavData), and a thread_local in-tick RAII flag (set inside `RunFrameTick`) making the `GlobalElevation`/`GlobalNormal` Frame Purity Constraint fail fast. Debug-only, zero release change |
| 64 | [Frame/Refactor_StyleMechanics.md](Frame/Refactor_StyleMechanics.md) | Quick Win | 1 | 1 | 1 | 1 | Mechanical style sweep: two raw `.m128_f32[0]`→`XMVectorGetX` (`NavQuery.cpp:616/653`), `.at()` consistency, `std::memset`, designated init, strip 12 misleading `[[maybe_unused]]` (`FrameBase.cpp`), template-param naming, `cols` rename, redundant `inline`, stateless `IslandChainPlacement` class→free function. Fold items into whichever co-scheduled session owns each file |
| 65 | [Graphics/Architecture_IncludeHygiene.md](Graphics/Architecture_IncludeHygiene.md) | Quick Win | 1 | 1 | 1 | 1 | Include/fwd-decl drift across the seven Graphics top-level pairs: CameraBase.cpp redundant `Pch.h` + unused game `Camera.h` + wrong wrapper header (its three wrappers live in `WrapperBase.h`); GraphicsUtils.cpp unused `Game.h`; Graphics.cpp missing direct `Ui/WrapperBase.h` (5 wrappers ride transitive pulls); Screenshot.cpp missing direct `TextureCache.h` (non-aggregated); Islands.h stale `game::Frame`/`engine::Texture` fwd decls. `MemoryManager.h` removals stay with `Engine/DeadCodeAndUnusedIncludesSweep`. Compile-checked, no grill decisions |
| 66 | [Graphics/Architecture_RenderInterpolatesOwnership.md](Graphics/Architecture_RenderInterpolatesOwnership.md) | Small | 2 | 2 | 1 | 1 | `Graphics.h:103` holds `mRenderInterpolates` (by-value `game::FrameInterpolate` map) that Graphics never touches — populated/pruned by `GameBase::Render` + the Main.cpp boot path, then passed back in as a parameter; the by-value game-typed member compiles only via `Pch.h:96-97` include ordering. Move the member to `GameBase` (`BT_CLIENT` span, explicit game `Frame/Frame.h` include); Graphics keeps the const-ref parameter + fwd decl. Render-side only, no CRC/determinism exposure |
| 67 | [Graphics/Architecture_SharedConstantDuplication.md](Graphics/Architecture_SharedConstantDuplication.md) | Small | 2 | 2 | 1 | 1 | Four comment-synchronized constants get single definitions: the `8 *` snap block-size duplicated between `FullDetail`/`WaterFullDetail` (`Graphics.cpp:18/:51`, stale comment corrected); the 3840/8192 reference-resolution literals named once; engine `kfMinEyeHeight` 150.0f (`CameraBase.cpp:128`) single-sourced with game `kfEyeHeightMin` (`Camera.cpp:18`) as a protected `CameraBase` constant; glTF `uiInterpolation`/`uiTargetPath` magic numbers → named enumerators on `common::AnimationChannel` shared with the DataPacker writer (`SceneAnimationLoader.cpp:92-118`; values unchanged, no `.pack` rebuild). Eye-height item co-schedules with `Frame/ScaleEngineToMeters` |
| 68 | [Graphics/Architecture_EncapsulationSeams.md](Graphics/Architecture_EncapsulationSeams.md) | Quick Win | 1 | 1 | 1 | 1 | Add `SwapchainManager::ReleaseHandleForRecreation()` so the old-swapchain ownership transfer (stash+null at `Graphics.cpp:686-687`, consumed at `:285-286`) lives in one named method instead of a raw cross-manager member write. Verification dropped the tweaks-settings `GameBase` virtuals (engine→game free-function calls are accepted convention) and the resolution-policy relocation (organizational-only). Recreate-path smoke test |
| 69 | [Graphics/Refactor_RefreshAndCornerDecomposition.md](Graphics/Refactor_RefreshAndCornerDecomposition.md) | Small | 2 | 2 | 1 | 1 | Behavior-preserving dedup of the two copy-paste-dominated Graphics functions: `Graphics::Refresh`'s ~173-line settings ladder → per-setting poll helper (13 instances; every wrapper still polled once per call since `Changed()` mutates wrapper state; null-gates and the special cases stay straight-line), and `CameraBase::CalculateMatricesAndVisibleArea`'s four corner-unproject blocks → 4-entry table loop with the identical per-corner call sequence (bit-identical corners) |
| 70 | [Graphics/Refactor_ApiAndHotPathCleanups.md](Graphics/Refactor_ApiAndHotPathCleanups.md) | Small | 2 | 2 | 1 | 1 | Three mechanical cleanups: remove `OneShotCommandBuffer::Execute`'s dead `bWait` parameter (all 7 call sites pass `true`; the false-path no-fence submit would let the dtor free a pending CB); delete the zero-value `AabbIntersectsVisibleArea` pass-through (single call site → `common::AabbIntersectsArea`); convert `Game.cpp:268`'s per-frame `subscribedCoords` vector to workbuffer + `std::span` on `UpdateActiveIslands` (block already suppression-scoped — the win is the per-frame alloc itself). `InVisibleArea` adjust-fold dropped at verification (premise false: no call site passes partial adjusts) |
| 71 | [Input/Refactor_InputButtonFlags.md](Input/Refactor_InputButtonFlags.md) | Small | 2 | 2 | 1 | 1 | Convert `RawInput`'s `pMouseButtons[5]`/`pGamepadButtons[8]` parallel bool arrays to `common::Flags<MouseButtons/GamepadButtons>` (bitmask enums `: uint32_t` — `Flags` static_asserts unsigned underlying) per the root Flags-over-booleans pattern; rewrite the `Update` writers and game `MousePressed`/`GamepadPressed` edge detection (`Input.h:93-94` + direct reads). Never serialized, display-rate only, compile-checked in both builds (keyboard VK array stays) |
| 72 | [Ui/Architecture_WaterWrapperInvariants.md](Ui/Architecture_WaterWrapperInvariants.md) | Quick Win | 1 | 1 | 1 | 1 | Reconcile the stale water-normal pairing: `WaterWrappersBase.cpp:7-8` claims defaults 0/1/11 "must match `TextureManager::kiWaterNormalSeaWavesIndex`" but the actual defaults are 12/15/2 (retuned without updating comment or constant; the constant has zero code consumers — likely both dead, git-history check at execution); fix the `.h:104-109` vs `.cpp:57-60/70/88` intra-pair declaration-order drift. Comments/dead constant/ordering only, no value changes |
| 73 | [Ui/Refactor_WaterZOffsetTempDecision.md](Ui/Refactor_WaterZOffsetTempDecision.md) | Quick Win | 1 | 1 | 1 | 1 | **Decision plan (present options).** `gWaterZOffsetTemp` is `// DT: TEMP` end to end (wrapper `WaterWrappersBase.h:81`, uniform member `fWaterZOffsetTemp` `ShaderLayoutsBase.h:392` written at `GlobalUniforms.cpp:528`, consumed `Water.vert:53/80`, slider label "DT: TEMP Z Offset") yet ships as a live tunable, default 0. Promote (rename the chain) vs remove (delete the chain; value-neutral at the default). Either option needs a DataPacker shader recompile (`GlobalLayout` member) |
| 74 | [Profile/Architecture_ClientServerGuardScope.md](Profile/Architecture_ClientServerGuardScope.md) | Small | 2 | 2 | 1 | 1 | `UpdateProfileText`'s whole-body `BT_CLIENT` gate (`ProfileManagerBase.cpp:392-431`) wraps build-shared smoothing + the only `giAllocationsThisFrame` reset → hoist the shared prefix and swap `ServerDisplay.cpp:80`'s direct `SmoothCpuTimers()` for `UpdateProfileText()` (deletes the prose-only server contract; the server counter finally resets per tick); gate the GPU-timer enum/struct/array/accessor client-side (~34 KB dead `Smoothed` rings on the server today); mimalloc fields → `BT_SERVER && !ENABLE_CRT_DEBUG_HEAP` (writer/readers are ServerDisplay-only). Profiling/display only, no CRC/determinism exposure |
| 75 | [Profile/Refactor_InFunctionCleanups.md](Profile/Refactor_InFunctionCleanups.md) | Small | 2 | 2 | 1 | 1 | `CpuStop`'s two positional bools (`CpuStop(timer, true, true)`) → `common::Flags<CpuStopFlags>` across 17 call sites (root Flags-over-booleans; `LoadVoiceFlags` precedent); decompose ~158-line `FormatGpuScreen` along its three existing arena-scoped blocks (graphics info / timer rows + annotations / VMA stats); empty ctor/dtor → `= default`. Co-schedule with `SmoothNowDoubleLatch` (same `CpuStop` function) |
| 76 | [Network/Architecture_PausedServerSubscriptionQueueDrop.md](Network/Architecture_PausedServerSubscriptionQueueDrop.md) | Small | 2 | 4 | 3 | 1 | `Server::Poll` clears `mPendingNewSubscriptions`/`mPendingResyncClientIds` every poll, but their consumers run only in the per-tick broadcast path — a subscribe accepted + ACKed while the server is paused (`iFullTicks == 0`) is wiped before any full state sends, wedging the client in `kWaitingFullState` (no timeout exists; resync requests drop the same way). Consume on poll (`PreTickNetwork`, where the other drains live) or persist-until-consumed — grill. Send-once-per-subscription preserved; no wire change |
| 77 | [Network/Architecture_ClientBuildServerTransportGate.md](Network/Architecture_ClientBuildServerTransportGate.md) | Small | 2 | 2 | 1 | 1 | Client binary compiles/links ~1,100 dead lines of Server transport on a stale `Server/CLAUDE.md` rationale ("shared headers reference `ClientConnection`" — grep: all external refs are server-vcxproj-only; `Engine.h` gates `Server.h` under `BT_SERVER`); enforcement is asymmetric (Client/ double-protected, Server/ unprotected). Remove the 5 files from the client vcxproj + add `BT_SERVER` wraps (or re-document if keeping both halves compiling is intentional — grill). Build-graph only, compile-self-verifying |
| 78 | [Network/Architecture_NetworkSimPlumbingDedup.md](Network/Architecture_NetworkSimPlumbingDedup.md) | Small | 2 | 2 | 1 | 1 | The compile-time network-sim plumbing exists ×5 across the seam: `DispatchIncoming`+delay block line-identical in `Client.cpp`/`Server.cpp` (incl. the `miTimeMultiply` fast-forward), purge logic ×3 with two distinct predicates (slot-channel ×2, peer ×1). Extract shared helpers into `NetworkSimulation.h` (`if constexpr` zero-cost preserved); fold the clock-seeded `Random01` + drop-counter function-local statics into a struct owned beside the delay queue, constant-seeded (reproducible sim runs) |
| 79 | [Network/Architecture_ClientSessionBoundary.md](Network/Architecture_ClientSessionBoundary.md) | Small | 2 | 2 | 1 | 1 | Three seam cleanups: move GUID disk persistence out of `Client`'s packet handlers (`SendHello` load / `ServerConnectionResponse` atomic write — the directory's only `gpFileManager` ref) up to `ClientSessionBase`; add intent-named `Client::CancelSubscription` (+ reset-all op for the game force-reset path) replacing the session's non-const `GetCoordSlots()` mutation; delete or document the write-only `gpNetworkManager` global (grep: zero readers — RAII-only) |
| 80 | [Network/Refactor_CoordUpdateAllocAndJitter.md](Network/Refactor_CoordUpdateAllocAndJitter.md) | Small | 2 | 3 | 2 | 1 | `ServerCoordUpdateOrResend` allocates ~200 KB per coord-update packet (`resize(1024)` of POD `StatusChange`) and the shrunk vector keeps full capacity through `std::move` into `CoordFrames::serverUpdates` — megabytes of steady-state slack; decompress into workbuffer scratch + exact-size `assign` (trivially copyable — no construction hazard). Plus: the jitter expectation `1'000'000/kiTickRate` is timescale-blind, inflating `targetBehind` under debug timescale with slow `Smoothed` decay — skip-sample vs scale at grill. Client receive only, no CRC exposure |
| 81 | [Network/Refactor_ClientStateFlags.md](Network/Refactor_ClientStateFlags.md) | Small | 2 | 2 | 1 | 1 | Root-CLAUDE.md Flags-over-bools: `Client.h` 6 bools → `common::Flags<ClientStateFlags>`, `ClientSessionBase.h` 4, `ServerTypes.h` 2 (none serialized — verified all wire writes are field-by-field). Mechanical but wide-touch; co-schedule with `ClientSessionBoundary` (same `Client.h` surface) rather than standalone |
| 82 | [Frame/BlastersTerrainMarchDegenerate.md](Frame/BlastersTerrainMarchDegenerate.md) | Small | 2 | 2 | 2 | 2 | `BlastersUpdate.cpp:293-310` terrain-impact back-search is degenerate — both lerp endpoints equal `vecPosition` and the loaded `vecVelocity` is unused, so effects spawn at the over-shot post-integration position + jitter (the "binary search" comment is also wrong — it is a linear march). Reconstruct the travel segment (`pos − vel·dt`, uses the dead load) to find the true surface crossing, vs delete the vestigial march. Determinism caveat: the impact block draws from the **shared** `randomEngine` (Blasters discipline) — the fix must not change draw count/order on either side. One grill decision |
| 83 | [Network/TryRelinkClientForLoadOrdering.md](Network/TryRelinkClientForLoadOrdering.md) | Small | 2 | 2 | 2 | 2 | **Decision plan (present options).** `ServerSession::TryRelinkClientForLoad` (`:524-551`) relinks load-path players in `mCoordFrames` map × array-index order with **no global-ID sort**, while the reconnect path `TryRelinkNewClient` (`ServerClientManager.cpp:122-164`) sorts by global ID (`:149-151`) "to preserve creation order". The assign/spawn **send order** is client-visible. Add the sort to match reconnect (recommended if any `mClientOwnedPlayerIds` consumer is order-sensitive) vs document the intentional difference. Server-only, determinism-adjacent (replicated ordering, not shared-CRC) |
| 84 | [Frame/MissilesDeadExplosionRadiusField.md](Frame/MissilesDeadExplosionRadiusField.md) | Small | 2 | 2 | 2 | 2 | Remove the dead per-element `MissilesPostRender::pfExplosionRadii` (`Missiles.h:118`): zeroed at spawn (`Missiles.cpp:460`), memcpy'd forward (`:312`), in `SharedMembers` (CRC'd, `:132`) and `LogDifferences` (`:567`) — but **never read** (real AoE is the `HealthDamage.h` constant). Execute via the `add-collection-member` checklist in reverse; bumps `MissilesPostRender::kiVersion`→`Frame::kiVersion` (stale saves/replays reject cleanly — expected). Field is always zero so no CRC *value* changes, only layout; rebuild client+server together |
| 85 | [Graphics/PipelineGraphicsGlobalSet0Indexing.md](Graphics/PipelineGraphicsGlobalSet0Indexing.md) | Small | 2 | 2 | 2 | 2 | `Objects/CLAUDE.md` says global Set 0 is **always** framebuffer-indexed, but `BindGraphicsDescriptorSets` (`Pipeline.cpp`) indexes Set 0 with `iDescriptorSetIndex` = `mbPerCommandBuffer ? iCommandBuffer : 0`, so non-per-command-buffer plain draws bind `mGlobalDescriptorSets[0]` into every framebuffer's CB — while `BindComputeDescriptorSets` and `RecordDrawIndirect` always use `iCommandBuffer` (graphics path internally inconsistent). Investigate whether Set-0 globals are framebuffer-invariant for the affected pipelines (doc overstatement → reword) or a latent cross-frame GPU hazard (→ framebuffer-index Set 0 on the plain-draw path, mirroring compute). Client/graphics-only |
| 86 | [Network/StaticDataEpochCheckOnSubscribingSlot.md](Network/StaticDataEpochCheckOnSubscribingSlot.md) | Small | 2 | 2 | 2 | 2 | `Client::ServerCoordStaticData` (`ClientReceive.cpp:251`) applies the epoch check to `kWaitingFullState` **and** `kSubscribing` slots, but a `kSubscribing` placeholder has no server-assigned epoch (its `ackState.uiEpoch` is never set by `SendSubscribe`), and the sibling classifiers `ClassifyFullState`/`ClassifyCoordUpdate` **skip** the epoch check for `kSubscribing` — which `Client/CLAUDE.md` documents as the rule. Could wrongly drop once-per-subscription static data (NavData) for a racing `kSubscribing` slot. Trace the slot lifecycle, then align to the siblings (remove `kSubscribing` from the condition, recommended) vs document a stricter static-data rule + seed the epoch. Client receive only — no shared-CRC/determinism exposure |
| 87 | [Graphics/InlineAreaMappingDryToHelper.md](Graphics/InlineAreaMappingDryToHelper.md) | Small | 2 | 1 | 1 | 2 | 8 shaders hand-inline the `VisibleAreaToWorld(uv, area)` inverse (verbose `(1-uv)*lo + uv*hi` lerp) instead of calling the shared `ShaderFunctions.h:21-33` helper (the lighting site `LightingSpread.frag:74` was just fixed). Route `Terrain.frag`/`Water.vert`/`WaterDisplacement.comp`/`SmokeSpreadCommon.h`/`SmokeOccupancyDilateRemap.comp`/`WindSpreadOne.comp`/`WindSpreadTwo.comp`/`WindOccupancyDilate.comp` through it after a per-site exact-inverse (Y-flip) equivalence gate; decide whether Smoke/Wind local helpers route through the shared helper or stay in-family. DataPacker recompile + visual smoke-test (not compile-checked) |
| 88 | [Graphics/WindHistoryResetOnRecreate.md](Graphics/WindHistoryResetOnRecreate.md) | Small | 2 | 2 | 2 | 2 | Wind sibling of the shadow `gbShadowTemporalReset` fix: a Graphics device-lost/settings recreate rebuilds `mWindTextureOne/Two` with undefined contents while `giWindTextureIndex` (`Render.h:36`) survives, and `gbWindClear`'s path (`WindUniforms.cpp:75-78`) only flips a flag relying on in-shader soft-decay to fade garbage over several frames — a soft transient, not one hard frame. Choose hard GPU clear (mirror smoke's `gbSmokeClear` + `kPipelineSmokeClearA/B`) vs reset-index re-seed (mirror shadow's reset). Smoke already fully mitigated. Playtest-validated |
| 89 | [Engine/VcxprojAndBuildConfigReconciliation.md](Engine/VcxprojAndBuildConfigReconciliation.md) | Small | 2 | 1 | 1 | 2 | Build-graph / IDE-view reconciliation (no runtime): game Debug `AdditionalIncludeDirectories` duplicate `..\..\Data` and omit the `..\..\..\Data` entry Profile/Release have; Prebuilts vcxproj unused `..\..\..\..\Common` include path; StackWalker orphan (`Prebuilts/Source/Engine/StackWalker.cpp` registered in no vcxproj; only `DataPacker/StackWalker.cpp` compiles) — register/delete/recategorize; `ThirdParty.vcxproj.filters:123` `cmft.cpp` missing `<Filter>`; `HexShieldWrappers.cpp` vestigial in server vcxproj. Two decision items (StackWalker, HexShieldWrappers) + four mechanical; item 6 (Input/Memory run): `RawInputManager.h` server `ClInclude` (partial-guard header compiles in both builds; IDE-only) |
| 90 | [Audio/Refactor_UpdateLifecycleDecomposition.md](Audio/Refactor_UpdateLifecycleDecomposition.md) | Small | 2 | 2 | 2 | 2 | Split the ~330-line `StaticVoices::UpdateLifecycle` (`StaticVoices.cpp:144-473`) into five named pass methods matching the `Audio/CLAUDE.md` pass vocabulary; replace the O(n²) `FadeOutCount()` lambda with a maintained `miFadeOutCount`; replace the `pActivatedIds` workbuffer array + linear membership scan with a transient `kActivatedThisFrame` voice flag. Pure decomposition — audible behavior unchanged; playtest |
| 91 | [File/Architecture_AnimationDataLoadPlacement.md](File/Architecture_AnimationDataLoadPlacement.md) | Small | 2 | 2 | 2 | 2 | Move the GLTF scene-chunk animation parse out of `FileManager::LoadPackFiles` (`FileManager.cpp:353-375` — hand-computed DataPacker layout offsets + direct write into the Graphics global `gAnimationDataMap`) into a Graphics-side consumer of `GetEagerChunkMap()`; the File layer goes format-agnostic. Client-only boot path, no CRC/determinism/`.pack`-layout exposure. One grill decision: boot call-site placement (post-future-drain vs async continuation) |
| 92 | [File/Architecture_LoadThreadLifecycleSafety.md](File/Architecture_LoadThreadLifecycleSafety.md) | Small | 2 | 2 | 2 | 2 | Close the File load-thread lifecycle holes: `~FileManager` joins `mLoadingThread` without draining `mLoadingFuture` (thread assigned *on* the async task; the server never drains the future, so a fast teardown can join a not-yet-joinable thread → terminate); gate the unsynchronized boot-window `mEagerChunkMap` reads (`ReadChunkData:670`, `IsChunkReady:404`) behind an atomic eager-load-complete flag (invariant-hardening — safe today only via undocumented cross-subsystem boot ordering); resolve the misleading `mQueueMutex` in `ReadChunkData` vs the lockless writers (document the `eState` acquire-pair or drop the lock). Process-local, no CRC exposure. Three grill decisions pre-staged |
| 93 | [File/Architecture_VersionedHeaderDedup.md](File/Architecture_VersionedHeaderDedup.md) | Small | 2 | 2 | 2 | 2 | Extract the hand-duplicated version+size file-header convention — `Write/ReadVersionedFile` (`FileManager.h:262-290`) vs `DifferenceStreamWriter::Save`/reader ctor (`DifferenceStream.h:73-77`/`162-183`, comments literally say "matches WriteVersionedFile pattern") — into shared `WriteVersionHeader<T>`/`ReadAndValidateVersionHeader<T>` helpers with read-value out-params (callers keep their LOGs + `DEBUG_BREAK`). Byte-identical output; verify via record/replay round-trip. Needs `IncludeHygiene`'s DifferenceStream→FileManager include (or land together) |
| 94 | [Frame/Architecture_NavPredicateDedup.md](Frame/Architecture_NavPredicateDedup.md) | Small | 2 | 2 | 2 | 2 | `SegmentsIntersect` token-identical in `NavBuild.cpp:271-292` + `NavQuery.cpp:13-34`, winding-number core duplicated — epsilon drift between build-time visibility graph and runtime query = paths through walls; share one definition (extends `NavBuildSplit`'s `NavBuildInternal.h` promotion). Behavior-identical; land after the split |
| 95 | [Frame/Architecture_CrcMixingStrength.md](Frame/Architecture_CrcMixingStrength.md) | Quick Win | 1 | 2 | 3 | 2 | XOR-combined frame CRCs cancel even-multiplicity errors (swapped fields between alignment pairs/collections hash identical) — replace with an ordered fold in `Alignments::Crc`, both engine `Crcs()`, and the game-layer XOR continuation (`Frame.cpp:534-549/612-627/692`); iteration order verified deterministic at every site, no parallel accumulation. CRC values change: rebuild client+server together; old replay `.checksums` log per-tick mismatches (accepted class) |
| 96 | [Frame/Refactor_AStarEndVisibleLazy.md](Frame/Refactor_AStarEndVisibleLazy.md) | Small | 2 | 2 | 2 | 2 | `AStarPath` eagerly precomputes end-visibility for all V cell vertices per invocation (`NavQuery.cpp:383-387`) but consumes it only per expanded vertex — lazily memoize a tri-state in the same workbuffer block (start side stays eager: fully consumed on first pop). Result-identical pure-geometry queries; halves worst-case LOS precompute on the per-player tick path |
| 97 | [Frame/Refactor_CollisionDecomposition.md](Frame/Refactor_CollisionDecomposition.md) | Small | 2 | 2 | 2 | 2 | Flatten 177-line depth-5 `CollideLayerPair` (extract per-pair body; dedupe the mirrored A/B result-record blocks into one swapped-args helper), collapse `InsertObjectIntoZones`' duplicated `bIsLayerA` branches, hoist rare-overflow `sLayers.at` lookups out of the hot zone loop. Behavior-identical — collision feeds CRC'd state, replay-verify |
| 98 | [Graphics/Architecture_ScreenshotFenceDeadlock.md](Graphics/Architecture_ScreenshotFenceDeadlock.md) | Small | 2 | 2 | 2 | 2 | Latent deadlock (masked by `kbScreenshots=false`): `SubmitMainToQueue` resets the per-framebuffer fence, then calls `SaveScreenshot`, which waits that fence — but only the later UI submit signals it (`ImGuiManager.cpp:376`), and the main thread is parked on `mSubmitMain.Wait()` → circular wait → `VK_TIMEOUT` → `DEBUG_BREAK`+throw under both `kbRenderThread` settings (chain re-derived at verification). Fix shapes: wait-before-reset (stale-capture caveat), fence-the-Main-submit, or recommended main-thread capture between UI submit and Present; plus `ScopedSuppressAllocationTracking` + `// Heap:` on the capture-side allocations. Client-only; validate with `kbScreenshots=true` |
| 99 | [Ui/Architecture_LibraryReplacement.md](Ui/Architecture_LibraryReplacement.md) | Small | 2 | 2 | 2 | 2 | Rebuild the hand-rolled ImGui curve editor `CurveWidget.cpp` (178 lines) on already-vendored ImPlot (MIT — coverage extension, no new import): `DragPoint`/`PlotLine`/`PlotInfLines` replace bespoke hit-testing, pixel↔curve transforms, gridlines, and the spread-pass tick overlay (~100-120 LOC removable; widget becomes ~60-80 lines); per-ID drag state removes the documented one-widget-at-a-time `siDragIndex` limitation. Single call site (`TweaksScreenLighting.cpp:228` bool-return contract); client-only debug UI; hand re-verify interactions in the Tweaks Lighting tab |
| 100 | [Network/Architecture_SubscribeRejectSlotNegotiation.md](Network/Architecture_SubscribeRejectSlotNegotiation.md) | Small | 2 | 3 | 3 | 2 | Server rejects subscribes with sentinel slot `0xFF` but the client detects rejection as `slot >= 16` while the server allocates from 64 un-negotiated slots (hello carries no count) — a slot-≥16 *accept* is misread as rejection and uniquely sends no unsubscribe → permanent server slot leak (per-tick broadcasts to a deaf channel, pinned active coord, swallowed re-subscribes). Name the sentinel in `NetworkProtocol.h` + check it explicitly + defensive-unsubscribe; negotiation shape (hello byte + version bump vs shared constant vs sentinel-only) at grill. Edge-case trigger (needs >16 in-flight subs), but the leak is permanent |
| 101 | [Network/Refactor_ServerFrameBufferRecycle.md](Network/Refactor_ServerFrameBufferRecycle.md) | Small | 2 | 2 | 2 | 2 | `Server::BufferFullFrame` (once per tick; per-active-coord serialization inside) builds a fresh `ostringstream` + `.str()` string copy into the debug-frame ring — the directory's biggest steady-state allocator load (suppressed + `// Heap:`-compliant, still 2 large allocs + a copy per coord per tick); recycle evicted `BufferedFullFrame` storage or serialize via a reusable streambuf — grill. Ring serves only `ClientDebugFrameRequest`; payload bytes identical |
| 102 | [Frame/ScaleEngineToMeters.md](Frame/ScaleEngineToMeters.md) | Large | 4 | 3 | 2 | 3 | Three-batch pass to bring camera, gameplay, and audio tunables into the "1 unit = 1 m" convention introduced when `kfMetersToUnits = 1.0f` and `kfCellWidth/kfCellHeight = 600` landed. Catalog: ~15 literals across `Camera.{h,cpp}`, `CameraBase.cpp`, `Game.{cpp,h}`, `FrameBase.h`, `Frame.cpp`, `TerrainUtils.cpp`, `Players{,Navigation}.{cpp,h}`, `Spaceships.cpp`, `SoundSettingsWrappersBase.cpp`, `WrapperBase.cpp`. Final cleanup deletes `engine::kfMetersToUnits` and simplifies the three `IslandTerrain.cpp` multiplications |
| 103 | [Graphics/WaterDisplacementIndirectCompute.md](Graphics/WaterDisplacementIndirectCompute.md) | Medium | 3 | 2 | 2 | 3 | Two deferred perf items in the Gerstner-displacement pre-compute. Item 1: replace the `ASSERT(false)` for `kIndirectHostVisible\|kCompute` at `PipelineCreator.cpp:624` with the symmetric host-visible-indirect branch, add `Pipeline::WriteIndirectComputeBuffer`, wire `kPipelineWaterDisplacement` to indirect dispatch from `MainUniforms.cpp` (saves ~64x workgroup launches at LOD3). Item 2: extend the existing `fShoreAmplitude` check in `WaterDisplacement.comp` so `fTerrainElevation >= 0.0f` early-returns over land, skipping both Gerstner loops |
| 104 | [Graphics/IslandNavContourResidency.md](Graphics/IslandNavContourResidency.md) | Medium | 3 | 2 | 2 | 3 | Server-side per-template `IslandTemplate::mNavContour` (~5-50 KB per template × ~65 templates) is permanently resident — built once in `WaitForElevationMaps`, freed only at `~IslandTerrain`. Surfaced by the `IslandResidentMemoryScaling` sibling-sweep. Adopt whatever lifecycle the strategy plan picks, or close if measurement shows the aggregate cost is negligible. Depends on `IslandResidentMemoryScalingStrategy.md` for the lifecycle decision |
| 105 | [Frame/NavBuildSplit.md](Frame/NavBuildSplit.md) | Medium | 3 | 2 | 2 | 3 | Split `NavBuild.cpp` (934 lines, approaching `/reduce-file`) into `NavBuild.cpp` (contour extraction + visibility-graph build) + new `NavCellData.cpp` (cell-merge / acceleration / `NavData` serialize) sharing a private `NavBuildInternal.h` (promotes the one cross-domain helper `SegmentsIntersect`). Move-only: no behavior, no `kiNavDataVersion`/CRC/serialized-layout change. Wire the two new files into all four client+server vcxproj/.filters |
| 106 | [Frame/IslandTerrainSplit.md](Frame/IslandTerrainSplit.md) | Medium | 3 | 2 | 2 | 3 | `/reduce-file` split of `IslandTerrain.cpp` (890 lines, review-flagged) along its existing `#if defined(BT_CLIENT)` seam: keep the shared CPU domain (ctor/dtor, `WaitForElevationMaps`, `GlobalElevation`/`GlobalNormal`, `BuildElevationGrid`, `FrameElevation`/`FrameNormal`) in `IslandTerrain.cpp`; move the client GPU-residency span (`:410-832`+`:856-888` — `CreateClientMeshBuffers`, `AcquireTextureSlot`, eviction/restoration sweeps + pending pre-scans, `ReleaseGpuResources`, `ResetTextureSlots`) into a new fully-`BT_CLIENT` `IslandTerrainResidency.cpp`. Shared header `IslandTerrain.h` NOT split; no private `*Internal.h` (no cross-TU helper, unlike NavBuildSplit). Move-only: no behavior / `kiVersion` / `kiNavDataVersion` / CRC / `.pack`-layout change. New file wired into the **client** vcxproj+filters only (kept file stays in all four). Plan only — `/reduce-file` runs at execution. Coordinate with `IslandResidentMemoryScalingStrategy` (relocates the eviction sweeps it edits) |
| 107 | [Graphics/DisabledPassGatingPerfAudit.md](Graphics/DisabledPassGatingPerfAudit.md) | Medium | 3 | 2 | 2 | 3 | Measure-then-decide audit of record-once passes that run while their feature is disabled (CB re-record ban means gating-when-disabled needs a destroy-tier edge re-record; recorded clears/dispatches can't be cheaply indirect-gated). Sites: `RecordSmokeSpreadPipeline` / `RecordWindSpreadPipeline` (`CommandBufferRecordGlobal.cpp:121-445`), object-shadows render+blur and wind deposit (`CommandBufferRecordMain.cpp:130-178`). Documents the shadow Item-1 ACCEPT precedent; "accept" is the likely outcome unless a pass measures non-trivial. Object-shadows has no clean on/off edge (day-cycle continuous `fObjectShadowsIntensity`) |
| 108 | [Graphics/WindowedLightingShadowDispatch.md](Graphics/WindowedLightingShadowDispatch.md) | Large | 4 | 3 | 2 | 3 | Window the lighting (deposit/spread/combine/temporal) + shadow passes to the live cropped sub-region instead of processing the full headroom texture every frame: indirect-compute dispatch + min-texel invocation offset for the compute passes, dynamic scissor for deposit/spread, and fold the history write into `LightingTemporal` to drop the four full-texture copies. Enabled by this session's `CLAMP_TO_BORDER` edge samplers + steady-state-target texel ramp; shares `WriteIndirectComputeBuffer` with `WaterDisplacementIndirectCompute`. Client/graphics-only |
| 109 | [Frame/Refactor_NavFunctionDecomposition.md](Frame/Refactor_NavFunctionDecomposition.md) | Medium | 3 | 2 | 2 | 3 | Extract the 6-lambda indexed min-heap from 212-line `AStarPath`, the A*-miss fallback from `NavQueryDirection`, the 5-deep compiled-out crossing-check from `BuildCellNavData`, the first-mint block from `AcquireTextureSlot`; precompute the vertex→polygon map in `BuildVisibilityGraph` (drops O(V²·P) + two nesting levels). Move-only/result-identical; land after both Frame splits |
| 110 | [Input/Architecture_LibraryReplacement.md](Input/Architecture_LibraryReplacement.md) | Small | 2 | 1 | 2 | 3 | **Decision plan (present options).** Replace the in-house Raw Input keyboard path (~70-80 lines) with already-vendored DirectXTK `Keyboard` (MIT, coverage extension — `Keyboard.cpp` absent from the `Prebuilts` unity unit). Recommendation: **keep in-house (Option A)** — adoption loses the deliberate `RIDEV_NOLEGACY` semantics (Alt+F4 reroutes to WM_CLOSE bypassing the game `kQuit` binding, ImGui starts seeing WM_KEY*/WM_CHAR, generic `VK_MENU` binding breaks under DirectXTK's L/R-specific remap). Record-and-close candidate; if B is ever chosen, parts of `Refactor_RawInputHandling` + `DeadCodeAndUnusedIncludesSweep` evaporate |
| 111 | [Graphics/IslandResidentMemoryScalingStrategy.md](Graphics/IslandResidentMemoryScalingStrategy.md) | Large | 4 | 3 | 3 | 4 | Predecessor `IslandResidentMemoryScaling.md` landed step 1 only: `[DEBUG-resmem]` boot-time LOG instrumentation at the end of `IslandTerrain::CreateClientMeshBuffers` reporting per-template + aggregate mesh CPU / mesh GPU / heightmap / hull bytes. Once a Debug client boot log is captured, pick a strategy (a2 = LRU mesh + zero indirect entry; b = lazy `kIsland` chunk load; c = trim route table; d = compress/share; e = close with justification) and implement. Remove the `[DEBUG-resmem]` block when done |
| 112 | [File/Architecture_FileManagerSplitDecision.md](File/Architecture_FileManagerSplitDecision.md) | Architectural | 4 | 3 | 3 | 4 | **Decision plan (present options).** `FileManager` fuses two disjoint-consumer modules (general file I/O + versioned/atomic writes vs the packed-asset chunk system), and the lazy-chunk state machine is co-owned by four files via `GetLazyChunk()`'s mutable-ref leak under a documented-not-asserted thread contract (`FileManager.cpp:618-627`). Options: A — split the class (~22-TU churn); B — narrow the chunk seam with intent-named APIs (likely right size); C — accept + document. Resolve via grill; must not block the other File plans (land them first) |
| 113 | [Network/Architecture_WireFormatPairing.md](Network/Architecture_WireFormatPairing.md) | Large | 4 | 3 | 3 | 4 | Every engine message layout is hand-mirrored across send/receive (17 packet types; magic header sizes in ×2-3 copies incl. `NetworkSimulation.h`'s offset-4 tick parse; `SendSimplePacket` duplicated verbatim in `Client.h`/`Server.h`) — the directory's principal latent-desync hazard, lockstep by convention only. Colocate per-message write/read pairs (byte-identical, converted message-by-message with interop smoke tests), dedupe the send template, retire the magic offset. No wire change; land after the small Network plans (it moves every line they cite) |
### Reference / Index Documents

Meta/overview docs that are never executed as plans — excluded from the priority walk and orphan scans.

| Document | Purpose |
|----------|---------|
| [Graphics/ShaderReview/00_Overview.md](Graphics/ShaderReview/00_Overview.md) | Shader-review effort overview and cross-cutting-findings index; the per-subsystem plans it references were either executed-and-deleted or never authored |

## Dependencies

Plans that must be executed in order due to shared files or stale line numbers:

- **Common determinism-bytes session has LANDED and been removed** — `Common/Crc.md` (hash-core de-dup behind `kCrcSeed`/`kCrcMultiplier`, `CrcConsteval` forwards to `Crc`, `unsigned char` byte fold, generic-template XMVECTOR `requires` lock), `Common/DataFile.md` (`AlignOutputStream` modulo fix + `ASSERT`, `ChunkLocation::crc` init, `SceneHeader` explicit `uiPad2[4]`, per-struct `sizeof`/`offsetof` layout `static_assert`s, `is_trivially_copyable_v<ChunkHeader>`), and the `Common/Determinism_IndeterminateBytes.md` theme (R2 "equal values → equal bytes" contract comments on the `Crc.h`/`Serialization.h` generic byte paths; `Serialization.h` got the sibling XMVECTOR `requires` lock) all landed in one session. **No baked-CRC/replay regen and no `kiVersion` bump** — all persisted `.pack`/`.manifest`/generated-`.h` CRCs are ASCII asset-path hashes (unchanged by the fold); only the runtime client/server desync CRC + locally-recorded replays reset (both self-consistent after rebuild). The "Light" union scope (no structural `ChunkHeader` canonical-zero member) was chosen since the writer already zero-fills via `vector::resize`. The follow-up layout-lock coverage on the remaining persisted structs (animation stream, `MaterialShaderData`, `AudioHeader`) has also LANDED and been removed — those `sizeof`/`offsetof` `static_assert`s are now in `DataFile.h` alongside the rest.
- **Common log/diagnostics-safety session:** the per-file `Common/StackWalker.md` plan **has landed and been removed**; `Common/ThreadSafety_SharedStaticBuffers.md` (theme) still owns the shared-static-buffer race that plan excluded — its Log `static thread_local` change delivers StackWalker's fault-path fix for free. No co-scheduling constraint remains.
- `Network/Architecture_LayerViolations.md` **Plan A has landed and been removed** (the six one-way debug save/load/reset/replay/pause packets are now game-layer `GamePacketType` handled in `game::ServerSession::ParseReceivedGamePackets`). **Plan B has landed and been removed** — the timespeed flow (`kClientTimespeedRequest` + `kServerTimespeedUpdate`) is now reclassed to `GamePacketType`; handlers and broadcast live in `game::ClientSession::PollNetwork` and `game::ServerSession::{ParseReceivedGamePackets, BroadcastTimespeedIfChanged, SendTimespeedToNewClient}`; engine `GameBase::ServerUpdate` and `Server::ClientHello` invoke the game-session helpers. CoordFrames / Frame factory / Frame version / BufferFullFrame items were dropped per the user-confirmed layer-violation policy (engine reading game globals to access engine-base members is by design, not a violation).
- `Engine/Architecture_GameSaveLoadRelocation.md` **has landed and been removed** — `GameSaveLoad` relocated to the game layer (`game::GameSaveLoad` in `Projects/BrokenEngineSandbox/Source/Save/`, owned by `game::Game`); engine `GameBase::ServerUpdate` now drives it via `game::gpGame->mGameSaveLoad.X()`, and the `friend` + engine-side member were removed.
- Per-frame multi-island system (5 phases) has LANDED. Phase 5 (LRU eviction, dynamic subscription-driven loading, neutral slot-0 placeholder) executed in this session — no remaining follow-ups. See `Engine/Source/Frame/CLAUDE.md` for the current architecture description.
- `Graphics/IslandNavContourResidency.md` depends on `Graphics/IslandResidentMemoryScalingStrategy.md`. The NavContour residency lifecycle should mirror whatever strategy the predecessor picks (lazy chunk-load / LRU / cap / close). Do not pick a NavContour strategy before the predecessor lands. Both plans are gated on captured `[DEBUG-resmem]` boot-log data from a Debug client run.
- `Graphics/WindowedLightingShadowDispatch.md` shares the `kIndirectHostVisible | kCompute` `PipelineCreator.cpp` branch and the `WriteIndirectComputeBuffer` / `RecordComputeIndirect` helpers with `Graphics/WaterDisplacementIndirectCompute.md` (Item 1) — land one first or co-implement the helper, then the second only wires its pipelines. It also builds on two prerequisites that **landed this session** (no plan): the `CLAMP_TO_BORDER` edge samplers (`mVkSamplerBorderWhite` shadow / transparent-black `mVkSamplerBorder` lighting, wired into the Terrain/Water/WaterSkyboxOne consumer bindings) and the steady-state-target texel ramp (`Camera.cpp` reference-height clamp removal). Client/graphics-only — no CRC/determinism/network risk.
- The three deferred Shadow Temporal-Accumulation siblings (`Graphics/InlineAreaMappingDryToHelper.md`, `Graphics/WindHistoryResetOnRecreate.md`, `Graphics/DisabledPassGatingPerfAudit.md`) are order-independent of each other. `Graphics/LightingDepositSpreadFullPort.md` (which reworked the same lighting/area machinery) **has landed and been removed** — it already fixed the lighting `VisibleAreaToWorld` site (`LightingSpread.frag:74`) and left the eight *non-lighting* DRY sites untouched for `InlineAreaMappingDryToHelper.md`, so that plan can proceed on its eight sites + read-only `ShaderFunctions.h` without collision. `WindHistoryResetOnRecreate.md` and `DisabledPassGatingPerfAudit.md` both touch the wind/smoke render path (`WindUniforms.cpp` / `SmokeUniforms.cpp` / `RenderTargetTextures.cpp` / `CommandBufferRecordGlobal.cpp`); land them in one wind/smoke-render session or sequence to avoid stale line numbers. None of the three carry CRC/determinism/network risk (all client/graphics-only).

- `Engine/SingletonPublishGlobalGuardSweep.md` is order-independent of the plans it shares files with — it only inserts a ctor `ASSERT(gpFoo == nullptr)` + an `if (gpFoo == this)` dtor wrap at each publish site, touching no logic those plans edit — but shares files with them, so co-schedule or land it first to avoid stale line numbers: `Frame/ScaleEngineToMeters.md` (`Camera.cpp` ctor/dtor `:38/:45`). (`Common/DiagnosticLog.md` **has landed and been removed** — the `kDiagnosticLogCount` slot guard is in; the sweep's per-slot publish `ASSERT` now lands on the post-plan `DiagnosticLog.cpp`.) The sweep is line-stable (two inserted lines per site) so it is safe to land first.

- `Frame/IslandTerrainSplit.md` should land **before (or in the same session as)** `Graphics/IslandResidentMemoryScalingStrategy.md`. The split relocates `EvictionSweep`/`RestorationSweep`/`CreateClientMeshBuffers` from `IslandTerrain.cpp` into the new client-only `IslandTerrainResidency.cpp`; the memory-scaling plan then edits those exact functions (LRU mesh-buffer eviction + removing the `[DEBUG-resmem]` block). If the split lands first, the scaling plan must re-target `IslandTerrainResidency.cpp`. The other three plans sharing `IslandTerrain.cpp` (`ScaleEngineToMeters.md`, `IslandNavContourResidency.md`, `SingletonPublishGlobalGuardSweep.md`) edit functions the split leaves in `IslandTerrain.cpp` (or, for `ScaleEngineToMeters`'s `:818`, a re-targetable one-line multiply removal) → line-refresh only, no hard ordering constraint. The split is move-only (no `kiVersion`/`kiNavDataVersion`/CRC/`.pack`-layout change), so it carries no determinism risk; it is client-only build wiring (new file in the client vcxproj/filters only).

### CLAUDE.md-refresh follow-up plans (this run)

- The **decision plans** (`Common/AggregationAndScopeRuleQualifiers.md`, `DataPacker/DataPackerLogFloatSpecException.md`, `Frame/NavigationStraightLineSteeringRule.md`, `Graphics/ParentRuleFramingReconciliation.md`, `Common/PlanDocGovernanceDecisions.md` (**dropped as invalid** — its "0 of 31 plans have `## Out of scope`" premise was false: 42 of 45 have it; the Plans/Features canonical+deltas framing is already in place), and the second-pass `Common/DocCanonicalHomeReconciliation.md` + `Graphics/NvidiaPipelineHangAttributionReconcile.md`) are doc-rule resolutions tagged "Decision plan (present options)" — resolve each via `/external-grill-plan` before any edit. They are order-independent of each other. Several touch the **root / `Common` / `Engine/Source` CLAUDE.md**, whose edits land under the normal gated doc process (the root-CLAUDE.md change-ban applies). `AggregationAndScopeRuleQualifiers` (ExternalHeaders/Engine.h/BT_CLIENT) and `ParentRuleFramingReconciliation` (deposit-shader/layer-violation/singleton framing) both touch root + `Engine/Source` CLAUDE.md wording — co-schedule or sequence so they don't both rewrite the same parent bullets in conflicting ways. `DocCanonicalHomeReconciliation`'s DataPacker-bake item (Decision 1) overlaps `StaleDocClaimsSweep.md` item 9 (the bake/split delegation loop-break) — co-schedule or sequence so the parent `DataPacker/Source/CLAUDE.md` bake paragraphs aren't double-edited; its Common.h-aggregation item (Decision 4) is the canonical-side of `StaleDocClaimsSweep` item 10's root-doc note (records the choice, edits only the `Common/CLAUDE.md` side).
- `Common/StaleDocClaimsSweep.md` reconciles `Graphics/ShaderReview/00_Overview.md` (a Reference/Index doc) and confirms the Order.md LANDED-entry convention — it reads/lightly-touches `Order.md` and the ShaderReview overview, so co-schedule with any other plan editing those if one is queued (none currently). Its `FrameUpdatePipeline.md` 64→32 Hz fix routes through `/update-architecture-diagrams`, not a hand-edit.
- `Common/StaleCodeCommentsSweep.md` (source comments) and `Common/StaleDocClaimsSweep.md` (doc files) are deliberately split by target file-type; they touch different files and are order-independent, but both are comment/doc-only (Risks 0) and can land together.
- **Second-pass audit plans (this run)** — all order-independent of the rest of the queue and of each other; each is investigate-then-decide (verify the cited root cause before editing, per Diagnosis Discipline):
	- `Graphics/PipelineGraphicsGlobalSet0Indexing.md` (Set-0 framebuffer-index discrepancy) and `Graphics/ParticlesSpawnStructCopyGlslangBug.md` (whole-struct SSBO copy) are graphics/shader investigations — could resolve doc-only or as a small code/shader fix; client/graphics-only, no determinism/CRC exposure. The latter needs a DataPacker recompile + visual smoke-test only if a fix is taken.
	- `Network/StaticDataEpochCheckOnSubscribingSlot.md` (client receive epoch-check on epochless `kSubscribing` slot) is client-receive only — no shared-CRC/determinism exposure; sibling of the `ServerUnsubscribeHandshakeGate` "one handler diverges from siblings' gate" pattern (that plan was **dropped as invalid** — the unsubscribe exception is documented in `Server/CLAUDE.md` and behaviorally unreachable pre-handshake). `Network/BroadcastTickAllocationGuardScope.md` (blanket suppression nullifies the documented armed tracker) is server-only debug-tracking — narrow-the-guard vs fix-the-doc.
	- `Ui/ChineseFontPushMissingScreens.md` (missing CJK font push on Pause/Sound screens) is client-only UI; coordinate `PauseMenuScreen.cpp` edits with `Ui/MenuUtilsAppendUtf8DanglingPointer.md` if co-scheduled (both touch that file), but they edit different concerns (font-push scoping vs the `AppendUtf8` return-lifetime) — no hard ordering constraint.
	- `Graphics/NvidiaPipelineHangAttributionReconcile.md` (mat4-array vs `inverse()` hang attribution) and `Common/DocCanonicalHomeReconciliation.md` (6 cross-doc canonical-home decisions) are doc-reconciliations — see the decision-plans bullet above for `DocCanonicalHomeReconciliation`'s `StaleDocClaimsSweep` overlaps. `NvidiaPipelineHangAttributionReconcile` edits one source comment (`ModelCommon.h:46`) plus two CLAUDE.md sites; the source-comment edit is gated on its investigation outcome so it lives there, not in `StaleCodeCommentsSweep`.
- `Frame/MissilesDeadExplosionRadiusField.md` is the only new plan with **CRC/`kiVersion` exposure** — it bumps `MissilesPostRender::kiVersion`→`Frame::kiVersion` (stale saves/replays reject). No other queued plan touches the Missiles collection, so no co-scheduling constraint; rebuild client+server together. Execute via the `add-collection-member` checklist (reverse).
- `Engine/VcxprojAndBuildConfigReconciliation.md`, `Frame/IslandTerrainSplit.md`, and `Frame/NavBuildSplit.md` all edit `.vcxproj`/`.filters` files, but **different entries** (the reconciliation fixes include-paths / filter elements / orphan registration; the splits *add new file* entries). No hard ordering constraint — just avoid stomping each other's edits if co-scheduled in one session.
- **Audio deep-analysis plans (this run)** — `Audio/Architecture_AudioHelperPlacement.md` should land **before (or with)** `Audio/Architecture_IncludeHygiene.md`: the relocation moves `VolumeToPower`/`DestroyXAudio2SourceVoice` into the new `AudioUtility.h`, changing which direct includes the hygiene plan adds. `Audio/Architecture_ThreadContractDocs.md` lands **last** of any co-scheduled Audio session (comment-only; cites line numbers every code plan moves) and deliberately does not touch the `Audio/CLAUDE.md` replay-invariant claim — `Audio/Architecture_OneShotPathCleanup.md` restores that claim in code. All seven Audio plans are client-only with no CRC/determinism/network exposure.
- `Debug/Architecture_LibraryReplacement.md` touches `Engine.h` (the Debug include inside the `BT_CLIENT` span) and the client vcxproj/`.filters` (removes the `EnumToString.h` entries if the delete-the-file variant is chosen) — soft overlap with the Audio session (`Audio/Architecture_AudioHelperPlacement.md` adds its own `Engine.h` include + vcxproj entry) and `Engine/VcxprojAndBuildConfigReconciliation.md` (different vcxproj entries). No hard ordering constraint — line-refresh only if co-scheduled.
- **File deep-analysis plans (this run)** — land the seven code plans as one File session: `File/Architecture_AnimationDataLoadPlacement.md` **before** `File/Refactor_FunctionDecomposition.md` (the relocation shrinks `LoadPackFiles`, whose lines the decomposition cites) and before `File/Refactor_ClientGuardAndWideApi.md`'s nested-guard item (the moved block contains that guard — the item evaporates; check at execution). `File/Architecture_IncludeHygiene.md` **before (or with)** `File/Architecture_VersionedHeaderDedup.md` (the shared helpers rely on `DifferenceStream.h`'s new direct `FileManager.h` include rather than `Engine.h` ordering); its `AnimationData.h` include item also evaporates if `AnimationDataLoadPlacement` lands first. `File/Architecture_FileManagerEncapsulation.md` + `File/Architecture_LoadThreadLifecycleSafety.md` co-schedule (both edit `GetEagerChunkMap`/`mLoadingFuture`). `File/Architecture_FileManagerSplitDecision.md` (decision) resolves **last** and must not block the others. `Engine/SingletonPublishGlobalGuardSweep.md` and `Common/StaleCodeCommentsSweep.md` also touch `FileManager.cpp`/`FileManager.h` (publish-guard lines and the "pre-faulted" comments respectively) — disjoint edits, line-refresh only. No CRC/determinism/network/`kiVersion` exposure anywhere in the File set.
- **Frame deep-analysis plans (this run)** — `Frame/NavBuildSplit.md` should land **before (or with)** the four new plans editing functions it relocates or re-linkages: `Frame/Architecture_NavPredicateDedup.md` (extends the split's `NavBuildInternal.h`/`SegmentsIntersect` external-linkage promotion to also serve `NavQuery.cpp`), `Frame/Refactor_NavFunctionDecomposition.md` (its `BuildCellNavData` crossing-check extraction targets post-split `NavCellData.cpp`), `Frame/Refactor_DiagnosticAndDeadCodeRemoval.md` (deletes the dead bounds scan inside `BuildCellNavData`), and `Frame/Architecture_DeterminismGuards.md` (assert inside `ChainEdgesIntoPolygons` — stays in `NavBuild.cpp`; line refresh only). `Frame/IslandTerrainSplit.md` likewise relocates `AcquireTextureSlot` into `IslandTerrainResidency.cpp` — land it before `Refactor_NavFunctionDecomposition.md`'s first-mint extraction. `Frame/Architecture_CrcMixingStrength.md` + `Frame/Architecture_LogDifferencesServerCollections.md` edit adjacent `FrameBase.cpp` code — land in one session. `Frame/Refactor_AStarEndVisibleLazy.md` + `Refactor_NavFunctionDecomposition.md` both restructure `AStarPath` — co-schedule. `Frame/Refactor_StyleMechanics.md` items fold into whichever session owns each file; its `IslandChainPlacement.h` class→free-function item co-schedules with `Common/StaleCodeCommentsSweep.md` item 5 (log-spiral comment, same header). Only `Architecture_CrcMixingStrength.md` carries CRC exposure (values change, accepted class); everything else is behavior-identical, debug-only, doc-only, or includes-only.
- **Graphics top-level deep-analysis plans (this run)** — all eight are client-only with no CRC/determinism/network/`kiVersion` exposure. Ordering: `Graphics/Architecture_SharedConstantDuplication.md`'s eye-height item shares the 150.0f literals (`CameraBase.cpp:128`, game `Camera.cpp:18`) with `Frame/ScaleEngineToMeters.md`'s catalog — co-schedule or land after it so the single-sourced constant carries the post-rescale value. `Graphics/Architecture_InvariantHardening.md`'s `OneShotCommandBuffer` thread assert must encode non-concurrency unless `Graphics/Architecture_ScreenshotFenceDeadlock.md`'s fix shape (c) (main-thread capture) lands first — land the assert with or after the deadlock fix, or use the in-use-flag form. `Graphics/Refactor_ApiAndHotPathCleanups.md` and `Architecture_InvariantHardening.md` both edit `OneShotCommandBuffer.{h,cpp}` (disjoint edits — param removal vs assert) — co-schedule or refresh lines. The remaining overlaps are the shared `Graphics.cpp`/`CameraBase.{h,cpp}` edits — see File Groups.
- **Input deep-analysis plans (this run)** — all five are client-only display-rate input with no CRC/determinism/network/`kiVersion` exposure. `Input/Architecture_LibraryReplacement.md` (decision) resolves **first**: if Option B (DirectXTK Keyboard) were ever chosen, `Input/Refactor_RawInputHandling.md`'s `HandleRawInput` item and parts of `Engine/DeadCodeAndUnusedIncludesSweep.md`'s mouse-registration item evaporate (the recommendation is Option A / keep-in-house, which unblocks everything unchanged). The four code plans co-schedule as one Input session — see File Groups. Two related findings were deduped into existing sweeps rather than re-filed: the vestigial mouse `RIDEV_INPUTSINK` registration (`Engine/DeadCodeAndUnusedIncludesSweep.md`) and the incorrect `RawInputManager.cpp:42` legacy-mouse comment (`Common/StaleCodeCommentsSweep.md`). **Memory (same run)**: `Memory/Architecture_AllocationTrackingFlagAndDocs.md` is order-independent of the Input set (allocator TU + `ExternalHeaders.h` + `Memory/CLAUDE.md`); the Memory-side include cleanup is `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 — `ProfileManagerBase.cpp` must gain its direct `MemoryManager.h` include *before* the `CollectionMemory.h:3` channel include is dropped.

- **Ui deep-analysis plans (this run)** — all seven are client-presentation only with no CRC/determinism/network/`kiVersion` exposure (the one sim-feeding wrapper, `gBaseHeight`, was deduped into the pre-existing `Frame/Architecture_BaseHeightWrapperDeterminism.md` rather than re-filed). The three Water plans (`Ui/Architecture_WaterWrapperInvariants.md`, `Ui/Refactor_StyleMechanics.md`, `Ui/Refactor_WaterZOffsetTempDecision.md`) share `WaterWrappersBase.{h,cpp}` — land as one session (see File Groups). `Ui/Architecture_CurveDataDeadChangeDetection.md` + `Ui/Refactor_StyleMechanics.md` both edit `CurveData.h` (different lines — co-schedule). `Ui/Architecture_WrapperContractDocs.md` is comment-only and cites `WrapperBase.h` lines that `Frame/Architecture_BaseHeightWrapperDeterminism.md` / `Engine/WrapperGetIndexFailLoud.md` edit — land it last of any co-scheduled Wrapper session; its threading comment should cite the `kfBaseHeight` constant if the BaseHeight conversion has already landed. `Ui/Architecture_LibraryReplacement.md` (ImPlot rebuild) supersedes any `CurveWidget` decomposition (deliberately not filed) and is independent of the CurveData deletions (different symbols). Both `Ui/Refactor_WaterZOffsetTempDecision.md` options require a DataPacker shader recompile (the `GlobalLayout` member `fWaterZOffsetTemp` is renamed or removed).

- **Profile deep-analysis plans (this run)** — all six are profiling/display only with no CRC/determinism/network/`kiVersion` exposure; all touch `ProfileManagerBase.{h,cpp}`/`ProfileScreens.cpp`, so land as one Profile session (see File Groups). Ordering within the session: `Profile/Architecture_IncludeHygiene.md` **before (or with)** `Profile/Architecture_ClientServerGuardScope.md` (the hoisted `giAllocationsThisFrame` latch should sit on the new direct `Memory/MemoryManager.h` include, not the PCH accident); `Profile/Architecture_SmoothNowDoubleLatch.md` and `Profile/Refactor_InFunctionCleanups.md` both edit `CpuStop` — co-schedule, with the flags conversion landing with or after the latch fix; `Profile/Architecture_DocAccuracy.md` lands **last** (comment/doc-only; cites lines every code plan moves). Cross-queue overlaps: `Profile/Architecture_IncludeHygiene.md`'s direct-include item satisfies the include-first prerequisite of `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 — whichever lands first adds the `ProfileManagerBase.cpp` include, the other skips it (the `CollectionMemory.h:3` channel drop stays with the sweep, and the sweep's `ProfileScreens.cpp` grep hit is superseded by the hygiene plan's move). `Common/StaleDocClaimsSweep.md` item 6 rewords the same Profile CLAUDE.md timer-position sentence that `Profile/Architecture_InvariantHardening.md`'s `static_assert`s make compiler-enforced — whichever lands second keeps doc and assert telling the same story. `Engine/SingletonPublishGlobalGuardSweep.md` touches the game-side `ProfileManager.cpp` ctor/dtor publish sites — different file, no conflict.

- **Network engine-transport deep-analysis plans (this run)** — `Network/Architecture_SpecDocReconciliation.md` is doc-only but cites send/receive line numbers every code plan moves — land it first or refresh its citations. `Network/Architecture_ClientBuildServerTransportGate.md` should land **before (or with)** `Network/Architecture_IncludeHygiene.md` (the gate adds `BT_SERVER` wraps to the same server files whose include blocks the hygiene plan edits). `Network/Refactor_ClientStateFlags.md` co-schedules with `Network/Architecture_ClientSessionBoundary.md` (both rework the `Client.h` member/accessor surface). `Network/Architecture_WireFormatPairing.md` (Large) lands **last** of the set — it restructures every send/receive site the others cite. Protocol exposure is confined to `Architecture_SubscribeRejectSlotNegotiation.md` (only under its option (a): hello byte + `kuiProtocolVersion` bump) and `Architecture_WireFormatPairing.md` (byte-identical conversions, interop-verified per message family); nothing in the set touches shared-CRC sim state, replays, or `kiVersion`.

- **Graphics/Debug deep-analysis plans (this run)** — both Quick Wins, client-only, no CRC/determinism/network/`kiVersion` exposure, order-independent of each other. `Graphics/Architecture_DebugRenderDeadPrimitives.md` is a remove-vs-keep decision gated by `/external-grill-plan` (root never-remove-features rule); it shares `CommandBufferRecordMain.cpp` with the indirect-compute pair — see File Groups (its `PipelineManager.{h,cpp}` / `BufferManager.{h,cpp}` / `DebugRender.{h,cpp}` edits overlap no other live plan). `Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md` is comment-only after verification proved the empty-set transition unreachable (evidence recorded in the plan); the defensive-flush alternative stays pre-staged for the grill. The three doc nits from this run were appended to `Common/StaleDocClaimsSweep.md` (items 15-17) rather than filed as a separate plan.

## File Groups

Plans that touch the same files and should be done in a single session:

- **`Common/Log/Log.{h,cpp}` + `Common/StackWalker.h` + `Common/WindowsUtils.{h,cpp}` (+ `Common/Threading/ThreadLocal.h` for `LogTickScope`)**: `Common/ThreadSafety_SharedStaticBuffers.md` (per-thread buffers across all three; the theme's Log `thread_local` change resolves StackWalker's static-buffer race). (`Common/StackWalker.md` has landed and been removed — its DbgHelp serialization no longer needs co-scheduling.)
- **`Common/Log/DiagnosticLog.cpp`**: `Engine/SingletonPublishGlobalGuardSweep.md` (per-slot publish guard `ASSERT(gpDiagnosticLogs[miIndex] == nullptr)` in ctor + `if (gpDiagnosticLogs[miIndex] == this)` in dtor). (`Common/DiagnosticLog.md` has landed and been removed — the `kDiagnosticLogCount` + `static_assert` slot guard and the `ScopedSuppressAllocationTracking` move to Common are in.)
- **`Projects/.../Graphics/Camera.cpp`**: `Frame/ScaleEngineToMeters.md` (`kfEyeHeightMin`/`kfEyeHeightMax` literals), `Engine/SingletonPublishGlobalGuardSweep.md` (`Camera` ctor `ASSERT(gpCamera == nullptr)` + dtor `if (gpCamera == this)` at `:38`/`:45`)
- **`Engine/Source/Graphics/Objects/PipelineCreator.cpp` + `Pipeline.{h,cpp}` + `Managers/CommandBufferRecordMain.cpp` + `Render/MainUniforms.cpp` + `Engine/Data/Shaders/Water/WaterDisplacement.comp`**: `Graphics/WaterDisplacementIndirectCompute.md` (add `kIndirectHostVisible|kCompute` branch, `WriteIndirectComputeBuffer` helper, and `fTerrainElevation >= 0.0f` over-land skip), `Graphics/WindowedLightingShadowDispatch.md` (reuses the same `kIndirectHostVisible|kCompute` branch + `WriteIndirectComputeBuffer`/`RecordComputeIndirect` helpers — do not duplicate; then windows the lighting/shadow compute dispatches via indirect dispatch and scissors the deposit/spread render passes in `CommandBufferRecordMain.cpp` / `CommandBufferRecordGlobal.cpp`) **Graphics/Debug deep-analysis additions (this run)**: `Graphics/Architecture_DebugRenderDeadPrimitives.md` (removes the two debug `RecordDrawIndirect` lines at `CommandBufferRecordMain.cpp:313-314`; its other edits live outside this group) and `Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md` (comment at the `MainUniforms.cpp:163-166` early return) — disjoint lines from the indirect-compute pair; co-schedule or refresh citations.
- **`Engine/Source/Frame/IslandTerrain.cpp`**: `Frame/IslandTerrainSplit.md` (`/reduce-file` split along the `#if defined(BT_CLIENT)` GPU-residency seam — relocates the eviction/restoration sweeps + `CreateClientMeshBuffers` into a new client-only `IslandTerrainResidency.cpp`; **a file split invalidates the other entries' line refs** so sequence/co-schedule), `Frame/ScaleEngineToMeters.md` (drop the three `kfMetersToUnits` multiplications at `:49`/`:50`/`:147`/`:395`/`:818` as part of the final-cleanup batch — `:818` is inside the relocated client span, re-target if the split lands first), `Graphics/IslandResidentMemoryScalingStrategy.md` (extend `EvictionSweep`/`RestorationSweep` to per-template mesh buffers under strategy (a2); remove the `[DEBUG-resmem]` instrumentation block at the end of `CreateClientMeshBuffers` — **all three relocated by the split**; land the split first or co-schedule, see Dependencies), `Graphics/IslandNavContourResidency.md` (server-side `mNavContour` lifecycle in `WaitForElevationMaps` / `~IslandTerrain` — both stay in `IslandTerrain.cpp`, line refresh only), `Engine/SingletonPublishGlobalGuardSweep.md` (`IslandTerrain` ctor/dtor publish guard at `:28`/`:154` — both stay in `IslandTerrain.cpp`, line refresh only); **Frame deep-analysis additions (this run)**: `Frame/Architecture_IncludeHygiene.md` (removes the two unused includes at `:7`/`:13`), `Frame/Architecture_DeterminismGuards.md` (Frame Purity assert on `GlobalElevation`/`GlobalNormal` — stays in `IslandTerrain.cpp`), `Frame/Refactor_NavFunctionDecomposition.md` (`AcquireTextureSlot` first-mint extraction — relocated by the split; target `IslandTerrainResidency.cpp` after it lands)
- **`Engine/Data/Shaders/ShaderFunctions.h` + the area-mapping consumer shaders (`Terrain/Terrain.frag`, `Water/Water.vert`, `Water/WaterDisplacement.comp`, `Smoke/SmokeSpreadCommon.h`, `Smoke/SmokeOccupancyDilateRemap.comp`, `Wind/WindSpreadOne.comp`, `Wind/WindSpreadTwo.comp`, `Wind/WindOccupancyDilate.comp`)**: `Graphics/InlineAreaMappingDryToHelper.md` (route the eight inline `VisibleAreaToWorld` inverses through the shared helper; `ShaderFunctions.h` is read-only reference here). (`Graphics/LightingDepositSpreadFullPort.md` has landed and been removed — it already converted the lighting `LightingSpread.frag:74` site to the shared helper and left these eight non-lighting sites for this plan.)
- **`Engine/Source/Graphics/Render/WindUniforms.cpp` / `Render/SmokeUniforms.cpp` / `Render/Render.h` + `Managers/RenderTargetTextures.cpp` + `Managers/CommandBufferRecordGlobal.cpp` (wind/smoke render path)**: `Graphics/WindHistoryResetOnRecreate.md` (wind recreate clean-state mirror of `gbShadowTemporalReset`/`gbSmokeClear`; reads the smoke/shadow paths as templates, edits the wind path — may add a wind-clear pipeline + dispatch in `RecordWindSpreadPipeline`), `Graphics/DisabledPassGatingPerfAudit.md` (measures/optionally gates `RecordWindSpreadPipeline`/`RecordSmokeSpreadPipeline` + the wind-deposit / object-shadows passes in `CommandBufferRecordMain.cpp`; reads the `sbWind`/`sbSmoke` toggle latches). Sequence these two in one wind/smoke-render session to avoid stale line numbers
- **`Engine/Source/Audio/*` (`AudioManager.{h,cpp}`, `StaticVoice.{h,cpp}`, `StaticVoices.{h,cpp}`, `StreamingVoice.{h,cpp}`, `StreamingVoices.{h,cpp}`, `Engine.h`)**: all seven Audio plans overlap here — `Audio/Architecture_AudioHelperPlacement.md` (new `AudioUtility.h`; edits every voice file + `AudioManager.h` + `Engine.h` + client vcxproj), `Audio/Architecture_IncludeHygiene.md` (include blocks of five files), `Audio/Architecture_OneShotPathCleanup.md` (`StaticVoices.{h,cpp}` one-shot path + `AudioManager.{h,cpp}` signature), `Audio/Refactor_UpdateLifecycleDecomposition.md` (`StaticVoices.{h,cpp}` lifecycle + `StaticVoice.h` flag enum), `Audio/Refactor_VoiceStartClickDiscipline.md` (`StaticVoice.{h,cpp}` + `StaticVoices.cpp` creation site), `Audio/Refactor_AudioDeviceFallback.md` (`AudioManager.cpp` ctor only), `Audio/Architecture_ThreadContractDocs.md` (comments in `StaticVoices.h`/`StreamingVoices.cpp` + `Audio/CLAUDE.md`). Land as one Audio session in the Dependencies-bullet order. `Engine/SingletonPublishGlobalGuardSweep.md` also edits `AudioManager.cpp`'s `gpAudioManager` publish sites (ctor `:17` / dtor `:165`) — different lines than `AudioHelperPlacement`'s edits, no semantic conflict; co-schedule or refresh lines.
- **`Engine/Source/File/FileManager.{h,cpp}` + `File/DifferenceStream.h`**: all seven File code plans overlap here — `File/Architecture_IncludeHygiene.md` (include blocks of both `FileManager.cpp` and `DifferenceStream.h` + the `Engine.h:11` comment), `File/Architecture_FileManagerEncapsulation.md` (`FileManager.h` member visibility + `GetEagerChunkMap`), `File/Architecture_AnimationDataLoadPlacement.md` (deletes the `LoadPackFiles` scene-parse block; also edits `Graphics/AnimationData.{h,cpp}` + a client boot call site), `File/Architecture_LoadThreadLifecycleSafety.md` (dtor, async task, `ReadChunkData`, new atomic member), `File/Architecture_VersionedHeaderDedup.md` (versioned-file templates + DifferenceStream header read/write), `File/Refactor_FunctionDecomposition.md` (`LoadPackFiles`/`LoadChunk`/reader-ctor extraction), `File/Refactor_ClientGuardAndWideApi.md` (ctor + nested guard). Land as one File session in the Dependencies-bullet order. `Common/StaleCodeCommentsSweep.md` ("pre-faulted" comments) and `Engine/SingletonPublishGlobalGuardSweep.md` (`gpFileManager` publish guard) also touch `FileManager.{h,cpp}` — co-schedule or refresh lines.
- **`Engine/Source/Graphics/` top-level (`Graphics.{h,cpp}`, `CameraBase.{h,cpp}`, `OneShotCommandBuffer.{h,cpp}`, `Screenshot.cpp`, `Islands.h`, `GraphicsUtils.cpp`, `AnimationData.{h,cpp}`)**: the eight Graphics deep-analysis plans overlap here — `Graphics/Architecture_IncludeHygiene.md` (include blocks of four .cpps + `Islands.h` fwd decls), `Graphics/Architecture_RenderInterpolatesOwnership.md` (`Graphics.h` member + `GameBase.{h,cpp}`/`Main.cpp`), `Graphics/Architecture_SharedConstantDuplication.md` (`Graphics.cpp:15-82` constants, `CameraBase.{h,cpp}` eye-height, `AnimationData.cpp` enums), `Graphics/Architecture_EncapsulationSeams.md` (`Graphics.cpp` destroy/create + `Managers/SwapchainManager.{h,cpp}`), `Graphics/Architecture_ScreenshotFenceDeadlock.md` (`Screenshot.cpp` + `Managers/CommandBufferManager.cpp`), `Graphics/Architecture_InvariantHardening.md` (`AnimationData.h:41` comment + `OneShotCommandBuffer.cpp` assert), `Graphics/Refactor_RefreshAndCornerDecomposition.md` (`Graphics.cpp:350-523` + `CameraBase.cpp:53-101`), `Graphics/Refactor_ApiAndHotPathCleanups.md` (`OneShotCommandBuffer.{h,cpp}` + `CameraBase.h` + `Islands.{h,cpp}` + game `Game.cpp`). Land as one or two sessions in the Dependencies-bullet order; `Graphics.cpp` and `CameraBase.{h,cpp}` are each touched by four plans, so refresh line citations between landings.
- **`Engine/Source/Frame/Collision.{h,cpp}` (+ `AreaDamage.cpp`)**: `Frame/Refactor_CollisionOverflowGuards.md` (overflow suppressions + `sLayerBaseOffsets` bound), `Frame/Refactor_CollisionDecomposition.md` (`CollideLayerPair`/`InsertObjectIntoZones` restructure), `Frame/Architecture_CollisionHeaderSurface.md` (move four internal types into the .cpp), `Frame/Architecture_IncludeHygiene.md` (Collision.h/.cpp include items), `Frame/Refactor_StyleMechanics.md` (`memset`/designated-init/`.at()` items) — one Collision session; land the decomposition last (it moves the lines the others cite) or first (it owns the biggest diff) — pick one and refresh.
- **`Engine/Source/Frame/FrameBase.cpp`**: `Frame/Architecture_LogDifferencesServerCollections.md` (LogDifferences fold), `Frame/Architecture_CrcMixingStrength.md` (Crcs XOR→ordered fold; adjacent functions), `Frame/Refactor_StyleMechanics.md` (`[[maybe_unused]]` strip + `cols` rename), `Frame/Architecture_ContractDocGaps.md` (Write/Read comment), `Frame/Architecture_IncludeHygiene.md` (game `Frame.h` include) — co-schedule in one session.
- **`Engine/Source/Frame/NavBuild.cpp` + `NavQuery.cpp` (+ post-split `NavCellData.cpp`/`NavBuildInternal.h`)**: `Frame/NavBuildSplit.md` (the split — land first), then `Frame/Architecture_NavPredicateDedup.md`, `Frame/Architecture_DeterminismGuards.md` (ChainEdges assert), `Frame/Refactor_NavFunctionDecomposition.md`, `Frame/Refactor_AStarEndVisibleLazy.md` (same `AStarPath` as the decomposition — co-schedule), `Frame/Refactor_DiagnosticAndDeadCodeRemoval.md` (NavBuild dead-scan item), `Frame/Refactor_StyleMechanics.md` (m128 items), `Frame/Architecture_BaseHeightWrapperDeterminism.md` (NavQuery `gBaseHeight` reads + include removal) — one nav session after the split.
- **`Engine/Source/Input/RawInputManager.{h,cpp}` + game `Input/Input.{h,cpp}` + `Engine/Source/Main.cpp`**: the four Input code plans overlap here — `Input/Architecture_InterfaceNarrowingAndDocs.md` (member visibility, `mHwnd` single-sourcing, `Main.cpp:155`, `Game.h:19` fwd-decl), `Input/Refactor_RawInputHandling.md` (`HandleRawInput` + the WndProc `WM_INPUT` case), `Input/Architecture_GamepadDisconnectSnapshot.md` (`Update` disconnect branch + `f2Triggers` field deletion), `Input/Refactor_InputButtonFlags.md` (button enums/struct members + `Update` writers + game edge-detect helpers). Land as one Input session. `Engine/DeadCodeAndUnusedIncludesSweep.md` (mouse `RIDEV_INPUTSINK` registration + game `Input.h` deletions: `GetGamepadMode()`, `FrameInput::operator==`), `Common/StaleCodeCommentsSweep.md` (the cpp:42 comment), and `Engine/SingletonPublishGlobalGuardSweep.md` (`gpRawInputManager` ctor/dtor publish guards, cpp:12/30) also touch these files — disjoint edits, co-schedule or refresh lines.

- **`Engine/Source/Ui/WaterWrappersBase.{h,cpp}` (+ `Render/GlobalUniforms.cpp` / `Water.vert` for the decision plan)**: `Ui/Architecture_WaterWrapperInvariants.md` (stale `kiWaterNormalSeaWavesIndex` comment `:7-8` + `.h`/`.cpp` ordering drift), `Ui/Refactor_StyleMechanics.md` (`std::move`/`i64` literals `:70/:88`, `// DT: TEMP` deletions `:116-118`), `Ui/Refactor_WaterZOffsetTempDecision.md` (`gWaterZOffsetTemp` chain `.h:81`/`.cpp:103`) — disjoint lines; land as one Water-wrapper session.
- **`Engine/Source/Ui/WrapperBase.{h,cpp}`**: `Ui/Architecture_IncludeHygiene.md` (removes the `:4` `CurveData.h` include), `Ui/Architecture_WrapperContractDocs.md` (class/member contract comments — land last), `Frame/Architecture_BaseHeightWrapperDeterminism.md` (deletes `gBaseHeight`, `.h:214`/`.cpp:9`), `Engine/WrapperGetIndexFailLoud.md` (`GetIndex` fall-through `:185-186`), `Frame/ScaleEngineToMeters.md` (`.cpp` tunable literals) — all disjoint symbols; co-schedule or refresh lines between landings.
- **`Engine/Source/Ui/CurveData.h` + `Engine/Source/Ui/CLAUDE.md`**: `Ui/Architecture_CurveDataDeadChangeDetection.md` (`Changed()`/`ComputeHash` deletion + doc sentence), `Ui/Refactor_StyleMechanics.md` (dead `iCount` `:151/:171` + two doc sentences), `Ui/Architecture_LibraryReplacement.md` (drops the single-instance drag caveat from the doc) — co-schedule the `CLAUDE.md` edits.
- **`Engine/Source/Profile/ProfileManagerBase.{h,cpp}` + `ProfileScreens.cpp` (+ game `Server/ServerDisplay.cpp`, `Engine/Source/Profile/CLAUDE.md`)**: all six Profile plans overlap here — `Profile/Architecture_SmoothNowDoubleLatch.md` (`CpuTimer` flag + `CpuStop`/`SmoothCpuTimers`), `Profile/Architecture_ClientServerGuardScope.md` (`UpdateProfileText` restructure + header gating + the `ServerDisplay.cpp:80` swap + CLAUDE.md:11), `Profile/Architecture_InvariantHardening.md` (static_asserts after the `:5` include, `Create()` precondition, `LockCpuTimers()` accessor + formatter lock), `Profile/Architecture_IncludeHygiene.md` (include/declaration moves in all three files + CLAUDE.md:37), `Profile/Refactor_InFunctionCleanups.md` (`CpuStop` flags signature across 17 call sites + `FormatGpuScreen` decomposition + `= default`), `Profile/Architecture_DocAccuracy.md` (CLAUDE.md claims + `mCpuTimerMutex` comment — land last). Land as one Profile session in the Dependencies-bullet order. `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 and `Common/StaleDocClaimsSweep.md` item 6 also touch these files/docs — see the Profile Dependencies bullet for the partition.
- **`Engine/Source/Network/` engine transport (`Client/Client.{h,cpp}` + `ClientSend.cpp` + `ClientReceive.cpp` + `ClientSessionBase.{h,cpp}`, `Server/Server.{h,cpp}` + `ServerReceive.cpp` + `ServerSend.cpp` + `ServerSessionBase.{h,cpp}` + `ServerTypes.h`, `NetworkProtocol.h`, `NetworkSimulation.h`)**: the twelve Network deep-analysis plans overlap here — `ClientReceive.cpp` is touched by five (`Refactor_QuickWinMechanics` hoist, `Refactor_CoordUpdateAllocAndJitter`, `Architecture_SubscribeRejectSlotNegotiation`, `Architecture_ClientSessionBoundary` GUID write, `Architecture_IncludeHygiene`), `ClientSessionBase.{h,cpp}` by four, the Server transport files by five plus `Architecture_ClientBuildServerTransportGate`'s `BT_SERVER` wraps, and `Architecture_WireFormatPairing` restructures all the send/receive files. Land as one or two Network sessions in the Dependencies-bullet order; `WireFormatPairing` last. The pre-existing `Network/StaticDataEpochCheckOnSubscribingSlot.md` also edits `ClientReceive.cpp` (`ServerCoordStaticData` epoch condition) — fold into the same session.
