# Plan Execution Order

All refactor and bugfix plans — debt reduction that doesn't add a new engine capability — roughly ordered by score (lower = higher priority).

Score = Effort − Impact + Risks (lower = higher priority)

The scores and the resulting row order are rough estimates — a quick tiebreaker for what to pick up next, not a strictly enforced ranking. Approximate ordering is fine; don't spend effort re-scoring or precisely repositioning rows, and a row sitting slightly out of order is not a defect worth fixing.

`Tier` column is an informal size/risk descriptor (**Quick Win** / **Small** / **Medium** / **Large** / **Architectural**); it does not affect score or ordering.

Counterpart: `Documents/Features/Order.md` holds brand-new additions. See `Documents/AGENTS.md` for the distinction. Scores are not comparable across the two files — they were originally derived from a single sort and then split.

This file reflects only the CURRENT set of live plans. It is not a changelog — landed/dropped plans, and the sessions that produced them, are not recorded here.

## Plans

| Plan | Tier | Effort | Impact | Risks | Score | Depends On | Notes |
|------|------|--------|--------|-------|-------|------------|-------|
| [Network/Refactor_DrainContractUnification.md](Network/Refactor_DrainContractUnification.md) | Small | 2 | 2 | 1 | 1 | Documents/Plans/Network/Refactor_SessionBaseCollapse.md | **Decision plan (present options).** Rename the `Client`/`Server` receive accessors by contract — `Received*` (cleared-next-poll), `Pending*`+`ClearPending*` (persist-until-served), `Take*` (consume-on-call) — replacing one `Drain*` name that hides three incompatible lifetimes (`Server.h:182-186`: five look-alikes, two contracts). Compile-checked rename, no wire/behavior change. Decided Option A (contract-named rename, zero behavior change); Option B (unify to `Take*`, drop `Poll`'s silent discard) not taken |
| [Network/AgentTransportConcurrentCommands.md](Network/AgentTransportConcurrentCommands.md) | Small | 2 | 2 | 1 | 1 | - | **Decision plan (present options).** AgentHarness5 residual: the agent transport (`AgentCommandServer`, Harness1) is single-in-flight — backlog 1, request/response lockstep, and `Drain()`'s deferred-poll returns early while a frame-stepped input script is deferred, so no second command is accepted mid-script. Two documented AgentHarness5 behaviors are therefore unreachable: the `busy` error for concurrent input commands (defensive dead code in `AgentInput::BeginScript`/`BeginScriptAndDefer`) and "non-input commands stay allowed mid-script". A — accept + document (zero code); B — accept new requests during deferred polls, run non-script commands immediately (touches `Drain` accept/response ordering; localhost JSON shape may change). Client-only harness, no CRC/wire. |
| [Graphics/Architecture_ShadowLightingUniformDedup.md](Graphics/Architecture_ShadowLightingUniformDedup.md) | Small | 2 | 3 | 2 | 1 | - | Extract `ComputeWorldSizedTexelArea` + `TemporalAreaLatch` — the world-texel snap math and temporal latch are token-identical between GlobalUniforms (shadow) and LightingUniforms with a third partial copy in SmokeUniforms; tile-count floor-vs-ceil has already drifted between copies. Exact-float-sequence extraction; sequence with `WindowedLightingShadowDispatch`. `LightingSpreadEmptySkipCadence`'s cadence item gates the lighting previous-area latch advance to refresh frames — if `TemporalAreaLatch` is extracted first, the gate lands inside the shared helper |
| [Engine/Architecture_GameBaseDeadVirtuals.md](Engine/Architecture_GameBaseDeadVirtuals.md) | Small | 2 | 2 | 1 | 1 | - | Delete dead `GameBase::ReplayFile()` (zero call sites repo-wide), move `Reset()`/`QuicksaveFile()` to `game::Game` (zero engine callers), delete the server MenuInput/Quickload path outright (decided; resolved jointly with `Network/ServerPauseAndResetSemantics.md` — fresh-game reset item there is moot) |
| [Audio/StartupDeviceRecoveryHardening.md](Audio/StartupDeviceRecoveryHardening.md) | Small | 2 | 3 | 2 | 1 | - | Keep one silent-mode-capable `AudioEngine` when startup has no endpoint; split one-time voice ownership from live-graph setup, guard nullable `GetInterface`, and recover default→pinned 48 kHz when a device appears. Client-only, no CRC/wire/save/pack exposure; needs startup-no-device and pin-failure live verification |
| [Profile/Architecture_ProfileVirtualRouting.md](Profile/Architecture_ProfileVirtualRouting.md) | Small | 2 | 2 | 1 | 1 | - | Collapse the six single-comparison `ProfileManagerBase` index-routing virtuals (unreachable base bodies; game override restates the routing 4×; virtual dispatch inside the `CpuStart`/`CpuStop` mutex sections) into base-held spans registered by the derived ctor; keep the `static_assert` contracts + sanctioned by-name `game::` reads; `ProfileManagerBase.h` stays engine-include-free |
| [Graphics/Refactor_PipelineDeviceManagersQuickWins.md](Graphics/Refactor_PipelineDeviceManagersQuickWins.md) | Small | 2 | 2 | 1 | 1 | - | 13-item mechanical batch: dead `mbFoundKhronosValidation`, unchecked `vkEnumerateDeviceExtensionProperties`/`vkGetPipelineCacheData`, `ASSERT(false)`-before-working-CONCURRENT-fallback contradiction (delete the ASSERT, decided), dead `bSignalFence` param, vestigial `kGpuTimerTerrainGen`, `std::array`→C array, `CreateDepositPipeline` param struct |
| [Graphics/Refactor_CommandBufferRecordDedup.md](Graphics/Refactor_CommandBufferRecordDedup.md) | Small | 2 | 2 | 1 | 1 | - | Decompose the ~300-line `CommandBufferRecordMain::Record` into per-pass statics (Global sibling already is — intra-directory drift), extract `Pipeline::RecordComputeIndirectFrom` (4 copies), `TileCount` helper (~10 sites). Move-only; land after `DisabledPassGatingPerfAudit`/`WindowedLightingShadowDispatch` or refresh their citations |
| [Graphics/Refactor_StagingBufferRaii.md](Graphics/Refactor_StagingBufferRaii.md) | Small | 2 | 3 | 2 | 1 | - | RAII staging buffer — `DeviceLostException` unwind through `OneShotCommandBuffer::Execute` leaks the staging `VkBuffer`/`VmaAllocation` at `Texture::UploadImageData` + `Buffer::Create` (VMA assert during device-loss recovery; uncovered by any live plan); delete the dead write-only `VkDeviceMemory` plumbing (stale `vkMapMemory` comment, ~8 files); validate on-disk `puiIndexStarts` before `vkCmdDrawIndexedIndirect` |
| [Graphics/Refactor_GraphicsObjectsQuickWins.md](Graphics/Refactor_GraphicsObjectsQuickWins.md) | Small | 2 | 2 | 1 | 1 | - | Objects batch: `SetupIndirectBuffer` graphics/compute dedup, `Update{CombinedImageSampler,Sampler,StorageImage}` triplication collapse, `kSamplerAny` mask, descriptor-helper push-contract unification, `Shader::Create` missing self-`Destroy`. Pipeline-cluster sequencing applies |
| [Graphics/Refactor_RenderUniformsQuickWins.md](Graphics/Refactor_RenderUniformsQuickWins.md) | Small | 2 | 3 | 2 | 1 | - | Render/root batch: spread-decay End uniforms fed from the Start wrappers → shader `mix()` permanently no-op — add the missing End wrappers (decided), AnimationData keyframe-search dedup + wrong exception type on the topo check, CameraBase expansion asymmetry confirmed bug — fix (decided), snap-loop helper, screenshot OS/stb result checks |
| [Ui/Refactor_TweaksScreenQuickWins.md](Ui/Refactor_TweaksScreenQuickWins.md) | Small | 2 | 2 | 1 | 1 | - | `BeginSubtab` helper replacing the 15 verbatim subtab apply-flag handshake copies (TweaksScreen AGENTS.md currently *instructs* hand-replication), `string_view` `.data()` assignment fix, sibling guard-placement alignment. Co-schedule with `TweakSectionGameExtension` (same files) |
| [Ui/AestheticUiDesignPass.md](Ui/AestheticUiDesignPass.md) | Medium | 3 | 3 | 1 | 1 | - | Remaining Pause/Graphics/Sound/Modal/HUD aesthetic pass, using the user-approved Main Menu as the fixed visual anchor. Native-resolution rendered screenshots are authoritative; ImGui metrics are diagnostic only. Includes mandatory Graphics Font Size 3.0 overflow correction (`Back` off-screen, minus partly clipped), normal and Time-of-Day variants, HUD invariants, byte-stable harness labels, contract/docs sync, full C++ process, and explicit user screenshot approval. Client-only ImGui geometry; no CRC/wire/`kiVersion`/save exposure |
| [Ui/GraphicsSettingsPaddingDeterminism.md](Ui/GraphicsSettingsPaddingDeterminism.md) | Quick Win | 1 | 1 | 1 | 1 | - | Follow-up residual from the landed `SaveFileAtomicityAndByteDeterminism`: `GraphicsSettings` (persisted `GraphicsSettings.bin`) writes 3 bytes indeterminate interior padding — 1-byte `Flags<GraphicsSettingsFlags>` (`uint8_t` underlying) before 4-byte-aligned `ePresentMode` — from a partial designated-init aggregate (non-zeroing). Harmless (no memcmp/CRC/diff consumer) but non-byte-reproducible. Fix: `{}`+member-assign (mirrors the safe `SaveTweaksSettings`, zero layout change) or explicit `uiPad[3]`; no `kiVersion` bump. `TweaksSettings`/`SoundSettings` verified safe. Client-only |
| [Input/CameraZoomImGuiCaptureGate.md](Input/CameraZoomImGuiCaptureGate.md) | Quick Win | 1 | 2 | 2 | 1 | - | Camera mouse-wheel zoom has no ImGui-capture/menu gate: `Input::UpdateCameraInput` (`Input.cpp:129-136`) diffs `RawInput.iScrollWheelValue` unconditionally and `game::Camera::Update` (`Camera.cpp:225-240`) applies `iScrollDelta` to eye height with no `ImGui::GetIO().WantCaptureMouse`/`meUiState` check → scrolling an open menu also zooms the camera (real + harness synthetic wheel alike). Verified: no gate exists, no `WantCaptureMouse` anywhere in game source. Gate the scroll-delta consumption on `WantCaptureMouse`, absorbing swallowed ticks. Surfaced during `AgentMouseWheelCursorCoupling` execution, user-directed. Grill: gate site (input layer, rec — vs `Camera`). Client-only input/visual, camera state not CRC'd, no wire/`kiVersion`; user-visible behavior change → playtest |
| [Engine/GuardStandardizationResiduals.md](Engine/GuardStandardizationResiduals.md) | Quick Win | 1 | 1 | 1 | 1 | - | Standardize whole-file `#if defined(BT_CLIENT/BT_SERVER)` guards (guard-first) for the three single-build files left non-conforming when the session's guard rule landed. Actionable: (a) `Ui/HexShieldWrappers.{h,cpp}` — gate the unconditional `Pch.h:100` include with `BT_CLIENT`, wrap both files, drop from server vcxproj (sweep found no server use); (c) `Graphics/Islands.cpp` — move the pre-guard top `#include "Islands.h"` inside the `BT_CLIENT` guard. Site (b) `ServerSessionBase.h` partial guard is now a documented-accepted exception (`Server/AGENTS.md:14`) — not actionable, obviated by `SessionBaseCollapse`. No CRC/wire/`kiVersion`; compile-checked |
| [Common/ExponentialDecayInputPolicy.md](Common/ExponentialDecayInputPolicy.md) | Quick Win | 1 | 1 | 1 | 1 | - | Decide and document `ExponentialDecay` negative/non-finite scaled-time semantics, then align the implementation and stale broad never-overshoot claim while preserving all current finite-positive CRC-fed results; grill chooses total `[0,1]` fallback (recommended) vs a documented finite-non-negative precondition |
| [Graphics/DataTextureSamplerBias.md](Graphics/DataTextureSamplerBias.md) | Small | 2 | 2 | 1 | 1 | - | **Decision plan (present options).** The global albedo-sharpen `-gMipLodBias` on the base sampler create-info (`TextureManager.cpp` `CreateSamplers`) also biases non-color data fetches: `Model.frag` normal (`:184`) + metallic-roughness (`:231`) via the shared global `samplerRepeat` (specular consumer — compounds `ModelSpecularAntialiasing`'s zoom residual); terrain island/sand/rock normals (`PipelineManager.cpp:399/403-405/407-409`), masks (`:419`), AO (`:400`); water noise/depthLut (accept). A — dedicated no-bias (or slider) sampler for data fetches (Model normal/MR first — needs free Set-0 binding 2 + a shader edit; terrain second — per-binding flag swap only); B — accept + document. Generalizes the landed `kSamplerMirroredRepeatWater`/`gWaterNormalMipBias` split. Client/graphics-only, shader repack, no CRC/`kiVersion`/wire |
| [Graphics/SmokeSpreadFullTextureClearElimination.md](Graphics/SmokeSpreadFullTextureClearElimination.md) | Medium | 3 | 4 | 2 | 1 | - | Kill the two per-frame full-texture `vkCmdClearColorImage`s in `RecordSmokeSpreadHalf` (~1.05 ms/frame at 4K — measured 1210 µs idle vs wind's 129 µs at identical extents) — per-texture occupancy split + own-tile dilate union term guarantees stale-texel rewrite; creation-time hard clear mirrors wind. No visual change. Client/graphics-only, shader repack, no CRC/wire |
| [Graphics/TerrainInstanceAreaCull.md](Graphics/TerrainInstanceAreaCull.md) | Small | 2 | 3 | 2 | 1 | - | Two-tier spatial cull in `UpdateActiveIslands` (union-area SSBO cull + visible-first packing, mesh-pass `instanceCount` = visible prefix) — off-screen islands stop paying full vertex cost on `kGpuTimerTerrain` (measured 998 µs); ~150–400 µs, no visual change, `gIslandCullMargin` slider fallback. Client-only, no repack. Co-schedule with `TerrainMeshLodChain` (same `Islands.cpp` indirect writes) |
| [Graphics/TerrainDetailSampleTrim.md](Graphics/TerrainDetailSampleTrim.md) | Small | 2 | 2 | 1 | 1 | - | Height-fade + warp-coherent skip of the 6 rock/beach detail-normal octaves in `Terrain.frag` (mip-flattened to ≈+Z at altitude anyway); ~50–150 µs, imperceptible, fade-height sliders. Shader repack only; cite-refresh vs `DataTextureSamplerBias` |
| [Graphics/WaterFragmentCostReduction.md](Graphics/WaterFragmentCostReduction.md) | Small | 2 | 2 | 1 | 1 | - | `Water.frag` fetch diet: conservative smoke-inactive uniform gate (2 of 22 fetches, exact-zero skip), CPU-hoisted normal-rotation cos/sin uniforms, weight-Min=0 far-zoom shutdown of the finest octave group (existing sliders + FADE_HANDOFF define), reflected-projection gate rider. Est. 100–300 µs idle. Shader repack, client-only |
| [Graphics/ObjectShadowsPassCost.md](Graphics/ObjectShadowsPassCost.md) | Small | 2 | 2 | 1 | 1 | - | Bilinear-paired Gaussian taps in `ObjectShadowsBlurH/V.comp` (17→9 fetches, ~70 µs of the measured 168, exact-result technique) + optional terrain-blur rider + decision: `gObjectShadowsRenderMultiplier` default 1.0→0.5 (~100–150 µs of the 226; existing slider, A/B). Shader repack; client/graphics-only |
| [Frame/MissileLifetimeAndTargetLifecycle.md](Frame/MissileLifetimeAndTargetLifecycle.md) | Small | 2 | 4 | 3 | 1 | - | Missiles have no lifetime cap — `pfTimes`/`pfExaustDelays` are accumulated, CRC'd, and transferred with zero consumers while targetless missiles fly and cell-transfer forever (unbounded entity leak); + release the target subscription at `Explode` not `Destroy` (dead subscribers skew `GetMissileTarget` load-balancing), `TargetsPostRender::Remove` `.at()`-throw hardening + prev-vs-current map guard, Targets `SharedMembers()` pre-emption. Grill: expiry (recommended) vs delete the dead fields; `kiVersion` bump |
| [Frame/BlasterWindTrailTransferParams.md](Frame/BlasterWindTrailTransferParams.md) | Small | 2 | 2 | 1 | 1 | - | Blaster wind-trail `TransferData` fields are filled only under `BT_CLIENT` but the client replays the *server's* transfer StatusChanges → always serialized as defaults, trails permanently lost at every cell handoff (Blasters AGENTS.md handoff claim false). Grill: re-derive at `ClientInit` (recommended — deletes the dead wire fields) vs promote to shared SOA |
| [Frame/Refactor_CollectionHeadersQuickWins.md](Frame/Refactor_CollectionHeadersQuickWins.md) | Small | 2 | 2 | 1 | 1 | - | 9-item collection-headers batch (`Collection.h`/`CollectionMemory.h`/`CollectionController.h`/`CollectionId.h`): registration ASSERTs for `uiKeyframeCount>=2` + monotonic `pfTimes` (makes the `InterpolateKeyframes` NOLINT honest), stale `// Heap: operator[]` comments → `insert_or_assign`, fold vestigial `AllocateCore`→`Allocate` (dead `bool`), shared `kuiInvalidTypeIndex` sentinel (0xFF magic vs `kuiInvalidControllerType`), `void_t` `HasIdToIndex`→`requires`-concept, wrong "CRTP pattern" comment, mis-homed growth-formula comment, `global_id_t` formatter-location doc note, verify `MultiCrc` zero-count guard asymmetry. No behavior/CRC/wire change |
| [Graphics/ScreenshotStbHeaderCentralization.md](Graphics/ScreenshotStbHeaderCentralization.md) | Quick Win | 1 | 1 | 1 | 1 | - | **Decision plan.** Follow-up to `Architecture_UnusedIncludes`: `Screenshot.cpp`'s local relative `stb_image_write.h` include is required (`ExternalHeaders.h`'s copy is `BT_DATA_PACKER`-only, not client-visible) — route client stb-write consumption through a `BT_CLIENT` span in `ExternalHeaders.h` (mirrors the landed renderdoc move) and drop the local include, or keep-local + normalize the ugly relative path. Client-only, no repack |
| [Save/DirectLoadTickClockRetention.md](Save/DirectLoadTickClockRetention.md) | Quick Win | 1 | 2 | 2 | 1 | - | **Decision plan (present options).** Follow-up from `SaveLoadTrustBoundaryHardening` session-audit: on a direct `ServerLoad`, `ReadGrid` sets the loaded tick counter/current time but the subsequent `Game::Reset()` zeroes them — the loaded tick clock is discarded (the replay path re-derives it after `Reset()`; direct load does not). A restore (mirror replay) vs B accept+document (world state is frame-carried; remove the now-dead `SetTickCounter`/`SetCurrentTime`). Verify no CRC-tick / replay-from-load / post-load clock-sync dependency first. Server-only, no wire/`kiVersion`/CRC |
| [Network/ClientDisconnectModalFeedback.md](Network/ClientDisconnectModalFeedback.md) | Quick Win | 1 | 1 | 1 | 1 | - | A clean mid-session server death returns silently to the menu — `ClientSession::PollConnection` (`:318-321`) defaults `meUiState` to `kPause` when `mModalMessage` is empty, while connect-time rejection/`"Connection failed"`/desync all already surface the existing `ModalScreen`. Populate a generic "connection lost" default so 4-of-4 failure modes show the modal; reuses the existing modal (no new modal system). No disconnect reason available (ENet `data 0`; connect-time reason string only). Client-only UI state, no CRC/wire/`kiVersion`/save exposure |
| [DataPacker/Refactor_ShaderDependencyCacheSplit.md](DataPacker/Refactor_ShaderDependencyCacheSplit.md) | Small | 2 | 2 | 1 | 1 | - | Move shader depfile parsing, root-relative fingerprint metadata, `CheckDirty`, and cache-stability/commit methods out of 657-line `ExportShader.cpp` into `ExportShaderDependencies.cpp`; preserve metadata format and unescaped-space handling. Offline move-only refactor; no SPIR-V/pack/`kiVersion` change |
| [Graphics/Managers/Refactor_PipelineManagerSplit.md](Graphics/Managers/Refactor_PipelineManagerSplit.md) | Quick Win | 1 | 1 | 1 | 1 | - | Decided 2026-07-03: accept-and-document, do not split `PipelineManager.cpp` (846 lines, re-analysis confirmed cohesive — uniform declarative `Create*` shape, no responsibility boundary, load-bearing ctor boot order). Add a size-accepted comment at the top of `PipelineManager.cpp` so future `/reduce-file` passes don't re-flag it |
| [Save/ReplayGenerationCommitAtomicity.md](Save/ReplayGenerationCommitAtomicity.md) | Medium | 3 | 4 | 3 | 2 | Documents/Plans/Save/RecordedCoordReplayStopPolicy.md | Make manifest the replay-set commit marker: invalidate before fixed-name overwrite, attempt all data components, publish valid manifest only after total success, and prevent loadable mixed generations |
| [Save/RecordedCoordReplayStopPolicy.md](Save/RecordedCoordReplayStopPolicy.md) | Medium | 3 | 4 | 3 | 2 | - | **Decision plan (present options).** Own the last complete end Frame across recorded-coord eviction so stop never throws through `CurrentFrame`, always attempts later components, clears state, and reports aggregate result |
| [Graphics/Managers/CorruptTextureChunkLifecycleHardening.md](Graphics/Managers/CorruptTextureChunkLifecycleHardening.md) | Small | 2 | 2 | 2 | 2 | - | Corrupt-chunk soft-fail hardening: the upload-thread catch can strand a live `vkImage` on a `kReady` chunk (evict-without-destroy → VMA leak assert at teardown), and `WriteArrayElementFromLive`'s placeholder fallback misses non-null never-adopted Textures with null views (invalid bindless descriptor write via `RestorationSweep`'s `>= kReady` gate). Land before `BindlessSlotLifecycle` |
| [Graphics/DepositSpreadBarrierStageMismatch.md](Graphics/DepositSpreadBarrierStageMismatch.md) | Quick Win | 1 | 1 | 2 | 2 | - | **Verify-then-fix.** Lighting deposit→spread barrier (`CommandBufferRecordMain.cpp:77-85`, `vkDepositToComputeBarrier`) targets `dstStageMask=COMPUTE_SHADER` with a "compute shader reads" comment, but `RecordLightingSpreadPipeline` (`:303`) is a FRAGMENT fullscreen-MRT pass — wrong dst stage + misleading comment, masked today by the spread render pass's own subpass dependency. Verify `mSpreadVkRenderPass`'s external subpass dependency covers deposit→spread: if so delete the redundant barrier, else re-stage to `FRAGMENT_SHADER` + drop `SHADER_WRITE` dst access. Fix the comment either way. Pre-existing; surfaced by the `LightingOccupancyRemoval` session-audit. Client/graphics-only, no CRC/wire/`kiVersion`; validation-layer verify. Shares `CommandBufferRecordMain.cpp` File Group |
| [Network/PackIntegrityHandshake.md](Network/PackIntegrityHandshake.md) | Small | 2 | 2 | 2 | 2 | - | Manifest-derived pack-integrity token in `kClientHello` so a damaged/mismatched pack rejects at connect with a clear reason instead of silently desyncing on zero-filled sim chunks (`LoadChunk` soft-fail zero-fills heightmap/nav data). Wire change + protocol version bump. Decided: manifest CRC token, connect-time only |
| [Network/SubscriptionLifecycleRaceHardening.md](Network/SubscriptionLifecycleRaceHardening.md) | Medium | 3 | 4 | 3 | 2 | - | Three subscription-lifecycle races that strand slots permanently: (a) ghost full-state adoption on a freed `kUnsubscribed` slot → frozen cell; (b) server silently drops inactive-slot unsubscribe (no ack) + epochless `kClientUnsubscribe` frees reused slots — ack unconditionally, add epoch (1→2 wire bytes, version bump); (c) no watchdog on transitional slot states. Wire change + `kuiProtocolVersion` bump (batch per reciprocal Coordination contract); grill: watchdog threshold. Placeholder-in-slot generation-id redesign deferred (Out of scope) |
| [Network/ClientFullStateEdgeFixes.md](Network/ClientFullStateEdgeFixes.md) | Medium | 3 | 4 | 3 | 2 | - | Two game-client full-state edge bugs: (a) full states / reliable packets received while desync-stalled are drained-and-destroyed but leave the slot `kActive` → `mCoordFrames.at(coord)` throw + silent packet loss — decided A2: remove `PollConnection`'s `IsStalled` short-circuit, adoption runs pre-short-circuit; (b) `pendingFullState` past a `serverUpdates` gap is never injectable → per-frame full-replay livelock masked by the `has_value()` DEBUG_BREAK bypass — adopt directly as confirmed frame. Client-only, state-selection only, no wire/`kiVersion` change |
| [Network/ClientFrameTrustBoundaryResiduals.md](Network/ClientFrameTrustBoundaryResiduals.md) | Small | 2 | 2 | 2 | 2 | - | **Decision plan (present options).** Two trust-boundary residuals deferred by the review gate from the landed server-side trust-boundary fix (breach its `BT_SERVER` envelope): A1 — client game-packet dispatch backstop (log-and-drop try/catch around the untrusted parse/apply sections of `ClientSession::PollNetwork`, which runs on the `ClientUpdate` stack outside the engine `Client::Receive` backstop; EXCLUDES the data-receiver/reconciler tail so CRC exceptions surface — defense-in-depth, no confirmed throw path); B2 — read-side finite-check of `FrameInterpolate::fSpawnTimer` in `Read`/`ServerRead` (`+Inf`→infinite spawn-drain loop / hang, `NaN`→spawn freeze; substitute `0.0f`). `BT_CLIENT` + shared serialization, read-side check is a no-op on valid streams → no wire/CRC/`kiVersion` change. Decisions: A1 wrap scope (single vs per-section), B2 finite-check form |
| [Network/Refactor_ClientResetUnification.md](Network/Refactor_ClientResetUnification.md) | Medium | 3 | 4 | 3 | 2 | Documents/Plans/Network/Refactor_DrainContractUnification.md | Collapse four overlapping client reset paths (`DisconnectFromServerBase`, `ResetForServerLoad`, desync `ResetCoordStatesForResync`/`Reset`, initial-connect) into one `ResetSession(ResetReason)` with reason-gated steps over `= {}`-reset sub-structs (`ClockSyncState`/`SubscriptionTracking`), extending the `CoordFrames::ResetClientState` convention. Fixes the verified clock-field drift (`ResetForServerLoad` resets 4 of 9); union table is the plan. Reconciliation-adjacent state; sequence after `Refactor_SessionBaseCollapse` |
| [Network/DesyncDebugInfrastructureGating.md](Network/DesyncDebugInfrastructureGating.md) | Medium | 3 | 3 | 2 | 2 | - | Gate the per-tick full-frame `BufferFullFrame` serialization/256-ring (only `kClientDebugFrameRequest` consumes it) — production CPU + memory tax; add per-client cooldowns to unthrottled `kClientDesyncReport` + `kClientDebugFrameRequest` (log/CPU/bandwidth amplification). Server-only, no wire/CRC change. Decided: compile-time gating flag |
| [Network/NetworkSimToleranceRevalidation.md](Network/NetworkSimToleranceRevalidation.md) | Small | 2 | 1 | 1 | 2 | - | `NetworkSimulation` now delays reliable control packets (not just unreliable), so they incur simulated latency and the game profiler Network-screen annunciators may be miscalibrated — measurement-gated re-validation of `GetNetworkSimulationBounds` (`ProfileManager.cpp:339-344`) + the hard-coded rollback cap `iRollbackValue > 8` (`:291`) under `kChina`, re-tune if stale else accept+document. Needs a live sim run, now agent-executable via the agent-harness skill. Client-only profiler overlay, no wire/CRC/`kiVersion` |
| [Network/ServerPauseAndResetSemantics.md](Network/ServerPauseAndResetSemantics.md) | Medium | 3 | 3 | 2 | 2 | - | Two server tick-orchestration fixes: requests injected/drained while paused (`iFullTicks==0`) are wiped by `BuildFrameInputs` before any tick consumes them (weapon toggles lost, re-queued flagship updates dropped) — gate the flagship drain + the `PreTickNetwork` update-player clear on the pause flag, matching the new-sub/resync persist-until-served pattern; decided: two-site pause gate, no wire change. `FreshGameReset` extraction dropped; the local-menu `kResetFrame` fresh-game completion is moot (owned by `Engine/Architecture_GameBaseDeadVirtuals.md`'s server MenuInput/Quickload deletion) |
| [Network/Refactor_SessionBaseCollapse.md](Network/Refactor_SessionBaseCollapse.md) | Medium | 3 | 2 | 1 | 2 | - | Delete `ClientSessionBase`/`ServerSessionBase`/`ClientDataReceiver` (each one subclass, welded to `gpGame`), fold bodies into game `ClientSession`/`ServerSession` across sibling TUs; removes a zero-capability layer and reunites the clock + subscription mechanisms split across the seam. Engine calls already go through game globals (no call-site edits); only `Engine.h` + 2 vcxproj pairs flip. Move-only, no wire/CRC/determinism. Decided: clock correction folds into `ClientSession.cpp`; server timing stays in `ServerSession.cpp`; add one client receive TU and no dedicated clock/server timing TUs |
| [Frame/Architecture_CollectionCopyContract.md](Frame/Architecture_CollectionCopyContract.md) | Medium | 3 | 4 | 3 | 2 | - | Make the collection copy/zero-init contract structural: `Members()`-fold full-copy helper (4 collections), `PersistentMembers()` sub-tuple for selective-copy (4, incl. HexShields' documented asymmetric propagation), parameterized ID stream (Sounds re-inlines the shared helper for its dedicated UUID stream), generic paired-`Remove`, fold zero-init at spawn (makes the CRC invariant structural). Byte-identical migrations; helpers land in shared machinery game CRC'd collections also use — replay/CRC soak |
| [Frame/CollectionDeserializationHardening.md](Frame/CollectionDeserializationHardening.md) | Small | 2 | 3 | 3 | 2 | - | Three memory-safety gaps in the shared SOA deserialization path (save/load + client full-state receive), sibling to `CollectionReadIndexHardening`: A1 [HIGH] `SharedCollectionRead` memset under-zeroes after consecutive shrinking reuse loads (stale client-only garbage for live elements — fixable independently, highest priority) — track true physical layout capacity; A3 [MED] `MakeAligned`-null leaves nonzero-capacity + null-`pData`, breaking the `AllocateCore` invariant → AV on the memset — throw `CorruptStreamException`; A5 [MED] paired Interpolate/PostRender count/capacity parity unvalidated at read (OOB spawn write before `GrowPairedCollections`' too-late ASSERT). No-ops on valid input; no wire/CRC/`kiVersion` change; replay + full-state soak |
| [Frame/Refactor_FrameRootQuickWins.md](Frame/Refactor_FrameRootQuickWins.md) | Small | 2 | 2 | 2 | 2 | - | Nav/collision batch: delete the unreachable `AStarPath` LOS re-test (full DDA walk per invocation at 32 Hz), `PolygonRange` helper (~8 sites), collision pair-context struct, `ChannelCrcs`/`QualifiesForEviction` helpers, dead members. Sim files — byte-identical arithmetic |
| [Frame/MissileImpactTrailPhaseSeparation.md](Frame/MissileImpactTrailPhaseSeparation.md) | Small | 2 | 2 | 2 | 2 | - | Remove client-only `SmokeTrailsInterpolate::Sync` calls from missile PostCollision while preserving the exact rendered entity/terrain impact endpoint through Interpolate-owned synchronization; no Spawn, collision-order, shared-CRC, wire, or expected version change |
| [Graphics/SmokeSimTextureFormatReduction.md](Graphics/SmokeSimTextureFormatReduction.md) | Small | 2 | 2 | 2 | 2 | - | Smoke sim `keSmokeFormat` R32F→R16F: halves all smoke bandwidth (~540 µs idle standalone; combat spread/sampling + 151 MB VRAM after clear-elimination), retires the NEAREST-filter downgrade. **Prior attempt regressed** (compute 32↔16 conversions outweighed bandwidth) — plan gates on a conversion-free route (fragment-path/ROP conversion or fp32-ALU-with-r16f-boundary), A/B-measured, else closes rejected. Shader repack |
| [Graphics/TerrainMeshLodChain.md](Graphics/TerrainMeshLodChain.md) | Medium | 3 | 3 | 2 | 2 | Documents/Plans/Graphics/IslandMeshArenaResidency.md | Per-template 4-LOD concat index regions (`meshopt_simplify` at export) selected per frame via the existing host-visible indirect rewrite — mirrors water's `VisibleAreaMeshLod`, record-once CB untouched, latched to `miVisibleAreaLod`; ~300–600 µs off `kGpuTimerTerrain`, `gTerrainMeshLodBias` slider. `DataHeader::kiVersion` + `ExportIsland` 29→30; resolve `IslandMeshArenaResidency` A/B first or refresh |
| [Graphics/LightingSpreadEmptySkipCadence.md](Graphics/LightingSpreadEmptySkipCadence.md) | Medium | 3 | 3 | 2 | 2 | - | Gate the 40-pass lighting spread chain: zero-instance indirect draws when the frame's light deposit is empty (bit-identical output, ~700 µs idle win of the measured 841) + optional slider-gated refresh cadence with zero-group combine/temporal indirect dispatch (~440 µs avg at cadence 2). C++-only for item 1 (no repack); co-schedule with `WindowedLightingShadowDispatch` (same `RecordLightingSpreadPipeline`/`LightingUniforms.cpp`) |
| [Graphics/Refactor_GlobalUniformsWaterSplit.md](Graphics/Refactor_GlobalUniformsWaterSplit.md) | Small | 2 | 1 | 1 | 2 | - | Move the self-contained water-uniform block (`PopulateWaterSunsetFade`/`PopulateWaterDirectional`/`PopulateWaterReducedUv` incl. its `sdReducedTime*`/`sfPrevElapsedTime` accumulators/`PopulateWaterParameters`) out of the 669-line `GlobalUniforms.cpp` into a new client-only sibling `WaterUniforms.cpp`, matching the `Render/` per-subsystem split (`Wind`/`Smoke`/`LightingUniforms`). Optional debt reduction (file cohesive, under the 1000-line threshold) — verbatim move, no behavior/uniform-layout change, client/graphics-only, no repack. Grill: entry-point signature style (pass mapped ref vs sibling `iCommandBuffer` re-map) |
| [Frame/TransferSentinelConflation.md](Frame/TransferSentinelConflation.md) | Small | 2 | 3 | 3 | 2 | - | Cell-transfer "0 = unset" `SpawnInfo` sentinel conflation: depleted player shield refilled to max on crossing, missile homing ramp + exhaust delay re-rolled at every boundary (contradicts Missiles AGENTS.md), spaceship `pfDeltaRotations`/`pfFreezeTimes` silently dropped in transfer, latent armor-ternary twin. Give the transfer path authoritative semantics (explicit flag); spaceship `TransferData` fields added → version bump |
| [Frame/PlayerTransferUuidPreservation.md](Frame/PlayerTransferUuidPreservation.md) | Small | 2 | 3 | 3 | 2 | - | Player transfer re-adds mint a fresh uuid (plain `AddIndexableElement`; `TransferData` carries no uuid), contradicting the hub `AddIndexableElementWithId` convention — one-tick lost-update window silently drops `kUpdatePlayer` weapon toggles and `kUpdateFleet` (wingman navigates to the wrong cell for up to tens of seconds). Carry the uuid + id-preserving re-add; version bump. Grill: fix (recommended) vs accept+document |
| [Frame/FlagshipLossNavFallback.md](Frame/FlagshipLossNavFallback.md) | Small | 2 | 2 | 2 | 2 | - | Wingman stuck in flagship-follow nav mode 5 forever when no `kIsFlagship` player exists — the follow block has no exit path and mode 5 no arrival check; pathfinds to the stale last-flagship destination until a fleet StatusChange rescues. Verify the Game-level rescue window first, then add the no-flagship → island-seek fallback (RNG-draw-count-preserving). Grill: fallback target |
| [Engine/HeaderCompileFirewallSweep.md](Engine/HeaderCompileFirewallSweep.md) | Medium | 3 | 2 | 1 | 2 | - | Mirror the landed `FileManager`→`PackChunks` firewall on other `Engine.h`-aggregated classes whose heavy internals are pure impl detail: `AgentCommandServer` (private `nlohmann::json`/`jthread`/`function`/socket members, both builds), the `AudioManager`-owned by-value `StaticVoices`/`StreamingVoices` → `unique_ptr`+forward-decl (drops 4 headers from the PCH), and lower-priority higher-churn `TextureUploadManager`. Compile-hygiene only — every underlying header is already in the PCH via `ExternalHeaders.h`, so the win is incremental-build isolation (volatile internals leave the PCH → editing them recompiles the owner's TUs not the world) + encapsulation, NOT `#include` elimination. No runtime/CRC/wire/`kiVersion`/save exposure. Anti-candidates called out (base classes `ProfileManagerBase`/`GameBase`, public-member `ParticleManager`, core render/network managers, SOA/hot-path `Collection`). Grill: pimpl-shell vs de-aggregate-the-global per candidate. Best batched |
| [Engine/ReleaseStaticAnalysisGateEnforcement.md](Engine/ReleaseStaticAnalysisGateEnforcement.md) | Medium | 3 | 3 | 2 | 2 | - | **Decision plan.** Release client/server PREfast runs can emit Microsoft analysis diagnostics and still exit 0 because compiler `TreatWarningAsError` does not establish analyzer failure semantics. Verify a native Microsoft/MSBuild enforcement control first; otherwise add one deterministic compile-skill diagnostic-scan-and-fail wrapper. Define a narrow policy for existing C26498/C26459 output, preserve ordinary analysis-off builds, and prove both targets fail a controlled matching probe without manual log inspection. Build/workflow only; no runtime/CRC/wire/`kiVersion`/save exposure |
| [Frame/FireCooldownNegativeFloor.md](Frame/FireCooldownNegativeFloor.md) | Small | 2 | 2 | 3 | 3 | - | Floor two unbounded-negative-drift fire-cooldown timers at a small NEGATIVE sentinel (the three player cooldowns that are 'ready when `<= 0`' are already floored at `0.0f`; these two fire on strict `< 0.0f` so need a negative floor). Spaceship blaster cooldown `pfNextBlasterSpawnTimes` (`SpaceshipsPostRender::Update` decrement `Spaceships.cpp:679`; fires `:525`) drifts when no player is visible/facing; player missile cooldown `pfNextSecondarySpawnTimes` (`PlayersPostRender::SpawnMissiles` `PlayersCombat.cpp:388`; fires `:401`) drifts when not firing missiles. Both CRC'd (Spaceships `SharedMembers`, Players `SharedCrcMembers`) → floor shifts frame CRC → **`Frame::kiVersion` bump required** (`Frame.cpp:11-13`). Latent-only (gameplay-inert; any negative still fires), no wire/`.pack`/RNG change. Co-schedule with the Frame transfer/version-bump batch so one bump covers it |
| [Ui/Architecture_TweakSectionGameExtension.md](Ui/Architecture_TweakSectionGameExtension.md) | Medium | 3 | 2 | 2 | 3 | - | **Decision plan (present options).** Make the engine `TweakSection` enum game-extensible instead of naming game sections (`kHexShield`/`kParticles`) directly — the root-AGENTS.md reverse-direction violation. NOT a doc comment — the enum indexes fixed-size `[kCount]` arrays + a member-fn-pointer dispatch table + a `uint32_t` flags bitmask, and its index space is persisted by `game::TweaksSettings` (visibility/positions/subtabs/collapsed) → needs a `kiVersion` bump/migration. First step `/external-design-interface` on the game section-registration API. Client-only debug UI, no CRC/wire/save-game exposure |
| [Graphics/IslandNavContourResidency.md](Graphics/IslandNavContourResidency.md) | Medium | 3 | 2 | 2 | 3 | - | Server-side per-template `IslandTemplate::mNavContour` (~5-50 KB per template × ~65 templates) is permanently resident — built once in `WaitForElevationMaps`, freed only at `~IslandTerrain`. See `IslandResidentMemoryScaling_Overview.md`. Smallest island bucket; the heightmap it derives from is now R16-compressed in place (no residency lifecycle to attach to), so resolve on its own measurement — quantize/compress `mNavContour`, or close if the aggregate cost is negligible |
| [Graphics/DisabledPassGatingPerfAudit.md](Graphics/DisabledPassGatingPerfAudit.md) | Medium | 3 | 2 | 2 | 3 | - | Measure-then-decide audit of record-once passes that run while their feature is disabled (CB re-record ban means gating-when-disabled needs a destroy-tier edge re-record; recorded clears/dispatches can't be cheaply indirect-gated). Sites: `RecordSmokeSpreadPipeline` / `RecordWindSpreadPipeline` (`CommandBufferRecordGlobal.cpp:121-445`), object-shadows render+blur and wind deposit (`CommandBufferRecordMain.cpp:130-178`). Documents the shadow Item-1 ACCEPT precedent; "accept" is the likely outcome unless a pass measures non-trivial. Object-shadows has no clean on/off edge (day-cycle continuous `fObjectShadowsIntensity`). Measured 2026-07: smoke spread is 1210 µs idle, but that is the per-frame full-texture clears — addressed content-side by `SmokeSpreadFullTextureClearElimination`; re-measure the smoke residue here after it lands |
| [Graphics/WaterHalfResolutionShading.md](Graphics/WaterHalfResolutionShading.md) | Large | 4 | 3 | 2 | 3 | Documents/Plans/Graphics/WaterFragmentCostReduction.md | Half-res offscreen water shading (coarse-LOD mesh) + full-res depth-only draw + edge-aware composite — saves est. ~1.3–1.5 ms of the measured 2329 µs pass (largest single lever in the frame); new `gWaterShadingResolution` slider default-off until A/B. Record-once CBs and snap grid untouched. Land after `WaterFragmentCostReduction` |
| [Graphics/ModelSpecularAntialiasing.md](Graphics/ModelSpecularAntialiasing.md) | Medium | 3 | 2 | 2 | 3 | - | Geometric specular AA for `Model.frag`'s GGX Cook-Torrance specular (EWNS loop + direct sun BRDF `D_GGX` sites): normal-derivative slope-variance kernel added to `alphaRoughness^2` (Tokuyoshi/Kaplanyan / Vlachos GDC15 production form), compile-time `MODEL_SPEC_AA_MODE` select like Water's, model-specific `fPbrSpecAAVariance`/`fPbrSpecAAThreshold` sliders. De-flickers low-roughness metals so the model pipeline can optionally drop `kSampleShading` (per-sample shading perf win) — final drop gated behind A/B + user judgement. IBL path out of scope (cubemap-mip-prefiltered). Client/graphics-only, no determinism/CRC/`kiVersion`/wire; shader repack. Decided: model-specific uniforms, widen `D` only |
| [Graphics/Managers/Architecture_BindlessSlotLifecycle.md](Graphics/Managers/Architecture_BindlessSlotLifecycle.md) | Large | 4 | 4 | 3 | 3 | Documents/Plans/Graphics/Managers/CorruptTextureChunkLifecycleHardening.md | Consolidate the island bindless mint/evict/restore lifecycle — currently spread across `TextureDescriptors` patching (`WriteArrayElementFromLive`/`UnregisterBindingsForKey`), `IslandTerrain` sweeps (mint `:525-595`, EvictionSweep `:679-777`, restore `:819-829`), TextureManager placeholder arrays, the RenderGlobal fence window, and the verifier carve-out (`PipelineManager.cpp:856-862`) — into one `BindlessSlotRegistry` enforcing the `Managers/AGENTS.md:29` eviction-symmetry invariant in code and narrowing the staleness verifier's deliberate blind spot. Targets the directory's documented GPU use-after-free class; execute alone (see reciprocal Coordination) |
| [Graphics/Architecture_PipelineRegistrationOwnership.md](Graphics/Architecture_PipelineRegistrationOwnership.md) | Medium | 3 | 2 | 2 | 3 | - | Make pipeline registration symmetric and owned at one altitude: `PipelineDescriptorWriter::Write` plants raw `Pipeline*` in `TextureDescriptors` registries that only whole-`PipelineManager` rebuild clears (`Pipeline::Destroy` never unregisters — a use-after-free class for any future out-of-rebuild create, fenced off today only by full-rebuild discipline); move the `kModel` auto-append (lighting/shadow/smoke knowledge) out of generic `Pipeline::Create`; formalize the pre-`Create` member-poking protocol. Decided: `kModel` append moves into `ModelPipeline::Create`; lifecycle via new `TextureDescriptors::UnregisterPipeline` |
| [Graphics/PipelineDescriptorInfosRightSize.md](Graphics/PipelineDescriptorInfosRightSize.md) | Medium | 3 | 2 | 2 | 3 | - | **Decision plan (present options).** `PipelineInfo::pDescriptorInfos[32]` (`Pipeline.h:102`, ~2.5 KB per `Pipeline`) is a per-instance by-value array (per-pass + per-material-per-scene via `ModelPipeline::mpPipelines`) filled to a small runtime count. NOT a clean swap — `Pipeline::Create`/`ModelPipeline::Create`/`PipelineCreator`/`PipelineDescriptorWriter` scan/index to the compile-time max and the `kModel` auto-append writes trailing slots; dimension is the shared `.pack` `ShaderHeader::kiMaxDescriptorSetLayoutBindings`. A — accept + document (likely); B — right-size with populated-count + headroom container (touches GPU-setup scan sites, playtest); C — separate smaller runtime cap. Client/graphics-only, runtime mirror not serialized; profile slot waste first. Sequence with `PipelineRegistrationOwnership`/`BindlessSlotLifecycle` (same `Create`/`Write` paths) |
| [Graphics/TextureChunkCpuPoolReclaim.md](Graphics/TextureChunkCpuPoolReclaim.md) | Medium | 3 | 2 | 2 | 3 | - | **Decision plan (present options).** Reclaim the texture lazy-chunk CPU pool bytes that stay committed after the one-time GPU staging copy (`TextureManager.cpp:591-592` nulls the pointer but never decommits the pages) — an acknowledged gap in `IslandResidentMemoryScaling_Overview.md`. Reuses the existing `DecommitChunkRange`/`RecommitAndReloadChunkRange` lazy-pool API and threads a `MEM_COMMIT` recommit into `ResetTextureChunkStates`/`LoadChunk`. Unlike the mesh one-shot this runs at steady-state LRU cycle time, so it adds a main-loop decommit/recommit path + churn design decision (A accept / B grace-threshold / C unconditional). Client/graphics-only, no determinism/CRC exposure. Profile pool bytes + cycling churn first |
| [Network/ServerSpawnRateBounding.md](Network/ServerSpawnRateBounding.md) | Medium | 3 | 3 | 3 | 3 | - | **Decision plan (present options).** The per-fleet member cap bounds `Fleet::members` but not the player mint — `ServerBroadcaster::BuildFrameInputs` emits a `kSpawnPlayer` into the CRC'd origin frame per waiting client before `OnPlayerSpawned`'s cap runs, so a client holding a fleet at cap accumulates one un-rostered GUID-stamped "free agent" per tick (per-client owned / origin-frame player count unbounded). Strictly improves the pre-cap state + free agents are cleaned downstream, so: A enqueue-time gate (mint-avoiding, rec), B `BuildFrameInputs` mint gate (CRC/determinism-sensitive), C accept+document. Server-only, no wire/`kiVersion` change |
| [Network/FleetRequestsByGuid.md](Network/FleetRequestsByGuid.md) | Medium | 3 | 3 | 3 | 3 | - | Re-key the four fleet requests (delete/spawn-into/respawn/nav-delay) from mutable vector index to the existing stable `FleetGuid` — a same-tick delete (drained before spawn-into) makes an in-flight index-based request hit the wrong fleet. Wire change in game payloads (engine-opaque, no protocol-version gate); client/server must move together — batch under the reciprocal Coordination contract |
| [Network/Refactor_ServerClientPlayerRegistry.md](Network/Refactor_ServerClientPlayerRegistry.md) | Large | 4 | 4 | 3 | 3 | Documents/Plans/Network/SubscriptionLifecycleRaceHardening.md | `ClientPlayerRegistry` replaces the `mClientOwnedPlayerIds`/`authorizedCoords` parallel-vector invariant (mutated across 4 TUs); one relink routine for connect+load; shared fleet-refresh helper (adds the missing dead-flagship shift to `OnClientConnected`); engine `ClientConnection` four parallel per-slot vectors → one `SlotState` vector, defensive `FreeSlot` guards deleted. Server bookkeeping, no wire change |
| [Network/Refactor_RingLayoutRetention.md](Network/Refactor_RingLayoutRetention.md) | Medium | 3 | 3 | 3 | 3 | - | `RingLayout` value type + single `ComputeRetention` folds the triplicated head-advance math; one writeback consumer; merge `RunPrimaryReplay`/`RunTwoTierFallback` into one parameterized routine. Behavior-preserving but HIGH determinism exposure (reshapes rollback/replay state selection) — soak under network-sim loss/jitter. Client-only, no wire change |
| [Frame/Architecture_CollectionHelperDedup.md](Frame/Architecture_CollectionHelperDedup.md) | Medium | 3 | 2 | 2 | 3 | - | Helper dedup: shared `MinLightingDepositSize` (verbatim correctness rule ×2), controller interpolate/spawn-seed helpers (PointLights/Puffs; WindRadials parameterized or left out — verified divergent), IslandTerrain placement-sample + normal templates (must preserve `DeterministicSinCos` + reciprocal-multiply forms exactly — `/fp:strict` CRC path), `SmokeTrailsUpdate.cpp` file split |
| [Frame/CollectionReadIndexHardening.md](Frame/CollectionReadIndexHardening.md) | Medium | 3 | 3 | 3 | 3 | - | Validate deserialized SOA indices in the shared engine `Collection::Read`/`OptionalIdToIndex<T>::Read` — a raw `int64_t` index stored with no `[0,iCount)` check reaches `IdToIndex().at()`→`puiTargets[i]`, so a corrupt save OR a hostile/corrupt client network full-state → OOB SOA read; throw `CorruptStreamException` (caught on both paths). Plus thread the real per-element stride into `ValidateDeserializedCountCapacity` so the 256 MiB byte ceiling (landed inert with `iElementBytes=1`) fires. Shared engine/CRC/client-network path — replay + full-state soak; grill A (thread bound) vs B (second pass). Surfaced+deferred by `SaveLoadTrustBoundaryHardening` |
| [Frame/Architecture_PhaseHookOptIn.md](Frame/Architecture_PhaseHookOptIn.md) | Medium | 3 | 3 | 3 | 3 | - | Opt-in collection phase hooks via `if constexpr (requires ...)` in the `ForEach*` folds — deletes ~80 empty stubs + the phantom `uiFrameId` render-exclusion + the hand-maintained `InterpolateRenderTypes` list. Risk: a hook signature mismatch becomes a silent phase skip (replay-divergence class) — needs a static_assert backstop + same-session hub AGENTS.md and add-collection skill updates. Cross-layer (game stubs too) |
| [Graphics/WindowedLightingShadowDispatch.md](Graphics/WindowedLightingShadowDispatch.md) | Large | 4 | 2 | 2 | 4 | - | Window the lighting (deposit/spread/combine/temporal) + shadow passes to the live cropped sub-region instead of processing the full headroom texture every frame: indirect-compute dispatch + min-texel invocation offset for the compute passes, dynamic scissor for deposit/spread, and fold the history write into `LightingTemporal` to drop the four full-texture copies. Enabled by the existing `CLAMP_TO_BORDER` edge samplers + steady-state-target texel ramp; reuses the existing `WriteIndirectComputeBuffer`/`RecordComputeIndirect` compute-indirect helpers. Client/graphics-only. Measured 2026-07: spread textures are 0.05×/0.01×-multiplier tiny — the 841 µs Spread timer is in-window gather, which windowing does not touch (scissor recovers ~5–10%); realistic total ≈120–200 µs from combine/temporal/shadow. Content/cadence gating of Spread: `Graphics/LightingSpreadEmptySkipCadence.md` (co-schedule — same functions) |
| [Network/Architecture_WireFormatPairing.md](Network/Architecture_WireFormatPairing.md) | Large | 4 | 3 | 3 | 4 | - | Every engine message layout is hand-mirrored across send/receive (17 packet types; magic header sizes in ×2-3 copies incl. `NetworkSimulation.h`'s offset-4 tick parse; `SendSimplePacket` duplicated verbatim in `Client.h`/`Server.h`) — the directory's principal latent-desync hazard, lockstep by convention only. Colocate per-message write/read pairs (byte-identical, converted message-by-message with interop smoke tests), dedupe the send template, retire the magic offset. No wire change |
| [Network/WireFormatPairingGameSide.md](Network/WireFormatPairingGameSide.md) | Large | 4 | 3 | 3 | 4 | Documents/Plans/Network/Architecture_WireFormatPairing.md | Game-side extension of `Architecture_WireFormatPairing` (lands after it): paired write/read for assign/player-state/fleet-sync layouts (byte-identical — retires the hand-summed 16/17/36/9 size constants mirrored across three files), structural `PlayerStateWireType` send/decode coupling, migrate `kClientSpawnRequest`/`ClientRequestFlags`/`PendingSpawnRequest` out of the engine protocol to a `GamePacketType` (wire change, batch version bump), + parse-boundary riders |
| [Graphics/IslandMeshArenaResidency.md](Graphics/IslandMeshArenaResidency.md) | Large | 4 | 3 | 3 | 4 | - | GPU-side of the island residency strategy (the CPU payload is handled separately — the heightmap landed as R16 in-place quantization; see `IslandResidentMemoryScaling_Overview.md`). ~119.5 MiB permanently-resident per-template mesh (`mMeshBuffer`) can't use the texture-LRU evict path — the mesh is bound in the record-once terrain CB (`CommandBufferRecordMain.cpp:261-263`), so freeing it breaks the CB invariant. Decided: stable-handle VMA-virtual-block mesh arena + suballocation (degenerate evicted draws), option A over the bindless vertex-pull alternative (B). Bounds GPU mesh by concurrent residency. Client/graphics-only |
| [Graphics/IslandPlacementSsboResidency.md](Graphics/IslandPlacementSsboResidency.md) | Medium | 3 | 2 | 3 | 4 | - | **Decision plan.** Smallest island resident bucket: `Islands::mIslandsStorageBuffers` (~16.4 MiB at 70 templates) reserves a fixed `kiMaxPlacementsPerTemplate(1024)` slab per template × 4 framebuffers, scaling with total template count. A — compact arena keyed on active templates (touches the record-once-CB `firstInstance` bake + per-frame write); B — right-size `kiMaxPlacementsPerTemplate`; C — accept + fix the "negligible" comment (omits ×4). Profile first. Client/graphics-only. Lowest priority of the island series |
| [Graphics/IslandHeightmapRouteDedup.md](Graphics/IslandHeightmapRouteDedup.md) | Large | 4 | 2 | 2 | 4 | - | **Speculative — profile first.** Deferred dedup half of the landed R16 heightmap plan: content-address byte-identical heightmap regions shared across routes/leaves, alias one lazy-pool region from many templates. Touches `.pack` layout + `IslandHeader` + `DataHeader::kiVersion` + `ExportIsland::GetVersion` (29→30); both-builds heightmap aliasing stays byte-identical (no CRC/determinism change). Reading the bake, leaves are disjoint spatial tiles and routes are independent Gaea bakes, so byte-identical redundancy is expected near-zero — likely closes accept-and-document. Independent of the (not-taken) residency-lifecycle idea |
| [Documents/TrimOversizedAgentMemory.md](Documents/TrimOversizedAgentMemory.md) | Small | 2 | 3 | 0 | -1 | - | Trim nine oversized AGENTS.md documents to their bt-token-v1 targets without changing guidance semantics |
| [Tools/StructuredWorktreeCliBuildResults.md](Tools/StructuredWorktreeCliBuildResults.md) | Large | 4 | 3 | 3 | 4 | - | Replace inherited MSBuild console output with a retained-log structured result and add rollback-safe AgentTools promotion during approved session landing |
### Reference / Index Documents

Meta/overview docs that are never executed as plans — excluded from the priority walk and orphan scans.

| Document | Purpose |
|----------|---------|
| [Graphics/ShaderReview/00_Overview.md](Graphics/ShaderReview/00_Overview.md) | Shader-review effort overview and cross-cutting-findings index; the per-subsystem plans it references were either executed-and-deleted or never authored |
| [Graphics/IslandResidentMemoryScaling_Overview.md](Graphics/IslandResidentMemoryScaling_Overview.md) | Measurement + architecture findings anchoring the island resident-memory scaling plan series (GPU-mesh arena, heightmap R16 + route-dedup follow-up, SSBO arena, NavContour); records the record-once-CB + monolithic-pool constraints so the sub-plans don't re-derive them |
