# Plan Execution Order

All refactor and bugfix plans — debt reduction that doesn't add a new engine capability — sorted by score (lowest = highest priority).

Score = Effort − Impact + Risks (lower = higher priority)

`Tier` column is an informal size/risk descriptor (**Quick Win** / **Small** / **Medium** / **Large** / **Architectural**); it does not affect score or ordering.

Counterpart: `Documents/Features/Order.md` holds brand-new additions. See `Documents/CLAUDE.md` for the distinction. Scores are not comparable across the two files — they were originally derived from a single sort and then split.

This file reflects only the CURRENT set of live plans. It is not a changelog — landed/dropped plans, and the sessions that produced them, are not recorded here.

## Plans

| Plan | Tier | Effort | Impact | Risks | Score | Notes |
|------|--------|--------|--------|-------|-------|-------|
| [Frame/CrcVersionGateBump.md](Frame/CrcVersionGateBump.md) | Quick Win | 1 | 3 | 1 | -1 | Bump `Frame::kiVersion` base 115→116 — the landed CRC-mixing change (XOR → ordered `* kCrcMultiplier` fold, commit c1b403d3) changed every frame CRC without a bump, so straddling replays / client-server pairs pass the version gate yet false-desync; add the bump-on-CRC-algorithm-change rule comment at `kCrcMultiplier`. Grill: plain bump vs dedicated `kiCrcAlgorithmVersion` term |
| [Network/ResyncFullStateRepair.md](Network/ResyncFullStateRepair.md) | Small | 2 | 5 | 3 | 0 | Fix dead desync-recovery resync path: `ClassifyFullState` drops every resync full state on `kActive` slots (`ResetCoordStatesForResync` resets CoordFrames but not engine slot states), so recovery only ever "succeeds" by disconnect after `kiMaxDesyncsBeforeDisconnect`. Commit on `kActive` when coord+epoch match. No wire change; co-schedule with `SubscriptionLifecycleRaceHardening` (same function) |
| [File/MeshRecommitFailureObservability.md](File/MeshRecommitFailureObservability.md) | Quick Win | 1 | 2 | 1 | 0 | Return `bool` from `RecommitAndReloadChunkRange` — its `MEM_COMMIT` soft-fail is unobservable by sole caller `CreateClientMeshBuffers`, which then memcpys the still-decommitted pages (AV during device-loss recovery). Caller skips the template's upload on failure (zero-instance indirect draw). Grill: skip-and-log vs fail-loud |
| [Graphics/Managers/TextureUploadTeardownRaces.md](Graphics/Managers/TextureUploadTeardownRaces.md) | Small | 2 | 3 | 2 | 1 | Fix two verified defects in the `TextureUploadManager::WaitIdle` drain-probe handshake: device-loss recovery deadlocks when the upload thread exited via its own `DeviceLostException` catch (`mbShutdown` still false — nobody acks the probe), and the unconditional probe `release()` leaves a stray permit letting one real `vkQueueSubmit` race `vkDeviceWaitIdle`. Exit-ack flag + swallow the probe permit in the ack branch |
| [Meta/ReviewSweepQuickWins.md](Meta/ReviewSweepQuickWins.md) | Small | 2 | 2 | 1 | 1 | 15-item mechanical batch from the 9c0b44d8..672b292e post-implementation review: dead `fTerrainEarlyOut` uniform+wrapper, CurveWidget 5px-vs-24px grab radius, phantom `Projects\Data` include dirs (×6), `stoll` overflow in `RemoveOrphanedLeafFolders`, `UpdateFocus` alloc-suppress, `RecordDrawIndirectSet2` missing ASSERT, redundant pipeline `Destroy()`s, `u8string()` at EXR/KTX path boundaries, cached-chunk post-read check, `TweakSectionFlags` assert bound, etc. |
| [Network/StatusChangeCodecHardening.md](Network/StatusChangeCodecHardening.md) | Medium | 3 | 4 | 2 | 1 | Consolidated StatusChange codec hardening: check/log LZ4 compress failure + enforce the per-cell count cap send-side in `Server::BufferFrame` (silent-desync source — 64 KB buffer vs ~123 KB worst case), `BoundedCursor` per-type validation + unknown-type reject + truncation rollback in `DeserializeStatusChangeBatch`, clamp wire `iUncompressedSize` in `DecompressAndReadFrame` + `DecompressStatusChangeBatch` (~2 GB alloc), bulk workbuffer zero-fill. No wire change |
| [Network/ServerTrustBoundaryHardening.md](Network/ServerTrustBoundaryHardening.md) | Medium | 3 | 4 | 2 | 1 | Batch of server hostile-input/robustness fixes: clamp/reject NaN `fNavigationDelay` before it freezes fleet nav in server sim state; cap+dedup unbounded fleet/spawn/paused-subscribe/resync queues (DoS); `ClientHello` null-reject (ghost-accept) + handshake idempotency (identity-swap); `Server::Receive` try/catch parity; `SendPacket` ENet leak on send-fail; discovery/`enet_initialize` silent-init logs; resync kError→kWarning. No wire change |
| [Network/ClockErrorMachineryCleanup.md](Network/ClockErrorMachineryCleanup.md) | Small | 2 | 3 | 2 | 1 | **Decision plan (present options).** Sustained-clock-error disconnect is unreachable — snap (28) fires + zeroes the counter before the disconnect threshold (64/4 frames) can hit; worst case snap-loops forever. Delete the dead machinery vs make it real (snap-rate escalation). Riders: move `kiClockSnapThreshold` to `NetworkProtocol.h`; stop the snap discarding `miLatestServerTick` (restores the `ClientUpdate` hard ceiling during loss bursts); fix Network.md nudge claim (0.5 tick/frame, not 4 steps). Client clock policy only, no wire |
| [Network/NetworkSimulationReliableDelay.md](Network/NetworkSimulationReliableDelay.md) | Medium | 3 | 3 | 1 | 1 | Delay reliable packets per-channel FIFO in `NetworkSimulation` (delay-only, never drop — sim sits above ENet) so handshake/subscribe/full-state/unsubscribe flows see simulated latency instead of zero — where the subscription-race desync bugs hide. Preserve `if constexpr` zero-cost + constant-seed reproducibility; don't extend the coord-specific drop-log offset parse. Compile-disabled in production |
| [Network/AuditSweepQuickWins.md](Network/AuditSweepQuickWins.md) | Small | 2 | 2 | 1 | 1 | 13-item mechanical Network batch: variadic `Send*Request` helper, fleet-data pass-through/triple-hop collapse, `FindGuidForClient` scan removal, deterministic fleet-block save order (sort by `ClientGuid`), drop always-origin `spawnCoord`, rename `mSpawns`→`mBroadcastStatusChanges` + `ServerFleetManagerUtils`→`ServerFleetSerialization` (vcxproj/filters), always-true `UnsubscribeStaleCoords` check, `SendUnsubscribe` guard-before-mutate, `ServerSession` dtor simplification, `PlayerEvents` parse-boundary/`.at()`/Pch polish, stale Network.md forward-sim line. No wire/CRC change |
| [Network/Refactor_DrainContractUnification.md](Network/Refactor_DrainContractUnification.md) | Small | 2 | 2 | 1 | 1 | **Decision plan (present options).** Rename the `Client`/`Server` receive accessors by contract — `Received*` (cleared-next-poll), `Pending*`+`ClearPending*` (persist-until-served), `Take*` (consume-on-call) — replacing one `Drain*` name that hides three incompatible lifetimes (`Server.h:182-186`: five look-alikes, two contracts). Compile-checked rename, no wire/behavior change (Option A); Option B unifies to `Take*` and drops `Poll`'s silent discard (bigger). Grill: A vs B |
| [Graphics/Managers/CorruptTextureChunkLifecycleHardening.md](Graphics/Managers/CorruptTextureChunkLifecycleHardening.md) | Small | 2 | 2 | 2 | 2 | Corrupt-chunk soft-fail hardening: the upload-thread catch can strand a live `vkImage` on a `kReady` chunk (evict-without-destroy → VMA leak assert at teardown), and `WriteArrayElementFromLive`'s placeholder fallback misses non-null never-adopted Textures with null views (invalid bindless descriptor write via `RestorationSweep`'s `>= kReady` gate). Land before `BindlessSlotLifecycle` |
| [Network/PackIntegrityHandshake.md](Network/PackIntegrityHandshake.md) | Small | 2 | 2 | 2 | 2 | Manifest-derived pack-integrity token in `kClientHello` so a damaged/mismatched pack rejects at connect with a clear reason instead of silently desyncing on zero-filled sim chunks (`LoadChunk` soft-fail zero-fills heightmap/nav data). Wire change + protocol version bump. Grill: token option A (full hash) / B (manifest CRC, recommended) / C (B + runtime escalation) |
| [Network/SubscriptionLifecycleRaceHardening.md](Network/SubscriptionLifecycleRaceHardening.md) | Medium | 3 | 4 | 3 | 2 | Three subscription-lifecycle races that strand slots permanently: (a) ghost full-state adoption on a freed `kUnsubscribed` slot → frozen cell; (b) server silently drops inactive-slot unsubscribe (no ack) + epochless `kClientUnsubscribe` frees reused slots — ack unconditionally, add epoch (1→2 wire bytes, version bump); (c) no watchdog on transitional slot states. Wire change + `kuiProtocolVersion` bump (batch per Dependencies); grill: watchdog threshold. Placeholder-in-slot generation-id redesign deferred (Out of scope) |
| [Network/ClientFullStateEdgeFixes.md](Network/ClientFullStateEdgeFixes.md) | Medium | 3 | 4 | 3 | 2 | Two game-client full-state edge bugs: (a) full states / reliable packets received while desync-stalled are drained-and-destroyed but leave the slot `kActive` → `mCoordFrames.at(coord)` throw + silent packet loss (grill: skip `Poll()` while stalled vs run adoption pre-short-circuit); (b) `pendingFullState` past a `serverUpdates` gap is never injectable → per-frame full-replay livelock masked by the `has_value()` DEBUG_BREAK bypass — adopt directly as confirmed frame. Client-only, state-selection only, no wire/`kiVersion` change |
| [Network/Refactor_ClientResetUnification.md](Network/Refactor_ClientResetUnification.md) | Medium | 3 | 4 | 3 | 2 | Collapse four overlapping client reset paths (`DisconnectFromServerBase`, `ResetForServerLoad`, desync `ResetCoordStatesForResync`/`Reset`, initial-connect) into one `ResetSession(ResetReason)` with reason-gated steps over `= {}`-reset sub-structs (`ClockSyncState`/`SubscriptionTracking`), extending the `CoordFrames::ResetClientState` convention. Fixes the verified clock-field drift (`ResetForServerLoad` resets 4 of 9); union table is the plan. Reconciliation-adjacent state; sequence after `Refactor_SessionBaseCollapse` |
| [Network/DesyncDebugInfrastructureGating.md](Network/DesyncDebugInfrastructureGating.md) | Medium | 3 | 3 | 2 | 2 | Gate the per-tick full-frame `BufferFullFrame` serialization/256-ring (only `kClientDebugFrameRequest` consumes it) behind a flag/on-demand/shrunk-ring — production CPU + memory tax; add per-client cooldowns to unthrottled `kClientDesyncReport` + `kClientDebugFrameRequest` (log/CPU/bandwidth amplification). Server-only, no wire/CRC change. Grill: gating option |
| [Network/ServerPauseAndResetSemantics.md](Network/ServerPauseAndResetSemantics.md) | Medium | 3 | 3 | 2 | 2 | Two server tick-orchestration fixes: requests injected/drained while paused (`iFullTicks==0`) are wiped by `BuildFrameInputs` before any tick consumes them (weapon toggles lost, re-queued flagship updates dropped) — gate the flagship drain + the `PreTickNetwork` update-player clear on the pause flag, matching the new-sub/resync persist-until-served pattern; complete the local-menu `kResetFrame` fresh game (add `ResetClientsForLoad` + `SetNextGlobalId(1)` to match networked `ServerReset`). No wire change. Grill: gate granularity, extract shared `FreshGameReset` |
| [Network/Refactor_SessionBaseCollapse.md](Network/Refactor_SessionBaseCollapse.md) | Medium | 3 | 2 | 1 | 2 | Delete `ClientSessionBase`/`ServerSessionBase`/`ClientDataReceiver` (each one subclass, welded to `gpGame`), fold bodies into game `ClientSession`/`ServerSession` across sibling TUs; removes a zero-capability layer and reunites the clock + subscription mechanisms split across the seam. Engine calls already go through game globals (no call-site edits); only `Engine.h` + 2 vcxproj pairs flip. Move-only, no wire/CRC/determinism. Grill: clock-TU landing + server-timing split |
| [Graphics/IslandNavContourResidency.md](Graphics/IslandNavContourResidency.md) | Medium | 3 | 2 | 2 | 3 | Server-side per-template `IslandTemplate::mNavContour` (~5-50 KB per template × ~65 templates) is permanently resident — built once in `WaitForElevationMaps`, freed only at `~IslandTerrain`. See `IslandResidentMemoryScaling_Overview.md`. Smallest island bucket; the heightmap it derives from is now R16-compressed in place (no residency lifecycle to attach to), so resolve on its own measurement — quantize/compress `mNavContour`, or close if the aggregate cost is negligible |
| [Graphics/DisabledPassGatingPerfAudit.md](Graphics/DisabledPassGatingPerfAudit.md) | Medium | 3 | 2 | 2 | 3 | Measure-then-decide audit of record-once passes that run while their feature is disabled (CB re-record ban means gating-when-disabled needs a destroy-tier edge re-record; recorded clears/dispatches can't be cheaply indirect-gated). Sites: `RecordSmokeSpreadPipeline` / `RecordWindSpreadPipeline` (`CommandBufferRecordGlobal.cpp:121-445`), object-shadows render+blur and wind deposit (`CommandBufferRecordMain.cpp:130-178`). Documents the shadow Item-1 ACCEPT precedent; "accept" is the likely outcome unless a pass measures non-trivial. Object-shadows has no clean on/off edge (day-cycle continuous `fObjectShadowsIntensity`) |
| [Graphics/WindowedLightingShadowDispatch.md](Graphics/WindowedLightingShadowDispatch.md) | Large | 4 | 3 | 2 | 3 | Window the lighting (deposit/spread/combine/temporal) + shadow passes to the live cropped sub-region instead of processing the full headroom texture every frame: indirect-compute dispatch + min-texel invocation offset for the compute passes, dynamic scissor for deposit/spread, and fold the history write into `LightingTemporal` to drop the four full-texture copies. Enabled by the existing `CLAMP_TO_BORDER` edge samplers + steady-state-target texel ramp; reuses the existing `WriteIndirectComputeBuffer`/`RecordComputeIndirect` compute-indirect helpers. Client/graphics-only |
| [Graphics/ModelSpecularAntialiasing.md](Graphics/ModelSpecularAntialiasing.md) | Medium | 3 | 2 | 2 | 3 | Geometric specular AA for `Model.frag`'s GGX Cook-Torrance specular (EWNS loop + direct sun BRDF `D_GGX` sites): normal-derivative slope-variance kernel added to `alphaRoughness^2` (Tokuyoshi/Kaplanyan / Vlachos GDC15 production form), compile-time `MODEL_SPEC_AA_MODE` select like Water's, model-specific `fPbrSpecAAVariance`/`fPbrSpecAAThreshold` sliders. De-flickers low-roughness metals so the model pipeline can optionally drop `kSampleShading` (per-sample shading perf win) — final drop gated behind A/B + user judgement. IBL path out of scope (cubemap-mip-prefiltered). Client/graphics-only, no determinism/CRC/`kiVersion`/wire; shader repack. Grill: model-specific vs reused uniforms; widen `D` only vs `D`+`V` |
| [Graphics/Managers/Architecture_BindlessSlotLifecycle.md](Graphics/Managers/Architecture_BindlessSlotLifecycle.md) | Large | 4 | 4 | 3 | 3 | Consolidate the island bindless mint/evict/restore lifecycle — currently spread across `TextureDescriptors` patching (`WriteArrayElementFromLive`/`UnregisterBindingsForKey`), `IslandTerrain` sweeps (mint `:525-595`, EvictionSweep `:679-777`, restore `:819-829`), TextureManager placeholder arrays, the RenderGlobal fence window, and the verifier carve-out (`PipelineManager.cpp:856-862`) — into one `BindlessSlotRegistry` enforcing the `Managers/CLAUDE.md:29` eviction-symmetry invariant in code and narrowing the staleness verifier's deliberate blind spot. Targets the directory's documented GPU use-after-free class; execute alone (see Dependencies) |
| [Graphics/Architecture_PipelineRegistrationOwnership.md](Graphics/Architecture_PipelineRegistrationOwnership.md) | Medium | 3 | 2 | 2 | 3 | Make pipeline registration symmetric and owned at one altitude: `PipelineDescriptorWriter::Write` plants raw `Pipeline*` in `TextureDescriptors` registries that only whole-`PipelineManager` rebuild clears (`Pipeline::Destroy` never unregisters — a use-after-free class for any future out-of-rebuild create, fenced off today only by full-rebuild discipline); move the `kModel` auto-append (lighting/shadow/smoke knowledge) out of generic `Pipeline::Create`; formalize the pre-`Create` member-poking protocol. Grill: destination layer + register-up vs unregister-on-Destroy |
| [Graphics/PipelineDescriptorInfosRightSize.md](Graphics/PipelineDescriptorInfosRightSize.md) | Medium | 3 | 2 | 2 | 3 | **Decision plan (present options).** `PipelineInfo::pDescriptorInfos[32]` (`Pipeline.h:102`, ~2.5 KB per `Pipeline`) is a per-instance by-value array (per-pass + per-material-per-scene via `ModelPipeline::mpPipelines`) filled to a small runtime count. NOT a clean swap — `Pipeline::Create`/`ModelPipeline::Create`/`PipelineCreator`/`PipelineDescriptorWriter` scan/index to the compile-time max and the `kModel` auto-append writes trailing slots; dimension is the shared `.pack` `ShaderHeader::kiMaxDescriptorSetLayoutBindings`. A — accept + document (likely); B — right-size with populated-count + headroom container (touches GPU-setup scan sites, playtest); C — separate smaller runtime cap. Client/graphics-only, runtime mirror not serialized; profile slot waste first. Sequence with `PipelineRegistrationOwnership`/`BindlessSlotLifecycle` (same `Create`/`Write` paths) |
| [Graphics/Managers/Refactor_PipelineManagerSplit.md](Graphics/Managers/Refactor_PipelineManagerSplit.md) | Medium | 3 | 1 | 1 | 3 | `/reduce-file` split of `PipelineManager.cpp` (883 lines, review-flagged OPTIONAL) by render subsystem — whole-function relocation only (no body fragmentation; ctor boot-order orchestration + shader-load + dtor + `VerifyAllDescriptorGenerations` stay in `PipelineManager.cpp`). Move the lighting/shadow/water cluster (`CreateLightingPipelines`/`CreateLightingBlurPipelines`/`CreatePipelineShadows`/`CreateLightingShadowDependentPipelines`) into new `PipelineManagerLightingShadow.cpp`, and the terrain/smoke-wind/particle/debug cluster (`CreateTerrainDataPipelines`/`CreateSmokeWindPipelines`/`CreateParticlePipelines`/`CreateDebugRenderPipelines`) into new `PipelineManagerEffects.cpp`; the 8 `Create*` members have no external callers. Header `PipelineManager.h` + `gpPipelineManager` unchanged; new cpps whole-file `#if defined(BT_CLIENT)`-wrapped + client-vcxproj/filters only. Move-only: no `kiVersion`/`.pack`/CRC/wire/determinism exposure. Plan only — `/reduce-file` runs at execution; grill: split vs accept-and-document |
| [Graphics/TextureChunkCpuPoolReclaim.md](Graphics/TextureChunkCpuPoolReclaim.md) | Medium | 3 | 2 | 2 | 3 | **Decision plan (present options).** Reclaim the texture lazy-chunk CPU pool bytes that stay committed after the one-time GPU staging copy (`TextureManager.cpp:591-592` nulls the pointer but never decommits the pages) — an acknowledged gap in `IslandResidentMemoryScaling_Overview.md`. Reuses the existing `DecommitChunkRange`/`RecommitAndReloadChunkRange` lazy-pool API and threads a `MEM_COMMIT` recommit into `ResetTextureChunkStates`/`LoadChunk`. Unlike the mesh one-shot this runs at steady-state LRU cycle time, so it adds a main-loop decommit/recommit path + churn design decision (A accept / B grace-threshold / C unconditional). Client/graphics-only, no determinism/CRC exposure. Profile pool bytes + cycling churn first |
| [File/FileManagerReduceFile.md](File/FileManagerReduceFile.md) | Medium | 3 | 1 | 1 | 3 | `/reduce-file` mechanical whole-function split of `FileManager.cpp` (1023 lines, over the 1000-line threshold). Relocate cohesive groups (loading thread + chunk state machine + `ReadChunkData` + the decommit/recommit pair) into new shared sibling TU(s) under `Engine\File`; `FileManager.h`/`gpFileManager` unchanged; move-only, no `kiVersion`/`.pack`/CRC/determinism exposure. **Gate on `File/Architecture_FileManagerSplitDecision.md`** — its option A (class split) likely obviates this; option B/C makes this the concrete size fix. Plan only; `/reduce-file` runs at execution |
| [Network/FleetRequestsByGuid.md](Network/FleetRequestsByGuid.md) | Medium | 3 | 3 | 3 | 3 | Re-key the four fleet requests (delete/spawn-into/respawn/nav-delay) from mutable vector index to the existing stable `FleetGuid` — a same-tick delete (drained before spawn-into) makes an in-flight index-based request hit the wrong fleet. Wire change in game payloads (engine-opaque, no protocol-version gate); client/server must move together — batch with the wire-break Dependencies entry |
| [Network/Refactor_ServerClientPlayerRegistry.md](Network/Refactor_ServerClientPlayerRegistry.md) | Large | 4 | 4 | 3 | 3 | `ClientPlayerRegistry` replaces the `mClientOwnedPlayerIds`/`authorizedCoords` parallel-vector invariant (mutated across 4 TUs); one relink routine for connect+load; shared fleet-refresh helper (adds the missing dead-flagship shift to `OnClientConnected`); engine `ClientConnection` four parallel per-slot vectors → one `SlotState` vector, defensive `FreeSlot` guards deleted. Server bookkeeping, no wire change |
| [Network/Refactor_RingLayoutRetention.md](Network/Refactor_RingLayoutRetention.md) | Medium | 3 | 3 | 3 | 3 | `RingLayout` value type + single `ComputeRetention` folds the triplicated head-advance math; one writeback consumer; merge `RunPrimaryReplay`/`RunTwoTierFallback` into one parameterized routine. Behavior-preserving but HIGH determinism exposure (reshapes rollback/replay state selection) — soak under network-sim loss/jitter. Client-only, no wire change |
| [Network/DeadMachinerySweep.md](Network/DeadMachinerySweep.md) | Medium | 3 | 2 | 2 | 3 | Delete six verified-dead Network mechanisms: never-populated player-destroy queue (`mPendingPlayerDestroys` + `EnsureDestroyCoords` + injection loop; KEEP `kDestroyPlayer` enum/decode reserved — wire/save value not gated by `kiVersion`), write-only `mPlayerToGuid`, four dead `ReconcileInputs` fields + no-op frame-id self-max, vestigial `CoordFrames::uiGeneration`/`NextGeneration`, `FindMatchingPlayerInCoord` → `bool`. Plus decision item: the jitter replay throttle doesn't bound `RunFrameTick` cost (catch-up sims the remainder anyway) — delete it + fix 3 docs, or make catch-up honor the cap. Grill: enum keep-reserved vs bump; throttle A/B |
| [File/Architecture_FileManagerSplitDecision.md](File/Architecture_FileManagerSplitDecision.md) | Architectural | 4 | 3 | 3 | 4 | **Decision plan (present options).** `FileManager` fuses two disjoint-consumer modules (general file I/O + versioned/atomic writes vs the packed-asset chunk system), and the lazy-chunk state machine is co-owned by four files via `GetLazyChunk()`'s mutable-ref leak under a documented-not-asserted thread contract (`FileManager.cpp:618-627`). Options: A — split the class (~22-TU churn); B — narrow the chunk seam with intent-named APIs (likely right size); C — accept + document. Resolve via grill; must not block the other File plans (land them first) |
| [Network/Architecture_WireFormatPairing.md](Network/Architecture_WireFormatPairing.md) | Large | 4 | 3 | 3 | 4 | Every engine message layout is hand-mirrored across send/receive (17 packet types; magic header sizes in ×2-3 copies incl. `NetworkSimulation.h`'s offset-4 tick parse; `SendSimplePacket` duplicated verbatim in `Client.h`/`Server.h`) — the directory's principal latent-desync hazard, lockstep by convention only. Colocate per-message write/read pairs (byte-identical, converted message-by-message with interop smoke tests), dedupe the send template, retire the magic offset. No wire change |
| [Network/WireFormatPairingGameSide.md](Network/WireFormatPairingGameSide.md) | Large | 4 | 3 | 3 | 4 | Game-side extension of `Architecture_WireFormatPairing` (lands after it): paired write/read for assign/player-state/fleet-sync layouts (byte-identical — retires the hand-summed 16/17/36/9 size constants mirrored across three files), structural `PlayerStateWireType` send/decode coupling, migrate `kClientSpawnRequest`/`ClientRequestFlags`/`PendingSpawnRequest` out of the engine protocol to a `GamePacketType` (wire change, batch version bump), + parse-boundary riders |
| [Graphics/IslandMeshArenaResidency.md](Graphics/IslandMeshArenaResidency.md) | Large | 4 | 3 | 3 | 4 | **Decision plan (present options).** GPU-side of the island residency strategy pick (the CPU payload is handled separately — the heightmap landed as R16 in-place quantization; see `IslandResidentMemoryScaling_Overview.md`). ~119.5 MiB permanently-resident per-template mesh (`mMeshBuffer`) can't use the texture-LRU evict path — the mesh is bound in the record-once terrain CB (`CommandBufferRecordMain.cpp:261-263`), so freeing it breaks the CB invariant. A — persistent stable-handle mesh arena + suballocation (degenerate evicted draws); B — bindless vertex-pull from a persistent SSBO (`Terrain.vert` + DataPacker layout). Bounds GPU mesh by concurrent residency. Client/graphics-only. Grill: A vs B |
| [Graphics/IslandPlacementSsboResidency.md](Graphics/IslandPlacementSsboResidency.md) | Medium | 3 | 2 | 3 | 4 | **Decision plan.** Smallest island resident bucket: `Islands::mIslandsStorageBuffers` (~16.4 MiB at 70 templates) reserves a fixed `kiMaxPlacementsPerTemplate(1024)` slab per template × 4 framebuffers, scaling with total template count. A — compact arena keyed on active templates (touches the record-once-CB `firstInstance` bake + per-frame write); B — right-size `kiMaxPlacementsPerTemplate`; C — accept + fix the "negligible" comment (omits ×4). Profile first. Client/graphics-only. Lowest priority of the island series |
| [File/EagerPackBufferReclaim.md](File/EagerPackBufferReclaim.md) | Large | 4 | 2 | 2 | 4 | **Speculative — profile first.** Reclaim the eager pack vectors (`FileManager::mPackFileData`, `FileManager.h:172`) for types dead after boot (kModel GPU upload / kShader pipeline-create / font atlas) — whole-vector free + `mEagerChunkMap` alias rebuild + device-loss reload, NOT a lazy-pool sub-range decommit (different storage; the lazy-pool API can't touch it). **kScene must never be reclaimed** — AnimationData aliases it zero-copy at runtime. Low confidence the model/shader/font packs are large enough to justify the machinery; may resolve as accept+document. No determinism/CRC/`kiVersion` exposure. Independent of the lazy-pool plans |
| [Graphics/IslandHeightmapRouteDedup.md](Graphics/IslandHeightmapRouteDedup.md) | Large | 4 | 2 | 2 | 4 | **Speculative — profile first.** Deferred dedup half of the landed R16 heightmap plan: content-address byte-identical heightmap regions shared across routes/leaves, alias one lazy-pool region from many templates. Touches `.pack` layout + `IslandHeader` + `DataHeader::kiVersion` + `ExportIsland::GetVersion` (29→30); both-builds heightmap aliasing stays byte-identical (no CRC/determinism change). Reading the bake, leaves are disjoint spatial tiles and routes are independent Gaea bakes, so byte-identical redundancy is expected near-zero — likely closes accept-and-document. Independent of the (not-taken) residency-lifecycle idea |
### Reference / Index Documents

Meta/overview docs that are never executed as plans — excluded from the priority walk and orphan scans.

| Document | Purpose |
|----------|---------|
| [Graphics/ShaderReview/00_Overview.md](Graphics/ShaderReview/00_Overview.md) | Shader-review effort overview and cross-cutting-findings index; the per-subsystem plans it references were either executed-and-deleted or never authored |
| [Graphics/IslandResidentMemoryScaling_Overview.md](Graphics/IslandResidentMemoryScaling_Overview.md) | Measurement + architecture findings anchoring the island resident-memory scaling plan series (GPU-mesh arena, heightmap R16 + route-dedup follow-up, SSBO arena, NavContour); records the record-once-CB + monolithic-pool constraints so the sub-plans don't re-derive them |

## Dependencies

Ordering constraints between live plans (shared files, decision prerequisites):

- **Island residency series** (`Graphics/IslandMeshArenaResidency.md`, `Graphics/IslandPlacementSsboResidency.md`, `Graphics/IslandNavContourResidency.md`, `Graphics/IslandHeightmapRouteDedup.md`) — anchored by the `Graphics/IslandResidentMemoryScaling_Overview.md` reference doc. The heightmap CPU bucket landed as R16 in-place quantization (no residency lifecycle established), so `IslandNavContourResidency` no longer depends on it — it resolves on its own measurement. `IslandHeightmapRouteDedup` (speculative dedup follow-up) touches `.pack` layout + `ExportIsland` / `IslandHeader` / `DataHeader::kiVersion` but stays byte-identical (no CRC/determinism change); the rest are client/graphics-only.
- **Pipeline cluster** (`Graphics/Architecture_PipelineRegistrationOwnership.md`, `Graphics/Managers/Architecture_BindlessSlotLifecycle.md`, `Graphics/PipelineDescriptorInfosRightSize.md`, `Graphics/Managers/Refactor_PipelineManagerSplit.md`) — all reshape the `Pipeline::Create` / `PipelineDescriptorWriter::Write` paths and the `PipelineManager` / `TextureDescriptors` surface. `BindlessSlotLifecycle` (Large) executes **alone**; never interleave it with `PipelineRegistrationOwnership` or `Refactor_PipelineManagerSplit`. `Refactor_PipelineManagerSplit` (a `/reduce-file` file split) invalidates `PipelineManager.cpp` line cites — land it before, or refresh after, the plans that cite that file. `PipelineDescriptorInfosRightSize` sequences with `PipelineRegistrationOwnership` / `BindlessSlotLifecycle` (same scan-to-max `Create`/`Write` paths), never interleave. All client/graphics-only, no CRC/determinism/`kiVersion`/wire exposure.
- **File plans** (`File/Architecture_FileManagerSplitDecision.md`, `File/FileManagerReduceFile.md`, `File/EagerPackBufferReclaim.md`) — `Architecture_FileManagerSplitDecision` is a decision plan; resolve it **first** relative to `FileManagerReduceFile` (its option A likely carves `FileManager.cpp` under 1000 lines and obviates the mechanical split; options B/C leave it oversized and make `FileManagerReduceFile` the concrete size fix) — the two share `FileManager.{h,cpp}`, never interleave. `EagerPackBufferReclaim` is independent (different storage: the heap `mPackFileData` vectors, not the lazy chunk pool). No CRC/determinism/`kiVersion` exposure in the set.
- **Render command-buffer overlap** — `Graphics/DisabledPassGatingPerfAudit.md` and `Graphics/WindowedLightingShadowDispatch.md` both edit `CommandBufferRecordMain.cpp` / `CommandBufferRecordGlobal.cpp` (wind/smoke + object-shadows passes vs the windowed lighting/shadow dispatch); co-schedule or refresh citations. `WindowedLightingShadowDispatch` also touches `PipelineCreator.cpp` / `Pipeline.{h,cpp}` (the `kIndirectHostVisible | kCompute` branch), overlapping the pipeline cluster — refresh lines if interleaved. Both client/graphics-only.
- **`Graphics/TextureChunkCpuPoolReclaim.md`** reuses the existing `DecommitChunkRange`/`RecommitAndReloadChunkRange` lazy-pool API and threads a `MEM_COMMIT` recommit into `ResetTextureChunkStates`/`LoadChunk`; it shares `TextureManager.cpp` / `LoadChunk` (in `FileManager`) with the bindless and file work — refresh lines if co-scheduled. Profile pool bytes + cycling churn first. Client/graphics-only.
- **Texture-upload teardown pair** (`Graphics/Managers/TextureUploadTeardownRaces.md`, `Graphics/Managers/CorruptTextureChunkLifecycleHardening.md`) — both edit `TextureUploadManager.cpp`; co-schedule in one session, never interleave. `CorruptTextureChunkLifecycleHardening` also edits `TextureDescriptors.cpp` (`WriteArrayElementFromLive`), which `Architecture_BindlessSlotLifecycle` reshapes — land the small fix **before** the registry consolidation (or fold it in); never interleave with it.
- **`File/MeshRecommitFailureObservability.md`** — shares `FileManager.{h,cpp}` with the File cluster (resolve `Architecture_FileManagerSplitDecision` first or refresh citations) and `IslandTerrainResidency.cpp` with the island residency series; small enough to co-schedule with either.
- **Network wire-break batch** — `Network/PackIntegrityHandshake.md` (hello token), `Network/SubscriptionLifecycleRaceHardening.md` (unsubscribe epoch), `Network/FleetRequestsByGuid.md` (game fleet payloads), and `Network/WireFormatPairingGameSide.md` (spawn-request migration) all break wire compatibility — land behind one `kuiProtocolVersion` bump in a single client/server release (game payloads carry no version gate, so the engine bump is their only skew signal; last-lander owns the bump). None of them interleave with `Network/Architecture_WireFormatPairing.md`, which restructures the same send/receive sites — land it first or refresh citations; `WireFormatPairingGameSide` additionally **depends on** it (reuses its pairing scheme).
- **`ClassifyFullState` pair** — `Network/ResyncFullStateRepair.md` (adds the `kActive` commit) and `Network/SubscriptionLifecycleRaceHardening.md` (reworks the `kUnsubscribed` branch) edit the same function in `ClientReceive.cpp`; co-schedule, never interleave. `Network/ClientFullStateEdgeFixes.md` is the game-layer adoption side of the same slot-activation/frame-adoption invariant — co-schedule in the same network session if convenient.
- **Network session-refactor series** (`Network/Refactor_SessionBaseCollapse.md`, `Network/Refactor_DrainContractUnification.md`, `Network/Refactor_ClientResetUnification.md`) — land **SessionBaseCollapse first**: it deletes `ClientSessionBase`/`ServerSessionBase`/`ClientDataReceiver` and folds their bodies into the game sessions, which shrinks and relocates `DrainContractUnification`'s call-site list and is a prerequisite for `ClientResetUnification` (needs all reset fields owned by one class). Never interleave; refresh citations between them. The collapse also relocates code cited by `SubscriptionLifecycleRaceHardening` (watchdog tick site), `ResyncFullStateRepair` (resync reset region), and `ClockErrorMachineryCleanup` (clock-correction paths) — refresh those if it lands first. All three carry no wire/CRC/`kiVersion` exposure.
- **Server slot/registry overlap** — `Network/Refactor_ServerClientPlayerRegistry.md` reshapes `ClientConnection` slot state in `Server.h`/`ServerReceive.cpp`, shared with `Network/SubscriptionLifecycleRaceHardening.md` (slot state machine); do not interleave — land the race hardening first.
- **Game server manager order** — land `Network/DeadMachinerySweep.md` first (deletes the `ServerBroadcaster::BuildFrameInputs` destroy loop), then `Network/AuditSweepQuickWins.md` (renames `mSpawns` in the same function) and `Network/ServerPauseAndResetSemantics.md` (gates the same function's drains); `Network/Refactor_ServerClientPlayerRegistry.md` last (reshapes the state they all touch). `DeadMachinerySweep`'s enum grill option B (remove `kDestroyPlayer` + version bump) would couple to `Frame/CrcVersionGateBump.md`; the default keep-reserved resolution keeps them independent.
- **`NetworkSimulation.h` overlap** — `Network/NetworkSimulationReliableDelay.md` reshapes the reliable path in `EnqueueOrDrop`; `Network/Architecture_WireFormatPairing.md` retires the offset-4 tick parse in the same header — land either first and refresh the other's citations.
- **`Meta/ReviewSweepQuickWins.md`** — items 10-13 touch the pipeline-cluster files (`Pipeline.{h,cpp}`, `PipelineManager.cpp`, `DynamicPipelines.cpp`): respect the pipeline-cluster sequencing above (never interleave with `BindlessSlotLifecycle`; refresh citations if `Refactor_PipelineManagerSplit` lands first). Remaining items are standalone.
- **`Graphics/ModelSpecularAntialiasing.md`** — mostly standalone (client/graphics-only shader AA), with two additive shared-file overlaps: its optional `kSampleShading` drop edits `CreateModelPipeline` in `DynamicPipelines.cpp`, the same function `Meta/ReviewSweepQuickWins.md` item 13 folds the try/catch into; and its two new `MainLayout` fields append to `ShaderLayoutsBase.h`, which `Graphics/WindowedLightingShadowDispatch.md` may also grow. Both are line-drift only (no logic conflict) — refresh citations if co-scheduled; no ordering constraint.
- **Standalone** — `Frame/CrcVersionGateBump.md` (version-gate bump; deliberately invalidates pre-bump saves/replays) is a standalone quick win. `Network/Refactor_RingLayoutRetention.md` (client-only reconciliation bookkeeping; high determinism exposure but no shared-file constraints with other live plans beyond the reconciliation File Group) and `Network/DesyncDebugInfrastructureGating.md` (server debug infrastructure) are otherwise standalone.

## File Groups

Live plans that touch the same files and should be co-scheduled in one session:

- **`Engine/Source/Frame/IslandTerrain.cpp` + `IslandTerrainResidency.cpp`**: `Graphics/IslandMeshArenaResidency.md` (GPU-mesh `EvictionSweep`/`RestorationSweep`, in `IslandTerrainResidency.cpp`), `Graphics/IslandPlacementSsboResidency.md` (placement SSBO), `Graphics/IslandNavContourResidency.md` (server-side `mNavContour` lifecycle in `WaitForElevationMaps` / `~IslandTerrain`, stays in `IslandTerrain.cpp`), `Graphics/IslandHeightmapRouteDedup.md` (shared-heightmap pool-region aliasing in `WaitForElevationMaps`), `File/MeshRecommitFailureObservability.md` (`CreateClientMeshBuffers` in `IslandTerrainResidency.cpp`). See `Graphics/IslandResidentMemoryScaling_Overview.md`.
- **`Engine/Source/Graphics/Objects/Pipeline.{h,cpp}` + `PipelineCreator.cpp` + `PipelineDescriptorWriter.cpp`**: `Graphics/Architecture_PipelineRegistrationOwnership.md` (kModel append + pre-`Create` protocol), `Graphics/PipelineDescriptorInfosRightSize.md` (`pDescriptorInfos[32]` right-size — same scan-to-max paths), `Graphics/WindowedLightingShadowDispatch.md` (`kIndirectHostVisible | kCompute` branch), `Meta/ReviewSweepQuickWins.md` (items 10-11, 13: `miIndirectSlotCount` comment, `RecordDrawIndirectSet2` ASSERT, `DynamicPipelines` try/catch fold). Sequence per the pipeline-cluster Dependencies order; never interleave with `BindlessSlotLifecycle`.
- **`Engine/Source/Graphics/Managers/PipelineManager.{h,cpp}` + `TextureDescriptors.{h,cpp}`**: `Graphics/Managers/Architecture_BindlessSlotLifecycle.md` (reshapes `TextureDescriptors`; runs alone/last), `Graphics/Architecture_PipelineRegistrationOwnership.md` (registration surface), `Graphics/Managers/Refactor_PipelineManagerSplit.md` (splits `PipelineManager.cpp` — invalidates line cites), `Graphics/Managers/CorruptTextureChunkLifecycleHardening.md` (`WriteArrayElementFromLive` fallback — land before `BindlessSlotLifecycle`), `Meta/ReviewSweepQuickWins.md` (item 12: redundant `Destroy()`s in `PipelineManager.cpp`). Sequence per Dependencies; never interleave the split or `BindlessSlotLifecycle` with the others.
- **`Engine/Source/Graphics/Managers/CommandBufferRecordMain.cpp` + `CommandBufferRecordGlobal.cpp`**: `Graphics/DisabledPassGatingPerfAudit.md` (wind/smoke spread + object-shadows/wind-deposit passes) and `Graphics/WindowedLightingShadowDispatch.md` (windowed lighting/shadow indirect dispatch + deposit/spread scissors). Co-schedule or refresh citations.
- **`Engine/Source/File/FileManager.{h,cpp}`**: `File/Architecture_FileManagerSplitDecision.md` (class-split decision) and `File/FileManagerReduceFile.md` (`/reduce-file` split) — never interleave; resolve the decision first. `File/EagerPackBufferReclaim.md` (`mPackFileData` reclaim), `Graphics/TextureChunkCpuPoolReclaim.md` (`LoadChunk`/`ResetTextureChunkStates` recommit), `Graphics/IslandHeightmapRouteDedup.md` (lazy-pool region aliasing for shared heightmaps), `File/MeshRecommitFailureObservability.md` (`RecommitAndReloadChunkRange` signature), and `Network/PackIntegrityHandshake.md` (manifest-token computation) also touch this file — refresh lines if co-scheduled.
- **`Engine/Source/Graphics/Managers/TextureManager.cpp`**: `Graphics/TextureChunkCpuPoolReclaim.md` (lazy-chunk CPU pool decommit) and `Graphics/Managers/Architecture_BindlessSlotLifecycle.md` (TextureManager placeholder arrays) — refresh lines if co-scheduled.
- **`Engine/Source/Graphics/Managers/TextureUploadManager.cpp`**: `Graphics/Managers/TextureUploadTeardownRaces.md` (`WaitIdle`/`UploadThread` handshake) and `Graphics/Managers/CorruptTextureChunkLifecycleHardening.md` (`UploadThread` catch block) — co-schedule in one session.
- **`Engine/Source/Network/Client/ClientReceive.cpp`**: `Network/ResyncFullStateRepair.md` + `Network/SubscriptionLifecycleRaceHardening.md` (both edit `Client::ClassifyFullState`), `Network/StatusChangeCodecHardening.md` (`DecompressAndReadFrame` clamp + the receive-side count cap). Co-schedule or refresh citations.
- **`Engine/Source/Network/Server/Server.{h,cpp}` + `ServerReceive.cpp`**: `Network/ServerTrustBoundaryHardening.md` (hello/subscribe/resync handlers, receive try/catch), `Network/SubscriptionLifecycleRaceHardening.md` (`ClientUnsubscribe` ack+epoch), `Network/StatusChangeCodecHardening.md` (`BufferFrame`), `Network/DesyncDebugInfrastructureGating.md` (`BufferFullFrame` + debug handlers), `Network/Refactor_DrainContractUnification.md` (`Drain*` accessors), `Network/Refactor_ServerClientPlayerRegistry.md` (`ClientConnection` `SlotState` merge — last). Sequence the hardening/codec fixes before the drain/registry refactors; refresh citations between.
- **Game `Network/Server/` managers (`ServerSession.{h,cpp}`, `ServerBroadcaster.{h,cpp}`, `ServerFleetManager.{h,cpp}`, `ServerClientManager.{h,cpp}`)**: `Network/DeadMachinerySweep.md` (destroy pipeline + `mPlayerToGuid` — first), `Network/AuditSweepQuickWins.md` (pass-throughs, renames), `Network/ServerPauseAndResetSemantics.md` (`BuildFrameInputs` gating), `Network/FleetRequestsByGuid.md` (request re-key), `Network/ServerTrustBoundaryHardening.md` (caps/dedup/validation), `Network/Refactor_ServerClientPlayerRegistry.md` (registry + relink dedup — last). Sequence per the game-server-manager Dependencies entry.
- **Session bases + game sessions (`ClientSessionBase.*`, `ServerSessionBase.*`, game `ClientSession.{h,cpp}`/`ClientSessionSubscriptions.cpp`/`ClientDataReceiver.*`/`ServerSession.{h,cpp}`, `Engine.h`, both vcxproj/.filters pairs)**: `Network/Refactor_SessionBaseCollapse.md` (deletes the bases + `ClientDataReceiver`, redistributes into sibling TUs — invalidates line cites), `Network/Refactor_DrainContractUnification.md`, `Network/Refactor_ClientResetUnification.md`, `Network/ClockErrorMachineryCleanup.md` (clock constants/snap), `Network/ClientFullStateEdgeFixes.md` (`PollConnection` stall path). Co-schedule in dependency order (collapse → drain → reset); refresh citations if the collapse lands first.
- **Game `Network/Client/` reconciliation (`ReconcileReplay*.cpp`, `ClientReconciler.{h,cpp}`, `ClientSession.cpp`, `ClientDataReceiver.cpp`)**: `Network/ClientFullStateEdgeFixes.md` (pending-full-state adoption), `Network/DeadMachinerySweep.md` (dead fields + replay-throttle decision), `Network/AuditSweepQuickWins.md` (items 1, 10, 12), `Network/Refactor_RingLayoutRetention.md` (offset bookkeeping — land last). `DeadMachinerySweep`'s throttle item and `AuditSweepQuickWins`' Network.md doc item rewrite the same doc bullets — land together or refresh.
