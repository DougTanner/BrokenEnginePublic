# Plan Execution Order

All refactor and bugfix plans — debt reduction that doesn't add a new engine capability — sorted by score (lowest = highest priority).

Score = Effort − Impact + Risks (lower = higher priority)

`Tier` column is an informal size/risk descriptor (**Quick Win** / **Small** / **Medium** / **Large** / **Architectural**); it does not affect score or ordering.

Counterpart: `Documents/Features/Order.md` holds brand-new additions. See `Documents/CLAUDE.md` for the distinction. Scores are not comparable across the two files — they were originally derived from a single sort and then split.

## Debt Score (DataPacker, this run): MODERATE

Distribution: 6 Medium + 1 Architectural. No critical bugs, but `ExportScene.cpp` is approaching the `/reduce-file` threshold and a public header pulls tinygltf into unrelated TUs.

## Debt Score (Network, this run): MODERATE

Distribution: 7 Quick Win + 6 Medium + 1 Architectural across 14 plans. Network subtree is structurally sound — the main concentrations are (1) engine → game surgery in `ServerReceive.cpp` save/load/timespeed handlers and the `*SessionBase.cpp` CoordFrames mutation, (2) one replay-determinism bug (fleet RNG re-seeded from wall clock on load), and (3) mechanical duplication across ~20 `Send*` methods, ~14 connected-gate sites, and ~12 handshake-gate sites. `ServerFleetManager.cpp` (808 lines) and `ReconcileReplay.cpp` (668 lines) are genuine `/reduce-file` candidates.

## Debt Score (Graphics — Shaders, this run): MODERATE

60 first-party GLSL shaders reviewed via one `/glsl-review` Opus subagent per file. Original distribution: 9 Quick Win + 3 Medium across 12 subsystem plans. **Six performance-neutral bugfix plans landed this session** via parallel Opus subagents: `03_Terrain`, `05_Quads`, `07_Objects`, `04_Particles` fully executed and deleted; `09_Wind` and `12_Misc` partially executed (descriptor-set audit and `nonuniformEXT` audit deferred — both require C++ coordination). `08_Smoke` landed in a follow-up session (full execution, deleted). The particles sweep landed cross-cutting fixes folded in: 5 `writeonly` Shadow image2D sites, 2 `writeonly` Lighting blur sites, 4 `writeonly` Smoke/Wind spread sites, `LightCombine.comp` Uchimura `pow` base clamps, and the redundant outer `normalize` drop in `Model.frag`. No per-subsystem plan files remain on disk (each was executed-and-deleted or never authored); the residual Water defensive polish and the deferred descriptor-set / `nonuniformEXT` audits have no live plans — re-author if wanted. **`06_Lighting` landed this session**: its correctness/consistency items were either already mitigated by existing slider clamps or executed (ring-count slider min raised to 2 to guard the `LightingSpread` `fNorm` divide; `scalar` qualifier added to `lightOccupancyBuffer` in `AreaLight`/`PointLight`); the deferred performance items were split into the new `06_LightingPerf` plan, which **landed in a follow-up session** (incremental rotation recurrence for the `LightingSpread` direction-loop `cos`/`sin`, hue-preserving Reinhard dedup, `LightCombine` Uchimura skip at `fHuePreserve>=0.999`, blur reciprocal hoist, `AreaLight`/`PointLight` early-alpha discard; plus folded-in Pattern-3 pow-skip guards in `TerrainElevation.frag`, `Water.frag`, and `ShaderFunctions.h`). No `inverse()` driver-hang violations. Remaining recurring hazards: (1) unguarded `normalize()` on possibly-zero vectors (Model, Water — Particles now guarded), (2) `pow(neg, non-integer)` → NaN in `LightingSpread.frag` (LightCombine clamped), (3) implicit-LOD `texture()` in compute (Shadow, ObjectShadowsBlur). Standalone hard bugs: `Terrain.frag:76` (landed), `Log.vert:38` (landed), `ParticlesUpdate.comp` non-atomic RMW race (landed). Overview at `Graphics/ShaderReview/00_Overview.md`; all per-subsystem plan files have been executed-and-deleted or were never authored.

## Debt Score (Common, this run): MODERATE

29 files reviewed (one Opus `/external-deep-analysis` subagent per `.h`/`.cpp` pair, then a second Opus validation pass against source before authoring). 28 plans authored — 26 per-file + 2 cross-cutting themes (`Determinism_IndeterminateBytes`, `ThreadSafety_SharedStaticBuffers`); 3 files produced no plan (`Common.h` works-as-designed, `ScopedLambda` all-YAGNI, `Serialization` all error-handling/theme-deferred). Distribution: 7 Quick Win + 18 Small + 3 Medium; no Architectural. The foundation layer is structurally sound, but validation kept genuine correctness bugs worth fixing despite their low effort: a `Log` buffer-overrun, UB in `TextureFormat` / `WindowsUtils` (`FILETIME` cast) / `MathUtils` (`RoundUp`), an exception-unsafe `Dispatch` join (use-after-scope), and several determinism/desync sources (`Crc` `char` sign-extension + padding-byte hashing, `ConvexHull` transcendentals on the CRC path, indeterminate `.pack` bytes). Two themes coordinate the systemic issues. Validation dropped the majority of report findings as cosmetic, speculative-API, or defensive-validation the project policy forbids; the original `ErrorUtils` report was a hallucination and was re-authored from source.

## Debt Score (Repo-wide CLAUDE.md refresh, this run): LOW

70 CLAUDE.md docs refreshed by 140 Fable agents (summarize directory code, then update doc); 46 raw conflicts deduped into the refresh report's open-items sections. 16 follow-up plans authored from those sections: 7 correctness/cleanup bugs (fleet-sync overread, blaster degenerate march, unsubscribe handshake gate, relink-load ordering, stale `kTransferPlayer` assert, headerless-raw format claims, dead Missiles SOA field), 4 grouped cleanup sweeps (dead-code/unused-includes, stale code comments, vcxproj/build-config, stale doc claims), and 5 doc-rule **decision** plans (aggregation/scope-rule qualifiers, DataPacker LOG float-spec exception, Collections straight-line-steering rule, parent-rule framing reconciliation, plan-doc governance). The codebase is structurally sound — the only genuine correctness bug with runtime reach is the unvalidated `ParseFleetSync` network read; the blaster march and Missiles dead field are latent/visual. Most findings are documentation drift (the refresh's purpose). The five decision plans are tagged "Decision plan (present options)" and resolve via `/external-grill-plan` before any edit. No determinism/CRC regressions surfaced; the one CRC-touching plan (Missiles field removal) is a clean `kiVersion`-bumped layout change.

**Second-pass routing (this run): 7 more plans authored** from a code-side / cross-doc audit that surfaced findings out of scope of the doc edits, plus 5 appends to the existing sweep plans. New plans: 4 code/investigation (`Graphics/PipelineGraphicsGlobalSet0Indexing` — Set-0 framebuffer-index discrepancy; `Network/StaticDataEpochCheckOnSubscribingSlot` — epoch check on epochless `kSubscribing` slot; `Ui/ChineseFontPushMissingScreens` — missing CJK font push on Pause/Sound; `Network/BroadcastTickAllocationGuardScope` — blanket suppression nullifies the documented armed tracker), 1 shader investigation (`Graphics/ParticlesSpawnStructCopyGlslangBug` — whole-struct SSBO copy vs glslang trailing-field bug), 2 doc-reconciliation (`Graphics/NvidiaPipelineHangAttributionReconcile` — mat4-array vs `inverse()` hang attribution; `Common/DocCanonicalHomeReconciliation` — 6 cross-doc canonical-home/pick-one-wording decisions). Appends: `StaleCodeCommentsSweep` (+4: Smoke Pass-Two coords, Wind "Manhattan-2", RawInput legacy-mouse, Model ACES "Stephen Hill" → Narkowicz), `DeadCodeAndUnusedIncludesSweep` (+2: dead `FrameBase` fwd-decl, unused `FrameInput::operator==`), `StaleDocClaimsSweep` (item 1 R16/R32 marked RESOLVED-during-audit; item 11 FrameUpdatePipeline expanded with the missing Server-loop quickload/replay/resends/autosave steps). One audit finding was **not filed** — a `Model.frag` IBL `fPbrSun^3` comment flagged as "cubic comment vs quadratic math" turned out to be a correct, internally-consistent comment describing an *avoided* cubic (the code is linear in `fPbrSun`); no defect. The `cmft.cpp` missing-`<Filter>` finding was already item 4 of `VcxprojAndBuildConfigReconciliation` (not re-filed), and the IslandChainPlacement.h log-spiral comment was already item 5 of `StaleCodeCommentsSweep`. The EWNS deposit-rule framing conflict stays in `Graphics/ParentRuleFramingReconciliation`. No new CRC/determinism exposure (the two Network plans are client-receive / server-debug-tracking only; the Graphics Set-0 plan is a possible GPU-hazard fix, not determinism).

## Debt Score (Audio, this run): LOW

`/external-deep-analysis` on `Engine/Source/Audio` (10 files, ~1835 LOC, non-recursive). Distribution: 3 Quick Win + 4 Small — no Medium/Large/Architectural. The area is structurally healthy: all documented CLAUDE.md invariants verified against source (frame-phase asserts, replay guards, streaming Wait/Wake alternation, atomic slot machine, deferred device-reset clears), allocation discipline fully compliant, no layer violations, and no viable ThirdParty library replacements (the only candidate — extending DirectXTK Audio coverage — is blocked by `SoundEffect`'s buffer-ownership API vs FileManager `LazyChunk`). Findings are mechanical: include hygiene, two misplaced helpers (`DestroyXAudio2SourceVoice` semantic cycle + dtor-order brittleness), one verified presentation-only replay-invariant deviation (RNG advance before the `kRecalculated` guard), an avoidable recursive mutex, a ~330-line lifecycle function with O(n²) bookkeeping, an audible new-voice start pop, and a constructor that skips its own documented device fallback.

## Debt Score (Engine/Source/Debug, this run): LOW

`/external-deep-analysis` on `Engine/Source/Debug` (2 files: `EnumToString.h` + `CLAUDE.md`, non-recursive). One deep, healthy module: thread-safe immutable maps, client-only gating verified at both gates (Engine.h `BT_CLIENT` span + client-only vcxproj), no determinism/CRC exposure, allocation-discipline compliant, no layer violations, zero dead maps. Single Small plan — replace the hand tables with the SDK-shipped generated `vk_enum_string_helper.h` — subsumes every line-level finding as well (the missing-enumerator gaps and `Convert`'s five duplicated `if constexpr` dispatch blocks), so the refactor-clean phase produced no separate plans.

## Debt Score (Engine/Source/File, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/File` (3 files, ~1496 LOC, non-recursive). Distribution: 2 Quick Win + 5 Small + 1 Architectural decision. The directory is deliberate, documented performance architecture (unbuffered sector-aligned I/O, pool-layout-by-iteration-order, atomic chunk state machine) with full ExternalHeaders/allocation-discipline compliance and zero viable ThirdParty replacements (PhysFS/cereal/bsdiff all rejected for fit — the `.pack` system and DifferenceStream are engine-specific by design). Debt concentrates in one cross-layer leak (FileManager parsing GLTF scene layout into the Graphics `gAnimationDataMap`), latent boot/teardown-window thread-lifecycle holes (server fast-exit dtor can join a not-yet-joinable thread), interface leaks (public zero-user members, const-laundering, and the four-file chunk state machine — filed as a decision plan), and a hand-duplicated versioned-header convention. No critical bugs; no determinism/CRC exposure in any finding.

## Debt Score (Frame, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/Frame` (22 files, ~5,330 LOC, non-recursive — `Collections/` excluded). 14 plans authored (8 Architecture + 6 Refactor); distribution: 8 Quick Win + 5 Small + 1 Medium, no Large/Architectural. The core is structurally strong — determinism discipline verified exemplary (decorrelated seeded RNG streams, `DeterministicSinCos` coverage, index-tie-broken A* heap, commutative-max elevation blending, MXCSR asserts), thread model sound (thread_local everything, per-coord workers), layer integrity clean, and no viable ThirdParty replacements (Recast/Detour evaluated in depth and rejected on determinism/architecture fit; Clipper2 already absorbed the hard geometry). What pushes past LOW: three genuine latent-correctness items — a release-silent `sLayerBaseOffsets` array overrun on the collision layer-overflow path, the runtime-tunable `gBaseHeight` Ui `Wrapper` read by deterministic nav/spawn code, and XOR CRC combining that cancels even-multiplicity desyncs — plus the known oversized-file pair already queued as splits. All 14 plans passed a Fable verification pass against source (2 rewritten for incomplete reader list / wrong invariant claim; 0 deleted).

## Debt Score (Engine/Source/Graphics — top-level, this run): LOW

`/external-deep-analysis` on `Engine/Source/Graphics` (14 files, ~2,091 LOC, non-recursive — `Debug/`/`Managers/`/`Objects/`/`Render/` subtrees excluded). 8 plans authored (6 Architecture + 2 Refactor); distribution: 3 Quick Win + 5 Small, no Medium/Large/Architectural. The directory is structurally healthy: zero std-lib include violations, full aggregation-header compliance, determinism clean (no RNG, no sim writes from the render path), thread/frame-phase contracts verified against `Graphics/CLAUDE.md`, and zero viable ThirdParty replacements (the only library-shaped cluster, `AnimationData`, was rejected — ozz-animation would cost a pack-format/DataPacker rewrite plus an allocation-discipline conflict to delete ~400 working glTF-spec lines; stb/volk/VMA/DirectXMath already sit at the right seams). The standout finding is one latent deadlock: `SaveScreenshot` waits a just-reset per-framebuffer fence that only the subsequent UI submit signals — masked today by `kbScreenshots=false`. Everything else is mechanical: include/fwd-decl drift, a game-typed by-value member on `Graphics` that only `GameBase` actually uses (held up by PCH include ordering), comment-synchronized cross-module constants, a dead `Execute(bool)` parameter whose false-path would free a pending command buffer, the ~173-line `Refresh()` poll ladder, and four copy-pasted corner unprojects. All 8 plans passed a Fable verification pass against source (3 rewritten — two EncapsulationSeams items and one InVisibleArea item dropped, one constant-pairing claim corrected; 0 deleted).

## Debt Score (Engine/Source/Input, this run): LOW

`/external-deep-analysis` on `Engine/Source/Input` (2 files, 326 LOC, non-recursive; the requested `Engine/Source/Frame/Input` does not exist — `Engine/Source/Input` is the engine's only input directory). Distribution: 3 Quick Win + 2 Small (one a keep-in-house decision plan) — no Medium/Large/Architectural. The module is genuinely deep and healthy: layering clean (the one engine→game edge is the sanctioned `game::gpGame->ShouldTrapCursor()` virtual), client/server guard placement exactly the documented narrowest-practical scope, determinism firewall intact (nothing in the snapshot feeds `FrameInput`; the scroll accumulator and resolution-normalized mouse are handled game-side), all access main-thread (the two-phase scratch→snapshot design is frame-coherence, not concurrency), and zero unused includes / full ExternalHeaders.h compliance. Findings are mechanical: an over-wide public surface + dual-sourced HWND, a missed trigger/dpad clear on gamepad disconnect, a swallowed `WM_INPUT` `DefWindowProc` pass, a two-call raw-input fetch, and a bool-array→`Flags` pattern alignment. Two findings deduped into existing sweeps (mouse `RIDEV_INPUTSINK` → `Engine/DeadCodeAndUnusedIncludesSweep`; the wrong cpp:42 comment → `Common/StaleCodeCommentsSweep`). All five plans passed a Fable verification pass against source (2 citation fixes; 0 deleted). A second verification in the companion Input+Memory run consolidated parallel-authored duplicates: `GamepadDisconnectClear` → `Architecture_GamepadDisconnectSnapshot.md` (zeroing the consumer-less `f2Triggers` replaced by deleting the dead field) and `InputInterfaceTightening` → `Architecture_InterfaceNarrowingAndDocs.md` (absorbed unchanged).

## Debt Score (Engine/Source/Memory, this run): LOW

Companion to the Input sweep above (one `/external-deep-analysis` run on `Engine/Source/Input` + `Engine/Source/Memory`; Memory = 2 files, ~111 LOC, non-recursive). One Quick Win plan, plus two appends to existing sweeps: `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 (the `CollectionMemory.h:3` stale-include channel that accidentally distributes `GlobalAllocator.h` to every TU, paired with `ProfileManagerBase.cpp`'s missing direct include) and `Engine/VcxprojAndBuildConfigReconciliation.md` item 6 (`RawInputManager.h` server `ClInclude`). The module is an exemplary deep module: a 3-line header exposing exactly the two cross-TU symbols with real consumers (`Main.cpp`, `ProfileManagerBase.cpp`) while 16 linker-injected operator replacements + the arena/SIGABRT/stats machinery stay invisible; allocation-tracking gates verified (counter `kbProfiling`-gated, tripwire always-on, deletes never tracked, suppression thread-local); no determinism/CRC exposure anywhere. The one genuine code finding is the benign-by-sequencing `sbTrackingReady` data race (formal UB, free fix). Zero viable ThirdParty replacements — mimalloc's stock `mimalloc-new-delete.h` has no `TrackAllocation` hook point and partial use breaks ODR / the upstream-pristine rule; the in-house forwarders are correctly understood as a tracked fork of it (maintenance note filed). Both CLAUDE.md docs verified claim-by-claim (one misleading participation example fixed via the plan). All citations re-verified by a Fable verification pass; 0 items dropped.

## Debt Score (Engine/Source/Ui, this run): LOW

`/external-deep-analysis` on `Engine/Source/Ui` (30 files, ~1,832 LOC, non-recursive — `Ui/Screens/` excluded; the requested `Engine/Source/Frame/Ui` does not exist — `Engine/Source/Ui` is the engine's only Ui directory). Distribution: 6 Quick Win + 1 Small — no Medium/Large/Architectural. The directory is a near-perfect leaf: zero direct std-lib/third-party includes (full ExternalHeaders.h compliance), zero include cycles, zero dependencies on the rest of `Engine/Source`, no hot-path allocation (all wrapper globals are static-init; `CurveData` capacity is bounded by ctor reserve + 16-point cap), and determinism clean except the already-plan-covered `gBaseHeight` seam (deduped into the pre-existing `Frame/Architecture_BaseHeightWrapperDeterminism.md` — not re-filed). One viable ThirdParty replacement: rebuild `CurveWidget` on already-vendored ImPlot. Findings are mechanical: an unused include silently dragging `CurveData.h` into the game PCH, a zero-caller change-detection sub-interface on `CurveData`, a stale water-normal "must match" comment whose constant has no consumers, undocumented threading / float-storage / `Toggle()` contracts on `Wrapper` (verification surfaced a real exactness example: `VK_PRESENT_MODE_FIFO_LATEST_READY_KHR` = 1000361000 exceeds the 2^24 float ceiling and corrupts to 1000361024 in `gPresentMode`'s allowed set — latent, never UI-exposed), and a `// DT: TEMP` tunable shipping end to end. All seven plans passed a Fable verification pass against source (4 rewritten — added ctor-init deletion lines, corrected a false "max 255" exactness claim, fixed two engine-vs-game TweaksScreen paths, concretized the shader-consumer scope; 0 deleted).

## Debt Score (Engine/Source/Profile, this run): LOW

`/external-deep-analysis` on `Engine/Source/Profile` (3 files, ~1,212 LOC, non-recursive; the requested `Engine/Source/Frame/Profile` does not exist — `Engine/Source/Profile` is the engine's only profile directory). 6 plans authored (5 Architecture + 1 Refactor); distribution: 4 Quick Win + 2 Small, no Medium/Large/Architectural. The subsystem is structurally healthy: deep recording interface (two-call CPU scope hiding per-thread maps, cross-thread pairing, allocation diffing, smoothing), determinism clean (steady_clock only, no sim writes, no RNG), ExternalHeaders/allocation discipline fully compliant, and zero viable ThirdParty replacements (Tracy/microprofile/Optick/Remotery all rejected — every external profiler is viewer-oriented and architecturally violates the in-engine overlay + headless GDI server + per-scope allocation attribution + `kbProfiling` compile-out requirements; Palanteer license-rejected, AGPL viewer). The standout finding is one real display-correctness bug: `SmoothCpuTimers` double-latches every `bSmoothNow` timer except hard-coded index 0, halving the displayed client network timers and the server's headline frame-time row. Everything else is mechanical: guard-scope misalignment (whole-body `BT_CLIENT` on `UpdateProfileText`, ungated GPU-timer state, mis-gated mimalloc fields), a comment-only engine↔game enum contract, include drift (`GlobalAllocator.h` in the wrong sibling TU, unused `Game.h`), three stale CLAUDE.md claims, and in-function items (two-bool `CpuStop`, ~158-line `FormatGpuScreen`). All 6 plans passed a Fable verification pass against source (2 rewritten — one boot-crashing ASSERT item dropped, one count corrected; 0 deleted).

## Debt Score (Engine/Source/Network — engine transport, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/Network` (recursive; 23 files, ~3,965 LOC — engine transport only; the earlier "Network" debt score above covered the game-layer sweep). 12 plans authored (8 Architecture + 4 Refactor); distribution: 3 Quick Win + 8 Small + 1 Large. The wire path is determinism-safe (raw-memcpy scalars, byte-symmetric layouts verified for all message types, version-gated handshake), strictly main-thread with zero locks, and already outsources transport/compression to ENet/LZ4 at the right seams (ThirdParty replacement review: clean null result). What pushes past LOW: two genuine latent protocol bugs — the paused-server subscribe/resync queue drop that wedges clients in `kWaitingFullState` (no timeout exists), and the `0xFF` reject sentinel misread as a range check that permanently leaks server slots — plus the systemic hand-mirrored wire-format duplication (lockstep by convention only; one Large plan). All 12 plans passed a Fable verification pass against source (6 corrected in place — message-type count, purge-predicate split, handler name, StatusChange POD claim, call-rate precision, dead-param caller chain; 0 deleted).

## Debt Score (Engine/Source/Graphics/Debug, this run): LOW

`/external-deep-analysis` on `Engine/Source/Graphics/Debug` (2 files, ~190 LOC, non-recursive; the requested `Engine/Source/Frame/Graphics/Debug` does not exist — `Engine/Source/Graphics/Debug` is the only graphics-debug directory in the engine). Distribution: 2 Quick Win — no Small/Medium/Large/Architectural. The module is an exemplary deep module: seven static calls hiding lazy 128K-slot staging, CRC-keyed dynamic-buffer resize, update-after-bind descriptor refresh, and record-once-compatible indirect-count management in 165 lines. Everything verified clean: sanctioned `kbDebugRender` layering (game-PCH constexpr, same pattern as `kbProfiling`), whole-file `BT_CLIENT` wrap + client-only vcxproj membership, full main-thread call-site census (no `Dispatch`-region submissions), zero determinism/CRC exposure (the only sim-path `kbDebugRender` code writes client-only CRC-excluded SOA fields), shader/CPU layout shared via the dual-language `ShaderLayoutsBase.h` header with the set-1/binding-2 contract verified end-to-end, allocation discipline compliant, teardown/recreate ownership safe. Zero viable ThirdParty replacements (debug-draw / im3d / Jolt DebugRenderer / ImDrawList all rejected — per-frame CPU draw-list models fight the record-once-CB + instanced-indirect architecture; net LOC negative). Findings: dead public API (`Box`/`Sphere` — zero callers, full Debug-config GPU plumbing behind them) and one systemic hazard that verification proved unreachable (the `RenderFrameMain` empty-active-set early return skipping all indirect zero-writes — `ComputeActiveSet`/reset/boot all force a non-empty set; downgraded to invariant documentation). The three doc nits from this run ride `Common/StaleDocClaimsSweep.md` items 15-17. Both plans passed the Fable verification pass against source (1 rewritten, 0 deleted).

## Debt Score (Engine/Source/Graphics/Managers, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/Graphics/Managers` (36 code files, ~9,885 LOC, non-recursive). 14 plans authored (6 Architecture + 8 Refactor); distribution: 6 Quick Win + 7 Small + 1 Large. The directory is deep and disciplined where it counts — ExternalHeaders compliance is total (zero `#include <...>` in 36 files), allocation discipline verified compliant (the recreate path suppresses up-stack), determinism clean (no sim writes/RNG; the only RNG is a debug cache-invalidator), the submission chain and upload-thread handshake verified correct end-to-end, and the ThirdParty review returned a clean null (VMA/volk/ImGui/SPIRV-Cross already sit at the right seams; vk-bootstrap was evaluated for the instance/device/swapchain boilerplate and recommended against — the hairiest 40% of that code, RenderDoc force-load / layer-settings / clean-machine retry, is unexpressible in the library). What pushes past LOW: two verified latent memory-corruption bugs in the skinning bump-allocator grow path (post-bump-offset memcpy OOB read on every grow + single-doubling under-size on bulk allocations — `Refactor_BufferGrowthAndBounds`), a dangling `string_view` pipeline name, an unsynchronized worker-thread-reachable `CrcToIndex`, a `WaitIdle` TOCTOU, and the systemic pattern of comment-enforced shader/CPU contracts (Set-0 binding numbers, hand-mirrored water descriptor tables, a push-constant uint→vec4 pun, duplicated salts/counts). The one Large consolidation (`Architecture_BindlessSlotLifecycle`) targets the directory's documented GPU use-after-free class — the staleness verifier's deliberate carve-out is exactly where that bug class lives. All 14 plans passed a Fable verification pass against source (2 items killed — one fabricated unused-include claim, one already-covered verify item; `Architecture_LayerSeams`' GameBase-virtual proposal reshaped to a registered callback after verification proved `gpGame` is null at boot-time `PipelineManager` construction; 0 plans deleted).

## Debt Score (Engine/Source/Graphics/Render, this run): LOW

`/external-deep-analysis` on `Engine/Source/Graphics/Render` (6 files, ~1,500 LOC, non-recursive). 6 plans authored (4 Architecture + 2 Refactor); distribution: 4 Quick Win + 2 Small — no Medium/Large/Architectural. The directory is structurally healthy: every Render/CLAUDE.md invariant verified against source (ordering contract upheld, ownership-exception co-gating exact, fmod-reduction invariants match the doc, both temporal-reset re-arm paths fire, all latches genuinely single-TU), client/server separation belt-and-suspenders (whole-file `BT_CLIENT` wrap + client-vcxproj-only — stricter than the parent's documented mixed scope), thread/frame-phase clean (main-thread only, reads interpolated state, writes only GPU-mapped layouts and render-side globals), shader/CPU layout drift structurally impossible (dual-language `ShaderLayoutsBase.h`), and zero viable ThirdParty replacements (Perlin shake already delegates to the imported siv PerlinNoise; the day-cycle ramp and Gerstner packer are artist-slider engine glue — null result, HIGH confidence). Findings are mechanical: include hygiene (incl. an ill-formed forward declaration of a nonexistent enum), two comment-enforced shared constants, dead cross-region stores plus one dead uniform write, two ordering-contract doc gaps (the undocumented `giWindTextureIndex` global→main dependency is the most consequential), and two oversized functions with clean move-only seams. Adjacent defects surfaced were already plan-covered and not re-filed (`Graphics/WindHistoryResetOnRecreate`, `Graphics/Architecture_EmptyCoordsStaleIndirectCounts`, `Ui/Refactor_WaterZOffsetTempDecision`, the `MainUniforms.cpp:176-177` stale comment in `Common/StaleCodeCommentsSweep`). All 6 plans passed a Fable verification pass against source (5 corrected in place — including one internal-conflict fix in RegionOwnership and one dropped XMFLOAT4A item in MicroCleanups; 0 deleted).

## Debt Score (Engine/Source/Graphics/Objects, this run): MODERATE

`/external-deep-analysis` on `Engine/Source/Graphics/Objects` (16 files, ~3,440 LOC, non-recursive; the requested `Engine/Source/Frame/Graphics/Managers/Objects` does not exist — target confirmed with the user). 10 plans authored (6 Architecture + 4 Refactor); distribution: 4 Quick Win + 5 Small + 1 Medium. The directory's commodity layers are already outsourced at the right seams (VMA, offline SPIRV-Cross reflection, native `VkPipelineCache` — the ThirdParty review returned a HIGH-confidence null result), include hygiene is near-perfect (13 local directives, 0 unused, zero `#include <...>` violations), hot paths are allocation-clean (the only per-frame functions are the two `WriteIndirectBuffer`s; `Record*` is record-once), and determinism exposure is zero (client-only, no sim writes). What pushes past LOW: a genuine ownership inversion — `PipelineDescriptorWriter` registers raw `Pipeline*` back-references into manager-owned `TextureDescriptors` registries that only whole-`PipelineManager` rebuild clears, a use-after-free class fenced off today only by two zero-caller recreate APIs (delete-the-APIs Quick Win + a Medium ownership move) — plus an at-capacity MRT blend array whose 7th attachment would be a silent stack overwrite, two fully-copyable Vulkan-handle owners, and the set-index resolution kernel re-implemented four times. All 10 plans passed a Fable verification pass against source (3 rewritten — 3 items dropped: a false Pipeline copy-assign claim, a cosmetic Texture move-assign item, an over-abstracted latch dedup; 0 deleted).

## Debt Score (Engine/Source/Frame/Collections, this run): LOW

`/external-deep-analysis` on `Engine/Source/Frame/Collections` (2 files — `Collection.h` 767 + `CollectionMemory.h` 454 lines, non-recursive; the 11 per-collection subdirectories excluded — fills the `Collections/`-excluded gap from the earlier Frame run). 8 plans authored (7 Architecture + 1 Refactor) plus 2 appends to `Engine/DeadCodeAndUnusedIncludesSweep.md` (items 13/14: eight dead graphics forward decls, redundant `VkDeviceSize` alias); distribution: 3 Quick Win + 4 Small + 1 Medium — no Large/Architectural. The pair is the load-bearing core of the deterministic SOA design and verified deep where it matters: sorted-key map CRC/serialization, capacity-in-CRC with the single fixed `2c+1` growth path, total heap-policy compliance (every persistent allocation suppressed + `// Heap:` annotated), clean include shape (single-include fan-out, full ExternalHeaders compliance), and a HIGH-confidence null ThirdParty result (EnTT/flecs/cereal/tweeny all fit-rejected — the generic-looking ECS/serializer parts are exactly where the bespoke CRC-participating-capacity / shared-member-subset / rollback behavior lives). What keeps it above trivial: one confirmed latent heap-overrun (the `AllocateAndAssign` reuse guard self-compares post-`Read`; the replay-load path exercises the branch today, benign only by an unchecked write/read capacity pairing), a missing trust-boundary validation on deserialized counts, and the unenforced server-build `Members()` ≡ `SharedMembers()` wire-parity convention (verified passing on every current leaf — filed as a compile-time guard). Noted but deliberately not filed: the convention-enforced member-tuple protocol (redesign-scale; the parity guard lands the enforceable subset), leaf-owned stale-row zeroing (documented convention), the latent CRC-padding hazard (all current member element types are padding-free; no viable compile-time guard exists for float-bearing types), and the `game::FrameInterpolate`-typed helper signatures (sanctioned engine→game direction). All 8 plans passed a Fable verification pass against source (1 corrected — line-count and a `SwapElement` range fix; 0 deleted).

## Debt Score (DataPacker/Source — root files, this run): LOW

`/external-deep-analysis` on `DataPacker/Source/` (7 files — `Main.cpp` 688 + `FileManager.{h,cpp}`/`Attribution.{h,cpp}`/`Pch.{h,cpp}`, ~900 LOC, non-recursive; `ExportJobs/` excluded). 5 plans authored (4 Architecture + 1 Refactor); distribution: 3 Quick Win + 2 Small — no Medium/Large/Architectural. The directory is structurally clean: zero unused includes / std-header violations / cycles (both root headers are zero-include leaves), layering verified clean both directions, pack/manifest output verified byte-stable against filesystem-enumeration and job-completion order (sorted jobs + ordered future harvest), the one-`std::async`-per-asset divergence from `gpMultithreading` is documented-intentional and race-free (per-job `ThreadLocal`, main-thread-only stream writes), and the ThirdParty review returned a HIGH-confidence null (deflate/BC7 already route through imported zlib/bc7enc_rdo; the rest is orchestration glue and sub-50-line helpers). What the plans close: zero stream-state checks on any output write (disk-full → truncated `.pack`/`.manifest`/generated-`.h` renamed over good files at exit 0 — the generated-header case has no downstream detector), Attribution's six-level `../ThirdParty` derivation that throws post-export for any non-default output dir, deleted assets never dirtying the pack + no duplicate-CRC guard across the two input roots, and the ~250-line RDO sweep block leaking `Texture::sEncodeMutex` knowledge and a direct ThirdParty include into the orchestrator TU. All 5 plans passed a Fable verification pass against source (1 rewritten — the sort tie-break item removed as unreachable once the collision ASSERT lands; 0 deleted).

## Debt Score (DataPacker/Source/ExportJobs, this run): MODERATE

`/external-deep-analysis` on `DataPacker/Source/ExportJobs` (recursive; 40 files, ~6,750 LOC — the export-job subclasses plus the Scene/, Island/, Texture/ subfolders; complements the earlier DataPacker root-files run). 16 plans authored (9 Architecture + 7 Refactor); distribution: 4 Quick Win + 11 Small + 1 Medium — no Large/Architectural. The area is structurally strong where it counts: the `ExportJob` base is a genuinely deep module (cache I/O, versioned dirty checks, per-job `ThreadLocal`, CRC stamping behind a one-method override surface), third-party isolation is near-complete (every library include TU-local; the ThirdParty review returned only one candidate — extending already-imported meshoptimizer over the hand-rolled vertex dedup), pack output is deterministic per machine (sorted jobs, no unordered-container leakage, no timestamps/RNG in bytes), and the Scene headers correctly firewall the 35.5k-line tinygltf closure behind forward declarations. What pushes past LOW: a real chunk-cache versioning bug class (payload structs absent from per-job versions — a struct edit + `kiVersion` bump silently reassembles packs from stale-layout cached chunks; no manifest-version recovery path), chunk payload layouts hand-mirrored writer/reader with comment-only lockstep (shader/scene/animation offset math duplicated against three engine readers), cross-machine BCn/IBL byte variance vs the documented Steam-patching goal, and vestigial scene-loader plumbing (an identity `nodeToJointMap` threaded through three TUs). All 16 plans passed a Fable verification pass against source (15 kept, 1 item rewritten — the animation-offset ASSERT needed its math fixed first; a third scene-layout reader found and added; 0 deleted).

## Plans

| # | Plan | Tier | Effort | Impact | Risks | Score | Notes |
|---|------|--------|--------|--------|-------|-------|-------|
| 1 | [Graphics/Managers/Architecture_LayerSeams.md](Graphics/Managers/Architecture_LayerSeams.md) | Small | 2 | 2 | 1 | 1 | Two engine→game seams beyond sanctioned `gp*` reads: `PipelineManager` ctor calls `game::FrameInterpolate::GraphicsResources()` directly (verified: `gpGame` is **null** at boot-time construction — needs a registered callback, not a GameBase virtual; grill decision reshaped) and `game::Camera` headroom-multiplier statics consumed only by engine RTT sizing → move to `CameraBase`, making the two `Game.h` include removals durable. ImGuiManager screen ownership resolved by `ParentRuleFramingReconciliation` D2 (**landed** — sanctioned-exception carve-out in `Engine/Source/CLAUDE.md` + `ImGuiManager.CLAUDE.md`; not re-filed) |
| 2 | [Graphics/Managers/Architecture_ThreadAndLifetimeGuards.md](Graphics/Managers/Architecture_ThreadAndLifetimeGuards.md) | Small | 2 | 3 | 2 | 1 | Thread/lifetime guards, all verified: unsynchronized `TextureDescriptors::CrcToIndex` map/counter mutation reachable from worker-thread `ParticleManager::Spawn` vs un-mutexed main-thread callers (mutex vs ASSERT+doc, grill); `TextureUploadManager::WaitIdle` TOCTOU (signal consumed at `:156` before mutex at `:159` — teardown can race a transfer-queue submit); `ResizeDynamicBuffer` single-stash investigated and proven safe via the per-framebuffer fence-wait chain — document at the destruction site or add the per-framebuffer stash (grill); name the load-bearing `mPresent.Wait()` at the `meDestroyType` write |
| 3 | [Graphics/Managers/Refactor_PerFramePerf.md](Graphics/Managers/Refactor_PerFramePerf.md) | Small | 2 | 2 | 1 | 1 | Two per-frame costs, both verified: `ProcessPendingTextures` + `AnyAdoptionPending` each full-scan `mTextureMap` every frame (`Graphics.cpp:197/:204`; map holds every `kTexture` pack chunk) → exact atomic pending-adoption counter (device-lost/reset re-arm paths are the risk focus — a stuck counter either re-introduces the scan or stalls adoption); `ParticleManager::Spawn` culls inside `mSpawnMutex` → hoist the visible-area/intensity early-outs above the lock (real worker contention during parallel tick dispatch) |
| 4 | [Graphics/Managers/Refactor_TextureDecomposition.md](Graphics/Managers/Refactor_TextureDecomposition.md) | Small | 2 | 2 | 1 | 1 | Texture-cluster structure: 278-line TextureManager ctor (5 near-identical placeholder stanzas + acquire-CB block duplicated **verbatim** in `CreateScreenDependentResources`), `ProcessPendingTextures` adoption-body extraction, `RegisterTextureBinding` 8 params / 4 defaulted tails → `TextureBindingInfo` struct, `RewriteSamplerDescriptors` nesting, ~280-line `UploadThread` five-seam split. Pure extraction; sequence vs `Architecture_BindlessSlotLifecycle` (shared `TextureDescriptors`) — see Dependencies |
| 5 | [Graphics/Managers/Refactor_PipelineAndDeviceDecomposition.md](Graphics/Managers/Refactor_PipelineAndDeviceDecomposition.md) | Small | 2 | 2 | 1 | 1 | Boot-path structure: InstanceManager ctor (RenderDoc registry scan nests 7 deep → `TryLoadRenderDocDll`; layer-settings block), `SelectPhysicalDevice` 5× copy-pasted feature blocks → table, DeviceManager ctor pipeline-cache + maintenance9-probe extracts, `CreateDepositPipeline` 9 params → spec struct, 7× duplicated allocate/register tail. The declarative pipeline tables (PipelineManager ctor, water tables) deliberately untouched |
| 6 | [Graphics/Managers/Refactor_RecordAndSwapchainDecomposition.md](Graphics/Managers/Refactor_RecordAndSwapchainDecomposition.md) | Small | 2 | 2 | 1 | 1 | Record/creation structure: 383-line SwapchainManager ctor → 4 phase helpers, `RecordSmokeSpreadPipeline` A/B halves written out twice → parameterized helper (recorded commands byte-identical), `RegisterDebugTextures` extraction (fragile slot arithmetic, no-overflow re-derived: max 43 < 56), `RecordShadowPasses` symmetry + ceil-div hoist, ImGuiManager/BufferManager ctor extracts |
| 7 | [Graphics/Architecture_RenderRegionOwnership.md](Graphics/Architecture_RenderRegionOwnership.md) | Small | 2 | 2 | 1 | 1 | Restore Render's one-file-per-region contract: delete 5 dead cross-region Water-field stores (`GlobalUniforms.cpp:225/:434-437`) + the dead `f4SunMoonNormal.w` sun-angle stash (`:63`, no shader reads `.w`), move the 2 time-of-day lighting writes into `PopulateSunAndLighting`, relocate `PopulateLightingParameters` into `LightingUniforms.cpp` (grill decision), clamp the `MainUniforms.cpp` wave fill with `gWaterLowMax` like its paired count write (hygiene — overflow not currently possible). Render-only, no CRC |
| 8 | [Graphics/Refactor_RenderFunctionDecomposition.md](Graphics/Refactor_RenderFunctionDecomposition.md) | Small | 2 | 2 | 1 | 1 | Move-only decomposition of the two oversized Render functions: `PopulateShadowParameters` (~174 lines → stretch-envelope / area+temporal-latch / sun-extension helpers) and `RenderFrameMain` (~237 lines → Gerstner wave-packing + hex-shield helpers; the co-gated count zeroing stays with the fill per Render/CLAUDE.md:9). Lands last of the Render set; zero behavior |
| 9 | [Graphics/Architecture_DescriptorSetIndexDedup.md](Graphics/Architecture_DescriptorSetIndexDedup.md) | Small | 2 | 2 | 1 | 1 | The reflected set-index resolution kernel (index `pDescriptorBindings`, gate on `descriptorCount>0`, read `pDescriptorSetIndices`) is re-implemented 4× — `CreateDescriptorSetLayouts`, `CreateComputePipeline`, `BindingIsInSet0`, `RouteWritesBySet` — agreeing only by parallel evolution; extract one shared resolver, behavior-identical cold path. Grill: helper home |
| 10 | [Graphics/Refactor_WriteDescriptorDecomposition.md](Graphics/Refactor_WriteDescriptorDecomposition.md) | Small | 2 | 2 | 1 | 1 | `WriteModelDescriptor`'s 12 params → `DescriptorWriteCursor` struct; its three copy-pasted IBL sampler blocks → one `PushCombinedImageSamplerWrite` helper; the 6×-repeated registration predicate → `ShouldRegisterBinding`; 276-line 5-deep `Write` → named per-flag statics sharing the cursor. Cold-path extraction, byte-identical descriptor writes |
| 11 | [Graphics/Refactor_PipelineCreatorDecomposition.md](Graphics/Refactor_PipelineCreatorDecomposition.md) | Small | 2 | 2 | 1 | 1 | 362-line `CreateGraphicsPipeline` → `GraphicsPipelineState` aggregate (state structs must outlive the create call) + five `Configure*` stage helpers; `CreateDescriptorSetLayouts` 3-way split; drop the redundant `const PipelineInfo&` alias parameter across both helper headers (every call site passes `rPipeline.mInfo`; internals mix the aliases). Sequence with `WaterDisplacementIndirectCompute` (same `CreateComputePipeline` lines) |
| 12 | [Graphics/Refactor_ModelPipelineMembers.md](Graphics/Refactor_ModelPipelineMembers.md) | Small | 2 | 2 | 1 | 1 | `ModelPipeline` scalar bools → `common::Flags<ModelPipelineFlags>` (absorbs `Create`'s bool params; single call site via `ModelPipelineSpec`, `DynamicPipelines.cpp:22`); measure-then-right-size `mpPipelines[kiMaxMaterials=128]` (~320-380 KB/instance of mostly-empty by-value `Pipeline` storage; `Pipeline` non-movability constrains the container). Latch-dedup item dropped at verification. `ObjectsDeadRecreateApis` has landed (already deleted the two snapshot bools) — convert the two survivors |
| 13 | [Frame/Collections/Architecture_DeserializedCountValidation.md](Frame/Collections/Architecture_DeserializedCountValidation.md) | Small | 2 | 3 | 2 | 1 | Validate iCount/iCapacity/map size at the save/replay/network read boundary (iCount > iCapacity overruns MultiRead; huge iCapacity drives unbounded MakeAligned) |
| 14 | [Frame/Collections/Architecture_NonSharedLeafClientFieldGuard.md](Frame/Collections/Architecture_NonSharedLeafClientFieldGuard.md) | Small | 2 | 2 | 1 | 1 | **Decision plan (present options).** The landed SharedMembersParityGuard's static_assert fires only inside the `HasSharedMembers` branch; `SpaceshipsPostRender` (Members()-only sibling of the client-mixing `SpaceshipsInterpolate`) takes the `else` branch where any future `#if BT_CLIENT` field would be CRC'd + wire-serialized → silent desync, no compile error. Convert `SpaceshipsPostRender` to the SharedMembers() idiom (full set == SharedMembers, Members(){return SharedMembers}) so the parity guard covers it (recommended) vs document the rule in the Collections hub. Identical walked tuple — no CRC value / kiVersion change; compile-self-verifying |
| 15 | [Frame/Collections/Refactor_AllocationHelperDedup.md](Frame/Collections/Refactor_AllocationHelperDedup.md) | Small | 2 | 2 | 1 | 1 | Merge the near-identical Allocate / ReallocateIfCapacityChanged cores; resolve the pData-null condition asymmetry deliberately |
| 16 | [DataPacker/Architecture_RdoSweepRelocation.md](DataPacker/Architecture_RdoSweepRelocation.md) | Small | 2 | 2 | 1 | 1 | Move ~250-line RDO sweep block from Main.cpp to ExportJobs/Texture/RdoSweep.{h,cpp} — confines sEncodeMutex contract + bc7decomp.h include |
| 17 | [DataPacker/Architecture_IslandConstantsOwnership.md](DataPacker/Architecture_IslandConstantsOwnership.md) | Small | 2 | 2 | 1 | 1 | Move `kiElevationDivisor`/`kpcIslandIntermediatesDir` down from `ExportIsland.h` to `BakeIslandIntermediates.h` (kills the Island/→ExportJobs/ upward include edge pulling `ExportJob.h` into the 3 bake TUs); name the triplicated `Elevation.r32`/`AmbientOcclusion.r16`/`MeshProcessed.bin` literals + the writer/reader `BakedDimensions.json` keys; comment-disambiguate the coincidentally-equal version axes (`ExportIsland::Version(28)` vs `kiBakeVersion=28`). Bytes unchanged |
| 18 | [DataPacker/Architecture_PackByteDeterminism.md](DataPacker/Architecture_PackByteDeterminism.md) | Small | 2 | 2 | 1 | 1 | **Decision plan (present options).** Two cross-machine pack-byte variance sources vs the documented Steam-patching stable-bytes goal (`Main.cpp:382`): RDO `m_rdo_max_threads = HardwareCoreCount()-2` (`Texture.cpp:384` — ERT partitions blocks per thread, verified in library source, so BCn bytes vary by core count) and cmft OpenCL IBL convolution (GPU/driver-dependent half-floats). Pin-vs-document per path; recommendation: document-only until multi-machine bakes exist |
| 19 | [DataPacker/Architecture_OrchestrationDedup.md](DataPacker/Architecture_OrchestrationDedup.md) | Small | 2 | 2 | 1 | 1 | Three near-identical discovery/match/timestamp-dirty loops inside `ExportCubemapIbl.cpp` (:57-79/:189-209/:233-280) → one scan helper (the face-dir pass's 6-file dirty check parameterizes); duplicated legacy `[w][h][mips]` intermediate write merged; verbatim `CleanupOnFailure`+`mIntermediateFiles` duplication in ExportShader/ExportScene hoisted into the `ExportJob` base. Intermediate bytes identical |
| 20 | [DataPacker/Architecture_SceneLoaderVestigialPaths.md](DataPacker/Architecture_SceneLoaderVestigialPaths.md) | Small | 2 | 2 | 1 | 1 | Remove the identity `nodeToJointMap` threaded through 3 scene TUs (only uses: emptiness bool, always-true membership, identity passthrough — re-derived at verification; keep the `target_node` bounds check) → `bHasSkeleton` + direct indices; collapse `DetermineAnimationPath`'s converged branches (log-text-only divergence; keep the `!skins.empty()\|\|!animations.empty()` gate); merge the duplicated VkFormat→suffix switch whose drift silently dangles scene-chunk texture CRCs. `.MODEL`/chunk bytes identical |
| 21 | [DataPacker/Refactor_ExportTextureMechanics.md](DataPacker/Refactor_ExportTextureMechanics.md) | Small | 2 | 2 | 1 | 1 | Per-call 12-entry `unordered_set` in `Handles` → static array; the 9-`find()` path sniffing matches anywhere in the path (a directory named `Foo.ktx` misroutes every file under it) → one-pass `extension()`/`filename()` resolve, with `bRawTexture` derived from extension-matched cases only (a `[BC4]`-tagged PNG must stay on the encode path); per-job recursive `Fonts/**` rescan → `once_flag` stem cache (cross-domain coupling fix). After `ExportTextureHeaderlessRawFormatClaims` (reconciled set first) |
| 22 | [DataPacker/Refactor_TextureUtilDedup.md](DataPacker/Refactor_TextureUtilDedup.md) | Small | 2 | 2 | 1 | 1 | zlib-compress block ×3 (`ZlibCompress` survives; identical `Z_BEST_COMPRESSION` verified) and mip-chain size-summation ×3 → shared helpers across ExportTexture/Texture/MigrateLegacyIntermediates (`Main.cpp`'s BC7-hardcoded fourth loop stays with the PayloadLayout reader helper). Bytes unchanged |
| 23 | [DataPacker/Refactor_ShaderToolRunnerDedup.md](DataPacker/Refactor_ShaderToolRunnerDedup.md) | Small | 2 | 2 | 1 | 1 | `PreprocessShader`/`CompileShader`/`OptimizeShader` triplicate a ~50-line skeleton → one `RunVulkanTool` helper — error policies differ (glslc: any stdout fatal; the others: exit code + warn) so parameterize, don't unify; the thrice-identical `OutputDebugStringW` thread-id block → one `LOG` (LogFormatters handles wide strings) |
| 24 | [DataPacker/Refactor_IslandFunctionDecomposition.md](DataPacker/Refactor_IslandFunctionDecomposition.md) | Small | 2 | 2 | 1 | 1 | Decompose `BakeRoute` (337 lines → 4 stage helpers along the existing block comments + drop the 9-member context unpack), `ExportIslandData` (200 — table-drive the AO/Color/Normals encode blocks; the masks block shares only the tail; extract `VerifyHullCcwConvex`), `BeachSubdivider::Run` (183 → split + absorption member functions). Bake bytes identical; `sEncodeMutex` serialization preserved |
| 25 | [DataPacker/Refactor_SceneFunctionDecomposition.md](DataPacker/Refactor_SceneFunctionDecomposition.md) | Small | 2 | 2 | 1 | 1 | Decompose `LoadVertices` (260 lines, 4+ nesting → 4 extractions + `NodeLocalMatrix` deduping the near-verbatim TRS/matrix branch vs `ComputeNodeWorldTransform`), the 160-line `Texture` file-loading ctor (per-`FileType` loaders), `WriteAnimationSection`'s 32-line diagnostic dump. Bytes identical; land before the meshopt remap plan (same dedup block) |
| 26 | [Ui/SpaceshipExplosionLightingDeadSliderRemoval.md](Ui/SpaceshipExplosionLightingDeadSliderRemoval.md) | Quick Win | 1 | 1 | 1 | 1 | Follow-up from DeadCodeAndUnusedIncludesSweep item 2: removing write-only Explosions pfLightPercents orphaned the game::gSpaceshipExplosionLightingIntensity Tweaks slider (LightingWrappers.h:79 / .cpp:54 / TweaksScreenLightingEffectsLighting.cpp:50) -- its only functional reader (.fLightPercent .Get()) is gone, default 0.0 drove nothing anyway. Remove extern+def+slider row; verify whether the wrapper is persisted in a versioned tweaks blob (bump kiVersion if so) vs name-keyed/non-persisted. Client-only, no CRC/determinism exposure. One grill decision (persistence/version) |
| 27 | [Engine/OsApiStringOutPointerNullGuards.md](Engine/OsApiStringOutPointerNullGuards.md) | Small | 2 | 2 | 1 | 1 | Null-guard SHGetKnownFolderPath/GetProp out-pointers at boot + crash paths (graceful recovery, crash path stays allocator-free) |
| 28 | [Engine/MouseRawInputSinkRegistrationRemoval.md](Engine/MouseRawInputSinkRegistrationRemoval.md) | Quick Win | 1 | 1 | 2 | 2 | Behavior-verify split-out of DeadCodeAndUnusedIncludesSweep item 8: RawInputManager.cpp:43 registers the mouse for raw input with RIDEV_INPUTSINK, but HandleRawInput (:217/:231) handles only RIM_TYPEKEYBOARD so mouse packets are discarded (mouse driven by DirectXTK WndProc) -- registration vestigial. Confirm no raw-mouse consumer, drop only the mouse RAWINPUTDEVICE entry (keep keyboard), playtest mouse input. Client-only, no CRC/determinism exposure. Playtest-gated |
| 29 | [Graphics/Managers/Architecture_GlobalSet0BindingConstants.md](Graphics/Managers/Architecture_GlobalSet0BindingConstants.md) | Medium | 3 | 2 | 1 | 2 | Single-source the global Set-0 binding numbers 0/1/3/4/12 → `kiGlobalBinding*` in dual-language `ShaderLayoutsBase.h`, adopted in `TextureDescriptors.cpp` (layout + writes + array update) and across the dozens of shaders hardcoding `layout(set=0, binding=N)` (DataPacker rebuild, render-identical). Split out of `ShaderCpuContractConstants` at grill; gated on glslang accepting `const int` in layout qualifiers (else C++-side only). Client/graphics-only, no CRC/determinism |
| 30 | [Graphics/WaterNormalWeightCpuResolve.md](Graphics/WaterNormalWeightCpuResolve.md) | Small | 2 | 2 | 2 | 2 | Decision-5 spin-out from `DocCanonicalHomeReconciliation`: `Water.frag:157-159` lerps the 3 normal-weight samples in-shader (6 min/max endpoints + shared `fCameraHeightZoomFactor`) against `Render/CLAUDE.md`'s "resolve CPU-side, upload one float" rule. Resolve the 3 weights CPU-side in `LightingUniforms.cpp:108-118` (`HeightLerpWrapperQuartet::Resolve` or `std::lerp`), collapse `MainLayout`'s 6 members → 3 (drop `fCameraHeightZoomFactor` only if sole-consumer — audit `Water.frag`/`WaterSkyboxOne.frag`), mirror the block change in `WaterSkyboxOne.frag`, update `Water/CLAUDE.md:17`. Client/graphics-only, no CRC/determinism; DataPacker shader recompile + visual smoke-test (not compile-checked) |
| 31 | [Frame/BlastersTerrainMarchDegenerate.md](Frame/BlastersTerrainMarchDegenerate.md) | Small | 2 | 2 | 2 | 2 | `BlastersUpdate.cpp:293-310` terrain-impact back-search is degenerate — both lerp endpoints equal `vecPosition` and the loaded `vecVelocity` is unused, so effects spawn at the over-shot post-integration position + jitter (the "binary search" comment is also wrong — it is a linear march). Reconstruct the travel segment (`pos − vel·dt`, uses the dead load) to find the true surface crossing, vs delete the vestigial march. Determinism caveat: the impact block draws from the **shared** `randomEngine` (Blasters discipline) — the fix must not change draw count/order on either side. One grill decision |
| 32 | [Network/TryRelinkClientForLoadOrdering.md](Network/TryRelinkClientForLoadOrdering.md) | Small | 2 | 2 | 2 | 2 | **Decision plan (present options).** `ServerSession::TryRelinkClientForLoad` (`:524-551`) relinks load-path players in `mCoordFrames` map × array-index order with **no global-ID sort**, while the reconnect path `TryRelinkNewClient` (`ServerClientManager.cpp:122-164`) sorts by global ID (`:149-151`) "to preserve creation order". The assign/spawn **send order** is client-visible. Add the sort to match reconnect (recommended if any `mClientOwnedPlayerIds` consumer is order-sensitive) vs document the intentional difference. Server-only, determinism-adjacent (replicated ordering, not shared-CRC) |
| 33 | [Frame/MissilesDeadExplosionRadiusField.md](Frame/MissilesDeadExplosionRadiusField.md) | Small | 2 | 2 | 2 | 2 | Remove the dead per-element `MissilesPostRender::pfExplosionRadii` (`Missiles.h:118`): zeroed at spawn (`Missiles.cpp:460`), memcpy'd forward (`:312`), in `SharedMembers` (CRC'd, `:132`) and `LogDifferences` (`:567`) — but **never read** (real AoE is the `HealthDamage.h` constant). Execute via the `add-collection-member` checklist in reverse; bumps `MissilesPostRender::kiVersion`→`Frame::kiVersion` (stale saves/replays reject cleanly — expected). Field is always zero so no CRC *value* changes, only layout; rebuild client+server together |
| 34 | [Graphics/PipelineGraphicsGlobalSet0Indexing.md](Graphics/PipelineGraphicsGlobalSet0Indexing.md) | Small | 2 | 2 | 2 | 2 | `Objects/CLAUDE.md` says global Set 0 is **always** framebuffer-indexed, but `BindGraphicsDescriptorSets` (`Pipeline.cpp`) indexes Set 0 with `iDescriptorSetIndex` = `mbPerCommandBuffer ? iCommandBuffer : 0`, so non-per-command-buffer plain draws bind `mGlobalDescriptorSets[0]` into every framebuffer's CB — while `BindComputeDescriptorSets` and `RecordDrawIndirect` always use `iCommandBuffer` (graphics path internally inconsistent). Investigate whether Set-0 globals are framebuffer-invariant for the affected pipelines (doc overstatement → reword) or a latent cross-frame GPU hazard (→ framebuffer-index Set 0 on the plain-draw path, mirroring compute). Client/graphics-only |
| 35 | [Network/StaticDataEpochCheckOnSubscribingSlot.md](Network/StaticDataEpochCheckOnSubscribingSlot.md) | Small | 2 | 2 | 2 | 2 | `Client::ServerCoordStaticData` (`ClientReceive.cpp:251`) applies the epoch check to `kWaitingFullState` **and** `kSubscribing` slots, but a `kSubscribing` placeholder has no server-assigned epoch (its `ackState.uiEpoch` is never set by `SendSubscribe`), and the sibling classifiers `ClassifyFullState`/`ClassifyCoordUpdate` **skip** the epoch check for `kSubscribing` — which `Client/CLAUDE.md` documents as the rule. Could wrongly drop once-per-subscription static data (NavData) for a racing `kSubscribing` slot. Trace the slot lifecycle, then align to the siblings (remove `kSubscribing` from the condition, recommended) vs document a stricter static-data rule + seed the epoch. Client receive only — no shared-CRC/determinism exposure |
| 36 | [Graphics/InlineAreaMappingDryToHelper.md](Graphics/InlineAreaMappingDryToHelper.md) | Small | 2 | 1 | 1 | 2 | 8 shaders hand-inline the `VisibleAreaToWorld(uv, area)` inverse (verbose `(1-uv)*lo + uv*hi` lerp) instead of calling the shared `ShaderFunctions.h:21-33` helper (the lighting site `LightingSpread.frag:74` was just fixed). Route `Terrain.frag`/`Water.vert`/`WaterDisplacement.comp`/`SmokeSpreadCommon.h`/`SmokeOccupancyDilateRemap.comp`/`WindSpreadOne.comp`/`WindSpreadTwo.comp`/`WindOccupancyDilate.comp` through it after a per-site exact-inverse (Y-flip) equivalence gate; decide whether Smoke/Wind local helpers route through the shared helper or stay in-family. DataPacker recompile + visual smoke-test (not compile-checked) |
| 37 | [Graphics/WindHistoryResetOnRecreate.md](Graphics/WindHistoryResetOnRecreate.md) | Small | 2 | 2 | 2 | 2 | Wind sibling of the shadow `gbShadowTemporalReset` fix: a Graphics device-lost/settings recreate rebuilds `mWindTextureOne/Two` with undefined contents while `giWindTextureIndex` (`Render.h:36`) survives, and `gbWindClear`'s path (`WindUniforms.cpp:75-78`) only flips a flag relying on in-shader soft-decay to fade garbage over several frames — a soft transient, not one hard frame. Choose hard GPU clear (mirror smoke's `gbSmokeClear` + `kPipelineSmokeClearA/B`) vs reset-index re-seed (mirror shadow's reset). Smoke already fully mitigated. Playtest-validated |
| 38 | [Engine/VcxprojAndBuildConfigReconciliation.md](Engine/VcxprojAndBuildConfigReconciliation.md) | Small | 2 | 1 | 1 | 2 | Build-graph / IDE-view reconciliation (no runtime): game Debug `AdditionalIncludeDirectories` duplicate `..\..\Data` and omit the `..\..\..\Data` entry Profile/Release have; Prebuilts vcxproj unused `..\..\..\..\Common` include path; StackWalker orphan (`Prebuilts/Source/Engine/StackWalker.cpp` registered in no vcxproj; only `DataPacker/StackWalker.cpp` compiles) — register/delete/recategorize; `ThirdParty.vcxproj.filters:123` `cmft.cpp` missing `<Filter>`; `HexShieldWrappers.cpp` vestigial in server vcxproj. Two decision items (StackWalker, HexShieldWrappers) + four mechanical; item 6 (Input/Memory run): `RawInputManager.h` server `ClInclude` (partial-guard header compiles in both builds; IDE-only) |
| 39 | [Audio/Refactor_UpdateLifecycleDecomposition.md](Audio/Refactor_UpdateLifecycleDecomposition.md) | Small | 2 | 2 | 2 | 2 | Split the ~330-line `StaticVoices::UpdateLifecycle` (`StaticVoices.cpp:144-473`) into five named pass methods matching the `Audio/CLAUDE.md` pass vocabulary; replace the O(n²) `FadeOutCount()` lambda with a maintained `miFadeOutCount`; replace the `pActivatedIds` workbuffer array + linear membership scan with a transient `kActivatedThisFrame` voice flag. Pure decomposition — audible behavior unchanged; playtest |
| 40 | [File/Architecture_LoadThreadLifecycleSafety.md](File/Architecture_LoadThreadLifecycleSafety.md) | Small | 2 | 2 | 2 | 2 | Close the File load-thread lifecycle holes: `~FileManager` joins `mLoadingThread` without draining `mLoadingFuture` (thread assigned *on* the async task; the server never drains the future, so a fast teardown can join a not-yet-joinable thread → terminate); gate the unsynchronized boot-window `mEagerChunkMap` reads (`ReadChunkData:670`, `IsChunkReady:404`) behind an atomic eager-load-complete flag (invariant-hardening — safe today only via undocumented cross-subsystem boot ordering); resolve the misleading `mQueueMutex` in `ReadChunkData` vs the lockless writers (document the `eState` acquire-pair or drop the lock). Process-local, no CRC exposure. Three grill decisions pre-staged |
| 41 | [File/Architecture_VersionedHeaderDedup.md](File/Architecture_VersionedHeaderDedup.md) | Small | 2 | 2 | 2 | 2 | Extract the hand-duplicated version+size file-header convention — `Write/ReadVersionedFile` (`FileManager.h:262-290`) vs `DifferenceStreamWriter::Save`/reader ctor (`DifferenceStream.h:73-77`/`162-183`, comments literally say "matches WriteVersionedFile pattern") — into shared `WriteVersionHeader<T>`/`ReadAndValidateVersionHeader<T>` helpers with read-value out-params (callers keep their LOGs + `DEBUG_BREAK`). Byte-identical output; verify via record/replay round-trip. Needs `IncludeHygiene`'s DifferenceStream→FileManager include (or land together) |
| 42 | [Frame/Architecture_NavPredicateDedup.md](Frame/Architecture_NavPredicateDedup.md) | Small | 2 | 2 | 2 | 2 | `SegmentsIntersect` token-identical in `NavBuild.cpp:271-292` + `NavQuery.cpp:13-34`, winding-number core duplicated — epsilon drift between build-time visibility graph and runtime query = paths through walls; share one definition (extends `NavBuildSplit`'s `NavBuildInternal.h` promotion). Behavior-identical; land after the split |
| 43 | [Frame/Architecture_CrcMixingStrength.md](Frame/Architecture_CrcMixingStrength.md) | Quick Win | 1 | 2 | 3 | 2 | XOR-combined frame CRCs cancel even-multiplicity errors (swapped fields between alignment pairs/collections hash identical) — replace with an ordered fold in `Alignments::Crc`, both engine `Crcs()`, and the game-layer XOR continuation (`Frame.cpp:534-549/612-627/692`); iteration order verified deterministic at every site, no parallel accumulation. CRC values change: rebuild client+server together; old replay `.checksums` log per-tick mismatches (accepted class) |
| 44 | [Frame/Refactor_AStarEndVisibleLazy.md](Frame/Refactor_AStarEndVisibleLazy.md) | Small | 2 | 2 | 2 | 2 | `AStarPath` eagerly precomputes end-visibility for all V cell vertices per invocation (`NavQuery.cpp:383-387`) but consumes it only per expanded vertex — lazily memoize a tri-state in the same workbuffer block (start side stays eager: fully consumed on first pop). Result-identical pure-geometry queries; halves worst-case LOS precompute on the per-player tick path |
| 45 | [Frame/Refactor_CollisionDecomposition.md](Frame/Refactor_CollisionDecomposition.md) | Small | 2 | 2 | 2 | 2 | Flatten 177-line depth-5 `CollideLayerPair` (extract per-pair body; dedupe the mirrored A/B result-record blocks into one swapped-args helper), collapse `InsertObjectIntoZones`' duplicated `bIsLayerA` branches, hoist rare-overflow `sLayers.at` lookups out of the hot zone loop. Behavior-identical — collision feeds CRC'd state, replay-verify |
| 46 | [Graphics/Architecture_ScreenshotFenceDeadlock.md](Graphics/Architecture_ScreenshotFenceDeadlock.md) | Small | 2 | 2 | 2 | 2 | Latent deadlock (masked by `kbScreenshots=false`): `SubmitMainToQueue` resets the per-framebuffer fence, then calls `SaveScreenshot`, which waits that fence — but only the later UI submit signals it (`ImGuiManager.cpp:376`), and the main thread is parked on `mSubmitMain.Wait()` → circular wait → `VK_TIMEOUT` → `DEBUG_BREAK`+throw under both `kbRenderThread` settings (chain re-derived at verification). Fix shapes: wait-before-reset (stale-capture caveat), fence-the-Main-submit, or recommended main-thread capture between UI submit and Present; plus `ScopedSuppressAllocationTracking` + `// Heap:` on the capture-side allocations. Client-only; validate with `kbScreenshots=true` |
| 47 | [Ui/Architecture_LibraryReplacement.md](Ui/Architecture_LibraryReplacement.md) | Small | 2 | 2 | 2 | 2 | Rebuild the hand-rolled ImGui curve editor `CurveWidget.cpp` (178 lines) on already-vendored ImPlot (MIT — coverage extension, no new import): `DragPoint`/`PlotLine`/`PlotInfLines` replace bespoke hit-testing, pixel↔curve transforms, gridlines, and the spread-pass tick overlay (~100-120 LOC removable; widget becomes ~60-80 lines); per-ID drag state removes the documented one-widget-at-a-time `siDragIndex` limitation. Single call site (`TweaksScreenLighting.cpp:228` bool-return contract); client-only debug UI; hand re-verify interactions in the Tweaks Lighting tab |
| 48 | [Network/Architecture_SubscribeRejectSlotNegotiation.md](Network/Architecture_SubscribeRejectSlotNegotiation.md) | Small | 2 | 3 | 3 | 2 | Server rejects subscribes with sentinel slot `0xFF` but the client detects rejection as `slot >= 16` while the server allocates from 64 un-negotiated slots (hello carries no count) — a slot-≥16 *accept* is misread as rejection and uniquely sends no unsubscribe → permanent server slot leak (per-tick broadcasts to a deaf channel, pinned active coord, swallowed re-subscribes). Name the sentinel in `NetworkProtocol.h` + check it explicitly + defensive-unsubscribe; negotiation shape (hello byte + version bump vs shared constant vs sentinel-only) at grill. Edge-case trigger (needs >16 in-flight subs), but the leak is permanent |
| 49 | [Network/Refactor_ServerFrameBufferRecycle.md](Network/Refactor_ServerFrameBufferRecycle.md) | Small | 2 | 2 | 2 | 2 | `Server::BufferFullFrame` (once per tick; per-active-coord serialization inside) builds a fresh `ostringstream` + `.str()` string copy into the debug-frame ring — the directory's biggest steady-state allocator load (suppressed + `// Heap:`-compliant, still 2 large allocs + a copy per coord per tick); recycle evicted `BufferedFullFrame` storage or serialize via a reusable streambuf — grill. Ring serves only `ClientDebugFrameRequest`; payload bytes identical |
| 50 | [Graphics/Architecture_ObjectsShaderCpuConsistency.md](Graphics/Architecture_ObjectsShaderCpuConsistency.md) | Small | 2 | 2 | 2 | 2 | Three shader↔C++ pairings single-sourced: bindings 15/16 bare literals at the `kModel` append → the existing `kModelPipelineBinding*` constants (+ wrong "Set 0" comment → Set 1); dead `f4Material` push-constant channel removed (no shader reads it — the live channel is `f4Pipeline.w`; optional 32→16 B layout shrink at grill needs a DataPacker shader recompile); dead `uiPushConstantSize` knob deleted or asserted (latent range/write mismatch) |
| 51 | [Network/FleetSyncArmedUnderOuterGuard.md](Network/FleetSyncArmedUnderOuterGuard.md) | Small | 2 | 2 | 1 | 1 | **Decision plan (present options).** Spun out of the landed `BroadcastTickAllocationGuardScope` fix: the five `ServerFleetManager` guards wrapping the workbuffer-armed `SendFleetSync` are dominated by outer blanket guards (`PreTickNetwork`/`ResetClientsForLoad`), so the send is never armed and the inner guards are redundant + carry stale "allocates packet" comments. A — accept + delete redundant guards + fix comments + document (recommended) vs B — narrow the outer request-drain/load guards to truly arm the sends (larger). Server-only debug-tracking; no CRC/wire/determinism exposure |
| 52 | [Frame/Collections/Architecture_AllocateAndAssignReuseGuard.md](Frame/Collections/Architecture_AllocateAndAssignReuseGuard.md) | Small | 2 | 3 | 3 | 2 | Fix the self-comparing buffer-reuse guard on deserialize (latent heap overrun via stale member strides; replay-load exercises the branch today) |
| 53 | [Frame/Collections/Architecture_HeaderCohesionAndSplit.md](Frame/Collections/Architecture_HeaderCohesionAndSplit.md) | Medium | 3 | 2 | 1 | 2 | /reduce-file Collection.h (767 lines, 5 concerns); move lifecycle helpers out of CollectionMemory.h, top-of-file include, BT_CLIENT grouping - land last of the Collections set |
| 54 | [DataPacker/Architecture_PayloadLayoutSingleSource.md](DataPacker/Architecture_PayloadLayoutSingleSource.md) | Medium | 3 | 3 | 2 | 2 | Chunk payload layouts duplicated writer/reader with comment-only lockstep: shader `[bindings][setIndices][attrs][SPIR-V]` (`ExportShader.cpp:408-426` vs `PipelineManager.cpp:41-54`), scene `[crcs][indexStarts][MaterialShaderData]` (`ExportScene.cpp:530-537` vs `ModelPipeline.cpp:38-43` **and `FileManager.cpp:363-367`** — third reader found at verification), the animation-section walk (vs `AnimationData.cpp`), the `.MODEL` 16/32-bit index threshold (writer/reader untied), and the texture-intermediate magic-vs-legacy branch ×3 → shared constexpr offset helpers on the `DataFile.h` header structs. Pack bytes identical; full bake + game load verifies |
| 55 | [DataPacker/Architecture_LibraryReplacement.md](DataPacker/Architecture_LibraryReplacement.md) | Small | 2 | 2 | 2 | 2 | Replace the hand-rolled vertex dedup in `SceneVerticesLoader.cpp:233-345` (~30-45 LOC + the `std::hash<ModelVertex>` specialization at `DataFile.h:393-400`) with already-imported meshoptimizer's `meshopt_generateVertexRemap`/`remapIndexBuffer`/`remapVertexBuffer` (MIT, allow-listed, functions verified present; coverage extension, no new import). Vertex order changes → `ExportScene`/`ExportModel` version bumps + one full scene re-export; `ModelVertex` is static_assert-locked padding-free (byte-compare safe); land with/after the `LoadVertices` decomposition |
| 56 | [Frame/ScaleEngineToMeters.md](Frame/ScaleEngineToMeters.md) | Large | 4 | 3 | 2 | 3 | Three-batch pass to bring camera, gameplay, and audio tunables into the "1 unit = 1 m" convention introduced when `kfMetersToUnits = 1.0f` and `kfCellWidth/kfCellHeight = 600` landed. Catalog: ~15 literals across `Camera.{h,cpp}`, `CameraBase.cpp`, `Game.{cpp,h}`, `FrameBase.h`, `Frame.cpp`, `TerrainUtils.cpp`, `Players{,Navigation}.{cpp,h}`, `Spaceships.cpp`, `SoundSettingsWrappersBase.cpp`, `WrapperBase.cpp`. Final cleanup deletes `engine::kfMetersToUnits` and simplifies the three `IslandTerrain.cpp` multiplications |
| 57 | [Graphics/WaterDisplacementIndirectCompute.md](Graphics/WaterDisplacementIndirectCompute.md) | Medium | 3 | 2 | 2 | 3 | Two deferred perf items in the Gerstner-displacement pre-compute. Item 1: replace the `ASSERT(false)` for `kIndirectHostVisible\|kCompute` at `PipelineCreator.cpp:624` with the symmetric host-visible-indirect branch, add `Pipeline::WriteIndirectComputeBuffer`, wire `kPipelineWaterDisplacement` to indirect dispatch from `MainUniforms.cpp` (saves ~64x workgroup launches at LOD3). Item 2: extend the existing `fShoreAmplitude` check in `WaterDisplacement.comp` so `fTerrainElevation >= 0.0f` early-returns over land, skipping both Gerstner loops |
| 58 | [Graphics/IslandNavContourResidency.md](Graphics/IslandNavContourResidency.md) | Medium | 3 | 2 | 2 | 3 | Server-side per-template `IslandTemplate::mNavContour` (~5-50 KB per template × ~65 templates) is permanently resident — built once in `WaitForElevationMaps`, freed only at `~IslandTerrain`. Surfaced by the `IslandResidentMemoryScaling` sibling-sweep. Adopt whatever lifecycle the strategy plan picks, or close if measurement shows the aggregate cost is negligible. Depends on `IslandResidentMemoryScalingStrategy.md` for the lifecycle decision |
| 59 | [Frame/NavBuildSplit.md](Frame/NavBuildSplit.md) | Medium | 3 | 2 | 2 | 3 | Split `NavBuild.cpp` (934 lines, approaching `/reduce-file`) into `NavBuild.cpp` (contour extraction + visibility-graph build) + new `NavCellData.cpp` (cell-merge / acceleration / `NavData` serialize) sharing a private `NavBuildInternal.h` (promotes the one cross-domain helper `SegmentsIntersect`). Move-only: no behavior, no `kiNavDataVersion`/CRC/serialized-layout change. Wire the two new files into all four client+server vcxproj/.filters |
| 60 | [Frame/IslandTerrainSplit.md](Frame/IslandTerrainSplit.md) | Medium | 3 | 2 | 2 | 3 | `/reduce-file` split of `IslandTerrain.cpp` (890 lines, review-flagged) along its existing `#if defined(BT_CLIENT)` seam: keep the shared CPU domain (ctor/dtor, `WaitForElevationMaps`, `GlobalElevation`/`GlobalNormal`, `BuildElevationGrid`, `FrameElevation`/`FrameNormal`) in `IslandTerrain.cpp`; move the client GPU-residency span (`:410-832`+`:856-888` — `CreateClientMeshBuffers`, `AcquireTextureSlot`, eviction/restoration sweeps + pending pre-scans, `ReleaseGpuResources`, `ResetTextureSlots`) into a new fully-`BT_CLIENT` `IslandTerrainResidency.cpp`. Shared header `IslandTerrain.h` NOT split; no private `*Internal.h` (no cross-TU helper, unlike NavBuildSplit). Move-only: no behavior / `kiVersion` / `kiNavDataVersion` / CRC / `.pack`-layout change. New file wired into the **client** vcxproj+filters only (kept file stays in all four). Plan only — `/reduce-file` runs at execution. Coordinate with `IslandResidentMemoryScalingStrategy` (relocates the eviction sweeps it edits) |
| 61 | [Graphics/DisabledPassGatingPerfAudit.md](Graphics/DisabledPassGatingPerfAudit.md) | Medium | 3 | 2 | 2 | 3 | Measure-then-decide audit of record-once passes that run while their feature is disabled (CB re-record ban means gating-when-disabled needs a destroy-tier edge re-record; recorded clears/dispatches can't be cheaply indirect-gated). Sites: `RecordSmokeSpreadPipeline` / `RecordWindSpreadPipeline` (`CommandBufferRecordGlobal.cpp:121-445`), object-shadows render+blur and wind deposit (`CommandBufferRecordMain.cpp:130-178`). Documents the shadow Item-1 ACCEPT precedent; "accept" is the likely outcome unless a pass measures non-trivial. Object-shadows has no clean on/off edge (day-cycle continuous `fObjectShadowsIntensity`) |
| 62 | [Graphics/WindowedLightingShadowDispatch.md](Graphics/WindowedLightingShadowDispatch.md) | Large | 4 | 3 | 2 | 3 | Window the lighting (deposit/spread/combine/temporal) + shadow passes to the live cropped sub-region instead of processing the full headroom texture every frame: indirect-compute dispatch + min-texel invocation offset for the compute passes, dynamic scissor for deposit/spread, and fold the history write into `LightingTemporal` to drop the four full-texture copies. Enabled by this session's `CLAMP_TO_BORDER` edge samplers + steady-state-target texel ramp; shares `WriteIndirectComputeBuffer` with `WaterDisplacementIndirectCompute`. Client/graphics-only |
| 63 | [Frame/Refactor_NavFunctionDecomposition.md](Frame/Refactor_NavFunctionDecomposition.md) | Medium | 3 | 2 | 2 | 3 | Extract the 6-lambda indexed min-heap from 212-line `AStarPath`, the A*-miss fallback from `NavQueryDirection`, the 5-deep compiled-out crossing-check from `BuildCellNavData`, the first-mint block from `AcquireTextureSlot`; precompute the vertex→polygon map in `BuildVisibilityGraph` (drops O(V²·P) + two nesting levels). Move-only/result-identical; land after both Frame splits |
| 64 | [Graphics/Managers/Architecture_BindlessSlotLifecycle.md](Graphics/Managers/Architecture_BindlessSlotLifecycle.md) | Large | 4 | 4 | 3 | 3 | Consolidate the island bindless mint/evict/restore lifecycle — currently spread across `TextureDescriptors` patching (`WriteArrayElementFromLive`/`UnregisterBindingsForKey`), `IslandTerrain` sweeps (mint `:525-595`, EvictionSweep `:679-777`, restore `:819-829`), TextureManager placeholder arrays, the RenderGlobal fence window, and the verifier carve-out (`PipelineManager.cpp:856-862`) — into one `BindlessSlotRegistry` enforcing the `Managers/CLAUDE.md:29` eviction-symmetry invariant in code and narrowing the staleness verifier's deliberate blind spot. Targets the directory's documented GPU use-after-free class; execute alone, after the small TextureDescriptors plans (see Dependencies) |
| 65 | [Graphics/Architecture_PipelineRegistrationOwnership.md](Graphics/Architecture_PipelineRegistrationOwnership.md) | Medium | 3 | 2 | 2 | 3 | Make pipeline registration symmetric and owned at one altitude: `PipelineDescriptorWriter::Write` plants raw `Pipeline*` in `TextureDescriptors` registries that only whole-`PipelineManager` rebuild clears (`Pipeline::Destroy` never unregisters — a use-after-free class for any future out-of-rebuild create, fenced off today only by full-rebuild discipline now the dead recreate APIs are deleted — `ObjectsDeadRecreateApis` landed); move the `kModel` auto-append (lighting/shadow/smoke knowledge) out of generic `Pipeline::Create`; formalize the pre-`Create` member-poking protocol. Grill: destination layer + register-up vs unregister-on-Destroy |
| 66 | [Frame/Collections/Architecture_CollectionCrcMixing.md](Frame/Collections/Architecture_CollectionCrcMixing.md) | Small | 2 | 2 | 3 | 3 | Collection-internal CRC XOR -> ordered fold (iCount==iCapacity metadata self-cancel; map/member-array cancellation); extends CrcMixingStrength downward - replays re-record |
| 67 | [Graphics/IslandResidentMemoryScalingStrategy.md](Graphics/IslandResidentMemoryScalingStrategy.md) | Large | 4 | 3 | 3 | 4 | Predecessor `IslandResidentMemoryScaling.md` landed step 1 only: `[DEBUG-resmem]` boot-time LOG instrumentation at the end of `IslandTerrain::CreateClientMeshBuffers` reporting per-template + aggregate mesh CPU / mesh GPU / heightmap / hull bytes. Once a Debug client boot log is captured, pick a strategy (a2 = LRU mesh + zero indirect entry; b = lazy `kIsland` chunk load; c = trim route table; d = compress/share; e = close with justification) and implement. Remove the `[DEBUG-resmem]` block when done |
| 68 | [File/Architecture_FileManagerSplitDecision.md](File/Architecture_FileManagerSplitDecision.md) | Architectural | 4 | 3 | 3 | 4 | **Decision plan (present options).** `FileManager` fuses two disjoint-consumer modules (general file I/O + versioned/atomic writes vs the packed-asset chunk system), and the lazy-chunk state machine is co-owned by four files via `GetLazyChunk()`'s mutable-ref leak under a documented-not-asserted thread contract (`FileManager.cpp:618-627`). Options: A — split the class (~22-TU churn); B — narrow the chunk seam with intent-named APIs (likely right size); C — accept + document. Resolve via grill; must not block the other File plans (land them first) |
| 69 | [Network/Architecture_WireFormatPairing.md](Network/Architecture_WireFormatPairing.md) | Large | 4 | 3 | 3 | 4 | Every engine message layout is hand-mirrored across send/receive (17 packet types; magic header sizes in ×2-3 copies incl. `NetworkSimulation.h`'s offset-4 tick parse; `SendSimplePacket` duplicated verbatim in `Client.h`/`Server.h`) — the directory's principal latent-desync hazard, lockstep by convention only. Colocate per-message write/read pairs (byte-identical, converted message-by-message with interop smoke tests), dedupe the send template, retire the magic offset. No wire change; land after the small Network plans (it moves every line they cite) |
### Reference / Index Documents

Meta/overview docs that are never executed as plans — excluded from the priority walk and orphan scans.

| Document | Purpose |
|----------|---------|
| [Graphics/ShaderReview/00_Overview.md](Graphics/ShaderReview/00_Overview.md) | Shader-review effort overview and cross-cutting-findings index; the per-subsystem plans it references were either executed-and-deleted or never authored |

## Dependencies

Plans that must be executed in order due to shared files or stale line numbers:

- **Common determinism-bytes session has LANDED and been removed** — `Common/Crc.md` (hash-core de-dup behind `kCrcSeed`/`kCrcMultiplier`, `CrcConsteval` forwards to `Crc`, `unsigned char` byte fold, generic-template XMVECTOR `requires` lock), `Common/DataFile.md` (`AlignOutputStream` modulo fix + `ASSERT`, `ChunkLocation::crc` init, `SceneHeader` explicit `uiPad2[4]`, per-struct `sizeof`/`offsetof` layout `static_assert`s, `is_trivially_copyable_v<ChunkHeader>`), and the `Common/Determinism_IndeterminateBytes.md` theme (R2 "equal values → equal bytes" contract comments on the `Crc.h`/`Serialization.h` generic byte paths; `Serialization.h` got the sibling XMVECTOR `requires` lock) all landed in one session. **No baked-CRC/replay regen and no `kiVersion` bump** — all persisted `.pack`/`.manifest`/generated-`.h` CRCs are ASCII asset-path hashes (unchanged by the fold); only the runtime client/server desync CRC + locally-recorded replays reset (both self-consistent after rebuild). The "Light" union scope (no structural `ChunkHeader` canonical-zero member) was chosen since the writer already zero-fills via `vector::resize`. The follow-up layout-lock coverage on the remaining persisted structs (animation stream, `MaterialShaderData`, `AudioHeader`) has also LANDED and been removed — those `sizeof`/`offsetof` `static_assert`s are now in `DataFile.h` alongside the rest.
- **Common log/diagnostics-safety session:** the per-file `Common/StackWalker.md` plan **has landed and been removed**; `Common/ThreadSafety_SharedStaticBuffers.md` (theme) still owns the shared-static-buffer race that plan excluded — its Log `static thread_local` change delivers StackWalker's fault-path fix for free. No co-scheduling constraint remains.
- `Network/Architecture_LayerViolations.md` **Plan A has landed and been removed** (the six one-way debug save/load/reset/replay/pause packets are now game-layer `GamePacketType` handled in `game::ServerSession::ParseReceivedGamePackets`). **Plan B has landed and been removed** — the timespeed flow (`kClientTimespeedRequest` + `kServerTimespeedUpdate`) is now reclassed to `GamePacketType`; handlers and broadcast live in `game::ClientSession::PollNetwork` and `game::ServerSession::{ParseReceivedGamePackets, BroadcastTimespeedIfChanged, SendTimespeedToNewClient}`; engine `GameBase::ServerUpdate` and `Server::ClientHello` invoke the game-session helpers. CoordFrames / Frame factory / Frame version / BufferFullFrame items were dropped per the user-confirmed layer-violation policy (engine reading game globals to access engine-base members is by design, not a violation).
- `Engine/Architecture_GameSaveLoadRelocation.md` **has landed and been removed** — `GameSaveLoad` relocated to the game layer (`game::GameSaveLoad` in `Projects/BrokenEngineSandbox/Source/Save/`, owned by `game::Game`); engine `GameBase::ServerUpdate` now drives it via `game::gpGame->mGameSaveLoad.X()`, and the `friend` + engine-side member were removed.
- Per-frame multi-island system (5 phases) has LANDED. Phase 5 (LRU eviction, dynamic subscription-driven loading, neutral slot-0 placeholder) executed in this session — no remaining follow-ups. See `Engine/Source/Frame/CLAUDE.md` for the current architecture description.
- `Graphics/IslandNavContourResidency.md` depends on `Graphics/IslandResidentMemoryScalingStrategy.md`. The NavContour residency lifecycle should mirror whatever strategy the predecessor picks (lazy chunk-load / LRU / cap / close). Do not pick a NavContour strategy before the predecessor lands. Both plans are gated on captured `[DEBUG-resmem]` boot-log data from a Debug client run.
- `Graphics/WindowedLightingShadowDispatch.md` shares the `kIndirectHostVisible | kCompute` `PipelineCreator.cpp` branch and the `WriteIndirectComputeBuffer` / `RecordComputeIndirect` helpers with `Graphics/WaterDisplacementIndirectCompute.md` (Item 1) — land one first or co-implement the helper, then the second only wires its pipelines. It also builds on two prerequisites that **landed this session** (no plan): the `CLAMP_TO_BORDER` edge samplers (`mVkSamplerBorderWhite` shadow / transparent-black `mVkSamplerBorder` lighting, wired into the Terrain/Water/WaterSkyboxOne consumer bindings) and the steady-state-target texel ramp (`Camera.cpp` reference-height clamp removal). Client/graphics-only — no CRC/determinism/network risk.
- The three deferred Shadow Temporal-Accumulation siblings (`Graphics/InlineAreaMappingDryToHelper.md`, `Graphics/WindHistoryResetOnRecreate.md`, `Graphics/DisabledPassGatingPerfAudit.md`) are order-independent of each other. `Graphics/LightingDepositSpreadFullPort.md` (which reworked the same lighting/area machinery) **has landed and been removed** — it already fixed the lighting `VisibleAreaToWorld` site (`LightingSpread.frag:74`) and left the eight *non-lighting* DRY sites untouched for `InlineAreaMappingDryToHelper.md`, so that plan can proceed on its eight sites + read-only `ShaderFunctions.h` without collision. `WindHistoryResetOnRecreate.md` and `DisabledPassGatingPerfAudit.md` both touch the wind/smoke render path (`WindUniforms.cpp` / `SmokeUniforms.cpp` / `RenderTargetTextures.cpp` / `CommandBufferRecordGlobal.cpp`); land them in one wind/smoke-render session or sequence to avoid stale line numbers. None of the three carry CRC/determinism/network risk (all client/graphics-only).

- `Engine/SingletonPublishGlobalGuardSweep.md` is order-independent of the plans it shares files with — it only inserts a ctor `ASSERT(gpFoo == nullptr)` + an `if (gpFoo == this)` dtor wrap at each publish site, touching no logic those plans edit — but shares files with them, so co-schedule or land it first to avoid stale line numbers: `Frame/ScaleEngineToMeters.md` (`Camera.cpp` ctor/dtor `:38/:45`). (`Common/DiagnosticLog.md` **has landed and been removed** — the `kDiagnosticLogCount` slot guard is in; the sweep's per-slot publish `ASSERT` now lands on the post-plan `DiagnosticLog.cpp`.) The sweep is line-stable (two inserted lines per site) so it is safe to land first. `Engine/CrashReportDxDiagStringRace.md` also touches `Main.cpp` only if its fix shape (a)/(c) lands (the `readDxDiag` future at `Main.cpp:91/:96`) — disjoint from the sweep's `gpInput` publish (`Main.cpp:197`); line-refresh only, no hard ordering constraint.

- `Frame/IslandTerrainSplit.md` should land **before (or in the same session as)** `Graphics/IslandResidentMemoryScalingStrategy.md`. The split relocates `EvictionSweep`/`RestorationSweep`/`CreateClientMeshBuffers` from `IslandTerrain.cpp` into the new client-only `IslandTerrainResidency.cpp`; the memory-scaling plan then edits those exact functions (LRU mesh-buffer eviction + removing the `[DEBUG-resmem]` block). If the split lands first, the scaling plan must re-target `IslandTerrainResidency.cpp`. The other three plans sharing `IslandTerrain.cpp` (`ScaleEngineToMeters.md`, `IslandNavContourResidency.md`, `SingletonPublishGlobalGuardSweep.md`) edit functions the split leaves in `IslandTerrain.cpp` (or, for `ScaleEngineToMeters`'s `:818`, a re-targetable one-line multiply removal) → line-refresh only, no hard ordering constraint. The split is move-only (no `kiVersion`/`kiNavDataVersion`/CRC/`.pack`-layout change), so it carries no determinism risk; it is client-only build wiring (new file in the client vcxproj/filters only).

### CLAUDE.md-refresh follow-up plans (this run)

- The **decision plans** (`Common/AggregationAndScopeRuleQualifiers.md`, `DataPacker/DataPackerLogFloatSpecException.md` (**LANDED and removed** — Option A: LOG float-spec ban scoped to allocation-tracked builds in root CLAUDE.md, repo-code-review §2b (integer specs same scope), external-refactor-clean §4, plus a DataPacker/Source/CLAUDE.md exemption note; no code change), `Frame/NavigationStraightLineSteeringRule.md` (**LANDED and removed** — user chose to delete the absolute Navigation rule from the Collections hub entirely (bad absolute: Players straight-line fallback, Players roam `ComputeAiSteering`, Spaceships reactive steering, and Missiles homing all contradict it); doc-only, no code change), `Graphics/ParentRuleFramingReconciliation.md` (**LANDED and removed** — D1 two-family deposit wording, D2 ImGuiManager carve-out in Engine/Source/CLAUDE.md, D3 resolved as MemoryManager→GlobalAllocator file rename, D4 closed/already-done), `Common/PlanDocGovernanceDecisions.md` (**dropped as invalid** — its "0 of 31 plans have `## Out of scope`" premise was false: 42 of 45 have it; the Plans/Features canonical+deltas framing is already in place), and the second-pass `Common/DocCanonicalHomeReconciliation.md` + `Graphics/NvidiaPipelineHangAttributionReconcile.md` (**LANDED and removed**)) are doc-rule resolutions tagged "Decision plan (present options)" — resolve each via `/external-grill-plan` before any edit. They are order-independent of each other. Several touch the **root / `Common` / `Engine/Source` CLAUDE.md**, whose edits land under the normal gated doc process (the root-CLAUDE.md change-ban applies). `AggregationAndScopeRuleQualifiers` (ExternalHeaders/Engine.h/BT_CLIENT) and `ParentRuleFramingReconciliation` (deposit-shader/layer-violation/singleton framing) both touch root + `Engine/Source` CLAUDE.md wording — co-schedule or sequence so they don't both rewrite the same parent bullets in conflicting ways (constraint moot now `ParentRuleFramingReconciliation` has landed; `AggregationAndScopeRuleQualifiers` executes against the post-landing parent wording). `DocCanonicalHomeReconciliation`'s DataPacker-bake item (Decision 1) overlaps `StaleDocClaimsSweep.md` item 9 (the bake/split delegation loop-break) — co-schedule or sequence so the parent `DataPacker/Source/CLAUDE.md` bake paragraphs aren't double-edited; its Common.h-aggregation item (Decision 4) is the canonical-side of `StaleDocClaimsSweep` item 10's root-doc note (records the choice, edits only the `Common/CLAUDE.md` side).
- `Common/StaleDocClaimsSweep.md` reconciles `Graphics/ShaderReview/00_Overview.md` (a Reference/Index doc) and confirms the Order.md LANDED-entry convention — it reads/lightly-touches `Order.md` and the ShaderReview overview, so co-schedule with any other plan editing those if one is queued (none currently). Its `FrameUpdatePipeline.md` 64→32 Hz fix routes through `/update-architecture-diagrams`, not a hand-edit.
- `Common/StaleCodeCommentsSweep.md` (source comments) and `Common/StaleDocClaimsSweep.md` (doc files) are deliberately split by target file-type; they touch different files and are order-independent, but both are comment/doc-only (Risks 0) and can land together.
- **Second-pass audit plans (this run)** — all order-independent of the rest of the queue and of each other; each is investigate-then-decide (verify the cited root cause before editing, per Diagnosis Discipline):
	- `Graphics/PipelineGraphicsGlobalSet0Indexing.md` (Set-0 framebuffer-index discrepancy) and `Graphics/ParticlesSpawnStructCopyGlslangBug.md` (whole-struct SSBO copy) are graphics/shader investigations — could resolve doc-only or as a small code/shader fix; client/graphics-only, no determinism/CRC exposure. The latter needs a DataPacker recompile + visual smoke-test only if a fix is taken.
	- `Network/StaticDataEpochCheckOnSubscribingSlot.md` (client receive epoch-check on epochless `kSubscribing` slot) is client-receive only — no shared-CRC/determinism exposure; sibling of the `ServerUnsubscribeHandshakeGate` "one handler diverges from siblings' gate" pattern (that plan was **dropped as invalid** — the unsubscribe exception is documented in `Server/CLAUDE.md` and behaviorally unreachable pre-handshake). `Network/BroadcastTickAllocationGuardScope.md` (blanket suppression nullifies the documented armed tracker) is server-only debug-tracking — narrow-the-guard vs fix-the-doc.
	- `Ui/ChineseFontPushMissingScreens.md` (missing CJK font push on Pause/Sound screens) is client-only UI; coordinate `PauseMenuScreen.cpp` edits with `Ui/MenuUtilsAppendUtf8DanglingPointer.md` if co-scheduled (both touch that file), but they edit different concerns (font-push scoping vs the `AppendUtf8` return-lifetime) — no hard ordering constraint.
	- `Graphics/NvidiaPipelineHangAttributionReconcile.md` (**LANDED and removed** — determination: `inverse()` is the sole confirmed trigger (NVIDIA-staff-confirmed forum bug; both the inverse() removal and the joint split landed in one fix commit `fd772b99`, and the engine dynamically indexes the large `mat4 jointMatrices[]` SSBO hang-free), so the large-`mat4[]` attribution was a misdiagnosis; comments reworded as a layout choice across 6 sites + the determination recorded in `Shaders/CLAUDE.md` Known Issues) and `Common/DocCanonicalHomeReconciliation.md` (6 cross-doc canonical-home decisions) are doc-reconciliations — see the decision-plans bullet above for `DocCanonicalHomeReconciliation`'s `StaleDocClaimsSweep` overlaps. `NvidiaPipelineHangAttributionReconcile` edits one source comment (`ModelCommon.h:46`) plus two CLAUDE.md sites; the source-comment edit is gated on its investigation outcome so it lives there, not in `StaleCodeCommentsSweep`.
- `Frame/MissilesDeadExplosionRadiusField.md` is the only new plan with **CRC/`kiVersion` exposure** — it bumps `MissilesPostRender::kiVersion`→`Frame::kiVersion` (stale saves/replays reject). No other queued plan touches the Missiles collection, so no co-scheduling constraint; rebuild client+server together. Execute via the `add-collection-member` checklist (reverse).
- `Engine/VcxprojAndBuildConfigReconciliation.md`, `Frame/IslandTerrainSplit.md`, and `Frame/NavBuildSplit.md` all edit `.vcxproj`/`.filters` files, but **different entries** (the reconciliation fixes include-paths / filter elements / orphan registration; the splits *add new file* entries). No hard ordering constraint — just avoid stomping each other's edits if co-scheduled in one session.
- **Audio deep-analysis plans (this run)** — `Audio/Architecture_AudioHelperPlacement.md` should ideally have landed **before (or with)** `Audio/Architecture_IncludeHygiene.md` (**landed and removed** this session, standalone — when `AudioHelperPlacement` lands it must refresh the direct includes the hygiene plan added): the relocation moves `VolumeToPower`/`DestroyXAudio2SourceVoice` into the new `AudioUtility.h`, changing which direct includes the hygiene plan adds. `Audio/Architecture_ThreadContractDocs.md` (**LANDED and removed**) was the land-last comment-only plan of any co-scheduled Audio session and deliberately did not touch the `Audio/CLAUDE.md` replay-invariant claim — `Audio/Architecture_OneShotPathCleanup.md` restores that claim in code. All seven Audio plans are client-only with no CRC/determinism/network exposure.
- `Debug/Architecture_LibraryReplacement.md` touches `Engine.h` (the Debug include inside the `BT_CLIENT` span) and the client vcxproj/`.filters` (removes the `EnumToString.h` entries if the delete-the-file variant is chosen) — soft overlap with the Audio session (`Audio/Architecture_AudioHelperPlacement.md` adds its own `Engine.h` include + vcxproj entry) and `Engine/VcxprojAndBuildConfigReconciliation.md` (different vcxproj entries). No hard ordering constraint — line-refresh only if co-scheduled.
- **File deep-analysis plans (this run)** — land the remaining code plans as one File session. `File/Architecture_AnimationDataLoadPlacement.md` **has landed and been removed** — it relocated the scene-chunk animation-parse block out of `LoadPackFiles` into `Graphics/AnimationData.cpp` (`LoadAnimationDataFromEagerChunks`, called from the `Graphics` ctor), shrinking `LoadPackFiles` ~23 lines and removing its `BT_CLIENT` nest. Its downstream evaporations are now resolved: `File/Refactor_FunctionDecomposition.md`'s `LoadPackFiles` citations are refreshed against the post-landing source, and `File/Architecture_IncludeHygiene.md`'s `AnimationData.h` include item is dropped (the `gAnimationDataMap`/`AnimationData` references it targeted are gone from `FileManager.cpp`). `File/Architecture_IncludeHygiene.md` (**landed and removed** this session) cleared the way for `File/Architecture_VersionedHeaderDedup.md` (the shared helpers now rely on `DifferenceStream.h`'s direct `FileManager.h` include — added this session — rather than `Engine.h` ordering). `File/Architecture_FileManagerEncapsulation.md` + `File/Architecture_LoadThreadLifecycleSafety.md` co-schedule (both edit `GetEagerChunkMap`/`mLoadingFuture`). `File/Architecture_FileManagerSplitDecision.md` (decision) resolves **last** and must not block the others. `File/CorruptPackDataGracefulFailure.md` (follow-up from this session's `DataTypeFromFlags` graceful-failure landing — rewrites the `LoadPackFiles` manifest/`CreateFileW` and `LoadChunk` zlib/sub-read-loop crash-and-hang paths) is order-independent of the other File plans but shares `LoadPackFiles`/`LoadChunk` with `Refactor_FunctionDecomposition.md` (which extracts the guarded sub-read loop) — co-schedule or refresh lines; one grill decision (startup-missing-asset policy). `Engine/SingletonPublishGlobalGuardSweep.md` and `Common/StaleCodeCommentsSweep.md` also touch `FileManager.cpp`/`FileManager.h` (publish-guard lines and the "pre-faulted" comments respectively) — disjoint edits, line-refresh only. No CRC/determinism/network/`kiVersion` exposure anywhere in the File set.
- **Frame deep-analysis plans (this run)** — `Frame/NavBuildSplit.md` should land **before (or with)** the four new plans editing functions it relocates or re-linkages: `Frame/Architecture_NavPredicateDedup.md` (extends the split's `NavBuildInternal.h`/`SegmentsIntersect` external-linkage promotion to also serve `NavQuery.cpp`), `Frame/Refactor_NavFunctionDecomposition.md` (its `BuildCellNavData` crossing-check extraction targets post-split `NavCellData.cpp`), `Frame/Refactor_DiagnosticAndDeadCodeRemoval.md` (deletes the dead bounds scan inside `BuildCellNavData`), and `Frame/Architecture_DeterminismGuards.md` (assert inside `ChainEdgesIntoPolygons` — stays in `NavBuild.cpp`; line refresh only). `Frame/IslandTerrainSplit.md` likewise relocates `AcquireTextureSlot` into `IslandTerrainResidency.cpp` — land it before `Refactor_NavFunctionDecomposition.md`'s first-mint extraction. `Frame/Architecture_CrcMixingStrength.md` edits `FrameBase.cpp` code adjacent to `Frame/Architecture_LogDifferencesServerCollections.md` (**has landed and been removed** — LogDifferences now folds `ServerCollections()` via `LogDifferencesCollections` in `FrameUtils.h`; refresh `CrcMixingStrength` line cites against post-landing source). `Frame/Refactor_AStarEndVisibleLazy.md` + `Refactor_NavFunctionDecomposition.md` both restructure `AStarPath` — co-schedule. `Frame/Refactor_StyleMechanics.md` items fold into whichever session owns each file; its `IslandChainPlacement.h` class→free-function item co-schedules with `Common/StaleCodeCommentsSweep.md` item 5 (log-spiral comment, same header). Only `Architecture_CrcMixingStrength.md` carries CRC exposure (values change, accepted class); everything else is behavior-identical, debug-only, doc-only, or includes-only.
- **Graphics top-level deep-analysis plans (this run)** — all eight are client-only with no CRC/determinism/network/`kiVersion` exposure. Ordering: `Graphics/Architecture_SharedConstantDuplication.md`'s eye-height item shares the 150.0f literals (`CameraBase.cpp:128`, game `Camera.cpp:18`) with `Frame/ScaleEngineToMeters.md`'s catalog — co-schedule or land after it so the single-sourced constant carries the post-rescale value. `Graphics/Architecture_InvariantHardening.md`'s `OneShotCommandBuffer` thread assert must encode non-concurrency unless `Graphics/Architecture_ScreenshotFenceDeadlock.md`'s fix shape (c) (main-thread capture) lands first — land the assert with or after the deadlock fix, or use the in-use-flag form. `Graphics/Refactor_ApiAndHotPathCleanups.md` and `Architecture_InvariantHardening.md` both edit `OneShotCommandBuffer.{h,cpp}` (disjoint edits — param removal vs assert) — co-schedule or refresh lines. The remaining overlaps are the shared `Graphics.cpp`/`CameraBase.{h,cpp}` edits — see File Groups.
- **Input deep-analysis plans (this run)** — all five are client-only display-rate input with no CRC/determinism/network/`kiVersion` exposure. `Input/Architecture_LibraryReplacement.md` (decision) **has landed and been removed** — Option A chosen (keep in-house; the rejection is recorded in `Engine/Source/Input/CLAUDE.md` and `ThirdParty/CLAUDE.md`'s DirectXTK entry), so the four Input code plans are unblocked unchanged, including `Input/Refactor_RawInputHandling.md`'s `HandleRawInput` item and `Engine/DeadCodeAndUnusedIncludesSweep.md`'s mouse-registration item. The four code plans co-schedule as one Input session — see File Groups. Two related findings were deduped into existing sweeps rather than re-filed: the vestigial mouse `RIDEV_INPUTSINK` registration (`Engine/DeadCodeAndUnusedIncludesSweep.md`) and the incorrect `RawInputManager.cpp:42` legacy-mouse comment (`Common/StaleCodeCommentsSweep.md`). **Memory (same run)**: `Memory/Architecture_AllocationTrackingFlagAndDocs.md` **has landed and been removed** (allocator TU + `ExternalHeaders.h` + `Memory/CLAUDE.md` edits are in; it was order-independent of the Input set); the Memory-side include cleanup is `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 — `ProfileManagerBase.cpp` must gain its direct `GlobalAllocator.h` include *before* the `CollectionMemory.h:3` channel include is dropped.

- **Ui deep-analysis plans (this run)** — all seven are client-presentation only with no CRC/determinism/network/`kiVersion` exposure (the one sim-feeding wrapper, `gBaseHeight`, was deduped into the pre-existing `Frame/Architecture_BaseHeightWrapperDeterminism.md` rather than re-filed). The Water plans share `WaterWrappersBase.{h,cpp}` — land as one session (see File Groups): `Ui/Architecture_WaterWrapperInvariants.md` (**landed and removed** this session — stale `kiWaterNormalSeaWavesIndex` comment + dead constant deleted, `.h`↔`.cpp` declaration order reconciled to the source-of-truth render order), and the remaining `Ui/Refactor_StyleMechanics.md`, `Ui/Refactor_WaterZOffsetTempDecision.md`. `Ui/Architecture_CurveDataDeadChangeDetection.md` + `Ui/Refactor_StyleMechanics.md` both edit `CurveData.h` (different lines — co-schedule). `Ui/Architecture_WrapperContractDocs.md` is comment-only and cites `WrapperBase.h` lines that `Frame/Architecture_BaseHeightWrapperDeterminism.md` / `Engine/WrapperGetIndexFailLoud.md` edit — land it last of any co-scheduled Wrapper session; its threading comment should cite the `kfBaseHeight` constant if the BaseHeight conversion has already landed. `Ui/Architecture_LibraryReplacement.md` (ImPlot rebuild) supersedes any `CurveWidget` decomposition (deliberately not filed) and is independent of the CurveData deletions (different symbols). Both `Ui/Refactor_WaterZOffsetTempDecision.md` options require a DataPacker shader recompile (the `GlobalLayout` member `fWaterZOffsetTemp` is renamed or removed).

- **Profile deep-analysis plans (this run)** — all seven are profiling/display only with no CRC/determinism/network/`kiVersion` exposure; all touch `ProfileManagerBase.{h,cpp}`/`ProfileScreens.cpp`, so land as one Profile session (see File Groups). Ordering within the session: `Profile/Architecture_IncludeHygiene.md` **before (or with)** `Profile/Architecture_ClientServerGuardScope.md` (the hoisted `giAllocationsThisFrame` latch should sit on the new direct `Memory/GlobalAllocator.h` include, not the PCH accident); (`Profile/Architecture_SmoothNowDoubleLatch.md` **has landed and been removed** — the per-timer `bSmoothAtStop` latch fix is in; `Profile/Refactor_InFunctionCleanups.md`'s flags conversion now lands on the post-fix `CpuStop`); `Profile/Refactor_ProfileRowFlags.md` (**has landed and been removed** — the three profile-row structs' `bVisible`/`bSmoothAtStop` bools are now one shared `common::Flags<ProfileRowFlags>` enum; readers/writers in `CpuStop`/`SmoothCpuTimers`/`FormatCpuTimersText`/`FormatCpuCountersText`/`FormatGpuScreen` rewritten; co-scheduled `InFunctionCleanups` + `SmoothNowDoubleLatch` already landed); `Profile/Architecture_DocAccuracy.md` lands **last** (comment/doc-only; cites lines every code plan moves). Cross-queue overlaps: `Profile/Architecture_IncludeHygiene.md`'s direct-include item satisfies the include-first prerequisite of `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 — whichever lands first adds the `ProfileManagerBase.cpp` include, the other skips it (the `CollectionMemory.h:3` channel drop stays with the sweep, and the sweep's `ProfileScreens.cpp` grep hit is superseded by the hygiene plan's move). `Common/StaleDocClaimsSweep.md` item 6 rewords the same Profile CLAUDE.md timer-position sentence that `Profile/Architecture_InvariantHardening.md`'s `static_assert`s make compiler-enforced — whichever lands second keeps doc and assert telling the same story. `Engine/SingletonPublishGlobalGuardSweep.md` touches the game-side `ProfileManager.cpp` ctor/dtor publish sites — different file, no conflict.

- **Network engine-transport deep-analysis plans (this run)** — `Network/Architecture_SpecDocReconciliation.md` is doc-only but cites send/receive line numbers every code plan moves — land it first or refresh its citations. `Network/Architecture_ClientBuildServerTransportGate.md` **has landed and been removed** — the five engine Server-transport files (`Server.{h,cpp}`, `ServerTypes.h`, `ServerReceive.cpp`, `ServerSend.cpp`) are now whole-file `BT_SERVER`-wrapped and excluded from the client vcxproj/.filters (only the server build constructs `engine::Server`); `Network/Architecture_IncludeHygiene.md` should refresh its citations against the post-wrap server files. `Network/Refactor_ClientStateFlags.md` co-schedules with `Network/Architecture_ClientSessionBoundary.md` (both rework the `Client.h` member/accessor surface). `Network/Architecture_WireFormatPairing.md` (Large) lands **last** of the set — it restructures every send/receive site the others cite. Protocol exposure is confined to `Architecture_SubscribeRejectSlotNegotiation.md` (only under its option (a): hello byte + `kuiProtocolVersion` bump) and `Architecture_WireFormatPairing.md` (byte-identical conversions, interop-verified per message family); nothing in the set touches shared-CRC sim state, replays, or `kiVersion`.

- **Graphics/Debug deep-analysis plans (this run)** — both Quick Wins, client-only, no CRC/determinism/network/`kiVersion` exposure, order-independent of each other. `Graphics/Architecture_DebugRenderDeadPrimitives.md` is a remove-vs-keep decision gated by `/external-grill-plan` (root never-remove-features rule); it shares `CommandBufferRecordMain.cpp` with the indirect-compute pair — see File Groups (its `PipelineManager.{h,cpp}` / `BufferManager.{h,cpp}` / `DebugRender.{h,cpp}` edits overlap no other live plan). `Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md` is comment-only after verification proved the empty-set transition unreachable (evidence recorded in the plan); the defensive-flush alternative stays pre-staged for the grill. The three doc nits from this run were appended to `Common/StaleDocClaimsSweep.md` (items 15-17) rather than filed as a separate plan.

- **Graphics/Render deep-analysis plans (this run)** — all six are client-render-only with no CRC/determinism/network/`kiVersion` exposure. Ordering: `Graphics/Refactor_RenderFunctionDecomposition.md` lands **last** of the set (it relocates the exact `GlobalUniforms.cpp`/`MainUniforms.cpp` blocks that `Architecture_RenderRegionOwnership.md`, `Architecture_RenderSharedConstants.md`, and `Refactor_RenderMicroCleanups.md` edit). `Architecture_RenderRegionOwnership.md`'s `PopulateLightingParameters` relocation coordinates with `Architecture_RenderIncludeHygiene.md` (**landed and removed** this session) — the moved function relies on wrapper includes the hygiene plan already edited in both TUs; refresh against post-landing source. `Architecture_RenderOrderingContractDocs.md` (**landed and removed** this session) was doc-only and order-independent; its `:22` day-cycle nuance is resolved by RegionOwnership's dead-write deletion (whichever lands second checks the other's outcome). The set shares `WindUniforms.cpp`/`SmokeUniforms.cpp`/`Render.h` with the pre-existing wind/smoke render-path pair (`Graphics/WindHistoryResetOnRecreate.md`, `Graphics/DisabledPassGatingPerfAudit.md`) — see File Groups.
- **Graphics/Managers deep-analysis plans (this run)** — all fourteen are client-only with no CRC/determinism/network/`kiVersion` exposure. Ordering: `Graphics/Managers/Refactor_TextureDecomposition.md`'s `TextureDescriptors` items and `Graphics/Managers/Architecture_ThreadAndLifetimeGuards.md`'s `CrcToIndex` decision land **before (or fold into)** `Graphics/Managers/Architecture_BindlessSlotLifecycle.md` (same `TextureDescriptors.{h,cpp}` surface; the Large consolidation reshapes it — do not run concurrently, and execute the consolidation alone). `Architecture_IncludeHygiene.md` + `Architecture_LayerSeams.md` co-schedule (both edit `TextureManager.cpp`/`RenderTargetTextures.cpp`; the LayerSeams `CameraBase` move is what makes the two `Game.h` removals durable). The Set-0 binding-constant item in `Architecture_ShaderCpuContractConstants.md` is **distinct from** the pre-existing `Graphics/PipelineGraphicsGlobalSet0Indexing.md` (binding *numbers* vs per-framebuffer *indexing*) — no ordering constraint, don't merge. Its push-constant and shader-side Set-0 items require a DataPacker shader recompile. `Architecture_LayerSeams.md`'s callback mechanism replaces the original GameBase-virtual proposal (verified: `gpGame` is null at boot-time `PipelineManager` construction). Items deduped into existing plans rather than re-filed: TextureCache payload validation + LOG wording (`Graphics/TextureCacheValidatePayloadSize.md`), `TextureUploadManager.cpp` `GlobalAllocator.h` include (`Engine/DeadCodeAndUnusedIncludesSweep.md` item 1), ImGuiManager screen ownership (`Graphics/ParentRuleFramingReconciliation.md` Decision 2 — **landed**, sanctioned-exception carve-out in `Engine/Source/CLAUDE.md`), ParticleManager doc wording (`Common/StaleDocClaimsSweep.md` item 5, extended).
- **Graphics/Objects deep-analysis plans (this run)** — all ten client-only with no CRC/determinism/network/`kiVersion` exposure (`Architecture_ObjectsShaderCpuConsistency.md`'s optional `f4Material` layout shrink needs a DataPacker shader recompile only). Ordering: `Graphics/Architecture_ObjectsDeadRecreateApis.md` **has landed and been removed** — it deleted the `Recreate()`/`ReCreate()` dead APIs + the snapshot members (`mInfoTemplate`, `mbAddModelDescriptors`, `mbIsShadow`), so `Graphics/Refactor_ModelPipelineMembers.md`'s Flags conversion now covers only the two surviving bools, and the lifecycle `Graphics/Architecture_PipelineRegistrationOwnership.md` makes symmetric is now documented (Objects/CLAUDE.md) with no live violators. The four PipelineCreator/PipelineDescriptorWriter plans (`Architecture_DescriptorSetIndexDedup.md`, `Refactor_WriteDescriptorDecomposition.md`, `Refactor_PipelineCreatorDecomposition.md`, `Refactor_ObjectsQuickWinMechanics.md`) land as one session, with `PipelineCreatorDecomposition`'s alias-parameter drop landing with or after `WriteDescriptorDecomposition`; `RegistrationOwnership` restructures the same `Pipeline::Create`/`Write` paths — land it before or after that session, not interleaved; `ShaderCpuConsistency`'s bindings-15/16 item edits the `kModel` append block `RegistrationOwnership` relocates (land it first as the smaller diff, or refresh). Cross-queue: `RegistrationOwnership` reworks the `TextureDescriptors` registration surface that `Graphics/Managers/Architecture_BindlessSlotLifecycle.md` (Large) consolidates — sequence the two, never concurrent (whichever lands second re-targets); `ShaderCpuConsistency` and `Graphics/Managers/Architecture_ShaderCpuContractConstants.md` both edit push-constant fields in `ShaderLayoutsBase.h` — co-schedule their DataPacker recompiles; `Graphics/WaterDisplacementIndirectCompute.md` adds a `CreateComputePipeline` branch (`PipelineCreator.cpp:621-624`) — sequence with `PipelineCreatorDecomposition`; `Graphics/PipelineGraphicsGlobalSet0Indexing.md` edits `Pipeline.cpp`'s bind functions (disjoint lines — refresh only).

- **Frame/Collections deep-analysis plans (this run)** — the six content plans (`Frame/Collections/Architecture_AllocateAndAssignReuseGuard.md`, `Architecture_DeserializedCountValidation.md`, `Architecture_SharedMembersParityGuard.md` — **has landed and been removed** (`kbServerMembersParity` static_assert + `IsMemberTupleSubset` ASSERT in `Collection.h`; residual non-shared-leaf gap spun out to `Architecture_NonSharedLeafClientFieldGuard.md`), `Architecture_TypeRegistryGuards.md` (**landed and removed** this session), `Architecture_CollectionCrcMixing.md`, `Refactor_AllocationHelperDedup.md`) land **before** `Frame/Collections/Architecture_HeaderCohesionAndSplit.md` — the split relocates the lines/symbols they cite. `Engine/DeadCodeAndUnusedIncludesSweep.md` items 10/12/13/14 (the same `Collection.h` front matter and the `CollectionMemory.h:3` include channel) also land before or with the split so the new ID header is not created with the dead lines. `Architecture_CollectionCrcMixing.md` lands **with or after** `Frame/Architecture_CrcMixingStrength.md` (same ordered-fold convention; CRC values change once, replays re-record once). `Architecture_AllocateAndAssignReuseGuard.md` + `Architecture_DeserializedCountValidation.md` edit the same read helpers (`AllocateAndRead`/`SharedCollectionRead`/`Collection::Read`) — co-schedule. CRC exposure is confined to `Architecture_CollectionCrcMixing.md` (values change, accepted class); the reuse-guard and count-validation plans change behavior only for mismatched/malformed input; everything else is compile-time guards, asserts, comments, includes, or behavior-identical reorganization.

- **DataPacker root deep-analysis plans (this run)** — all five are offline-tool only: no runtime CRC/determinism/network/`kiVersion` exposure (the staleness check only forces re-exports; the CRC-header CRLF→LF item causes a one-time generated-`.h` regen per type). Ordering: `DataPacker/Architecture_RdoSweepRelocation.md` lands **last** of the set — it moves `Main.cpp:67-315` (including `LoadBc7AsFloatPixelsMip0`, whose hardening item lives in `DataPacker/Architecture_OutputWriteTrustBoundary.md`) into the new `ExportJobs/Texture/RdoSweep.{h,cpp}`, shifting every later `Main.cpp` citation. `DataPacker/Architecture_AttributionPathOwnership.md` and `DataPacker/Refactor_MainQuickWinMechanics.md` both edit `FileManager.cpp` (ctor member additions vs the `:46` LOG self-reference) — co-schedule or refresh lines. Cross-queue: `Common/StaleCodeCommentsSweep.md` edits the stale "current production knobs" comment at `Main.cpp:305`, which the relocation moves into `RdoSweep.cpp`, and the pre-existing `DataPacker/DataPackerLogFloatSpecException.md` decision (**LANDED and removed** — Option A, doc-only; the RDO-sweep LOGs stay as-is, no citation impact) read the same RDO-sweep LOG lines — land `StaleCodeCommentsSweep` before the relocation or rely on `/next-plan`'s citation refresh.

- **DataPacker ExportJobs deep-analysis plans (this run)** — all sixteen are offline-tool only: no runtime CRC/determinism/network exposure. The only output-byte changes are `DataPacker/Architecture_LibraryReplacement.md` (vertex order → `ExportScene`/`ExportModel` version bumps, one full scene re-export) and `DataPacker/Architecture_PackByteDeterminism.md`'s option-(A) variants (one-time re-export diff). Ordering: the pre-existing `DataPacker/ExportTextureHeaderlessRawFormatClaims.md` lands **before (or with)** `DataPacker/Refactor_ExportTextureMechanics.md` (the single-pass sniffing rework builds on the reconciled extension set). `DataPacker/Refactor_SceneFunctionDecomposition.md`'s `LoadVertices` split lands **before (or with)** `DataPacker/Architecture_LibraryReplacement.md` (the meshopt remap restructures the same dedup block). `DataPacker/Architecture_IslandConstantsOwnership.md` lands **before (or with)** `DataPacker/Refactor_IslandFunctionDecomposition.md` (the constants move shifts the bake-TU lines the decomposition cites). `DataPacker/Architecture_PayloadLayoutSingleSource.md` and `DataPacker/Refactor_TextureUtilDedup.md` should share one home for the texture helpers if co-scheduled (the intermediate *read* helper belongs to the former, zlib/mip-size to the latter; `Main.cpp`'s BC7-hardcoded fourth mip loop folds in only with the former). Cross-queue: `DataPacker/Architecture_ChunkCacheVersioning.md` (**LANDED and removed**) — its manifest-version item and the pre-existing `DataPacker/Architecture_PackStalenessAndCrcGuards.md` both edit `RunExportJobs`' dirty logic in `Main.cpp`, so `PackStalenessAndCrcGuards` should refresh its `Main.cpp` citations before landing; `DataPacker/Architecture_RdoSweepRelocation.md` moves `LoadBc7AsFloatPixelsMip0` (the `Main.cpp:216-241` texture-intermediate reader `PayloadLayoutSingleSource` cites) into `ExportJobs/Texture/RdoSweep.cpp` — land the relocation first or refresh citations; `PayloadLayoutSingleSource`'s engine-side edits (`PipelineManager.cpp`/`ModelPipeline.cpp`/`AnimationData.cpp`/`FileManager.cpp`) overlap the Graphics/Managers, Graphics/Objects, and File sessions — refresh lines if interleaved.

## File Groups

Plans that touch the same files and should be done in a single session:

- **`Common/Log/Log.{h,cpp}` + `Common/StackWalker.h` + `Common/WindowsUtils.{h,cpp}` (+ `Common/Threading/ThreadLocal.h` for `LogTickScope`)**: `Common/ThreadSafety_SharedStaticBuffers.md` (per-thread buffers across all three; the theme's Log `thread_local` change resolves StackWalker's static-buffer race). (`Common/StackWalker.md` has landed and been removed — its DbgHelp serialization no longer needs co-scheduling.)
- **`Common/Log/DiagnosticLog.cpp`**: `Engine/SingletonPublishGlobalGuardSweep.md` (per-slot publish guard `ASSERT(gpDiagnosticLogs[miIndex] == nullptr)` in ctor + `if (gpDiagnosticLogs[miIndex] == this)` in dtor). (`Common/DiagnosticLog.md` has landed and been removed — the `kDiagnosticLogCount` + `static_assert` slot guard and the `ScopedSuppressAllocationTracking` move to Common are in.)
- **`Projects/.../Graphics/Camera.cpp`**: `Frame/ScaleEngineToMeters.md` (`kfEyeHeightMin`/`kfEyeHeightMax` literals), `Engine/SingletonPublishGlobalGuardSweep.md` (`Camera` ctor `ASSERT(gpCamera == nullptr)` + dtor `if (gpCamera == this)` at `:38`/`:45`)
- **`Engine/Source/Graphics/Objects/PipelineCreator.cpp` + `Pipeline.{h,cpp}` + `Managers/CommandBufferRecordMain.cpp` + `Render/MainUniforms.cpp` + `Engine/Data/Shaders/Water/WaterDisplacement.comp`**: `Graphics/WaterDisplacementIndirectCompute.md` (add `kIndirectHostVisible|kCompute` branch, `WriteIndirectComputeBuffer` helper, and `fTerrainElevation >= 0.0f` over-land skip), `Graphics/WindowedLightingShadowDispatch.md` (reuses the same `kIndirectHostVisible|kCompute` branch + `WriteIndirectComputeBuffer`/`RecordComputeIndirect` helpers — do not duplicate; then windows the lighting/shadow compute dispatches via indirect dispatch and scissors the deposit/spread render passes in `CommandBufferRecordMain.cpp` / `CommandBufferRecordGlobal.cpp`) **Graphics/Debug deep-analysis additions (this run)**: `Graphics/Architecture_DebugRenderDeadPrimitives.md` (removes the two debug `RecordDrawIndirect` lines at `CommandBufferRecordMain.cpp:313-314`; its other edits live outside this group) and `Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md` (comment at the `MainUniforms.cpp:163-166` early return) — disjoint lines from the indirect-compute pair; co-schedule or refresh citations.
- **`Engine/Source/Frame/IslandTerrain.cpp`**: `Frame/IslandTerrainSplit.md` (`/reduce-file` split along the `#if defined(BT_CLIENT)` GPU-residency seam — relocates the eviction/restoration sweeps + `CreateClientMeshBuffers` into a new client-only `IslandTerrainResidency.cpp`; **a file split invalidates the other entries' line refs** so sequence/co-schedule), `Frame/ScaleEngineToMeters.md` (drop the three `kfMetersToUnits` multiplications at `:49`/`:50`/`:147`/`:395`/`:818` as part of the final-cleanup batch — `:818` is inside the relocated client span, re-target if the split lands first), `Graphics/IslandResidentMemoryScalingStrategy.md` (extend `EvictionSweep`/`RestorationSweep` to per-template mesh buffers under strategy (a2); remove the `[DEBUG-resmem]` instrumentation block at the end of `CreateClientMeshBuffers` — **all three relocated by the split**; land the split first or co-schedule, see Dependencies), `Graphics/IslandNavContourResidency.md` (server-side `mNavContour` lifecycle in `WaitForElevationMaps` / `~IslandTerrain` — both stay in `IslandTerrain.cpp`, line refresh only), `Engine/SingletonPublishGlobalGuardSweep.md` (`IslandTerrain` ctor/dtor publish guard at `:28`/`:154` — both stay in `IslandTerrain.cpp`, line refresh only); **Frame deep-analysis additions (this run)**: `Frame/Architecture_IncludeHygiene.md` (removes the two unused includes at `:7`/`:13`), `Frame/Architecture_DeterminismGuards.md` (Frame Purity assert on `GlobalElevation`/`GlobalNormal` — stays in `IslandTerrain.cpp`), `Frame/Refactor_NavFunctionDecomposition.md` (`AcquireTextureSlot` first-mint extraction — relocated by the split; target `IslandTerrainResidency.cpp` after it lands)
- **`Engine/Data/Shaders/ShaderFunctions.h` + the area-mapping consumer shaders (`Terrain/Terrain.frag`, `Water/Water.vert`, `Water/WaterDisplacement.comp`, `Smoke/SmokeSpreadCommon.h`, `Smoke/SmokeOccupancyDilateRemap.comp`, `Wind/WindSpreadOne.comp`, `Wind/WindSpreadTwo.comp`, `Wind/WindOccupancyDilate.comp`)**: `Graphics/InlineAreaMappingDryToHelper.md` (route the eight inline `VisibleAreaToWorld` inverses through the shared helper; `ShaderFunctions.h` is read-only reference here). (`Graphics/LightingDepositSpreadFullPort.md` has landed and been removed — it already converted the lighting `LightingSpread.frag:74` site to the shared helper and left these eight non-lighting sites for this plan.)
- **`Engine/Source/Graphics/Render/WindUniforms.cpp` / `Render/SmokeUniforms.cpp` / `Render/Render.h` + `Managers/RenderTargetTextures.cpp` + `Managers/CommandBufferRecordGlobal.cpp` (wind/smoke render path)**: `Graphics/WindHistoryResetOnRecreate.md` (wind recreate clean-state mirror of `gbShadowTemporalReset`/`gbSmokeClear`; reads the smoke/shadow paths as templates, edits the wind path — may add a wind-clear pipeline + dispatch in `RecordWindSpreadPipeline`), `Graphics/DisabledPassGatingPerfAudit.md` (measures/optionally gates `RecordWindSpreadPipeline`/`RecordSmokeSpreadPipeline` + the wind-deposit / object-shadows passes in `CommandBufferRecordMain.cpp`; reads the `sbWind`/`sbSmoke` toggle latches). Sequence these two in one wind/smoke-render session to avoid stale line numbers
- **`Engine/Source/Audio/*` (`AudioManager.{h,cpp}`, `StaticVoice.{h,cpp}`, `StaticVoices.{h,cpp}`, `StreamingVoice.{h,cpp}`, `StreamingVoices.{h,cpp}`, `Engine.h`)**: all seven Audio plans overlap here — `Audio/Architecture_AudioHelperPlacement.md` (new `AudioUtility.h`; edits every voice file + `AudioManager.h` + `Engine.h` + client vcxproj), `Audio/Architecture_IncludeHygiene.md` (**landed and removed** — include blocks of five files), `Audio/Architecture_OneShotPathCleanup.md` (`StaticVoices.{h,cpp}` one-shot path + `AudioManager.{h,cpp}` signature), `Audio/Refactor_UpdateLifecycleDecomposition.md` (`StaticVoices.{h,cpp}` lifecycle + `StaticVoice.h` flag enum), `Audio/Refactor_VoiceStartClickDiscipline.md` (`StaticVoice.{h,cpp}` + `StaticVoices.cpp` creation site), `Audio/Refactor_AudioDeviceFallback.md` (`AudioManager.cpp` ctor only), `Audio/Architecture_ThreadContractDocs.md` (**LANDED and removed** — comments in `StaticVoices.h`/`StreamingVoices.cpp` + `Audio/CLAUDE.md`, plus the sweep-found `SetNextTrackCallback` Wait()-skip comment and CLAUDE.md alternation qualifier; remaining code plans refresh line citations against the new comment blocks). Land as one Audio session in the Dependencies-bullet order. `Engine/SingletonPublishGlobalGuardSweep.md` also edits `AudioManager.cpp`'s `gpAudioManager` publish sites (ctor `:17` / dtor `:165`) — different lines than `AudioHelperPlacement`'s edits, no semantic conflict; co-schedule or refresh lines.
- **`Engine/Source/File/FileManager.{h,cpp}` + `File/DifferenceStream.h`**: the File code plans overlap here — `File/Architecture_IncludeHygiene.md` (**landed and removed** — include blocks of both `FileManager.cpp` and `DifferenceStream.h` + the `Engine.h:11` comment), `File/Architecture_FileManagerEncapsulation.md` (`FileManager.h` member visibility + `GetEagerChunkMap`), (`File/Architecture_AnimationDataLoadPlacement.md` **has landed and been removed** — it deleted the `LoadPackFiles` scene-parse block and relocated it to `Graphics/AnimationData.cpp`'s `LoadAnimationDataFromEagerChunks`, called from the `Graphics` ctor), `File/Architecture_LoadThreadLifecycleSafety.md` (dtor, async task, `ReadChunkData`, new atomic member), `File/Architecture_VersionedHeaderDedup.md` (versioned-file templates + DifferenceStream header read/write), `File/Refactor_FunctionDecomposition.md` (`LoadPackFiles`/`LoadChunk`/reader-ctor extraction), `File/Refactor_ClientGuardAndWideApi.md` (ctor + nested guard), `File/CorruptPackDataGracefulFailure.md` (graceful-failure rewrites of the `LoadPackFiles` manifest assert + `CreateFileW` check and the `LoadChunk` zlib assert + sub-read-loop guard — its guarded sub-read loop is the loop `Refactor_FunctionDecomposition` extracts; co-schedule or refresh lines). Land as one File session in the Dependencies-bullet order. `Common/StaleCodeCommentsSweep.md` ("pre-faulted" comments) and `Engine/SingletonPublishGlobalGuardSweep.md` (`gpFileManager` publish guard) also touch `FileManager.{h,cpp}` — co-schedule or refresh lines. `Engine/OsApiStringOutPointerNullGuards.md` also touches `FileManager.cpp` (the ctor `SHGetKnownFolderPath` out-pointer null guard, `:23-26`) — disjoint from the above edits; refresh lines if co-scheduled.
- **`Engine/Source/Graphics/` top-level (`Graphics.{h,cpp}`, `CameraBase.{h,cpp}`, `OneShotCommandBuffer.{h,cpp}`, `Screenshot.cpp`, `Islands.h`, `GraphicsUtils.cpp`, `AnimationData.{h,cpp}`)**: the eight Graphics deep-analysis plans overlap here — `Graphics/Architecture_IncludeHygiene.md` (include blocks of four .cpps + `Islands.h` fwd decls), `Graphics/Architecture_RenderInterpolatesOwnership.md` (`Graphics.h` member + `GameBase.{h,cpp}`/`Main.cpp`), `Graphics/Architecture_SharedConstantDuplication.md` (`Graphics.cpp:15-82` constants, `CameraBase.{h,cpp}` eye-height, `AnimationData.cpp` enums), `Graphics/Architecture_EncapsulationSeams.md` (`Graphics.cpp` destroy/create + `Managers/SwapchainManager.{h,cpp}`), `Graphics/Architecture_ScreenshotFenceDeadlock.md` (`Screenshot.cpp` + `Managers/CommandBufferManager.cpp`), `Graphics/Architecture_InvariantHardening.md` (`AnimationData.h:41` comment + `OneShotCommandBuffer.cpp` assert), `Graphics/Refactor_RefreshAndCornerDecomposition.md` (`Graphics.cpp:350-523` + `CameraBase.cpp:53-101`), `Graphics/Refactor_ApiAndHotPathCleanups.md` (`OneShotCommandBuffer.{h,cpp}` + `CameraBase.h` + `Islands.{h,cpp}` + game `Game.cpp`). Land as one or two sessions in the Dependencies-bullet order; `Graphics.cpp` and `CameraBase.{h,cpp}` are each touched by four plans, so refresh line citations between landings.
- **`Engine/Source/Frame/Collision.{h,cpp}` (+ `AreaDamage.cpp`)**: (`Frame/Refactor_CollisionOverflowGuards.md` **has landed and been removed** — overflow suppressions in, `AddLayer` overflow now fatal so `sLayerBaseOffsets` cannot overrun; the Workbuffer growth paths got the same suppression), `Frame/Refactor_CollisionDecomposition.md` (`CollideLayerPair`/`InsertObjectIntoZones` restructure), (`Frame/Architecture_CollisionHeaderSurface.md` **has landed and been removed** — `PendingCollisionResult`/`ZoneRange`/`ZonePair`/`LayerPairZones` moved out of `Collision.h` into `Collision.cpp`; header keeps only `struct ZoneRange;`/`struct LayerPairZones;` fwd decls; member initializers added to `PendingCollisionResult`/`ZoneRange`), `Frame/Architecture_IncludeHygiene.md` (Collision.h/.cpp include items), `Frame/Refactor_StyleMechanics.md` (`memset`/designated-init/`.at()` items) — one Collision session; land the decomposition last (it moves the lines the others cite) or first (it owns the biggest diff) — pick one and refresh.
- **`Engine/Source/Frame/FrameBase.{cpp,h}`**: (`Frame/Architecture_LogDifferencesServerCollections.md` **has landed and been removed** — both base `LogDifferences` now fold `ServerCollections()` via `LogDifferencesCollections` in `FrameUtils.h`; the game `Frame.cpp` `LogDifferences` got the same tuple fold), `Frame/Architecture_CrcMixingStrength.md` (Crcs XOR→ordered fold; adjacent functions, `.cpp`), `Frame/Refactor_StyleMechanics.md` (`[[maybe_unused]]` strip + `cols` rename, `.cpp`), `Frame/Architecture_ContractDocGaps.md` (Write/Read comment, `.cpp`), `Frame/Architecture_IncludeHygiene.md` (game `Frame.h` include), and `Frame/Architecture_CollectionsTupleWireParity.md` (server-build `Collections()`==`ServerCollections()` `static_assert` in `FrameBase.h`, near the existing `kCollectionCount` asserts) **has landed and been removed** — co-schedule the remaining `.cpp` plans in one session. The TupleWireParity assert reasoned about the same `Collections()`/`ServerCollections()` relationship as the LogDifferences fold but edited only `.h` (disjoint from every `.cpp` edit above).
- **`Engine/Source/Frame/NavBuild.cpp` + `NavQuery.cpp` (+ post-split `NavCellData.cpp`/`NavBuildInternal.h`)**: `Frame/NavBuildSplit.md` (the split — land first), then `Frame/Architecture_NavPredicateDedup.md`, `Frame/Architecture_DeterminismGuards.md` (ChainEdges assert), `Frame/Refactor_NavFunctionDecomposition.md`, `Frame/Refactor_AStarEndVisibleLazy.md` (same `AStarPath` as the decomposition — co-schedule), `Frame/Refactor_DiagnosticAndDeadCodeRemoval.md` (NavBuild dead-scan item), `Frame/Refactor_StyleMechanics.md` (m128 items), `Frame/Architecture_BaseHeightWrapperDeterminism.md` (NavQuery `gBaseHeight` reads + include removal) — one nav session after the split.
- **`Engine/Source/Input/RawInputManager.{h,cpp}` + game `Input/Input.{h,cpp}` + `Engine/Source/Main.cpp`**: the four Input code plans overlap here — `Input/Architecture_InterfaceNarrowingAndDocs.md` (**LANDED and removed** — `mHwnd`/`mpbKeyboardKeysDown`/`mMouse`/`TrapCursor` now private, `mHwnd` assigned in `UpdateFocus`, `Main.cpp` external write + `Game.h` fwd-decl deleted, two Input/CLAUDE.md bullets added), `Input/Refactor_RawInputHandling.md` (`HandleRawInput` + the WndProc `WM_INPUT` case), `Input/Architecture_GamepadDisconnectSnapshot.md` (**LANDED and removed** — disconnect branch now zeroes `f2Dpad`; dead `f2Triggers` field deleted), `Input/Refactor_InputButtonFlags.md` (button enums/struct members + `Update` writers + game edge-detect helpers). Land as one Input session. `Engine/DeadCodeAndUnusedIncludesSweep.md` (mouse `RIDEV_INPUTSINK` registration + game `Input.h` deletions: `GetGamepadMode()`, `FrameInput::operator==`), `Common/StaleCodeCommentsSweep.md` (the cpp:42 comment), and `Engine/SingletonPublishGlobalGuardSweep.md` (`gpRawInputManager` ctor/dtor publish guards, cpp:12/30) also touch these files — disjoint edits, co-schedule or refresh lines.

- **`Engine/Source/Ui/WaterWrappersBase.{h,cpp}` (+ `Render/GlobalUniforms.cpp` / `Water.vert` for the decision plan)**: `Ui/Architecture_WaterWrapperInvariants.md` (**landed and removed** this session — stale `kiWaterNormalSeaWavesIndex` comment + dead constant deleted, `.h`↔`.cpp` declaration order reconciled), `Ui/Refactor_StyleMechanics.md` (`std::move`/`i64` literals `:70/:88`, `// DT: TEMP` deletions `:116-118`), `Ui/Refactor_WaterZOffsetTempDecision.md` (`gWaterZOffsetTemp` chain `.h:81`/`.cpp:103`) — disjoint lines; land as one Water-wrapper session.
- **`Engine/Source/Ui/WrapperBase.{h,cpp}`**: `Ui/Architecture_IncludeHygiene.md` (removes the `:4` `CurveData.h` include), `Ui/Architecture_WrapperContractDocs.md` (class/member contract comments — land last), `Frame/Architecture_BaseHeightWrapperDeterminism.md` (deletes `gBaseHeight`, `.h:214`/`.cpp:9`), `Engine/WrapperGetIndexFailLoud.md` (`GetIndex` fall-through `:185-186`), `Frame/ScaleEngineToMeters.md` (`.cpp` tunable literals) — all disjoint symbols; co-schedule or refresh lines between landings.
- **`Engine/Source/Ui/CurveData.h` + `Engine/Source/Ui/CLAUDE.md`**: `Ui/Architecture_CurveDataDeadChangeDetection.md` (`Changed()`/`ComputeHash` deletion + doc sentence), `Ui/Refactor_StyleMechanics.md` (dead `iCount` `:151/:171` + two doc sentences), `Ui/Architecture_LibraryReplacement.md` (drops the single-instance drag caveat from the doc) — co-schedule the `CLAUDE.md` edits.
- **`Engine/Source/Profile/ProfileManagerBase.{h,cpp}` + `ProfileScreens.cpp` (+ game `Server/ServerDisplay.cpp`, `Engine/Source/Profile/CLAUDE.md`)**: the Profile plans overlap here (`Profile/Architecture_SmoothNowDoubleLatch.md` **has landed and been removed** — `CpuTimer` `bSmoothAtStop` flag + `CpuStop`/`SmoothCpuTimers`) — `Profile/Architecture_ClientServerGuardScope.md` (`UpdateProfileText` restructure + header gating + the `ServerDisplay.cpp:80` swap + CLAUDE.md:11), `Profile/Architecture_InvariantHardening.md` (static_asserts after the `:5` include, `Create()` precondition, `LockCpuTimers()` accessor + formatter lock), `Profile/Architecture_IncludeHygiene.md` (include/declaration moves in all three files + CLAUDE.md:37), `Profile/Refactor_InFunctionCleanups.md` (`CpuStop` flags signature across 17 call sites + `FormatGpuScreen` decomposition + `= default`), (`Profile/Refactor_ProfileRowFlags.md` **has landed and been removed** — the three profile-row structs' `bVisible`/`bSmoothAtStop` members are now one shared `common::Flags<ProfileRowFlags>`; `CpuStop`/`SmoothCpuTimers` + `FormatCpuTimersText`/`FormatCpuCountersText`/`FormatGpuScreen` rewritten), `Profile/Architecture_DocAccuracy.md` (CLAUDE.md claims + `mCpuTimerMutex` comment — land last). Land as one Profile session in the Dependencies-bullet order. `Engine/DeadCodeAndUnusedIncludesSweep.md` item 12 and `Common/StaleDocClaimsSweep.md` item 6 also touch these files/docs — see the Profile Dependencies bullet for the partition.
- **`Engine/Source/Network/` engine transport (`Client/Client.{h,cpp}` + `ClientSend.cpp` + `ClientReceive.cpp` + `ClientSessionBase.{h,cpp}`, `Server/Server.{h,cpp}` + `ServerReceive.cpp` + `ServerSend.cpp` + `ServerSessionBase.{h,cpp}` + `ServerTypes.h`, `NetworkProtocol.h`, `NetworkSimulation.h`)**: the twelve Network deep-analysis plans overlap here — `ClientReceive.cpp` is touched by five (`Refactor_QuickWinMechanics` hoist, `Refactor_CoordUpdateAllocAndJitter`, `Architecture_SubscribeRejectSlotNegotiation`, `Architecture_ClientSessionBoundary` GUID write, `Architecture_IncludeHygiene`), `ClientSessionBase.{h,cpp}` by four, the Server transport files by five (`Architecture_ClientBuildServerTransportGate`'s `BT_SERVER` wraps have **landed and been removed**), and `Architecture_WireFormatPairing` restructures all the send/receive files. Land as one or two Network sessions in the Dependencies-bullet order; `WireFormatPairing` last. The pre-existing `Network/StaticDataEpochCheckOnSubscribingSlot.md` also edits `ClientReceive.cpp` (`ServerCoordStaticData` epoch condition) — fold into the same session.
- **`Engine/Source/Graphics/Render/` (`Render.h`, `GlobalUniforms.cpp`, `LightingUniforms.cpp`, `MainUniforms.cpp`, `SmokeUniforms.cpp`, `WindUniforms.cpp`)**: the six Render deep-analysis plans overlap here — `GlobalUniforms.cpp` is touched by five (`Architecture_RenderIncludeHygiene` (**landed and removed**) include block, `Architecture_RenderSharedConstants` `kfWaveFadeEnd`/elevation width, `Architecture_RenderRegionOwnership` dead stores + relocation, `Refactor_RenderMicroCleanups` comment items, `Refactor_RenderFunctionDecomposition` shadow split — land last), `MainUniforms.cpp` by four of the six plus the pre-existing `Graphics/Architecture_EmptyCoordsStaleIndirectCounts.md` (comment at the `:163-166` early return) and `Common/StaleCodeCommentsSweep.md` (the `:176-177` terrain comment). `WindUniforms.cpp`/`SmokeUniforms.cpp`/`Render.h` also belong to the wind/smoke render-path group above (`WindHistoryResetOnRecreate`, `DisabledPassGatingPerfAudit`) — co-schedule or refresh citations. `Ui/Refactor_WaterZOffsetTempDecision.md` also edits `GlobalUniforms.cpp:528` — disjoint line, refresh only. Land as one Render session in the Dependencies-bullet order.
- **`Engine/Source/Graphics/Managers/` (the whole directory)**: the fourteen Graphics/Managers deep-analysis plans densely overlap — co-schedule by cluster. *Texture* (`TextureManager.{h,cpp}`, `TextureDescriptors.{h,cpp}`, `TextureCache.cpp`, `TextureUploadManager.{h,cpp}`): `Architecture_IncludeHygiene`, `Architecture_LayerSeams`, `Architecture_ShaderCpuContractConstants` (`kBlurSalt`), `Architecture_ThreadAndLifetimeGuards`, `Refactor_CorrectnessHardenings`, `Refactor_PerFramePerf`, `Refactor_TextureDecomposition`, `Refactor_DeadCodeRemoval`, `Refactor_MicroCleanups`, then `Architecture_BindlessSlotLifecycle` **alone and last** (it reshapes `TextureDescriptors`). *Pipeline/device* (`PipelineManager.{h,cpp}`, `DynamicPipelines.{h,cpp}`, `InstanceManager.{h,cpp}`, `DeviceManager.{h,cpp}`): `Architecture_IncludeHygiene`, `Architecture_LayerSeams`, `Architecture_ShaderCpuContractConstants`, `Refactor_CorrectnessHardenings`, `Refactor_DeadCodeRemoval`, `Refactor_MicroCleanups`, `Refactor_PipelineAndDeviceDecomposition`. *Buffer/record/swapchain* (`BufferManager.{h,cpp}`, `CommandBufferManager.cpp`, `CommandBufferRecord{Global,Main}.cpp`, `SwapchainManager.cpp`, `RenderTargetTextures*.cpp`, `ImGuiManager.cpp`, `TextManager.{h,cpp}`, `ParticleManager.cpp`): `Refactor_BufferGrowthAndBounds`, `Refactor_CorrectnessHardenings`, `Refactor_PerFramePerf`, `Refactor_MicroCleanups`, `Refactor_RecordAndSwapchainDecomposition`, plus `Architecture_ClientGuardConsistency` (wraps ten of these cpps — land it first in its session or refresh lines). Cross-queue: `CommandBufferRecordMain.cpp` also belongs to the indirect-compute group above (`WindowedLightingShadowDispatch`, `DebugRenderDeadPrimitives`) and `RenderTargetTextures.cpp`/`CommandBufferRecordGlobal.cpp` to the wind/smoke group — refresh citations if sessions interleave.
- **`Engine/Source/Graphics/Objects/` (the whole directory: `Pipeline.{h,cpp}`, `PipelineCreator.{h,cpp}`, `PipelineDescriptorWriter.{h,cpp}`, `ModelPipeline.{h,cpp}`, `Texture.{h,cpp}`, `Shader.{h,cpp}`, `CommandBuffers.{h,cpp}`, `Buffer.cpp`)**: the ten Graphics/Objects deep-analysis plans densely overlap — `Pipeline.{h,cpp}` is touched by six (`Architecture_ObjectsIncludeHygiene` (**landed and removed**) Buffer.h include, `Architecture_ObjectsShaderCpuConsistency` 15/16 constants + push-constant items, `Architecture_PipelineRegistrationOwnership` kModel append + pre-Create protocol, `Refactor_ObjectsQuickWinMechanics` WriteIndirectBuffer/Record asserts, `Architecture_DescriptorSetIndexDedup` helper-home option, `Refactor_PipelineCreatorDecomposition` call sites), `PipelineCreator.cpp` by four, `PipelineDescriptorWriter.cpp` by four, `ModelPipeline.{h,cpp}` by three (`Architecture_ObjectsDeadRecreateApis` **landed and removed** — its `ModelPipeline`/`Texture` edits are in; see Dependencies). Land as one or two sessions in the Dependencies-bullet order. Cross-queue: `Graphics/WaterDisplacementIndirectCompute.md` + `Graphics/WindowedLightingShadowDispatch.md` share `PipelineCreator.cpp`/`Pipeline.{h,cpp}` (see their entry above); `Graphics/PipelineGraphicsGlobalSet0Indexing.md` shares `Pipeline.cpp`; `Architecture_PipelineRegistrationOwnership`'s `Managers/PipelineManager.{h,cpp}` + `Managers/TextureDescriptors.{h,cpp}` edits overlap the Graphics/Managers clusters above — sequence with `Architecture_BindlessSlotLifecycle` (see Dependencies); `Refactor_ModelPipelineMembers`' `DynamicPipelines.{h,cpp}` spec-field edits overlap that cluster too (disjoint symbols, refresh lines).
- **`Engine/Source/Frame/Collections/Collection.h` + `CollectionMemory.h`**: all eight Frame/Collections deep-analysis plans overlap here — `Architecture_AllocateAndAssignReuseGuard.md` (read helpers + `AllocateAndAssign`), `Architecture_DeserializedCountValidation.md` (`Collection::Read`/`OptionalIdToIndex::Read`), `Architecture_SharedMembersParityGuard.md` (`SharedCollectionCrc` static_assert — **has landed and been removed**; successor `Architecture_NonSharedLeafClientFieldGuard.md` only reads `Collection.h`), `Architecture_TypeRegistryGuards.md` (**landed and removed** — registry mixins), `Architecture_CollectionCrcMixing.md` (the five CRC combine sites), `Refactor_AllocationHelperDedup.md` (`Allocate`/`ReallocateIfCapacityChanged`), and `Architecture_HeaderCohesionAndSplit.md` (**a file split invalidates the other entries' line refs** — land it last; see Dependencies). `Architecture_ExternalHeadersGap.md` (**landed and removed**) only read these files (edited `Common/ExternalHeaders.h`). `Engine/DeadCodeAndUnusedIncludesSweep.md` items 10/12/13/14 also touch the same front matter — co-schedule or refresh lines.
- **`DataPacker/Source/Main.cpp` (+ `FileManager.{h,cpp}`)**: four of the five DataPacker root deep-analysis plans share `Main.cpp` — `DataPacker/Architecture_OutputWriteTrustBoundary.md` (stream checks in `WriteIfChanged`/`WriteCrcHeader`/`RunExportJobs` + the `main` mutex), `DataPacker/Architecture_PackStalenessAndCrcGuards.md` (`RunExportJobs` dirty logic + post-sort collision ASSERT), `DataPacker/Refactor_MainQuickWinMechanics.md` (include/`using enum`/`LogIndent`/temp-header/binary-mode items, plus `FileManager.cpp:46`), and `DataPacker/Architecture_RdoSweepRelocation.md` (moves `Main.cpp:67-315` — land last, see Dependencies). `DataPacker/Architecture_AttributionPathOwnership.md` shares `FileManager.{h,cpp}` with the mechanics plan. Land as one DataPacker session. `Common/StaleCodeCommentsSweep.md` (the `Main.cpp:305` knobs comment, relocated by the sweep move) also touches the moved block — co-schedule or refresh citations. (`DataPacker/DataPackerLogFloatSpecException.md` **LANDED and removed** — Option A, doc-only; the RDO-sweep LOG lines were read-only confirmation, untouched.)
- **`DataPacker/Source/ExportJobs/` (the whole directory)**: the sixteen ExportJobs deep-analysis plans densely overlap — co-schedule by cluster. *Scene* (`ExportScene.{h,cpp}`, `Scene/*.{h,cpp}`): `DataPacker/Architecture_ExportJobsIncludeHygiene.md` (**landed and removed** — include move + using-enum), `DataPacker/Architecture_ChunkCacheVersioning.md` (**LANDED and removed** — GetVersion overrides + the animation expected-offset ASSERT), `DataPacker/Architecture_SceneLoaderVestigialPaths.md`, `DataPacker/Refactor_SceneCheckDirtySideEffect.md`, `DataPacker/Refactor_SceneFunctionDecomposition.md`, `DataPacker/Architecture_LibraryReplacement.md` (last — after the `LoadVertices` split), `DataPacker/Refactor_ExportJobsQuickWins.md` (free-function statics + glTF defines), `DataPacker/Architecture_PayloadLayoutSingleSource.md` (scene/animation writers). *Texture* (`ExportTexture.cpp`, `Texture/Texture.{h,cpp}`, `Texture/MigrateLegacyIntermediates.cpp`): pre-existing `DataPacker/ExportTextureHeaderlessRawFormatClaims.md` (first), `DataPacker/Refactor_ExportTextureMechanics.md`, `DataPacker/Refactor_TextureUtilDedup.md`, `DataPacker/Architecture_PayloadLayoutSingleSource.md` (intermediate read helper), `DataPacker/Architecture_PackByteDeterminism.md` (RDO knob), `DataPacker/Refactor_SceneFunctionDecomposition.md` (Texture ctor split), `DataPacker/Architecture_ExportJobsIncludeHygiene.md` (**landed and removed** — codeanalysis include). *Island* (`ExportIsland.{h,cpp}`, `Island/*`): `DataPacker/Architecture_IslandConstantsOwnership.md` (first), `DataPacker/Refactor_IslandFunctionDecomposition.md`, `DataPacker/Refactor_ExportJobsQuickWins.md` (SubdivideBeachBand/cast/Handles items). *Shader* (`ExportShader.{h,cpp}`): `DataPacker/Architecture_ShaderReflectionGuards.md` (**LANDED and removed** — WriteBinding double-write ASSERT, vertex-stage Float-basetype ASSERT, shared `ShaderHeader::kuiSpirvMagic`), `DataPacker/Refactor_ShaderToolRunnerDedup.md`, `DataPacker/Refactor_ExportJobsQuickWins.md` (statics + access specifiers), `DataPacker/Architecture_PayloadLayoutSingleSource.md` (payload offsets). *IBL* (`ExportCubemapIbl.cpp`): `DataPacker/Architecture_OrchestrationDedup.md`, `DataPacker/Refactor_ExportJobsQuickWins.md` (stream conversions + face arrays), `DataPacker/Architecture_PackByteDeterminism.md` (OpenCL path). `ExportJob.{h,cpp}` is shared by `DataPacker/Architecture_OrchestrationDedup.md` (CleanupOnFailure hoist), `DataPacker/Architecture_ChunkCacheVersioning.md` (`Version` helper), `DataPacker/Refactor_ExportJobsQuickWins.md` (`starts_with`), and the pre-existing `DataPacker/ExportJobCleanPathCachedChunkReadCheck.md` — co-schedule or refresh lines. `DataPacker/OutputWriteTrustBoundaryExportJobsSweep.md` adds a `good()` stream-state check to one output write in nearly every TU in this directory (Scene `.MODEL`/`.PreExport`, Texture/Migrate intermediates, Island bake leaves + version sentinels + `GaeaArchetype` `.terrain`, IBL cubemaps, `ExportJob` `.chunk`/mtime) — disjoint single-line additions, co-schedule or refresh lines with any of the above.
