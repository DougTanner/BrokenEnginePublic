# Investigations

Non-executable reference material: findings records, series overviews, and investigations that present options rather than a decision-complete implementation. WorktreeCli never validates, claims, stores locks for, or mutates `Documents/Investigations`.

Work blocked on another change is a Plan — it belongs in `Documents/Plans` with a `dependsOn` edge. Work blocked on a decision belongs here until the decision exists; it earns a Plan by moving to `Documents/Plans/<area>/` with byte-zero metadata.

Use existing subject-area subdirectories.
