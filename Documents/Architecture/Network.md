# Architecture: Network

> Auto-generated by `/generate-architecture-diagram` from `Engine/Source/Network/`

## Network Component Relationships

```mermaid
%%{init: {'theme': 'default'}}%%
graph TD
    classDef clientOnly fill:#dbeafe,stroke:#3b82f6
    classDef serverOnly fill:#fee2e2,stroke:#ef4444
    classDef shared fill:#f3f4f6,stroke:#6b7280
    classDef protocol fill:#fef3c7,stroke:#d97706

    subgraph engine_network ["engine:: Network Subsystem"]
        gpNetworkManager["gpNetworkManager<br/>NetworkManager<br/>(ENet lifecycle)<br/>kiMaxEnetCoordSlots=64<br/>kuiChannelCount=130<br/>(2 control + 64 slot pairs)<br/>IsUnreliableChannel()"]:::shared

        gpClient["gpClient<br/>Client<br/>(mpHost, mpServerPeer)"]:::clientOnly
        gpServer["gpServer<br/>Server<br/>(mpHost, mClients[])"]:::serverOnly

        server_types["ServerTypes.h<br/>ClientCoordSubscription<br/>PendingSpawnRequest<br/>PendingDisconnect<br/>PendingNewSubscription<br/>GridUpdateData"]:::serverOnly
        protocol["NetworkProtocol.h<br/>PacketType enum (15 types)<br/>AckState struct<br/>kuiProtocolVersion<br/>kiClockErrorDisconnectThreshold"]:::protocol
        simulation["NetworkSimulation.h<br/>NetworkSimulationLevel enum<br/>NetworkSimulationConfig<br/>DelayedPacket<br/>EnqueueOrDrop / ProcessDelayed<br/>(latency + loss injection)"]:::shared
        serialization["NetworkSerialization<br/>StatusChange compress/decompress<br/>(LZ4)"]:::shared
        cursor["NetworkCursor<br/>Inline binary read/write helpers<br/>(ReadUint8..ReadVec4, ReadGridCoord,<br/>WriteUint8..WriteVec4)"]:::shared
        discovery_scanner["NetworkDiscoveryScanner<br/>(UDP broadcast probe)"]:::clientOnly
        discovery_responder["NetworkDiscoveryResponder<br/>(UDP listen on port 27016)"]:::serverOnly
        client_session_base["ClientSessionBase<br/>(owns Client +<br/>DiscoveryScanner)<br/>ConnectToServer, DisconnectFromServerBase,<br/>StartServerDiscovery, PollLANDiscovery,<br/>TrySubscribeNext, UnsubscribeStaleCoords,<br/>BuildSubscriptionQueue,<br/>ApplyReceivedUpdatesBase,<br/>ComputeClockCorrectionNs,<br/>(mbClockErrorDisconnect on extreme drift),<br/>IsExtrapolating, PrepareExtrapolationTick,<br/>BuildExtrapolationFrameRef,<br/>RecordExtrapolationSnapshot, GetSnapshotFrame,<br/>GetConfirmedTick, GetServerUpdateBufferSize"]:::clientOnly
        session_base["ServerSessionBase<br/>(owns DiscoveryResponder,<br/>mTimerHandle for tick sleep)<br/>WaitForTick, PollNetworkBase,<br/>SendNewSubscriptionFullStates"]:::serverOnly
    end

    subgraph server_data ["Server-Side Data"]
        client_conn["ClientConnection<br/>pPeer, iClientId, humanPlayerId,<br/>humanGridCoord,<br/>coordSubscriptions (vector),<br/>coordAckStates (vector) (floor+bitfield+epoch),<br/>iClientTimestampNs"]:::serverOnly
        buffered["mPerCoordBufferedFrames<br/>(per-coord ring buffers, 256 max)<br/>PerCoordBufferedFrame: serverCrc + inputCrc<br/>+ compressed data<br/>mBufferedFullFrames<br/>(debug frame ring buffer)"]:::serverOnly
        pending["mPendingSpawnRequests<br/>mPendingDisconnects<br/>mPendingNewSubscriptions"]:::serverOnly
        delayed_server["mDelayedPackets<br/>(network simulation delay queue)"]:::serverOnly
    end

    subgraph client_data ["Client-Side Data"]
        received["mReceivedCoordUpdates (vector)<br/>(per-slot update buffers)<br/>ReceivedCoordUpdate: serverCrc + inputCrc<br/>+ statusChanges<br/>mReceivedFullStates<br/>mReceivedGamePackets<br/>(raw bytes for game-layer parsing)"]:::clientOnly
        ack_state["Per-Slot ACK State<br/>ClientCoordSlot (vector)<br/>(coord, state, ackFloor, bitfield, epoch)"]:::clientOnly
        delayed["mDelayedPackets<br/>(network simulation delay queue)"]:::clientOnly
    end

    gpServer -->|includes| server_types
    gpNetworkManager -->|initializes| gpClient
    gpNetworkManager -->|initializes| gpServer
    gpClient -->|uses| protocol
    gpServer -->|uses| protocol
    gpClient -->|uses| simulation
    gpServer -->|uses| simulation
    gpClient -->|uses| serialization
    gpServer -->|uses| serialization
    gpClient -->|uses| cursor
    gpServer -->|uses| cursor
    serialization -->|uses| cursor
    gpServer -->|manages| client_conn
    gpServer -->|stores| buffered
    gpServer -->|queues| pending
    gpServer -->|delays| delayed_server
    gpClient -->|buffers| received
    gpClient -->|tracks| ack_state
    client_session_base -->|owns| gpClient
    client_session_base -->|owns| discovery_scanner
    session_base -->|owns| discovery_responder
    discovery_scanner -->|probes| discovery_responder
```

## Connection Handshake Sequence

```mermaid
%%{init: {'theme': 'default'}}%%
sequenceDiagram
    participant C as Client
    participant E as ENet
    participant S as Server

    C->>E: enet_host_connect()
    E->>S: CONNECT event
    S->>S: Create ClientConnection<br/>Disable ENet throttle

    E->>C: CONNECT event
    C->>C: mbConnected = true<br/>Disable ENet throttle

    C->>S: kClientHello (reliable)<br/>[kuiProtocolVersion, kpcBuildConfigName]

    alt Protocol version mismatch
        S->>C: kServerConnectionResponse (reliable)<br/>[accepted = false, reason]
        C->>C: mpcRejectionReason = reason
        S->>E: enet_peer_disconnect_later()
    else Protocol matches, build config matches
        S->>C: kServerConnectionResponse (reliable)<br/>[accepted = true]
        C->>C: mbConnectionAccepted = true
        Note over C,S: Game layer proceeds to spawn
        C->>S: kClientSpawnRequest (reliable)<br/>[kSpawnRequested]
        S->>S: Assign player_t + GridCoord
        S->>C: kServerAssignPlayer (reliable)<br/>[player_t, GridCoord]
        Note over C,S: Client subscribes to coords
        C->>S: kClientSubscribe (reliable)<br/>[GridCoord]
        S->>S: AllocateSlot() for coord
        S->>C: kServerSubscribeAccept (reliable)<br/>[slotIndex, epoch, GridCoord]
        C->>C: CoordSlot → kWaitingFullState<br/>Set slot epoch
        S->>C: kServerCoordFullState (reliable, slot channel)<br/>[slotIndex, epoch, frame#, coord, LZ4-compressed Frame]
        C->>C: CoordSlot → kActive<br/>Set slot ackFloor = frame#, epoch
    else Build config mismatch
        S->>C: kServerConnectionResponse (reliable)<br/>[accepted = false, reason]
        C->>C: mpcRejectionReason = reason
        S->>E: enet_peer_disconnect_later()
    end
```

## Steady-State Update Flow

```mermaid
%%{init: {'theme': 'default'}}%%
sequenceDiagram
    participant C as Client (64 Hz)
    participant S as Server (64 Hz)

    loop Every Client Frame
        C->>S: kClientAckStream (unreliable)<br/>[per-slot ACK (slotIndex, epoch, ackFloor, bitfield),<br/>timestamp]
    end

    loop Every Server Tick
        S->>S: RunFrameTick per-Frame<br/>(5 phases via Dispatch)<br/>BufferFrame(N) to per-coord ring buffers

        S->>S: DetectPlayerDeaths()<br/>SubscriptionUpdates()<br/>(check deaths, frame changes)
        opt Player state changed
            S->>C: kServerPlayerState (reliable)<br/>[uint8_t stateType, int64_t playerId, GridCoord]
        end

        loop Each active subscription slot
            S->>C: kServerCoordUpdate (unreliable, slot channel)<br/>[slotIndex, epoch, frame N, echoed timestamp,<br/>serverCRC + inputCRC + LZ4 StatusChanges]
        end

        Note over S: Check per-slot ACK bitfields<br/>for unset bits (missing frames)<br/>ACK guarded by epoch match

        opt Unacked frames detected (up to 8 per slot)
            S->>C: kServerCoordResend (unreliable, slot channel)<br/>[slotIndex, epoch, resend frame M, echoed timestamp,<br/>serverCRC + inputCRC + data from per-coord ring buffer]
        end
    end

    Note over C: Client receives per-slot updates:<br/>Epoch validated (discard mismatched)<br/>kWaitingFullState: buffered but not ACK-tracked<br/>kActive: TrackReceivedFrame() updates<br/>per-slot ACK floor + bitfield
    Note over C: Next ACK packet carries<br/>per-slot ACK state (with epoch)<br/>back to server
```

## ACK & Resend Mechanism

Per-slot bitfield-based acknowledgement with epoch validation and capped resend budget. Client ACKs are sent every frame; server scans unset bits to detect and resend missing frames.

```mermaid
%%{init: {'theme': 'default'}}%%
flowchart TD
    classDef client fill:#dbeafe,stroke:#3b82f6
    classDef server fill:#fee2e2,stroke:#ef4444
    classDef decision fill:#f3e8ff,stroke:#9333ea
    classDef data fill:#fef3c7,stroke:#d97706

    subgraph client_side ["Client (every frame)"]
        ACK_BUILD["Build kClientAckStream<br/>Per active slot:<br/>(slotIndex, epoch, ackFloor, receivedBitfield)<br/>+ client timestamp"]:::client
        ACK_SEND["Send unreliable to server"]:::client
        ACK_BUILD --> ACK_SEND
    end

    subgraph server_ack ["Server: ClientAckStream()"]
        PARSE["Parse per-slot ACK tuples<br/>(slotIndex, epoch, ackFloor, bitfield)"]:::server
        VALIDATE{"Slot active?<br/>Epoch matches?<br/>Floor >= current floor?"}:::decision
        CLAMP["Clamp ackFloor to<br/>miLatestBufferedTick<br/>(prevent future ACK floors)"]:::server
        UPDATE_ACK{"Same floor as<br/>current AckState?"}:::decision
        MERGE["Merge: bitfield |= new bitfield"]:::server
        REPLACE["Replace: floor = new floor,<br/>bitfield = new bitfield"]:::server
        TIMESTAMP["Update iClientTimestampNs<br/>(monotonically increasing<br/>for pipeline RTT echo)"]:::server

        PARSE --> VALIDATE
        VALIDATE -->|"Yes"| CLAMP --> UPDATE_ACK
        VALIDATE -->|"No (stale epoch<br/>or freed slot)"| DISCARD["Discard ACK"]:::server
        UPDATE_ACK -->|"Same floor"| MERGE
        UPDATE_ACK -->|"New floor"| REPLACE
    end

    subgraph server_resend ["Server: SendResends() per client"]
        ITER_SLOTS["Iterate active subscription slots"]:::server
        CHECK_FLOOR{"AckState.iAckFloor >= 0<br/>and bitfield != 0?"}:::decision
        SCAN["Scan unset bits in bitfield<br/>up to bit_width(bitfield)"]:::server
        LOOKUP["Look up PerCoordBufferedFrame<br/>in per-coord ring buffer<br/>at missing frame tick"]:::server
        SEND_RESEND["Send kServerCoordResend<br/>(unreliable, slot channel)<br/>with epoch + echoed timestamp"]:::server
        CAP{"iSlotResendCount<br/>< kiMaxResendFrames (8)?"}:::decision

        ITER_SLOTS --> CHECK_FLOOR
        CHECK_FLOOR -->|"Yes"| SCAN --> LOOKUP --> CAP
        CHECK_FLOOR -->|"No"| ITER_SLOTS
        CAP -->|"Yes"| SEND_RESEND --> SCAN
        CAP -->|"No (budget exhausted)"| ITER_SLOTS
    end

    ACK_SEND --> PARSE
    TIMESTAMP --> ITER_SLOTS
    MERGE --> TIMESTAMP
    REPLACE --> TIMESTAMP
```

## Server Tick Broadcasting Pipeline

Per-tick server flow from finalizing new clients through flushing all packets. Status changes include both spawns and transfers; per-coord data is buffered into ring buffers then sent per-client per-slot.

```mermaid
%%{init: {'theme': 'default'}}%%
sequenceDiagram
    participant SS as ServerSession
    participant S as Server (engine)
    participant C as Client(s)

    Note over SS: BroadcastTick(iTick) entry

    SS->>SS: FinalizeNewClients(iTick)<br/>Match spawned player IDs to waiting clients<br/>Send kServerAssignPlayer + kServerPlayerState(kSpawned)

    SS->>SS: DetectPlayerDeaths()<br/>Scan clients: humanPlayerId missing from frame?<br/>→ mark mDeadClientIds, send kServerPlayerState(kDied)

    SS->>SS: BroadcastStatusChanges(iTick)
    Note over SS: 1. Merge spawns + transfers into allChanges per coord

    SS->>S: BufferFrame(iTick, allGridUpdates)<br/>Per active coord: build GridUpdateData<br/>(serverCrc, inputCrc, statusChanges span)<br/>→ compress StatusChanges via LZ4<br/>→ store PerCoordBufferedFrame in per-coord ring

    SS->>S: BufferFullFrame(iTick, fullFrames)<br/>Serialize full frames for debug requests

    loop Each connected client
        SS->>S: SendUpdate(client, iTick)
        Note over S: Per active subscription slot:<br/>Find buffered frame for slot's coord<br/>→ kServerCoordUpdate (unreliable, slot channel)
        S->>C: kServerCoordUpdate per slot

        SS->>S: SendResends(client, iTick)
        Note over S: Per active slot: scan ACK bitfield<br/>for unset bits → resend from ring buffer<br/>(up to 8 per slot per tick)
        S->>C: kServerCoordResend (if needed)
    end

    SS->>SS: SubscriptionUpdates(iTick)<br/>SendNewSubscriptionFullStates<br/>+ process coord transfer assignments<br/>(kServerAssignPlayer + kServerPlayerState(kChangedFrame))

    SS->>S: Flush()<br/>enet_host_flush
```

## Death & Respawn Lifecycle

Full death-to-respawn cycle covering server detection, client UI, and spawn request processing.

```mermaid
%%{init: {'theme': 'default'}}%%
sequenceDiagram
    participant Physics as Server Physics
    participant SS as ServerSession
    participant S as Server (engine)
    participant C as Client (engine)
    participant CS as ClientSession
    participant UI as DeathMenuScreen

    Note over Physics: Player entity destroyed<br/>(e.g., armor reaches zero)<br/>humanPlayerId no longer<br/>in frame's Players collection

    SS->>SS: DetectPlayerDeaths()<br/>Scan clients: humanPlayerId<br/>missing from frame.interpolate.pPlayers<br/>(skip if in mDeadClientIds or<br/>mPendingSubscriptionUpdates)

    SS->>SS: Mark mDeadClientIds.insert(clientId)<br/>Clear client.humanPlayerId

    S->>C: kServerPlayerState (reliable)<br/>[kDied, playerId, humanGridCoord]

    CS->>CS: PollNetwork() → ParsePlayerEvents()<br/>→ ReceivedPlayerEvent(kDied)

    CS->>CS: Set kDeathScreen game flag<br/>Clear humanPlayerId<br/>Clear previousHumanArmor

    CS->>CS: UpdateSubscriptions()<br/>Dead: subscribe to death coord + origin

    UI->>UI: DeathMenuScreen::Render()<br/>Shows "Game Over" + Respawn button

    Note over UI: Player clicks Respawn

    UI->>C: SendSpawnRequest(kRespawnRequested)
    C->>S: kClientSpawnRequest (reliable)<br/>[flags: kRespawnRequested]

    S->>SS: DrainPendingSpawnRequests()
    SS->>SS: ProcessSpawnRequests()<br/>Remove from mDeadClientIds<br/>Add to mClientsWaitingForSpawn<br/>(spawn coord = origin)

    SS->>SS: BuildFrameInputs()<br/>Add kSpawnPlayer StatusChange<br/>at origin coord

    Note over Physics: RunFrameTick processes<br/>kSpawnPlayer → new player entity

    SS->>SS: FinalizeNewClients(iTick)<br/>Compare post-spawn player IDs<br/>with pre-spawn snapshot<br/>→ find new player_t

    S->>C: kServerAssignPlayer (reliable)<br/>[new playerId, origin coord]
    S->>C: kServerPlayerState (reliable)<br/>[kSpawned, playerId, origin coord]

    CS->>CS: PollNetwork() → kAssigned event<br/>Set humanPlayerId, humanGridCoord<br/>Clear kDeathScreen game flag<br/>UpdateSubscriptions() → alive set
    CS->>CS: kSpawned event<br/>Update humanGridCoord
```

## Coord Transfer Flow

Cross-cell entity migration when physics produces `TransferRequest`s at grid cell boundaries. The server harvests transfers, spawns entities in destination frames, and notifies clients of human player relocations.

```mermaid
%%{init: {'theme': 'default'}}%%
flowchart TD
    classDef physics fill:#fef3c7,stroke:#d97706
    classDef server fill:#fee2e2,stroke:#ef4444
    classDef client fill:#dbeafe,stroke:#3b82f6
    classDef decision fill:#f3e8ff,stroke:#9333ea

    PHYSICS["Physics postRender phase<br/>produces TransferRequests<br/>(eType, data, iDeltaX/Y, iEntityId)"]:::physics

    HARVEST["ServerSession::HarvestTransfers()<br/>Called between RunFrameTick<br/>and BroadcastTick"]:::server

    COLLECT["CollectTransfers()<br/>For each active coord's transferRequests:<br/>Compute dest = coord + delta<br/>Store StatusChange in mTickBroadcast.transfers[dest]<br/>Track human transfers (iEntityId != 0)"]:::server

    SORT["SortTransfersByType()<br/>Sort StatusChanges per dest coord<br/>by eType (deterministic order)"]:::server

    SPAWN["SpawnTransfers()<br/>For each dest coord's transfers:<br/>SpawnTransfer() into dest next frame<br/>(creates entity in destination)<br/>Then recompute CRCs for dest frames<br/>(serverCrc + crc)"]:::server

    TRACK["TrackHumanTransfers()<br/>For each human transfer:<br/>Find client by old humanPlayerId<br/>Look up new player ID in dest frame<br/>(last spawned player in dest)<br/>Queue SubscriptionUpdate"]:::server

    BROADCAST["BroadcastStatusChanges(iTick)<br/>Merges transfers with spawns<br/>→ compressed into PerCoordBufferedFrame<br/>→ sent to all subscribed clients"]:::server

    SUBUPDATE["SubscriptionUpdates(iTick)<br/>For each pending SubscriptionUpdate:<br/>SendAssignPlayer(clientId, newPlayerId, newCoord)<br/>SendPlayerState(kChangedFrame, newPlayerId, newCoord)"]:::server

    CLIENT_RECV["Client receives:<br/>kServerAssignPlayer → update humanPlayerId<br/>kServerPlayerState(kChangedFrame)<br/>→ update humanGridCoord<br/>→ recompute desired subscriptions<br/>(human cell + 3 quadrant neighbors)"]:::client

    PHYSICS --> HARVEST --> COLLECT --> SORT --> SPAWN --> TRACK
    TRACK --> BROADCAST --> SUBUPDATE --> CLIENT_RECV
```

## Client Disconnection

Cleanup flow when a client disconnects, covering server-side resource cleanup and broadcasting the player destruction to remaining clients.

```mermaid
%%{init: {'theme': 'default'}}%%
sequenceDiagram
    participant E as ENet
    participant S as Server (engine)
    participant SS as ServerSession
    participant Physics as Server Physics
    participant C as Other Clients

    E->>S: ENET_EVENT_TYPE_DISCONNECT

    S->>S: Disconnect(event)<br/>Find ClientConnection by peer data<br/>Create PendingDisconnect<br/>(clientId, humanPlayerId, humanGridCoord)<br/>RemoveClient() from mClients<br/>Clear delayed packets for peer

    Note over SS: Next tick: PreTickNetwork()

    SS->>S: DrainPendingDisconnects()

    SS->>SS: Disconnects()<br/>For each PendingDisconnect:<br/>If player valid → queue PendingPlayerDestroy<br/>(coord, playerId)<br/>Remove from mDeadClientIds<br/>Remove from mClientsWaitingForSpawn

    Note over SS: BuildFrameInputs()
    SS->>SS: Add kDestroyPlayer StatusChange<br/>at player's coord<br/>(playerId encoded in data.vecPosition)

    Note over Physics: RunFrameTick processes<br/>kDestroyPlayer → removes entity

    Note over SS: BroadcastStatusChanges()
    SS->>SS: StatusChange included in<br/>GridUpdateData for coord<br/>→ compressed into PerCoordBufferedFrame

    SS->>C: kServerCoordUpdate<br/>includes destroy StatusChange<br/>(clients subscribed to that coord<br/>process it during reconciliation)
```

## Packet Types

```mermaid
%%{init: {'theme': 'default'}}%%
graph LR
    classDef reliable fill:#d1fae5,stroke:#059669
    classDef unreliable fill:#fef3c7,stroke:#d97706
    classDef clientPkt fill:#dbeafe,stroke:#3b82f6
    classDef serverPkt fill:#fee2e2,stroke:#ef4444

    subgraph client_to_server ["Client -> Server"]
        kClientHello["kClientHello<br/>(reliable)"]:::reliable
        kClientAckStream["kClientAckStream<br/>(unreliable, every frame)<br/>per-slot ACK state, timestamp"]:::unreliable
        kClientSpawnRequest["kClientSpawnRequest<br/>(reliable)"]:::reliable
        kClientSubscribe["kClientSubscribe<br/>(reliable, coord request)"]:::reliable
        kClientUnsubscribe["kClientUnsubscribe<br/>(reliable, slot release)"]:::reliable
        kClientDesyncReport["kClientDesyncReport<br/>(reliable, on CRC mismatch)"]:::reliable
        kClientDebugFrameRequest["kClientDebugFrameRequest<br/>(reliable)"]:::reliable
    end

    subgraph server_to_client ["Server -> Client"]
        kServerConnectionResponse["kServerConnectionResponse<br/>(reliable)"]:::reliable
        kServerAssignPlayer["kServerAssignPlayer<br/>(reliable, on spawn)"]:::reliable
        kServerSubscribeAccept["kServerSubscribeAccept<br/>(reliable, slot+epoch+coord)"]:::reliable
        kServerUnsubscribeAck["kServerUnsubscribeAck<br/>(reliable, slot release)"]:::reliable
        kServerCoordFullState["kServerCoordFullState<br/>(reliable, slot channel, epoch)"]:::reliable
        kServerCoordUpdate["kServerCoordUpdate<br/>(unreliable, slot channel, epoch, every tick)"]:::unreliable
        kServerCoordResend["kServerCoordResend<br/>(unreliable, slot channel, epoch, as needed)"]:::unreliable
        kServerPlayerState["kServerPlayerState<br/>(reliable, raw bytes<br/>for game-layer parsing)"]:::reliable
        kServerDebugFrame["kServerDebugFrame<br/>(reliable, on request)"]:::reliable
    end
```

## Coord Subscription Lifecycle

```mermaid
%%{init: {'theme': 'default'}}%%
stateDiagram-v2
    [*] --> kUnsubscribed
    kUnsubscribed --> kSubscribing : SendSubscribe(coord)
    kSubscribing --> kWaitingFullState : ServerSubscribeAccept<br/>(slot assigned, epoch set)
    kWaitingFullState --> kActive : ServerCoordFullState<br/>(ackFloor initialized, epoch set)
    kWaitingFullState --> kWaitingFullState : ServerCoordUpdate<br/>(buffered but not ACK-tracked,<br/>epoch must match)
    kSubscribing --> kActive : ServerCoordFullState<br/>(full state arrived at<br/>assigned slot, cross-channel)
    kUnsubscribed --> kActive : ServerCoordFullState<br/>(full state arrived before<br/>subscribe accept, cross-channel)
    kActive --> kUnsubscribing : SendUnsubscribe(slot)
    kUnsubscribing --> kUnsubscribed : ServerUnsubscribeAck<br/>(slot freed)
    kUnsubscribing --> kSubscribing : SendSubscribe(coord)<br/>(reuse slot for new coord)
```

## Client-Side Reconciliation

```mermaid
%%{init: {'theme': 'default'}}%%
flowchart TD
    classDef mainThread fill:#dbeafe,stroke:#3b82f6
    classDef worker fill:#fef3c7,stroke:#d97706
    classDef decision fill:#f3e8ff,stroke:#9333ea
    classDef desync fill:#fee2e2,stroke:#ef4444

    subgraph main ["Main Thread (ClientSession + ClientReconciler)"]
        poll["ClientSession::PollNetwork()<br/>Buffer per-slot received updates<br/>into CoordReconcileStates"]:::mainThread
        kick["ClientReconciler::TryKick()<br/>Gate: mbHasNewData<br/>Build CoordReconcileWork per coord<br/>+ kick worker"]:::mainThread
        wait["ClientReconciler::Wait()<br/>Block until worker done"]:::mainThread
        apply["ClientReconciler::ApplyResult()<br/>Merge per-coord results<br/>into mCurrentFrames"]:::mainThread
    end

    subgraph worker_thread ["Reconcile Worker (PersistentWorker in ClientReconciler)"]
        check_crc{"ReconcileCrcFastPath():<br/>All coords' Frame CRCs<br/>(postRender.serverCrc +<br/>postRender.previousInputCrc)<br/>match server updates?<br/>(gapped coords skipped)"}:::decision

        fast["Use last matched snapshot<br/>as confirmed state per coord<br/>(no replay needed)"]:::worker

        restore["Restore per-coord<br/>from confirmed serialized frames"]:::worker
        replay["Replay consecutive server updates<br/>(per-coord StatusChanges)<br/>RunFrameTick per frame<br/>(same code path as GameBase)"]:::worker
        validate{"Input CRC + state CRC<br/>match per coord per frame?"}:::decision
        save_confirmed["Save new confirmed state<br/>per coord"]:::worker
        catchup["Predictive catch-up<br/>Simulate to iTargetFrame<br/>with extrapolated inputs"]:::worker
        snapshot["Store per-coord<br/>SnapshotEntries<br/>(Frames with cached CRCs<br/>for next CRC fast-path)"]:::worker

        desync["Desync detected!<br/>Store desync info<br/>(frame, coord, CRCs)<br/>Return early"]:::desync
    end

    poll --> kick --> wait --> apply
    kick -->|build work items| check_crc
    check_crc -->|Yes| fast --> snapshot
    check_crc -->|No| restore --> replay --> validate
    validate -->|Yes| save_confirmed --> catchup --> snapshot
    validate -->|No| desync
```
