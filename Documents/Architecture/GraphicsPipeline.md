# Architecture: Graphics Pipeline

> Auto-generated by `/generate-architecture-diagram` from `Engine/Source/Graphics/`

## Manager Initialization Order

Strict dependency DAG — each manager requires all predecessors to be fully initialized. Violations cause Vulkan validation errors or crashes.

```mermaid
%%{init: {'theme': 'default'}}%%
graph TD
    classDef manager fill:#dbeafe,stroke:#3b82f6
    classDef orchestrator fill:#fef3c7,stroke:#d97706

    gpGraphics["gpGraphics<br/>Graphics (orchestrator)"]:::orchestrator

    gpGraphics --> IM
    IM["1. gpInstanceManager<br/>VkInstance, physical device"]:::manager
    IM --> DM
    DM["2. gpDeviceManager<br/>VkDevice, queues, descriptor pool, VMA"]:::manager
    DM --> TUM["TextureUploadManager<br/>InitTransferResources()"]:::manager
    DM --> SM
    SM["3. gpSwapchainManager<br/>VkSwapchainKHR, framebuffers, depth"]:::manager
    SM --> CBM
    CBM["4. gpCommandBufferManager<br/>Command pools, per-framebuffer buffers"]:::manager
    CBM --> BM
    BM["5. gpBufferManager<br/>Vertex/index/uniform/storage buffers"]:::manager
    BM --> TXM
    TXM["6. gpTextureManager<br/>Textures, samplers; delegates to<br/>TextureDescriptors, TextureCache,<br/>RenderTargetTextures"]:::manager
    TXM --> TM
    TM["7. gpTextManager<br/>Font rendering, text layout"]:::manager
    TM --> ISL
    ISL["8. gpIslands<br/>GPU terrain quad storage buffer"]:::manager
    ISL --> PM
    PM["9. gpPipelineManager<br/>SPIR-V shaders, ~60 pipelines;<br/>delegates dynamic pipelines to<br/>DynamicPipelines"]:::manager
    PM --> PAR
    PAR["10. gpParticleManager<br/>GPU particle compute shaders"]:::manager
    PAR --> IMGUI
    IMGUI["11. gpImGuiManager<br/>ImGui UI rendering"]:::manager
```

## Per-Frame GPU Submission

Three semaphore-synchronized submissions per frame, plus present. `mVkFence` gates the CPU from running ahead of the GPU.

```mermaid
%%{init: {'theme': 'default'}}%%
sequenceDiagram
    participant CPU as Main Thread
    participant GW as Global Worker
    participant MW as Main Worker
    participant PW as Present Worker
    participant GPU

    Note over CPU: RenderGlobal()
    CPU->>CPU: Wait mVkFence (previous frame)
    CPU->>CPU: ProcessPendingTextures()
    CPU->>GW: Dispatch SubmitGlobalCommandBuffer

    Note over GW: Phase 1 — Global (Shadows & Particles)
    GW->>GPU: vkQueueSubmit (mGlobalVkCommandBuffer)
    Note right of GPU: Waits: mParticleSyncVkSemaphore (if signaled)<br/>Signals: mGlobalFinishedVkSemaphore

    Note over CPU: RenderMainPresentAcquire()
    CPU->>MW: Dispatch SubmitMainCommandBuffer

    Note over MW: Phase 2 — Main (Lighting & Scene)
    MW->>GPU: vkQueueSubmit (mMainVkCommandBuffer)
    Note right of GPU: Waits: mGlobalFinishedVkSemaphore,<br/>mImageAvailableVkSemaphore<br/>Signals: mMainFinishedVkSemaphore,<br/>mParticleSyncVkSemaphore<br/>Fence: mVkFence

    Note over CPU: SubmitUiCommandBuffer()
    CPU->>CPU: ImGuiManager::Submit() (re-records each frame)

    Note over CPU: Phase 3 — ImGui (UI Overlay)
    CPU->>GPU: vkQueueSubmit (mImGuiVkCommandBuffer)
    Note right of GPU: Waits: mMainFinishedVkSemaphore<br/>Signals: mImGuiFinishedVkSemaphore

    CPU->>PW: Dispatch Present

    Note over PW: Present
    PW->>GPU: vkQueuePresentKHR
    Note right of GPU: Waits: mImGuiFinishedVkSemaphore

    CPU->>CPU: Signal TextureUploadManager
    CPU->>CPU: Wait Present, AcquireNextImage
```

## Semaphore Chain

```mermaid
%%{init: {'theme': 'default'}}%%
flowchart LR
    ACQ["AcquireNextImage<br/>mImageAvailableVkSemaphore"]
    GLOBAL["Global Phase<br/>(shadows, particles, terrain)"]
    MAIN["Main Phase<br/>(lighting, scene)"]
    IMGUI["ImGui Phase<br/>(UI overlay)"]
    PRESENT["vkQueuePresentKHR"]
    NEXT["Next Frame Global"]

    ACQ -->|"COLOR_ATTACHMENT_OUTPUT"| MAIN
    GLOBAL -->|"mGlobalFinishedVkSemaphore<br/>TOP_OF_PIPE"| MAIN
    MAIN -->|"mMainFinishedVkSemaphore"| IMGUI
    IMGUI -->|"mImGuiFinishedVkSemaphore"| PRESENT
    MAIN -.->|"mParticleSyncVkSemaphore<br/>COMPUTE_SHADER"| NEXT
```

## Descriptor Set Ownership

```mermaid
%%{init: {'theme': 'default'}}%%
graph LR
    classDef set0 fill:#dcfce7,stroke:#16a34a
    classDef set12 fill:#dbeafe,stroke:#3b82f6
    classDef compute fill:#fef3c7,stroke:#d97706

    subgraph SET0 ["Set 0 — Global (TextureDescriptors)"]
        S0["mGlobalDescriptorSets<br/>(one per framebuffer)<br/>Uniform buffers +<br/>Bindless texture array +<br/>Samplers"]:::set0
    end

    subgraph SET12 ["Sets 1 & 2 — Per-Pipeline"]
        S1["Set 1<br/>Shared across materials<br/>(owned by first material)"]:::set12
        S2["Set 2<br/>Per-material descriptors<br/>(lighting, shadows, smoke,<br/>mesh data, joint matrices)"]:::set12
    end

    subgraph COMPUTE ["Compute Pipelines"]
        SC["Single descriptor set<br/>(no Set 0 inheritance)<br/>One per framebuffer"]:::compute
    end

    TXM["TextureDescriptors<br/>(in TextureManager)"] -->|"owns & updates"| S0
    PIPE["Pipeline /<br/>ModelPipeline"] -->|"allocates"| S1
    PIPE -->|"allocates per material"| S2
    POOL["DeviceManager<br/>mVkDescriptorPool"] -->|"serves all"| S0
    POOL -->|"serves all"| S1
    POOL -->|"serves all"| S2
    POOL -->|"serves all"| SC
```

## Global Phase Workload

Recorded once at startup via `RecordGlobalCommandBuffer()`, resubmitted every frame.

```
Particle Spawn (compute) ─► Shadow Elevation (render) ─► Shadow + Blur (compute)
    │
    ├─► Terrain Elevation/Color/Normal/AO (render)
    ├─► Wind Spread A/B + Clear (indirect render)
    ├─► Smoke Spread A/B + Clear (indirect render)
    └─► Particle Update (compute, indirect)
```

## Main Phase Workload

Recorded once at startup via `RecordMainCommandBuffer()`, resubmitted every frame.

```
Lighting Pass (dynamic + particles) ─► Lighting Blur (ping-pong) ─► Lighting Combine
    │
    ├─► Smoke Emit
    ├─► Wind Deposit (2 passes)
    ├─► Object Shadows + Blur
    └─► Main Image Pass:
            Opaque models (indirect) ─► Terrain ─► Water
            ─► Hex shields ─► Transparent models (indirect)
            ─► Particles (indirect) ─► Visible lights ─► Billboards ─► Profile text
```

## Resource Recreation Cascade

`DestroyType` enum — each level includes all lower levels:

| Level | Trigger | What gets recreated |
|-------|---------|-------------------|
| `kCommandBuffers` | Settings change (blur divisors) | Re-record command buffers |
| `kSamplers` | Anisotropy / mip bias change | Samplers + descriptor sets |
| `kPipelines` | Wireframe / sample shading / shader specialization | All ~60 pipelines |
| `kSwapchain` | Window resize / present mode | Full swapchain (TextureManager/BufferManager survive) |
| `kSurface` | Device lost | Everything destroyed and reconstructed |

`DestroyFlags` bitflags for targeted recreation: `kTerrainMesh`, `kShadowTextures`, `kObjectShadows`, `kLightingTextures`, `kWaterMesh`, `kTerrainElevation`, `kTerrainColor`, `kTerrainNormal`, `kTerrainAO`, `kSmokeTextures`

## Command Buffer Architecture

Per-framebuffer `CommandBuffers` struct:

| Buffer | Content | Recording |
|--------|---------|-----------|
| `mGlobalVkCommandBuffer` | Shadows, particles, terrain | Recorded once, reused |
| `mMainVkCommandBuffer` | Deferred lighting, scene | Recorded once, reused |
| `mImGuiVkCommandBuffer` | UI overlay | Re-recorded every frame |

Synchronization primitives per `CommandBuffers`:
- `mVkFence` — CPU/GPU sync (signaled by Main, waited by next frame's Global)
- `mGlobalFinishedVkSemaphore` — Global → Main
- `mMainFinishedVkSemaphore` — Main → ImGui
- `mImGuiFinishedVkSemaphore` — ImGui → Present
