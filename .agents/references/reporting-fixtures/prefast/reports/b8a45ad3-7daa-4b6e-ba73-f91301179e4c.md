Schema: be-agent-report/v1
Requested role: Fable
Actual executor: Sol (Codex)
Fallback: Fable -> Sol mapping required by repository AGENTS.md
Paired-review diversity: N/A
Worktree: <WORKTREE>
Session-start baseline: ca6f005addca80e8273cc7732e436fe351c1f71c
Plan/intent: <WORKTREE>\Documents\Plans\Engine\ReleaseStaticAnalysisWarningCleanup.md (not approved)

# Plan Audit

F001 — `Documents/Plans/Engine/ReleaseStaticAnalysisWarningCleanup.md:32` — **verification workflow** — the required Release PREfast builds have no executable repository-approved build route. The plan requires code analysis enabled, but the mandatory `/compile` workflow explicitly says every orchestrated build must preserve `/p:RunCodeAnalysis=false`; its documented client/server commands also hard-code that property. A normal Release compile therefore cannot satisfy acceptance criteria at lines 44-45, while bypassing `/compile` would violate the repository build process and lose WorktreeCli serialization/data-mode checks. Evidence: `.agents/skills/compile/SKILL.md:64`, `.agents/skills/compile/SKILL.md:107-108`; the project defaults themselves do enable PREfast and warnings-as-errors at `Projects/BrokenEngineSandbox/Platforms/VisualStudio2026/BrokenEngineSandbox.vcxproj:241,259` and `Projects/BrokenEngineSandbox/Platforms/VisualStudio2026/BrokenEngineSandboxServer.vcxproj:239,257`. Proposed improvement: resolve this before approval by defining an authorized WorktreeCli-serialized analysis-build invocation (including the exact exception to `RunCodeAnalysis=false`, data properties, and client/server commands), or first update `/compile` to support a dedicated PREfast mode; then make that route the explicit step-5 verification command.

F002 — `Documents/Plans/Engine/ReleaseStaticAnalysisWarningCleanup.md:16` — **stale coordination dependency** — the ImGui overlap names `Engine/Architecture_LibraryReplacement.md`, but no such live plan file or `Order.md` plan row exists. `Documents/Plans/Order.md` defines itself as the current live-plan set, yet repeats the same dead reference in its overlap entry, so the later-lander instruction cannot be acted on and “Dependency resolution: none” is based partly on stale queue metadata. Evidence: `Documents/Plans/Order.md:19,158`; repository-wide plan/feature filename and content search finds no `Architecture_LibraryReplacement.md` outside those two references. Proposed improvement: remove the dead overlap from both this plan and the `Order.md` overlap entry, or replace it with the actual current plan path if the work was renamed.

F003 — `Documents/Plans/Engine/ReleaseStaticAnalysisWarningCleanup.md:44-48` — **verification gap** — acceptance requires preserving `get_logs` chronological-tail, regex-filtering, and count-limit behavior, but the plan declares verification compile/static-analysis-only. Step 4 may change caller bounds or the shared `LogBuffer::Tail` contract; clean PREfast output cannot demonstrate those observable JSON semantics. Evidence: `Projects/BrokenEngineSandbox/Source/Agent/AgentCommands.cpp:61-89` performs filtering and final count selection, and `:108-143` exposes it through `get_logs`; `Common/Log/Log.h:40-74` supplies the shared tail ordering/count behavior. Proposed improvement: add a targeted agent-harness check after the builds that emits identifiable lines and queries `get_logs` with (a) a count limit, (b) a regex with more matches than the limit, and (c) category/no-category paths, asserting chronological order and count. If runtime launch is deliberately excluded, narrow the acceptance criterion to compile-time proof plus explicit equivalence inspection instead of claiming runtime behavior was verified.

Files changed: none
Functions/regions touched: none
Residuals: none
