Schema: be-agent-report/v1
Requested role: Luna (resolve-findings fix role)
Actual executor: Codex GPT-5
Fallback: none
Paired-review diversity: N/A
Worktree: <WORKTREE>
Session-start baseline: ca6f005addca80e8273cc7732e436fe351c1f71c
Plan/intent: Restore serialized Release PREfast builds; no approved delta. Accepted B001/R001/R002 from Temp/AgentReports/<GUID>.md; edit scope limited to Get-AgentCliRepositoryIdentity.

## Finding Resolution

Mode: fix

Caller classification: Intent `conformance`; Scope `non_structural`.

PLAN DELTA REQUIRED: no.

### Root-Cause Confirmation

Suspected root cause: `Get-AgentCliRepositoryIdentity` used the modern static APIs `SHA256.HashData(byte[])` and `Convert.ToHexString(byte[])`, but the MSBuild pre-build target launches Windows PowerShell 5.1 on .NET Framework, whose loaded framework exposes neither API.

Confirmed:

- Source failure report records `Method invocation failed because [System.Security.Cryptography.SHA256] does not contain a method named 'HashData'` from both client and server pre-build provisioning, before C++ compilation.
- Direct host probe reported Windows PowerShell Desktop 5.1.26100.8655: `HashData=false`, `ToHexString=false`; pwsh Core 7.6.3: `HashData=true`, `ToHexString=true`.
- Direct inspection found both incompatible calls at `.agents/scripts/AgentCliSessionExclusion.psm1:15-16` before the fix. The failure is deterministic before ledger or mutex identity construction.

### Item Results

- B001: FIXED
  - Root cause: confirmed as above from the failure evidence, host API probes, and direct inspection.
  - Change: in `Get-AgentCliRepositoryIdentity`, create a `SHA256` instance, compute the UTF-8 hash in a `try` block, guarantee disposal in `finally`, and format the bytes through `BitConverter.ToString(...).Replace('-', '').ToLowerInvariant()`.
  - Verification: the replacement produced the exact prior pwsh digest `031090b2dea52ac49878dfe54a2c383b9e68b0aabd17c3dbe9abb14f332273ee`. Both hosts imported and used the module successfully and returned identical repository, ledger path, and mutex name.
- R001: UNRESOLVED
  - Root cause: client Release PREfast remained incomplete only because B001 stopped its earlier invocation before C++ analysis.
  - Change: no client-build-specific change; B001's shared provisioning fix addresses the proven pre-build cause.
  - Verification: full client build intentionally not rerun because the caller explicitly retained full game-build verification.
- R002: UNRESOLVED
  - Root cause: server Release PREfast remained incomplete only because B001 stopped its earlier invocation before C++ analysis.
  - Change: no server-build-specific change; B001's shared provisioning fix addresses the proven pre-build cause.
  - Verification: full server build intentionally not rerun because the caller explicitly retained full game-build verification.

### Files Changed and Regions Touched

- `.agents/scripts/AgentCliSessionExclusion.psm1` — `Get-AgentCliRepositoryIdentity`, lines 10-25; three replacement statements only.

Assignment-owned tracked mutation: one file, 3 insertions and 2 deletions. Pre-existing changes in `.agents/skills/compile/SKILL.md`, `Common/Log/Log.h`, plan files, and the three engine `.cpp` files were preserved and not edited.

### Scoped Verification

1. `git diff --check -- .agents/scripts/AgentCliSessionExclusion.psm1`: PASS, no output.
2. Old/new pwsh hash equivalence for the current canonical common directory `<USER_HOME>\Documents\BrokenEnginePublic\.git`: PASS.
   - Old hash through `SHA256.HashData` plus `Convert.ToHexString`: `031090b2dea52ac49878dfe54a2c383b9e68b0aabd17c3dbe9abb14f332273ee`.
   - New hash through `SHA256.Create().ComputeHash` plus `BitConverter`: `031090b2dea52ac49878dfe54a2c383b9e68b0aabd17c3dbe9abb14f332273ee`.
3. `pwsh.exe -NoProfile` module import and `Get-AgentCliRepositoryIdentity`: PASS, exit 0.
4. `powershell.exe -NoProfile -ExecutionPolicy Bypass` module import and `Get-AgentCliRepositoryIdentity`: PASS, exit 0. `-ExecutionPolicy Bypass` matches the failing MSBuild pre-build command and is necessary under this machine's script policy.
5. Cross-host identity comparison: PASS. Repository, 64-character lowercase hash, ledger path, and mutex name were case-exact matches. Both child hosts inherited the live wrapper owner `<GUID>` and the current session worktree path.
6. Primary/shared output preservation: PASS. Before/after metadata inventory covered 91 entries under primary ThirdParty output, primary AgentCli output, and primary/shared BrokenEngineSandbox output; `Compare-Object` reported 0 changes while both module-use checks ran.
7. Complete fixed region re-read: PASS. Hash input remains the uppercase canonical Git common-directory UTF-8 bytes; output remains lowercase separator-free SHA-256 and feeds the unchanged ledger and mutex templates.

### Residuals

- R001: caller must rerun the client Release PREfast build and evaluate requested warning codes now that provisioning can pass Windows PowerShell identity creation.
- R002: caller must rerun the server Release PREfast build and evaluate requested warning codes now that provisioning can pass Windows PowerShell identity creation.

### Decision Index

- X001 | fixed | `.agents/scripts/AgentCliSessionExclusion.psm1:10` | B001 compatibility fix applied; scoped cross-host verification passed with exact identity preservation.
- R001 | unresolved | client Release PREfast | Full build remains caller-owned by explicit instruction; no game build was rerun here.
- R002 | unresolved | server Release PREfast | Full build remains caller-owned by explicit instruction; no game build was rerun here.
