Schema: be-agent-report/v1
Requested role: Luna (resolve-findings fix role)
Actual executor: Codex GPT-5
Fallback: none
Paired-review diversity: N/A
Worktree: <WORKTREE>
Session-start baseline: ca6f005addca80e8273cc7732e436fe351c1f71c
Plan/intent: Restore serialized Release PREfast builds; no approved delta. Accepted retry failure B001/R001/R002 from Temp/AgentReports/<GUID>.md; prior hash fix report Temp/AgentReports/<GUID>.md; edit scope limited to Read-AgentCliLedger.

## Finding Resolution

Mode: fix

Caller classification: Intent conformance; Scope non_structural.

PLAN DELTA REQUIRED: no.

### Root-Cause Confirmation

Suspected root cause: Read-AgentCliLedger unconditionally passed DateKind String to ConvertFrom-Json. Windows PowerShell 5.1 does not expose that parameter, while pwsh 7 converts ISO JSON strings to DateTime without it. The Windows PowerShell host therefore failed before ledger validation.

Confirmed:

- Source report <GUID>.md records both client and server pre-build provisioning failures at AgentCliSessionExclusion.psm1:69 with: A parameter cannot be found that matches parameter name 'DateKind'.
- Direct command metadata probes reported Windows PowerShell Desktop 5.1.26100.8655 without DateKind and pwsh Core 7.6.3 with DateKind.
- Direct parse probes of 2026-07-14T12:34:56.1234567Z reported System.String under Windows PowerShell 5.1 default parsing, System.DateTime under pwsh 7 default parsing, and System.String under pwsh 7 with DateKind String.
- Direct inspection found the unconditional parameter in Read-AgentCliLedger. The failure is deterministic before the unchanged Test-StrictUtcRoundTrip validation can run.

### Item Results

- B001: FIXED
  - Root cause: confirmed as above from the failure report, host capability probes, parse type probes, and direct source inspection.
  - Change: Read-AgentCliLedger now capability-detects the ConvertFrom-Json DateKind parameter, splats DateKind = String only when supported, and otherwise uses Windows PowerShell's string-preserving default path.
  - Verification: both hosts imported the module and validated the same live ledger at exit 0. Each exposed all three processStartUtc values as System.String, reported session count 3, and emitted byte-identical canonical status JSON with SHA-256 65487347207fdf1536205956d6dcda32b338d73694d53cd3acc950a1ecd456a9. The original DateKind failure no longer occurs.
- R001: UNRESOLVED
  - Root cause: client Release PREfast remains incomplete. The DateKind cause is fixed, but the prescribed Windows PowerShell provisioning retry reaches a separate pre-existing unsupported File.Move overload before C++ compilation.
  - Change: no client-build-specific change; caller retained full build verification.
  - Verification: full client build was not rerun, per caller instruction.
- R002: UNRESOLVED
  - Root cause: server Release PREfast remains incomplete for the same next provisioning incompatibility.
  - Change: no server-build-specific change; caller retained full build verification.
  - Verification: full server build was not rerun, per caller instruction.

### Files Changed and Regions Touched

- .agents/scripts/AgentCliSessionExclusion.psm1 — Read-AgentCliLedger, lines 67-73; one capability-detected JSON parse path.

Assignment-owned tracked mutation: one file, 4 inserted lines and 1 deleted line in Read-AgentCliLedger. The prior Get-AgentCliRepositoryIdentity hash compatibility hunk and all unrelated session changes were preserved and not edited.

### Scoped Verification

1. git diff --check -- .agents/scripts/AgentCliSessionExclusion.psm1: PASS, no output.
2. Host capability and type probes: PASS.
   - Windows PowerShell 5.1 ConvertFrom-Json parameters omit DateKind; its default ISO value type is System.String.
   - pwsh 7.6.3 exposes DateKind; default ISO value type is System.DateTime and DateKind String yields System.String.
3. Same-ledger cross-host parse and validation: PASS.
   - Command path: module import followed by Get-AgentCliExclusionStatus against the current repository and live wrapper ledger.
   - powershell.exe -NoProfile -ExecutionPolicy Bypass: exit 0.
   - pwsh.exe -NoProfile: exit 0.
   - Both: processStartUtc type System.String, session count 3, byte-identical canonical status JSON SHA-256 65487347207fdf1536205956d6dcda32b338d73694d53cd3acc950a1ecd456a9.
4. Complete fixed-region and directly affected validation path re-read: PASS. Test-StrictUtcRoundTrip at lines 45-51 and Test-StrictClaim at lines 53-65 are unchanged; malformed or non-canonical timestamps remain rejected.
5. Complete provisioning with live wrapper owner <GUID>:
   - pwsh.exe -NoProfile -File .agents/scripts/Provision-WorktreeThirdParty.ps1 -RepositoryRoot ROOT: PASS, exit 0.
   - Output: Shared worktree dependencies validated for '<WORKTREE>' using primary '<USER_HOME>\Documents\BrokenEnginePublic'.
   - powershell.exe -NoProfile -ExecutionPolicy Bypass -File .agents/scripts/Provision-WorktreeThirdParty.ps1 -RepositoryRoot ROOT: FAIL, exit 1 after the DateKind path passed.
   - Decisive output: Cannot find an overload for "Move" and the argument count: "3".
   - FullyQualifiedErrorId: MethodCountCouldNotFindBest,Provision-WorktreeThirdParty.ps1.
   - Direct inspection maps this to Write-AgentCliLedger line 116: [IO.File]::Move($temp, $Identity.LedgerPath, $true).
   - Direct method probes confirm Windows PowerShell 5.1 exposes only Move(String,String); pwsh 7.6.3 also exposes Move(String,String,Boolean).
   - git show of baseline ca6f005addca80e8273cc7732e436fe351c1f71c confirms the three-argument call predates this assignment.
6. Primary output preservation across the failed Windows PowerShell run and successful pwsh run: PASS.
   - Inventory roots: primary ThirdParty Prebuilts VisualStudio2026 Output, primary AgentCli VisualStudio2026 Output, and primary/shared BrokenEngineSandbox VisualStudio2026 Output.
   - Metadata inventory: 91 entries, aggregate SHA-256 19cb80970b056a75fb184fd820543e84f717e502180d25edb0ef63d011f8427a.
   - Content inventory: 68 files, 2,102,408,053 bytes, path/length/per-file-SHA-256 aggregate 8c47b7e43848cc7ce3ff6290e885081f4648deeab97cc03d0a5f9dc47b88dbe2.
   - Metadata diff after powershell.exe: 0.
   - Metadata diff after pwsh.exe: 0.
   - Content diff after both runs: 0.
7. Full client/server builds: not run, per caller instruction.

### Residuals

- R001: caller must rerun client Release PREfast after Windows PowerShell provisioning succeeds, then evaluate requested warning codes.
- R002: caller must rerun server Release PREfast after Windows PowerShell provisioning succeeds, then evaluate requested warning codes.
- R003: required Windows PowerShell provisioning remains blocked by the pre-existing three-argument IO.File.Move call in Write-AgentCliLedger, outside the assigned Read-AgentCliLedger edit scope. Main session must adjudicate and assign a host-compatible atomic replacement fix, then rerun complete provisioning under both hosts before the builds.

### Decision Index

- X001 | fixed | .agents/scripts/AgentCliSessionExclusion.psm1:67 | B001 DateKind compatibility fix applied; identical live-ledger parsing and strict validation passed under Windows PowerShell 5.1 and pwsh 7.6.3.
- R001 | unresolved | client Release PREfast | Full build remains caller-owned and cannot proceed until Windows PowerShell provisioning passes.
- R002 | unresolved | server Release PREfast | Full build remains caller-owned and cannot proceed until Windows PowerShell provisioning passes.
- R003 | blocker | .agents/scripts/AgentCliSessionExclusion.psm1:109 | Windows PowerShell 5.1 lacks IO.File.Move(String,String,Boolean); accept and assign a scoped Write-AgentCliLedger compatibility fix, then repeat provisioning.
